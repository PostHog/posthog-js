# issue 4670: @posthog/rollup-plugin 1.4.9: random chunk IDs injected in renderChunk change every content hash, so every build renames every chunk

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4670
- Conclusion: Confirmed bug: default symbol-set mode still injects a random chunk ID during renderChunk, making otherwise identical content-hashed chunks change on every build.
- Labels: feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/33175638432
- Updated: 2026-08-28T13:31:09.960Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug: default symbol-set mode still injects a random chunk ID during renderChunk, making otherwise identical content-hashed chunks change on every build.",
    "summary": "The Rollup plugin's in-memory sourcemap chunk-ID injection introduced in 1.4.9 adds a fresh UUID before Rollup derives output hashes. This invalidates long-term caching for all JavaScript chunks whenever sourcemaps are enabled.",
    "issueType": "bug",
    "confidence": 0.97,
    "labels": [
      "bug: Something isn't working",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. This is confirmed by the current implementation: with sourcemaps enabled, the default `symbol-set` release mode calls `createChunkId()` from `renderChunk`, and that function returns `crypto.randomUUID()`. Since the plugin intentionally injects before Rollup resolves `[hash]` filenames, the random ID necessarily changes the emitted content hash on unchanged builds.\n\n`createStableChunkId(code)` already exists and is used by the plugin's `event` release mode specifically to preserve IDs for unchanged chunk content across rebuilds. The default mode remains random in the current 1.5.1 source, so the problem is not addressed by the Vite 8 comment-restoration fix.\n\nA focused fix should switch the default-mode injection to the existing content-derived ID and add an integration regression test that builds the same Vite fixture twice and verifies the emitted JS filenames are identical. Before release, validate that symbol-set upload/association semantics remain correct when unchanged output reuses a chunk ID.",
    "investigation": {
      "relevantFiles": [
        "packages/rollup-plugin/src/index.ts",
        "packages/rollup-plugin/src/index.spec.ts",
        "packages/rollup-plugin/test/vite8.test.mjs",
        "packages/rollup-plugin/CHANGELOG.md",
        "packages/plugin-utils/src/chunk-ids.ts",
        "packages/plugin-utils/src/chunk-ids.spec.ts",
        "packages/plugin-utils/src/config.ts"
      ],
      "findings": [
        "`packages/rollup-plugin/src/index.ts` runs its `renderChunk` hook with `order: 'post'`; when sourcemaps are enabled and release mode is not `event`, it calls `createChunkId()` and injects that value into the emitted JavaScript.",
        "`createChunkId()` in `packages/plugin-utils/src/chunk-ids.ts` returns `crypto.randomUUID()`, so the injected runtime snippet and trailing `//# chunkId=` comment differ on every build.",
        "The plugin comments explicitly state that injection occurs before Rollup resolves `[hash]` file names, confirming the random ID participates in content hashing.",
        "`createStableChunkId(code)` derives a UUIDv5-shaped ID from the rendered chunk bytes. The `event` release-mode branch already uses it, and its unit test verifies unchanged content produces the same ID across separate plugin instances.",
        "The default release mode is `symbol-set`, as established in `packages/plugin-utils/src/config.ts`; therefore the random-ID branch affects the default configuration, not only an opt-in mode.",
        "The Rollup plugin changelog documents 1.4.9 as intentionally switching to a fresh random ID injected in `renderChunk`; the current 1.5.1 source still retains this behavior. The 1.5.1 Vite 8 fix restores stripped comments after minification but does not change ID generation."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The smallest code change is to use the existing content-derived chunk-ID helper in the default renderChunk branch. The helper is already tested and used in the same plugin, but the change affects symbol-set identity and should be verified against sourcemap upload/association behavior.",
      "suggestedApproach": "Replace the default-branch `createChunkId()` call in `packages/rollup-plugin/src/index.ts` with `createStableChunkId(code)`. Update the unit test that currently expects a fresh ID per injection, and add a focused Vite/Rollup integration test that performs two unchanged content-hashed builds and compares emitted JavaScript filenames. Retain a test showing changed rendered code receives a different ID.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4512,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(rollup): inject chunk ids in-memory in renderChunk for SRI compatibility",
      "url": "https://github.com/PostHog/posthog-js/pull/4512",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\n`@posthog/rollup-plugin` shells out to `posthog-cli sourcemap process` in `writeBundle`, which rewrites the emitted `.js` files **on disk** (prepending the `_posthogChunkIds` IIFE). SRI plugins like `vite-plugin-sri3` compute integrity hashes earlier, in `generateBundle`, from the in-memory chunks — so the served files no longer match their hashes and the browser blocks every script:\r\n\r\n```\r\nFailed to find a valid digest in the 'integrity' attribute for resource '…/assets/index-*.js'\r\n```\r\n\r\nReported by a customer using Vite + `vite-plugin-sri3`; the site broke on every attempt to add the plugin. Plugin ordering cannot fix this — `generateBundle` always completes before any `writeBundle` runs (the previous ordering fix in #2943 only helps plugins that read `chunk.code` in a later `writeBundle`).\r\n\r\n## Fix\r\n\r\nI basically moved part of a CLI into our plugin utils so we are able to inject chunk ID during chunk rendering. Hash computes after that so we are good here.\r\n\r\nRollu…",
      "createdAt": "2026-08-12T18:38:32Z",
      "reason": "explicit-reference"
    },
    {
      "number": 4596,
      "type": "issue",
      "state": "closed",
      "title": "@posthog/rollup-plugin 1.4.9+ silently skips Vite 8 client sourcemaps after Oxc strips chunkId comments",
      "url": "https://github.com/PostHog/posthog-js/issues/4596",
      "labels": [
        "feature/error-tracking",
        "team/error-tracking"
      ],
      "bodyExcerpt": "## Bug description\n\n`@posthog/rollup-plugin` 1.4.9 introduced a regression for Vite 8 client builds using the default Oxc minifier.\n\nPR #4512 moved chunk-ID injection into `renderChunk`. It injects both the executable `_posthogChunkIds` IIFE and a trailing `//# chunkId=...` comment.\n\nVite 8 minifies client output after that hook. The executable IIFE survives, but Oxc removes the trailing comment. During `writeBundle`, the plugin only includes chunks where `determineChunkIdFromSource(chunk.code)` finds that comment. The detector intentionally ignores the IIFE.\n\nConsequently every client chunk is excluded, `filePaths` remains empty, and the build succeeds with:\n\n```text\nNo chunks found, skipping sourcemap processing for this stage. Your build may be multi-stage and this stage may not be relevant\n```\n\nNo client source maps are uploaded. SSR maps can still upload because Vite disables minification for server builds by default, making a multi-stage build appear partially successful.\n\nThis r…",
      "createdAt": "2026-08-21T12:29:37Z",
      "reason": "explicit-reference"
    },
    {
      "number": 4601,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(rollup): upload Vite 8 client sourcemaps",
      "url": "https://github.com/PostHog/posthog-js/pull/4601",
      "labels": [],
      "bodyExcerpt": "## Problem\n\nVite 8 uses Oxc to minify client output after the Rollup `renderChunk` hook. Oxc removes the `//# chunkId=` comment added by the PostHog plugin but keeps the runtime chunk ID code. The plugin then skips every client chunk in `writeBundle`, so the build succeeds without uploading client source maps.\n\nCloses #4596\n\n## Changes\n\nThe plugin now remembers each chunk ID during `renderChunk` and restores a missing chunk ID comment in an early `generateBundle` hook after output minification. It uses Rollup's preliminary filename and the tracked ID to avoid adding comments to prebuilt or unrelated chunks.\n\nThis also adds focused unit tests and a functional test that runs a minified Vite 8 client build with Oxc and verifies that the client chunk is passed to `posthog-cli sourcemap upload`.\n\n## Release info Sub-libraries affected\n\n### Libraries affected\n\n- [ ] All of them\n- [ ] posthog-js (web)\n- [ ] posthog-js-lite (web lite)\n- [ ] posthog-node\n- [ ] posthog-react-native\n- [ ] @postho…",
      "createdAt": "2026-08-22T19:19:11Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.09219858156028368,
    "canonical": {
      "number": 4596,
      "type": "issue",
      "state": "closed",
      "title": "@posthog/rollup-plugin 1.4.9+ silently skips Vite 8 client sourcemaps after Oxc strips chunkId comments",
      "url": "https://github.com/PostHog/posthog-js/issues/4596",
      "labels": [
        "feature/error-tracking",
        "team/error-tracking"
      ],
      "bodyExcerpt": "## Bug description\n\n`@posthog/rollup-plugin` 1.4.9 introduced a regression for Vite 8 client builds using the default Oxc minifier.\n\nPR #4512 moved chunk-ID injection into `renderChunk`. It injects both the executable `_posthogChunkIds` IIFE and a trailing `//# chunkId=...` comment.\n\nVite 8 minifies client output after that hook. The executable IIFE survives, but Oxc removes the trailing comment. During `writeBundle`, the plugin only includes chunks where `determineChunkIdFromSource(chunk.code)` finds that comment. The detector intentionally ignores the IIFE.\n\nConsequently every client chunk is excluded, `filePaths` remains empty, and the build succeeds with:\n\n```text\nNo chunks found, skipping sourcemap processing for this stage. Your build may be multi-stage and this stage may not be relevant\n```\n\nNo client source maps are uploaded. SSR maps can still upload because Vite disables minification for server builds by default, making a multi-stage build appear partially successful.\n\nThis r…",
      "createdAt": "2026-08-21T12:29:37Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 9%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "9475d40b75740563587b916bdcb09df71b491fde9f6b43f0c539fe0f10095d0f",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/d21e70d017379b0f42f5c8b7ade2cea1",
    "gistId": "d21e70d017379b0f42f5c8b7ade2cea1",
    "files": [
      {
        "name": "call-1-1-2026-08-28T13-30-25-083Z_01a04890-81bb-7467-8c1e-f6fd262c88b0.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/d21e70d017379b0f42f5c8b7ade2cea1/raw/f71127b1153a133f8e7238f2236fbba3f6fe564d/call-1-1-2026-08-28T13-30-25-083Z_01a04890-81bb-7467-8c1e-f6fd262c88b0.jsonl"
      }
    ]
  },
  "runId": 33175638432,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/33175638432"
}
```
