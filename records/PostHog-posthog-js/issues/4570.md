# issue 4570: Extract the three duplicated OTLP senders; logs drops batches on 5xx where metrics and traces retry

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4570
- Conclusion: Confirmed bug: logs drops batches after exhausted retryable HTTP failures, unlike metrics.
- Labels: team/client-libraries
- PR: https://github.com/PostHog/posthog-js/pull/4575
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/32303605131
- Updated: 2026-08-19T21:30:20.668Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug: logs drops batches after exhausted retryable HTTP failures, unlike metrics.",
    "summary": "`_sendLogsBatch` classifies only network failures as `retry-later`; exhausted 408, 429, and 5xx failures become `fatal` and the logs queue advances, dropping those records.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug: Something isn't working",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed in the current core implementation. `_sendLogsBatch` uses `isPostHogFetchRetryableError` for `fetchWithRetry`, but its final outcome mapping only treats `PostHogFetchNetworkError` as `retry-later`. Therefore, once internal retries are exhausted, HTTP 408/429/5xx failures are returned as `fatal`; `PostHogLogs` advances its persisted queue for fatal outcomes and drops the records.\n\n`_sendMetricsBatch` already maps the same retryable predicate to `retry-later`, and merges the unsent window back for a later flush. The smallest safe fix is to make the logs outcome mapping use the same predicate and add focused regression coverage for exhausted 429 and 5xx responses (while preserving 413 as `too-large` and non-retryable 4xx as `fatal`).\n\nOne correction to the report: the current tree has two OTLP sender methods, logs and metrics; there is no `_sendTracesBatch` or traces OTLP payload implementation to extract. Since the behavior fix is small and verifiable, a broader sender abstraction should be evaluated separately rather than coupled to this data-retention fix.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/posthog-core-stateless.ts",
        "packages/core/src/logs/index.ts",
        "packages/core/src/metrics/index.ts",
        "packages/core/src/__tests__/posthog.flush.spec.ts",
        "packages/core/src/logs/types.ts",
        "packages/core/src/metrics/types.ts"
      ],
      "findings": [
        "`isPostHogFetchRetryableError` returns true for network errors and HTTP 408, 429, or statuses >=500 in `packages/core/src/posthog-core-stateless.ts`.",
        "Both logs and metrics pass that predicate to `fetchWithRetry`, excluding 413 from internal retries.",
        "After a send failure, `_sendLogsBatch` returns `retry-later` only for `PostHogFetchNetworkError`; retryable HTTP errors therefore fall through to `fatal`.",
        "`_sendMetricsBatch` instead returns `retry-later` for every `isPostHogFetchRetryableError` result, explicitly including exhausted 408/429/5xx failures.",
        "`PostHogLogs._doFlush` preserves queued records only for `retry-later`; it persists queue advancement before throwing for `fatal`, so the logs classification causes data loss.",
        "`PostHogMetrics._doFlush` merges a `retry-later` window back and re-arms its flush timer.",
        "The current repository contains `_sendLogsBatch` and `_sendMetricsBatch`, but no `_sendTracesBatch`, OTLP traces endpoint, or OTLP traces payload type."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The divergent outcome classification is directly evidenced and can be corrected with a one-condition change plus narrow sender-level regression tests. A generalized extraction is not required to fix the loss and is unsupported by the current two-sender implementation.",
      "suggestedApproach": "Change `_sendLogsBatch` to return `retry-later` when `isPostHogFetchRetryableError(err)` is true, matching metrics. Add focused tests using exhausted 429 and 5xx responses, and retain assertions that 413 maps to `too-large` and a non-retryable 4xx maps to `fatal`.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4571,
      "type": "issue",
      "state": "open",
      "title": "Logs OTLP encoder emits int64 as a JSON number instead of a string",
      "url": "https://github.com/PostHog/posthog-js/issues/4571",
      "labels": [],
      "bodyExcerpt": "`toOtlpAnyValue` in `packages/core/src/logs/logs-utils.ts` encodes integers as a JSON number:\n\n```ts\nif (Number.isInteger(value)) {\n  return { intValue: value }\n}\n```\n\nOTLP/HTTP rides the proto3 JSON mapping, which requires int64 fields to be **strings**. The canonical form is `{ \"intValue\": \"42\" }`, not `{ \"intValue\": 42 }`.\n\n## Consequences\n\n1. **Precision loss above 2^53.** A JS integer larger than `Number.MAX_SAFE_INTEGER` serializes to a JSON number that cannot round-trip — the receiver reads a different value than the caller set.\n2. **No int64 range guard.** An integer outside `[-2^63, 2^63-1]` is emitted as `intValue` regardless. A value the service can't fit into a signed 64-bit field risks rejecting the request, which for a batched endpoint takes every other record in the same request with it.\n3. **Strict parsers.** Any consumer validating against the proto3 JSON mapping is entitled to reject a numeric `intValue`.\n\nThe same function also flattens objects to a JSON string rathe…",
      "createdAt": "2026-08-19T18:00:11Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "375105a1be7275c69867545d4c09637a076f1a12351d8be0fcd4c0bd55b66fb7",
  "piCalls": 3,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/a69e3d182381174afb30bd4a8bef2bd6",
    "gistId": "a69e3d182381174afb30bd4a8bef2bd6",
    "files": [
      {
        "name": "call-1-1-2026-08-19T21-28-17-357Z_01a01bec-c6cd-71af-917c-574738cf06dc.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/a69e3d182381174afb30bd4a8bef2bd6/raw/1f0f1bb8bb162d6aa4f10a1ad0df8da90d5b6c22/call-1-1-2026-08-19T21-28-17-357Z_01a01bec-c6cd-71af-917c-574738cf06dc.jsonl"
      }
    ]
  },
  "runId": 32303605131,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/32303605131"
}
```
