# issue 4174: Re-translate displayed survey on language change

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4174
- Conclusion: Valid browser SDK surveys enhancement; active surveys are not re-translated after display language changes.
- Labels: enhancement, feature/surveys, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29564841496
- Updated: 2026-07-17T08:04:03.947Z

```json
{
  "triage": {
    "conclusion": "Valid browser SDK surveys enhancement; active surveys are not re-translated after display language changes.",
    "summary": "Survey translation is resolved when rendering starts, but active popovers/widgets are skipped by the automatic display loop, so a language change while displayed does not update the UI or later survey language metadata.",
    "issueType": "feature",
    "confidence": 0.89,
    "labels": [
      "enhancement",
      "feature/surveys",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the browser SDK code. `detectUserLanguage()` already resolves `override_display_language` → stored person property `language` → browser language, and SurveyManager applies translation at render entry points. However, once a popover is focused the display loop will not call `handlePopoverSurvey()` again, and widgets return early once their shadow container exists. I also found no current `languagechange` listener or person-property-change event hook for surveys.\n\nA focused fix looks reasonable, but should be careful to avoid resetting in-progress answers or duplicating `survey shown` events. Suggested scope: track the matched rendered language per active survey, re-run translation during the existing survey evaluation loop and on `window.languagechange`, and re-render the active popover/widget only when the matched language changes. Add tests for active popover and widget updates, preserving response state, and `$survey_language` on subsequent sent/dismissed events.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/surveys.tsx",
        "packages/browser/src/utils/survey-translations.ts",
        "packages/core/src/surveys/translations.ts",
        "packages/browser/src/extensions/surveys/surveys-extension-utils.tsx",
        "packages/browser/src/posthog-featureflags.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-surveys.ts",
        "packages/browser/src/__tests__/utils/survey-translations.test.ts"
      ],
      "findings": [
        "`packages/browser/src/utils/survey-translations.ts` implements `detectUserLanguage()` with the documented priority order: `override_display_language`, stored person property `language`, then browser language; the same file has a TODO for dynamic language change detection and mid-session re-rendering.",
        "`packages/core/src/surveys/translations.ts` applies survey-level and question-level translations and returns the matched translation key, so the translation helper can support comparing whether the active matched language changed.",
        "`packages/browser/src/extensions/surveys.tsx` calls `_translateSurveyForRendering()` in `handlePopoverSurvey`, `_handleWidget`, `renderPopover`, and `renderSurvey`, confirming translation is currently tied to render attempts.",
        "`callSurveysAndEvaluateDisplayLogic()` only calls `handlePopoverSurvey()` for popovers when `_surveyInFocus` is null, so an already displayed popover is not re-rendered by the 1-second survey evaluation loop.",
        "`_handleWidget()` translates the survey, calls `retrieveSurveyShadow()`, and returns immediately when the widget container already exists, so mounted widgets are not re-rendered with newly resolved translations.",
        "`generateSurveys()` starts a 1-second interval that re-evaluates survey display logic, but there is no current `languagechange` listener in the inspected browser source.",
        "`posthog-featureflags.ts` `setPersonPropertiesForFlags()` updates stored person properties and may reload flags, but does not emit a survey-specific language/person-property change notification.",
        "`posthog-core.ts` public `on()` currently documents only `eventCaptured` and `featureFlagsReloading`, so there is not an obvious existing public person-property-change subscription for surveys to use.",
        "`packages/browser/src/extensions/surveys/surveys-extension-utils.tsx` includes `surveyLanguage` in survey sent/dismissed/in-progress event paths, meaning the rendered component's `surveyLanguage` prop controls later `$survey_language` metadata.",
        "`packages/browser/src/__tests__/utils/survey-translations.test.ts` covers static language detection and translation application, but the inspected tests do not cover re-translating an already displayed survey."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small PR is appropriate because the root behavior is localized to SurveyManager render gating and the existing translation helper already returns the matched language. The main risk is lifecycle/state handling, not API design.",
      "suggestedApproach": "In `SurveyManager`, track the last rendered matched language for active survey IDs. During the existing display evaluation loop, if a focused popover or mounted widget is still active, re-run `_translateSurveyForRendering()` and re-render into the existing shadow root only when the matched language changes. Add a `window.languagechange` listener to trigger the same evaluation promptly. Ensure re-rendering does not emit another `survey shown` event and preserves in-progress responses. Add focused tests for active popover and widget translation updates and subsequent `$survey_language` values.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4175,
      "type": "issue",
      "state": "open",
      "title": "Re-translate displayed survey on language change for React Native",
      "url": "https://github.com/PostHog/posthog-js/issues/4175",
      "labels": [
        "enhancement",
        "feature/surveys",
        "react-native",
        "feature/mobile"
      ],
      "bodyExcerpt": "## Summary\n\nThe React Native SDK applies a survey's translation once per activated survey. If the `language` person property changes while a survey is on screen, the displayed survey keeps the old language, and the new one only applies to the next survey that activates.\n\n## Current State\n\n- The translation is computed in a `useMemo` keyed on `[activeSurvey, posthog]` in `packages/react-native/src/surveys/PostHogSurveyProvider.tsx`. The language sources (`overrideDisplayLanguage` → person property `language` → device `$locale`) are read inside the memo but aren't dependencies, so a person-property change never invalidates it.\n- There's no person-property or identify listener in the surveys code that re-runs translation. Flag reloads re-render the provider, but the memo returns the cached translation.\n\n## Expected Behavior\n\nWhen the resolved display language changes while a survey is displayed, the SDK should re-resolve the translation (shared logic in `packages/core/src/surveys/translat…",
      "createdAt": "2026-07-17T07:58:42Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "7bc4a520018262526355628e417e920da87ffe8bfc16de668f22c9408a42c6e0",
  "piCalls": 2,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/89e2e76af19a3d9fe2ad03fe0b763766",
    "gistId": "89e2e76af19a3d9fe2ad03fe0b763766",
    "files": [
      {
        "name": "call-2-1-2026-07-17T08-02-00-380Z_019f6f18-be3c-7e34-920e-84bfb3139085.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/89e2e76af19a3d9fe2ad03fe0b763766/raw/94fe8cc513737fed3627bfb4018a5f671affd50a/call-2-1-2026-07-17T08-02-00-380Z_019f6f18-be3c-7e34-920e-84bfb3139085.jsonl"
      }
    ]
  },
  "runId": 29564841496,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29564841496"
}
```
