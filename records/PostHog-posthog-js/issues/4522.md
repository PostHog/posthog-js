# issue 4522: Session replay: uncaught TypeError on stop() when observe() returned undefined after a swallowed observer-init error — recorder is then permanently broken for the session

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4522
- Conclusion: Confirmed session-replay teardown bug in the bundled rrweb recorder.
- Labels: feature/replay, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/31784004973
- Updated: 2026-08-14T08:29:42.037Z

```json
{
  "triage": {
    "conclusion": "Confirmed session-replay teardown bug in the bundled rrweb recorder.",
    "summary": "A swallowed rrweb observer-initialization error can make callbackWrapper return undefined, which is stored as a teardown handler. Stopping recording then calls that value and throws, preventing normal cleanup and breaking later recorder restarts.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report — the reported failure mode is confirmed by the current source.\n\n`record()` wraps `initObservers()` in `callbackWrapper()`, and a recorder-owned error that the configured handler swallows makes that wrapper return `undefined`. The result is inserted into `handlers` for both the main document and iframe load path. The returned stop function then passes every entry to `callSafely()`, which unconditionally invokes it, so teardown throws before it reaches the remaining recorder cleanup.\n\nA fix should defensively preserve the teardown-handler invariant at both insertion sites and in `callSafely()`. It should also make observer initialization failure-safe where practical: simply skipping the invalid cleanup lets the outer recorder stop, but a late initialization failure may already have installed earlier observers. Please add a regression test that forces a late observer-init failure with an error handler returning `true`, verifies `stop()` does not throw, verifies a subsequent start/stop succeeds, and checks that already-created observer resources are not retained.\n\nThis changes code shipped in the lazy recorder bundle, so it needs the normal lazy-bundle compatibility coverage. It should not intentionally change rotation or recording-volume semantics, but the existing forced-idle/session-rotation scenarios should be run because those paths invoke `stop()`.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts",
        "packages/rrweb/rrweb/src/record/index.ts",
        "packages/rrweb/rrweb/src/record/error-handler.ts",
        "packages/rrweb/rrweb/src/record/observer.ts",
        "packages/rrweb/rrweb/src/utils.ts",
        "packages/rrweb/rrweb/test/record/error-handler.test.ts",
        "packages/rrweb/README.md"
      ],
      "findings": [
        "LazyLoadedSessionRecording supplies an error handler that returns true only for context === 'rrweb', intentionally swallowing recorder-owned exceptions while preserving host-operation exceptions.",
        "callbackWrapper() returns undefined when its callback throws and the registered handler returns true, despite being cast back to the original callback type.",
        "record/index.ts pushes observe(document) directly into handlers and likewise pushes the iframe observe result into both handlers and iframeObserverCleanups without a runtime function check.",
        "The record stop closure runs handlers.forEach((h) => callSafely(h)), while callSafely() immediately executes fn() and only suppresses SecurityError. A runtime undefined handler therefore throws TypeError and aborts the rest of teardown.",
        "initObservers() initializes observers sequentially and returns its aggregate cleanup only after all setup completes. If a later initializer throws, cleanups for earlier successfully initialized observers are not returned to record(), so insertion/call guards alone do not establish complete cleanup.",
        "Current tests verify that the lazy recorder configures an error handler and that callbackWrapper swallows errors when its handler returns true, but no inspected test exercises a swallowed initObservers failure followed by stop and restart.",
        "Replay incident review: this is a lazy-loaded bundle path, but the proposed repair need not change the core/extension API or persisted-state shape. It also reaches forced-idle and session-rotation stop paths; no recording-volume change is intended, but those scenario tests should protect that invariant."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The immediate uncaught TypeError has a small defensive fix, but only filtering/ignoring non-function handlers can leave observers installed before a late initObservers failure. A safe repair needs a focused regression test and exception-safe cleanup for successfully initialized observer resources, without broadening the error-swallowing policy.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "f12cc20193ec171c06cf3c07dc83d374b6ce5a08bc96ed6a188b840b2691c156",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/e72d142d232c924be7e48debcc8e3022",
    "gistId": "e72d142d232c924be7e48debcc8e3022",
    "files": [
      {
        "name": "call-1-1-2026-08-14T08-28-40-192Z_019fff63-3780-7a47-b0f4-b72d815e5e6c.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/e72d142d232c924be7e48debcc8e3022/raw/88b14a35822a66303544f9e0c5c1d9351206d2dd/call-1-1-2026-08-14T08-28-40-192Z_019fff63-3780-7a47-b0f4-b72d815e5e6c.jsonl"
      }
    ]
  },
  "runId": 31784004973,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/31784004973"
}
```
