# issue 3573: register_for_session properties persist across PostHog sessions (new $session_id) within same browser tab

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3573
- Conclusion: Valid bug: `register_for_session` is currently scoped to browser `sessionStorage`, not to PostHog `$session_id` rotation.
- Labels: feature/product-analytics, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28363800378
- Updated: 2026-06-29T09:59:27.593Z

```json
{
  "triage": {
    "conclusion": "Valid bug: `register_for_session` is currently scoped to browser `sessionStorage`, not to PostHog `$session_id` rotation.",
    "summary": "Session super properties registered via `posthog.register_for_session()` persist in the same tab after the PostHog session ID changes, so later events in a new `$session_id` still include old session properties.",
    "issueType": "bug",
    "confidence": 0.86,
    "labels": [
      "bug: Something isn't working",
      "feature/product-analytics",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the report. I verified the current browser SDK behavior matches this: `register_for_session` writes directly to `sessionPersistence`, which is backed by browser `sessionStorage`, while `$session_id` rotation is managed separately by `SessionIdManager`. The internal session-entry attribution path already listens for session ID changes, but user/session super properties do not. A fix should avoid clearing all of `sessionPersistence` blindly, because that store also contains other SDK-managed values; it should track and clear only properties registered via `register_for_session` when an existing PostHog session rotates.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-persistence.ts",
        "packages/browser/src/sessionid.ts",
        "packages/browser/src/session-props.ts",
        "packages/browser/src/page-view.ts",
        "packages/browser/src/persistence-key-policy.ts",
        "packages/browser/src/__tests__/session-props.test.ts"
      ],
      "findings": [
        "`PostHog.register_for_session()` in `packages/browser/src/posthog-core.ts` only calls `this.sessionPersistence?.register(properties)`; there is no association with the current `$session_id`.",
        "`sessionPersistence` is created as a `PostHogPersistence` instance backed by `sessionStorage` for normal browser persistence, so its lifetime is the browser tab/sessionStorage lifetime.",
        "`calculateEventProperties()` merges `this.sessionPersistence.properties()` into every event after checking/updating the PostHog session ID, so old session super properties continue to be sent unless explicitly removed.",
        "`SessionIdManager` emits `onSessionId` callbacks when session IDs are generated or rotated, including idle-timeout and max-length rotations.",
        "`SessionPropsManager` already uses `onSessionId` to refresh internal `$client_session_props` when the session ID changes, confirming there is an existing mechanism for PostHog-session-scoped state.",
        "There is no analogous callback or cleanup path for properties registered via `register_for_session`.",
        "A naive `sessionPersistence.clear()` on session rotation would be risky because the same store also contains SDK-managed campaign/referrer/search/replay-related properties."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The bug is clear, but a safe small PR needs careful scoping: clearing all session persistence would remove unrelated SDK-managed state, while clearing on every `noSessionId` callback could also remove properties registered before the first event. The fix should define and test exactly which registered keys are tied to a PostHog session.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "4f44db3a16fd99e0b8828f23da202f7b70eeeac653faa459c7e62ea5b57ec3d5",
  "piCalls": 1,
  "runId": 28363800378,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28363800378"
}
```
