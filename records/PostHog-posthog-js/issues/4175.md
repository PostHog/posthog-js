# issue 4175: Re-translate displayed survey on language change for React Native

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4175
- Conclusion: Valid React Native surveys enhancement; active survey translation is cached across language changes.
- Labels: enhancement, feature/surveys, react-native, feature/mobile
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29564841496
- Updated: 2026-07-17T08:01:40.038Z

```json
{
  "triage": {
    "conclusion": "Valid React Native surveys enhancement; active survey translation is cached across language changes.",
    "summary": "React Native survey UI resolves translations once for the active survey, so changing the resolved display language while the modal is open does not update the displayed copy or subsequent survey language metadata.",
    "issueType": "feature",
    "confidence": 0.91,
    "labels": [
      "enhancement",
      "feature/surveys",
      "react-native",
      "feature/mobile"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the code path. `PostHogSurveyProvider` memoizes `translatedActiveSurvey` only on `[activeSurvey, posthog]`, while `applySurveyTranslationForUser` reads mutable language sources from the client. A feature flag reload can re-render the provider after person properties change, but the memo can still return the old translated survey if the active survey/client are unchanged.\n\nThis looks suitable for a small RN surveys fix: introduce a resolved display-language signal in the provider, update it when relevant PostHog/person-property changes occur, include it in the translation memo, and add a provider/modal test proving visible copy and `$survey_language` update for the active survey.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/surveys/PostHogSurveyProvider.tsx",
        "packages/react-native/src/surveys/survey-translations.ts",
        "packages/core/src/surveys/translations.ts",
        "packages/react-native/src/posthog-rn.ts",
        "packages/core/src/posthog-core.ts",
        "packages/core/src/eventemitter.ts",
        "packages/react-native/src/hooks/useFeatureFlags.ts",
        "packages/react-native/src/surveys/components/Surveys.tsx",
        "packages/react-native/test/surveyTranslations.spec.ts",
        "packages/react-native/test/SurveyModal.spec.tsx"
      ],
      "findings": [
        "`packages/react-native/src/surveys/PostHogSurveyProvider.tsx` computes `translatedActiveSurvey` with `useMemo(..., [activeSurvey, posthog])`, so changes to language sources alone do not invalidate the translation.",
        "`packages/react-native/src/surveys/survey-translations.ts` resolves language from `overrideDisplayLanguage`, persisted `PostHogPersistedProperty.PersonProperties.language`, then `getCommonEventProperties().$locale` before calling shared core translation logic.",
        "`packages/core/src/surveys/translations.ts` already contains the shared matching and merge logic, including exact/base language matching and returning the matched translation key.",
        "`packages/react-native/src/hooks/useFeatureFlags.ts` subscribes to feature flag changes and can re-render the survey provider, but the current translation memo can remain cached when `activeSurvey` and `posthog` identities are unchanged.",
        "`packages/core/src/posthog-core.ts` persists person properties through `setPersonPropertiesForFlags`, and React Native `identify` in `packages/react-native/src/posthog-rn.ts` also caches identify properties for flag evaluation; these are the stored properties read by survey language detection.",
        "`packages/core/src/eventemitter.ts` supports SDK event subscriptions, but the inspected survey provider does not subscribe to identify/person-property-related events.",
        "`packages/react-native/src/surveys/components/Surveys.tsx` sends `survey shown`, `survey sent`, and `survey dismissed` with the `surveyLanguage` value passed from `PostHogSurveyProvider`, so a stale translated survey can also produce stale `$survey_language` metadata."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The product behavior and shared translation logic already exist; the missing piece is invalidating/recomputing the active React Native survey translation when the resolved display language changes.",
      "suggestedApproach": "In `PostHogSurveyProvider`, track or compute the resolved display language separately from `activeSurvey`, update that signal on relevant PostHog changes, and include it in the `translatedActiveSurvey` memo dependencies. Re-run `applySurveyTranslationForUser` for the same active survey only when the resolved language changes, preserving active survey state. Add a React Native provider/modal test that changes the persisted `language` person property while a survey is visible and asserts the rendered question text plus subsequent `$survey_language` match the new translation.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4174,
      "type": "issue",
      "state": "open",
      "title": "Re-translate displayed survey on language change",
      "url": "https://github.com/PostHog/posthog-js/issues/4174",
      "labels": [
        "enhancement",
        "feature/surveys"
      ],
      "bodyExcerpt": "## Summary\n\nThe browser SDK resolves a survey's translation on every render attempt, but never re-translates a survey that's already on screen. If the `language` person property (or the browser language) changes while a popover or widget is displayed, the survey stays in the old language until the next fresh render.\n\n## Current State\n\n- Language resolution runs per render attempt in `_translateSurveyForRendering` (`packages/browser/src/extensions/surveys.tsx`): `override_display_language` → person property `language` → `navigator.language`.\n- Once a popover is shown the poll loop skips it (`_surveyInFocus` guard), and `_handleWidget` early-returns when the widget is already mounted, so a displayed survey is never re-resolved.\n- The gap is already called out as a TODO in `packages/browser/src/utils/survey-translations.ts`: \"Consider adding dynamic language change detection in the future … Re-render survey when language changes mid-session.\"\n\n## Expected Behavior\n\nWhen the resolved displ…",
      "createdAt": "2026-07-17T07:58:41Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.25,
    "canonical": {
      "number": 4174,
      "type": "issue",
      "state": "open",
      "title": "Re-translate displayed survey on language change",
      "url": "https://github.com/PostHog/posthog-js/issues/4174",
      "labels": [
        "enhancement",
        "feature/surveys"
      ],
      "bodyExcerpt": "## Summary\n\nThe browser SDK resolves a survey's translation on every render attempt, but never re-translates a survey that's already on screen. If the `language` person property (or the browser language) changes while a popover or widget is displayed, the survey stays in the old language until the next fresh render.\n\n## Current State\n\n- Language resolution runs per render attempt in `_translateSurveyForRendering` (`packages/browser/src/extensions/surveys.tsx`): `override_display_language` → person property `language` → `navigator.language`.\n- Once a popover is shown the poll loop skips it (`_surveyInFocus` guard), and `_handleWidget` early-returns when the widget is already mounted, so a displayed survey is never re-resolved.\n- The gap is already called out as a TODO in `packages/browser/src/utils/survey-translations.ts`: \"Consider adding dynamic language change detection in the future … Re-render survey when language changes mid-session.\"\n\n## Expected Behavior\n\nWhen the resolved displ…",
      "createdAt": "2026-07-17T07:58:41Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 25%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "0fe8b451363b761d6a768a0efcf7e41b999dc03e65a0ff2575093b197fd47a7a",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/f09acfceae9525adcf276a19840cfae1",
    "gistId": "f09acfceae9525adcf276a19840cfae1",
    "files": [
      {
        "name": "call-1-1-2026-07-17T07-59-41-927Z_019f6f16-a167-7d2a-a2cc-7238df0b83e8.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/f09acfceae9525adcf276a19840cfae1/raw/734fcbe39fb83fae08b076bd657841cd0694eed0/call-1-1-2026-07-17T07-59-41-927Z_019f6f16-a167-7d2a-a2cc-7238df0b83e8.jsonl"
      }
    ]
  },
  "runId": 29564841496,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29564841496"
}
```
