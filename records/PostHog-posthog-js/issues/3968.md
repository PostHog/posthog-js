# issue 3968: @posthog/nuxt missing @nuxt/schema type augmentation for posthogConfig in NuxtConfig

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3968
- Conclusion: Valid Nuxt typing bug: posthogConfig is not registered in Nuxt schema types.
- Labels: nuxt
- PR: https://github.com/PostHog/posthog-js/pull/3969
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28159743689
- Updated: 2026-06-25T09:19:13.341Z

```json
{
  "triage": {
    "conclusion": "Valid Nuxt typing bug: posthogConfig is not registered in Nuxt schema types.",
    "summary": "@posthog/nuxt defines and uses the `posthogConfig` Nuxt module config key, and the example Nuxt app uses it, but the package source does not augment `@nuxt/schema` so TypeScript can reject `posthogConfig` in `nuxt.config.ts`.",
    "issueType": "bug",
    "confidence": 0.88,
    "labels": [
      "bug: Something isn't working",
      "nuxt"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. This looks valid from the repository source: `@posthog/nuxt` declares `configKey: 'posthogConfig'` and exports `ModuleOptions`, but I could not find an `@nuxt/schema` augmentation for `NuxtConfig`/`NuxtOptions`. The Nuxt example also uses top-level `posthogConfig`, so adding the module augmentation to the emitted types should be a small, type-only fix.",
    "investigation": {
      "relevantFiles": [
        "packages/nuxt/src/module.ts",
        "packages/nuxt/package.json",
        "examples/example-nuxt/nuxt.config.ts"
      ],
      "findings": [
        "`packages/nuxt/src/module.ts` exports `ModuleOptions` and calls `defineNuxtModule<ModuleOptions>` with `meta.configKey: 'posthogConfig'`.",
        "A repository search found no `declare module '@nuxt/schema'` augmentation for `posthogConfig`.",
        "`examples/example-nuxt/nuxt.config.ts` uses a top-level `posthogConfig` key, matching the reported usage pattern.",
        "`packages/nuxt/package.json` exports generated package types via `./dist/types.d.mts`, so the augmentation should be added in source so it is emitted during the module build."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The change appears to be type-only and localized to the Nuxt package. It should not affect runtime behavior.",
      "suggestedApproach": "Add an ambient `declare module '@nuxt/schema'` block in `packages/nuxt/src/module.ts` near `ModuleOptions`, extending `NuxtConfig` with `posthogConfig?: Partial<ModuleOptions>` and `NuxtOptions` with the resolved `posthogConfig` type. If practical, verify with the Nuxt example/typecheck.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "10c3e592f78889ad760661b1045179baebe2601e3803a64ce9906ad0e74605b6",
  "piCalls": 3,
  "runId": 28159743689,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28159743689"
}
```
