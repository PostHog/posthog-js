# issue 3359: React Native: multiple clients can rotate $session_id twice on foreground, orphaning session replay

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3359
- Conclusion: Valid React Native multi-client session replay/session-id race.
- Labels: bug, feature/replay, react-native, Android, iOS
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28317863973
- Updated: 2026-06-28T09:30:04.533Z

```json
{
  "triage": {
    "conclusion": "Valid React Native multi-client session replay/session-id race.",
    "summary": "Multiple React Native PostHog clients can each handle AppState active, rotate an expired shared session from stale per-instance storage state, and cause native session replay to bind to a different session id than later analytics events.",
    "issueType": "bug",
    "confidence": 0.88,
    "labels": [
      "bug",
      "feature/replay",
      "react-native",
      "Android",
      "iOS"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks, this looks valid from the current RN code. Each `PostHog` instance installs its own AppState listener and calls `getSessionId()` on `active`; session state is read from a per-instance RN storage cache, while the native replay plugin is module-scoped. That makes it plausible for two clients sharing the same persisted file to rotate independently from stale in-memory state and rebind replay twice. A correct fix should make session rotation atomic/shared across RN clients and include a regression test with two clients sharing the same storage and both foreground handlers firing after expiry. I would avoid a simple re-read-before-rotate-only fix because the check/write still needs to be serialized.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/posthog-rn.ts",
        "packages/react-native/src/storage.ts",
        "packages/core/src/posthog-core.ts",
        "packages/react-native/src/optional/OptionalPlugin.ts",
        "packages/react-native/test/posthog.spec.ts",
        "packages/react-native/test/session-replay-event-triggers.spec.ts"
      ],
      "findings": [
        "The RN constructor registers an `AppState.addEventListener('change', ...)` per `PostHog` instance and calls `this.getSessionId()` whenever state becomes `active`.",
        "`PostHogCore.getSessionId()` rotates when the session is missing, expired, or over max length, then writes `SessionId`, `SessionStartTimestamp`, and `SessionLastTimestamp` via `setPersistedProperty`; there is no atomic compare/update.",
        "`PostHogRNStorage` keeps a per-storage-instance `memoryCache`; `getItem()` reads only that cache and `setItem()` mutates that cache before a debounced persist to the shared `.posthog-rn.json` file. Separate clients can therefore have stale session state.",
        "RN `getSessionId()` overrides core behavior to call `OptionalReactNativePlugin.endSession()` and `startSession(sessionId)` when the returned session id differs from the instance-local `_currentSessionId`.",
        "`OptionalReactNativePlugin` is a module-level optional native plugin reference, so multiple JS clients can affect the same native replay instance.",
        "Existing tests cover single-client session rotation and replay re-arm behavior, but I did not find a multi-client shared-storage AppState regression test."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A safe fix needs shared/serialized session rotation semantics across RN clients and storage caches. The obvious fresh-read change is not sufficient without atomic check-and-write or a process-level lock/shared session manager, and `getSessionId()` is currently synchronous while custom storage may be async.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "4c9440b0ad39f89752b52f4954d5639547b28a75a13c340e89b7f4530243159d",
  "piCalls": 1,
  "runId": 28317863973,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28317863973"
}
```
