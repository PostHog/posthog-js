# issue 4968: Canvas recording enabled, but rrweb emits zero canvas mutations (posthog-js 1.428.6)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4968
- Conclusion: Likely a canvas FPS capture compatibility/failure-path issue, distinct from a playback-only canvas replacement fix.
- Labels: feature/replay, javascript
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/34969842145
- Updated: 2026-09-15T12:38:19.796Z

```json
{
  "triage": {
    "conclusion": "Likely a canvas FPS capture compatibility/failure-path issue, distinct from a playback-only canvas replacement fix.",
    "summary": "With server-side canvas FPS set to 3, the SDK selects rrweb's sampled-frame canvas pipeline. The report's zero source-9 events is consistent with that pipeline producing no frames; the exact cause requires the affected browser/version and worker/CSP diagnostics.",
    "issueType": "bug",
    "confidence": 0.87,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "javascript"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "Thanks for the detailed isolation work. The absence of `type: 3, source: 9` events is a recording-side problem, so it would be separate from a change that only fixes applying already-recorded canvas pixels during playback.\n\nThere is a relevant implementation detail: the remote `canvasFps: 3` setting makes posthog-js use rrweb's sampled-frame canvas path rather than the per-2D-call observer. That path requires `OffscreenCanvas` and an inline worker; it intentionally emits no frames when `OffscreenCanvas` is unavailable, and it can also stop without affecting DOM recording if the worker cannot load. The repository also currently skips its browser test for this path in headless WebKit because it emits no canvas mutations there despite the relevant primitives being present.\n\nCould you provide:\n\n1. The browser name and exact version for an affected recording (and whether it is Safari/WebKit or an in-app webview).\n2. The exact direct rrweb `record(...)` invocation, especially its `sampling.canvas` value, plus a minimal standalone HTML reproduction if possible.\n3. Output of `typeof OffscreenCanvas`, `typeof createImageBitmap`, and `typeof Worker` on the affected page.\n4. Any console CSP/worker errors involving `blob:` URLs or the recorder worker, and the page's `worker-src` / `script-src` CSP directives.\n5. Whether the full snapshot contains `rr_dataURL` on the canvas nodes. Counting source-9 events alone does not show whether the initial canvas still was captured.\n\nWe should reproduce this in the reported browser before proposing a fallback or changing the capture path: such a change affects recorder performance, recording volume, and canvas-pixel handling across all customers. There is no ETA determination possible from this SDK repository alone.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/rrweb/rrweb/src/record/observers/canvas/canvas-manager.ts",
        "packages/rrweb/rrweb/src/record/observers/canvas/2d.ts",
        "packages/rrweb/rrweb-snapshot/src/snapshot.ts",
        "packages/rrweb/types/src/index.ts",
        "packages/browser/playwright/mocked/session-recording/canvas-resolution.spec.ts",
        "packages/browser/CHANGELOG.md",
        ".agents/skills/replay-incident-risk/INCIDENTS.md"
      ],
      "findings": [
        "The SDK converts enabled remote canvas configuration into `recordCanvas: true` and `sampling.canvas: canvasFps`; the reported `canvasFps: 3` therefore selects rrweb's numeric FPS snapshot path.",
        "The numeric FPS path returns immediately, with no canvas mutation observer or frame loop, when `OffscreenCanvas` is absent. rrweb's public type documentation explicitly says numeric canvas sampling is supported only where `OffscreenCanvas` is supported.",
        "The same FPS path creates an inline encoding worker and silently disables canvas snapshotting if worker construction fails or its error handler fires. DOM recording is independent of this path.",
        "The browser Playwright canvas-resolution test documents a pre-existing case where the FPS pipeline produces no canvas mutations under headless WebKit even though `OffscreenCanvas`, image-bitmap operations, and the worker are present; the test is skipped there.",
        "The repository changelog records an intentional behavior change to skip FPS canvas recording on browsers without `OffscreenCanvas` support, specifically Safari before 16.4.",
        "Initial full snapshots can serialize a nonblank 2D canvas as `rr_dataURL`; source-9 incremental events are not the only possible recorded canvas pixels.",
        "The direct rrweb control is not yet independently reproducible from the issue because its complete options, browser, timing, and CSP are absent. If it used numeric sampling, it exercises the same FPS path; if it truly used default sampling, it should instead exercise the 2D method-patching observer."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A safe fix depends on distinguishing unsupported WebKit behavior, a blocked/failed inline worker, and a regression in the default per-call observer. A fallback between capture modes would change fleet-wide replay CPU, payload volume, and canvas-pixel capture behavior, so it needs a real-browser reproduction and regression test first.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.95,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "df39fee3b925a42bd5595e76fec421bce817d6f7af73a00059e185e806131ec2",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/fb676c1d6209952357cb9f44efa4dada",
    "gistId": "fb676c1d6209952357cb9f44efa4dada",
    "files": [
      {
        "name": "call-1-1-2026-09-15T12-37-10-224Z_01a0a512-39d0-7221-9b64-f9148bbb4411.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/fb676c1d6209952357cb9f44efa4dada/raw/cfd1052047c5c940db3616dbc1457f5d2e9fa85f/call-1-1-2026-09-15T12-37-10-224Z_01a0a512-39d0-7221-9b64-f9148bbb4411.jsonl"
      }
    ]
  },
  "runId": 34969842145,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/34969842145"
}
```
