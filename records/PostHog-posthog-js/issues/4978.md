# issue 4978: Feature Request: React Native - support bootstrapping an existing session ID

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4978
- Conclusion: Confirmed React Native feature gap; initialization-time session bootstrap is plausible, but reset parity and runtime session replacement require separate public-API and replay-lifecycle decisions.
- Labels: enhancement, feature, feature/replay, feature/mobile, react-native, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/35036969954
- Updated: 2026-09-15T23:46:34.671Z

```json
{
  "triage": {
    "conclusion": "Confirmed React Native feature gap; initialization-time session bootstrap is plausible, but reset parity and runtime session replacement require separate public-API and replay-lifecycle decisions.",
    "summary": "React Native inherits a core bootstrap type that lacks sessionID and has no path to validate, persist, and synchronize a supplied session ID with native replay. Caller-supplied $session_id on capture cannot override the SDK-managed value.",
    "issueType": "feature",
    "confidence": 0.96,
    "labels": [
      "enhancement",
      "feature",
      "feature/replay",
      "feature/mobile",
      "react-native",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "This is a valid SDK parity gap. A minimal first scope should be construction-time `bootstrap: { sessionID }` only, provided maintainers agree on the public API and precedence over an existing persisted React Native session.\n\nThe supplied ID should be validated as UUIDv7, reject excessive future clock skew, derive the session start timestamp from the UUID, and respect the existing 24-hour session limit. It must also reach the native plugin before recording begins so JS events and native replay use the same ID.\n\n`reset({ bootstrap })` parity and a mid-session setter should remain out of scope for the initial change: React Native currently exposes `reset(propertiesToKeep?)`, unlike the web reset-options API, and changing an active session has native replay/session-rotation semantics that need a separately agreed contract.\n\nSuggested coverage: valid and invalid/future/expired UUIDv7 IDs; event `$session_id` enrichment and persisted timestamps; precedence with `enablePersistSessionIdAcrossRestart`; and native-plugin/replay assertions that initialization receives the bootstrapped ID without an extra rotation. The prospective change falls into the session-rotation/replay risk class, so verify it cannot create mismatched or orphaned recordings.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/types.ts",
        "packages/core/src/posthog-core.ts",
        "packages/core/src/vendor/uuidv7.ts",
        "packages/react-native/src/posthog-rn.ts",
        "packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt",
        "packages/react-native-plugin/ios/PosthogReactNativePlugin.swift",
        "packages/browser/src/sessionid.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/__tests__/sessionid.test.ts",
        "packages/types/src/posthog-config.ts",
        ".agents/skills/replay-incident-risk/INCIDENTS.md"
      ],
      "findings": [
        "`PostHogCoreOptions.bootstrap` currently contains only identity and feature-flag fields; `PostHogCore.setupBootstrap()` likewise has no session-ID handling. Consequently, `PostHogOptions`, which extends core options, does not type or implement `bootstrap.sessionID`.",
        "`PostHogCore.enrichProperties()` assigns `$session_id: this.getSessionId()` after caller properties, confirming that passing `$session_id` to `capture()` cannot override the managed ID.",
        "`PostHogCore.getSessionId()` generates a UUIDv7 when the stored session is absent, idle-expired, or older than the 24-hour maximum, and `resetSessionId()` only clears stored session values. Core's vendored UUID implementation can parse UUIDs and report their version, but it does not expose the browser timestamp helper.",
        "React Native clears persisted session state at initialization unless `enablePersistSessionIdAcrossRestart` is enabled, then calls `setupBootstrap(options)` before marking the client initialized. A new bootstrap path therefore needs an explicit persisted-session precedence rule when restart persistence is enabled.",
        "React Native's native-plugin initialization calls `getSessionId()` and passes it into native setup. Android parses it as a UUID and calls `PostHogSessionManager.setSessionId`; iOS calls `PostHogSessionManager.shared.setSessionId`, so a correctly bootstrapped JS session can be propagated during initial native setup.",
        "React Native's `getSessionId()` detects a changed JS session ID and calls native `endSession()` followed by `startSession(sessionId)`. This makes post-initialization replacement materially different from bootstrap and relevant to replay continuity.",
        "The browser `SessionIdManager` already supports `bootstrap.sessionID`: it parses the UUIDv7 timestamp, rejects IDs more than one minute in the future, stores the UUID timestamp as the session start, and rejects sessions beyond the 24-hour maximum at session evaluation. Browser `reset()` also accepts `ResetOptions.bootstrap`; React Native `reset()` instead accepts a persisted-properties array.",
        "The replay incident guidance identifies session rotation as a medium-risk area because rotation can affect recording continuity and recording volume. This initialization-only request does not itself establish the idle-tab over-recording failure mode, but tests must prove native replay does not rotate independently or use a different ID."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The narrow initialization mechanism is localized, but it introduces a public configuration option and requires an agreed precedence policy for persisted sessions. Reset parity and runtime replacement have distinct API shapes and replay/native lifecycle effects, so they should not be bundled into a speculative small PR.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3359,
      "type": "issue",
      "state": "open",
      "title": "React Native: multiple clients can rotate $session_id twice on foreground, orphaning session replay",
      "url": "https://github.com/PostHog/posthog-js/issues/3359",
      "labels": [
        "bug",
        "feature/replay",
        "react-native",
        "iOS",
        "Android"
      ],
      "bodyExcerpt": "## Bug description\n\nWhen a React Native app has multiple `PostHog` clients, foregrounding the app after the session has expired can rotate `$session_id` more than once.\n\nIf session replay is enabled, replay can then end up attached to a different session id than the one used by subsequent events, effectively orphaning the replay from the active analytics session.\n\nObserved on Android. Based on the current React Native SDK implementation, this likely affects iOS as well.\n\n## How to reproduce\n\n1. Initialize two `PostHog` clients in the same React Native app.\n2. Enable session replay on at least one client.\n3. Set a short `sessionExpirationTimeSeconds` to make this easier to reproduce.\n4. Background the app long enough for the session to expire.\n5. Foreground the app again.\n6. Both clients handle `AppState=active`, and the session can rotate twice. Replay may then be rebound to the later session id while later captured events use a different active session.\n\n## Related sub-libraries\n\n- [ …",
      "createdAt": "2026-04-08T23:34:16Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.07142857142857142,
    "canonical": {
      "number": 3359,
      "type": "issue",
      "state": "open",
      "title": "React Native: multiple clients can rotate $session_id twice on foreground, orphaning session replay",
      "url": "https://github.com/PostHog/posthog-js/issues/3359",
      "labels": [
        "bug",
        "feature/replay",
        "react-native",
        "iOS",
        "Android"
      ],
      "bodyExcerpt": "## Bug description\n\nWhen a React Native app has multiple `PostHog` clients, foregrounding the app after the session has expired can rotate `$session_id` more than once.\n\nIf session replay is enabled, replay can then end up attached to a different session id than the one used by subsequent events, effectively orphaning the replay from the active analytics session.\n\nObserved on Android. Based on the current React Native SDK implementation, this likely affects iOS as well.\n\n## How to reproduce\n\n1. Initialize two `PostHog` clients in the same React Native app.\n2. Enable session replay on at least one client.\n3. Set a short `sessionExpirationTimeSeconds` to make this easier to reproduce.\n4. Background the app long enough for the session to expire.\n5. Foreground the app again.\n6. Both clients handle `AppState=active`, and the session can rotate twice. Replay may then be rebound to the later session id while later captured events use a different active session.\n\n## Related sub-libraries\n\n- [ …",
      "createdAt": "2026-04-08T23:34:16Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 7%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "faeb2c5390bd78c21b7aaa75f11447e018bdecf6c494e3072ec5866c41f8ab0f",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/194855dd53e3b40e92fc5da772a0712c",
    "gistId": "194855dd53e3b40e92fc5da772a0712c",
    "files": [
      {
        "name": "call-1-1-2026-09-15T23-44-43-907Z_01a0a775-6583-7a8c-8102-e27551f43e1e.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/194855dd53e3b40e92fc5da772a0712c/raw/39391de565c9b1c934d4554373189ba59f37ecc5/call-1-1-2026-09-15T23-44-43-907Z_01a0a775-6583-7a8c-8102-e27551f43e1e.jsonl"
      }
    ]
  },
  "runId": 35036969954,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/35036969954"
}
```
