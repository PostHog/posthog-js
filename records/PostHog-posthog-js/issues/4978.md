# issue 4978: React Native: support bootstrapping an existing session ID

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4978
- Conclusion: Valid React Native feature gap; initial bootstrap support is feasible, but reset and runtime replacement require separate public-API decisions.
- Labels: enhancement, react-native, feature/mobile, feature, feature/replay, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/35036653896
- Updated: 2026-09-15T23:41:59.019Z

```json
{
  "triage": {
    "conclusion": "Valid React Native feature gap; initial bootstrap support is feasible, but reset and runtime replacement require separate public-API decisions.",
    "summary": "React Native cannot accept a caller-provided session ID at initialization, preventing web-to-native session continuity. Caller-supplied $session_id capture properties are correctly overwritten by the SDK.",
    "issueType": "feature",
    "confidence": 0.94,
    "labels": [
      "enhancement",
      "react-native",
      "feature/mobile",
      "feature",
      "feature/replay",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed as a gap. `PostHogCoreOptions.bootstrap` and `setupBootstrap()` currently support identity and flags only; React Native therefore has no typed or implemented `bootstrap.sessionID` path. As reported, `$session_id` supplied to `capture()` cannot override the SDK value because core enrichment writes its session ID last.\n\nA narrow initialisation-only API appears viable: React Native calls `setupBootstrap(options)` before marking itself initialized, and native plugin setup subsequently obtains the JS session ID and passes it to both Android and iOS native session managers. The implementation must validate a UUIDv7 and use its embedded timestamp as the session start, matching the web behavior rather than treating it as a normal newly generated session.\n\nI recommend explicitly scoping this issue's first change to `bootstrap: { sessionID }` at construction time. `reset()` parity and a mid-session setter are distinct public API/semantics decisions: React Native's `reset()` currently takes a persisted-properties array, unlike web's reset-options object, and replacing an active session has replay/identity lifecycle implications. In particular, we should decide whether bootstrap overrides an existing persisted RN session and whether reset should support a new options shape before implementation.\n\nSuggested regression coverage: valid bootstrap persistence and event enrichment; invalid/future/over-24-hour UUIDv7 rejection; interaction with persisted sessions and `enablePersistSessionIdAcrossRestart`; and a replay/native-plugin mock asserting that the same bootstrapped ID reaches native without an additional rotation. The session-rotation/replay path merits careful review because mismatches can orphan recordings (see #3359).",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/types.ts",
        "packages/core/src/posthog-core.ts",
        "packages/core/src/vendor/uuidv7.ts",
        "packages/core/src/__tests__/posthog.sessions.spec.ts",
        "packages/react-native/src/posthog-rn.ts",
        "packages/react-native/test/posthog.spec.ts",
        "packages/react-native/test/session-replay-request-headers.spec.ts",
        "packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt",
        "packages/react-native-plugin/ios/PosthogReactNativePlugin.swift",
        "packages/browser/src/sessionid.ts",
        "packages/browser/src/__tests__/sessionid.test.ts",
        "packages/browser/src/__tests__/posthog-core.reset.test.ts",
        "packages/types/src/posthog-config.ts"
      ],
      "findings": [
        "`PostHogCoreOptions.bootstrap` declares only `distinctId`, `isIdentifiedId`, feature flags, and feature-flag payloads; `PostHogCore.setupBootstrap()` implements only those fields.",
        "`PostHogCore.getSessionId()` generates and persists a UUIDv7 when no current session exists, while `enrichProperties()` assigns `$session_id` after caller properties. This confirms that passing `$session_id` to `capture()` cannot work.",
        "React Native invokes `setupBootstrap(options)` during initialization after applying its restart-session clearing behavior and before setting `_isInitialized = true`; there is currently no session bootstrap handling in that path.",
        "React Native's native initialization calls `getSessionId()` and passes the result to its plugin. Android parses it as a UUID and calls `PostHogSessionManager.setSessionId`; iOS calls `PostHogSessionManager.shared.setSessionId`. Existing replay bridge tests already assert that native setup receives the JS session ID.",
        "The web `SessionIdManager` already invokes `setBootstrapSessionId()` from `config.bootstrap.sessionID`. It derives the UUIDv7 timestamp, rejects IDs more than one minute in the future, and rejects a bootstrapped session beyond the 24-hour maximum when it is checked.",
        "Core vendors a UUID implementation that can parse UUIDs and report their UUID version, so validation primitives exist in the shared package, though core does not currently expose a timestamp parser equivalent to browser's `uuid7ToTimestampMs`.",
        "React Native's public `reset()` accepts `PostHogPersistedProperty[]` and delegates to core reset; it then performs native identity/reset work. This does not match the browser's `ResetOptions` API, so reset bootstrap parity is not a one-line extension.",
        "`resetSessionId()` only clears the JS session state and ends the native session when applicable; it offers no supplied-ID replacement path. Its lifecycle behavior is relevant to the replay-rotation risk noted in #3359."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The initialization-only mechanism is localized, but this issue also requests decisions on two additional public APIs. Per contributor guidance, the API shape must be agreed before implementation. Session timestamp validation, persisted-session precedence, and native replay synchronization need focused tests to avoid session/replay divergence.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3359,
      "type": "issue",
      "state": "open",
      "title": "React Native: multiple clients can rotate $session_id twice on foreground, orphaning session replay",
      "url": "https://github.com/PostHog/posthog-js/issues/3359",
      "labels": [
        "bug",
        "feature/replay",
        "react-native",
        "iOS",
        "Android"
      ],
      "bodyExcerpt": "## Bug description\n\nWhen a React Native app has multiple `PostHog` clients, foregrounding the app after the session has expired can rotate `$session_id` more than once.\n\nIf session replay is enabled, replay can then end up attached to a different session id than the one used by subsequent events, effectively orphaning the replay from the active analytics session.\n\nObserved on Android. Based on the current React Native SDK implementation, this likely affects iOS as well.\n\n## How to reproduce\n\n1. Initialize two `PostHog` clients in the same React Native app.\n2. Enable session replay on at least one client.\n3. Set a short `sessionExpirationTimeSeconds` to make this easier to reproduce.\n4. Background the app long enough for the session to expire.\n5. Foreground the app again.\n6. Both clients handle `AppState=active`, and the session can rotate twice. Replay may then be rebound to the later session id while later captured events use a different active session.\n\n## Related sub-libraries\n\n- [ …",
      "createdAt": "2026-04-08T23:34:16Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.07142857142857142,
    "canonical": {
      "number": 3359,
      "type": "issue",
      "state": "open",
      "title": "React Native: multiple clients can rotate $session_id twice on foreground, orphaning session replay",
      "url": "https://github.com/PostHog/posthog-js/issues/3359",
      "labels": [
        "bug",
        "feature/replay",
        "react-native",
        "iOS",
        "Android"
      ],
      "bodyExcerpt": "## Bug description\n\nWhen a React Native app has multiple `PostHog` clients, foregrounding the app after the session has expired can rotate `$session_id` more than once.\n\nIf session replay is enabled, replay can then end up attached to a different session id than the one used by subsequent events, effectively orphaning the replay from the active analytics session.\n\nObserved on Android. Based on the current React Native SDK implementation, this likely affects iOS as well.\n\n## How to reproduce\n\n1. Initialize two `PostHog` clients in the same React Native app.\n2. Enable session replay on at least one client.\n3. Set a short `sessionExpirationTimeSeconds` to make this easier to reproduce.\n4. Background the app long enough for the session to expire.\n5. Foreground the app again.\n6. Both clients handle `AppState=active`, and the session can rotate twice. Replay may then be rebound to the later session id while later captured events use a different active session.\n\n## Related sub-libraries\n\n- [ …",
      "createdAt": "2026-04-08T23:34:16Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 7%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "22550c0b67db339473ddd11e3073e4b09d831e2fc5059ad4169efd6e53e0d9fd",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/2d8d4606e37701c69fe27fab859101a9",
    "gistId": "2d8d4606e37701c69fe27fab859101a9",
    "files": [
      {
        "name": "call-1-1-2026-09-15T23-40-27-622Z_01a0a771-7c65-7013-8833-ea7c9b507a9a.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/2d8d4606e37701c69fe27fab859101a9/raw/e90b261ddb2c23fffb8849b0227ef61d019f475d/call-1-1-2026-09-15T23-40-27-622Z_01a0a771-7c65-7013-8833-ea7c9b507a9a.jsonl"
      }
    ]
  },
  "runId": 35036653896,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/35036653896"
}
```
