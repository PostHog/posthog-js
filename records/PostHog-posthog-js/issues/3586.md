# issue 3586: Add groupIdentifyImmediate() method to Node.js SDK

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3586
- Conclusion: Valid feature request; the Node SDK still lacks groupIdentifyImmediate().
- Labels: enhancement, feature/product-analytics, node
- PR: https://github.com/PostHog/posthog-js/pull/4006
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28363800378
- Updated: 2026-06-29T10:08:45.235Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; the Node SDK still lacks groupIdentifyImmediate().",
    "summary": "The Node.js SDK has immediate variants for capture, identify, and alias, but groupIdentify only supports the queued API. A minimal immediate method can mirror the existing $groupidentify payload and send it through the immediate batch path.",
    "issueType": "feature",
    "confidence": 0.93,
    "labels": [
      "enhancement",
      "feature/product-analytics",
      "node"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "This looks valid. I checked the current code and `posthog-node` exposes `captureImmediate`, `identifyImmediate`, and `aliasImmediate`, but not `groupIdentifyImmediate`. `groupIdentify()` currently builds a `$groupidentify` event in core and enqueues it as a capture event, and the Convex package has a workaround comment noting that `posthog-node` does not expose `groupIdentifyImmediate`.\n\nA small PR should be reasonable: add a core `groupIdentifyStatelessImmediate()` that mirrors `groupIdentifyStateless()` but calls `sendImmediate('capture', payload, options)`, then expose `groupIdentifyImmediate()` on the Node client and interface with a unit test asserting the immediate call awaits the network request and sends the same `$groupidentify` payload.",
    "investigation": {
      "relevantFiles": [
        "packages/node/src/client.ts",
        "packages/node/src/types.ts",
        "packages/core/src/posthog-core-stateless.ts",
        "packages/node/src/__tests__/posthog-node.spec.ts",
        "packages/convex/src/component/lib.ts"
      ],
      "findings": [
        "packages/node/src/client.ts exposes `captureImmediate`, `identifyImmediate`, and `aliasImmediate`, but only `groupIdentify()` exists for groups.",
        "packages/node/src/types.ts declares immediate methods for capture, identify, and alias in `IPostHog`, but declares only queued `groupIdentify()` for groups.",
        "packages/core/src/posthog-core-stateless.ts has `groupIdentifyStateless()` which sends event `$groupidentify` with `$group_type`, `$group_key`, and `$group_set`, then enqueues it as type `capture`.",
        "packages/core/src/posthog-core-stateless.ts has immediate stateless helpers for identify, capture, and alias, but no corresponding group identify immediate helper.",
        "packages/convex/src/component/lib.ts contains an explicit workaround comment: `posthog-node doesn't expose a groupIdentifyImmediate`, and manually sends the `$groupidentify` event via `captureImmediate`."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The missing API is well-scoped and can reuse the existing group-identify payload construction and immediate-send path. No new product semantics appear necessary.",
      "suggestedApproach": "Add `groupIdentifyStatelessImmediate()` in `packages/core/src/posthog-core-stateless.ts` mirroring `groupIdentifyStateless()` and using `sendImmediate('capture', payload, options)`. Add `groupIdentifyImmediate({ groupType, groupKey, properties, distinctId, disableGeoip })` to `packages/node/src/client.ts`, add it to `IPostHog` in `packages/node/src/types.ts`, and add a unit test in `packages/node/src/__tests__/posthog-node.spec.ts` verifying the immediate method awaits the request and emits the same `$groupidentify` payload as `groupIdentify()`.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 2177,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(node): Add group identify immediate",
      "url": "https://github.com/PostHog/posthog-js/pull/2177",
      "labels": [
        "stale"
      ],
      "bodyExcerpt": "## Problem\r\n\r\nRelated: https://github.com/PostHog/posthog-js/issues/3586\r\n\r\n## Changes\r\n\r\nAdded `groupIdentifyStatelessImmediate`, but tbh I'm not sure if this is necessary because user can use `captureImmediate`. Will check with him.\r\n\r\n(P.S, my first time making a PR here, if there is anything i'm not doing correctly please let me know, thanks!)\r\n\r\n## Release info Sub-libraries affected\r\n\r\n### Libraries affected\r\n\r\n<!-- Please mark which libraries will require a version bump. -->\r\n\r\n- [ ] All of them\r\n- [ ] posthog-js (web)\r\n- [ ] posthog-js-lite (web lite)\r\n- [x] posthog-node\r\n- [ ] posthog-react-native\r\n- [ ] @posthog/react\r\n- [ ] @posthog/ai\r\n- [ ] @posthog/nextjs-config\r\n\r\n## Checklist\r\n\r\n- [ ] Tests for new code\r\n- [ ] Accounted for the impact of any changes across different platforms\r\n- [x] Accounted for backwards compatibility of any changes (no breaking changes!)\r\n- [ ] Took care not to unnecessarily increase the bundle size\r\n\r\n### If releasing new changes\r\n\r\n- [ ] Ran `pnpm …",
      "createdAt": "2025-07-31T23:59:56Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "d147bf031d0f7246423654b55c4f6dfabbee8146806d5294c97fc26a442d30a7",
  "piCalls": 7,
  "runId": 28363800378,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28363800378"
}
```
