# issue 5034: cross_subdomain_cookie: false does not prevent the dmn_chk_ domain probe (rejected cookies on public-suffix hosts)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/5034
- Conclusion: Confirmed browser SDK bug: explicit `cross_subdomain_cookie: false` still triggers cross-subdomain cookie-domain probing during persistence cleanup.
- Labels: javascript, web, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/35509098689
- Updated: 2026-09-20T11:55:56.880Z

```json
{
  "triage": {
    "conclusion": "Confirmed browser SDK bug: explicit `cross_subdomain_cookie: false` still triggers cross-subdomain cookie-domain probing during persistence cleanup.",
    "summary": "Persistence cleanup unconditionally removes a cross-subdomain cookie, which invokes public-suffix domain probing even when the configured cookie scope is host-only.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug: Something isn't working",
      "javascript",
      "web",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report and write-up. This is confirmed from the current persistence path: `set_cross_subdomain(false)` runs during construction, then `remove()` always calls `_remove(..., true)`. For cookie-backed persistence, that reaches `chooseCookieDomain(..., true)` and the `dmn_chk_` probing loop.\n\nThe explicit `false` configuration should not perform that probe on ordinary initialization. A fix should be narrowly scoped to cleanup selection and preserve removal of a previously cross-subdomain cookie when a running instance changes from `true` to `false`.\n\nThe broader default exclusion-list / Public Suffix List question is related but should be evaluated separately rather than bundled into the fix for the explicit-option regression.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-persistence.ts",
        "packages/browser/src/storage.ts",
        "packages/browser/src/__tests__/posthog-persistence.test.ts",
        "packages/browser/src/__tests__/storage.test.ts",
        "packages/browser-common/src/utils/general-utils.ts"
      ],
      "findings": [
        "`PostHogPersistence.remove()` unconditionally invokes `this._storage._remove(this._name, false)` and then `this._storage._remove(this._name, true)` in `packages/browser/src/posthog-persistence.ts`.",
        "`set_cross_subdomain()` assigns the configured value and calls `remove({ keepGroupEntries: true })`; the constructor calls `update_config(config, config, ...)`, so an initially undefined in-memory value transitions to configured `false` during initialization.",
        "`cookieStore._remove()` calls `_set(..., cross_subdomain)`, and `_set()` calls `chooseCookieDomain(document.location.hostname, cross_subdomain)`. A true scope invokes `seekFirstNonPublicSubDomain()`, which writes `dmn_chk_` cookies for candidate domains.",
        "`cookieStore._remove()` returns before writing only when `document.cookie` is empty, consistent with the reporter's observation that the issue can be silent on a fresh origin and appear on later loads.",
        "Existing storage tests cover generic public-suffix probing, but no persistence test asserts that initializing with `cross_subdomain_cookie: false` never requests a cross-subdomain removal/probe.",
        "`isCrossDomainCookie()` has a deliberately small default exclusion list containing only `herokuapp.com`, `vercel.app`, and `netlify.app`; hosts under `github.io`, `gitlab.io`, and `pages.dev` are not excluded by that heuristic."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The explicit-false regression is localized to persistence cleanup and requires no public API change. The implementation must retain cleanup of an existing cross-subdomain cookie when configuration actually transitions between scopes, so simply replacing the literal `true` with the current configured value would be incomplete.",
      "suggestedApproach": "Add a private cleanup-scope option to `remove()` so normal reset/clear behavior can continue removing both scopes, while cookie-option setters request cross-subdomain cleanup only when either the previous or new scope is cross-subdomain. Capture the previous `_cross_subdomain` value in `set_cross_subdomain()` before assignment. Add focused tests showing construction with `cross_subdomain_cookie: false` never calls `_remove(..., true)`, and that a true-to-false runtime transition still removes the prior shared-scope cookie.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "35b6e4220063d04e154c47c3e0c1429edbac8670ff5d9f22cd04d3f56cebee18",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/8f8fc86f50df1a8a2a880fda84918476",
    "gistId": "8f8fc86f50df1a8a2a880fda84918476",
    "files": [
      {
        "name": "call-1-1-2026-09-20T11-54-46-774Z_01a0beab-3676-7d06-8654-da6aa4955007.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/8f8fc86f50df1a8a2a880fda84918476/raw/c0f13abf694b515eec10ea992faeecf268f9b03d/call-1-1-2026-09-20T11-54-46-774Z_01a0beab-3676-7d06-8654-da6aa4955007.jsonl"
      }
    ]
  },
  "runId": 35509098689,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/35509098689"
}
```
