# issue 2036: Surveys abruptly disappear on lose focus

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2036
- Conclusion: Valid selector-widget survey lifecycle bug; do not close.
- Labels: feature/surveys
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28095535073
- Updated: 2026-06-24T11:38:06.946Z

```json
{
  "triage": {
    "conclusion": "Valid selector-widget survey lifecycle bug; do not close.",
    "summary": "A feedback survey opened from a custom CSS selector inside a dropdown disappears when the dropdown trigger element unmounts, because the SDK removes the survey DOM when the selector element is no longer found.",
    "issueType": "bug",
    "confidence": 0.88,
    "labels": [
      "bug: Something isn't working",
      "feature/surveys"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clarification. This matches the current selector-widget lifecycle in the SDK: when the configured selector element disappears, `SurveyManager` removes the selector listener and also removes the survey shadow DOM, which tears down an open survey if the trigger lived inside a dropdown/menu. The intended behavior should be that an already-open selector survey stays open until close/submit, even if the trigger element unmounts. A fix should include a regression test for opening a selector survey, removing the trigger element, and verifying the survey remains visible.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/surveys.tsx",
        "packages/browser/src/extensions/surveys/surveys-extension-utils.tsx"
      ],
      "findings": [
        "`packages/browser/src/extensions/surveys.tsx` `_manageWidgetSelectorListener` calls `_removeWidgetSelectorListener(survey)` when `document.querySelector(selector)` no longer finds the trigger but an existing selector listener is registered.",
        "`_removeWidgetSelectorListener` unconditionally calls `_removeSurveyFromDom(survey)` before detaching the click listener, so losing the trigger element removes the shadow DOM that contains the feedback widget and popup.",
        "`FeedbackWidget` stores the open/closed popup state locally with `showSurvey`; the manager-side cleanup path does not distinguish an open survey from a hidden widget before removing the DOM.",
        "`retrieveSurveyShadow` in `surveys-extension-utils.tsx` creates/reuses a survey container appended to `document.body`, so the popup itself is not required to be a child of the trigger element; the disappearance is caused by explicit cleanup, not natural DOM removal of the dropdown."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small PR is appropriate because the failure is localized to selector-widget cleanup. The main caution is preserving cleanup for inactive surveys while avoiding teardown of an open popup.",
      "suggestedApproach": "Split selector listener cleanup from survey DOM cleanup. When the configured selector temporarily disappears, detach the stale trigger listener but keep the feedback widget DOM mounted so an open survey can remain visible and close normally. Continue removing the DOM when the survey is no longer active/eligible or explicitly closes. Add a regression test for a selector survey opened from a trigger that is then removed from the DOM.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "16b30d9b2ea8196b2fb3d0eb353cfd30fdca2b8b65bd44e39451c9efe622fdd8",
  "piCalls": 2,
  "runId": 28095535073,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28095535073"
}
```
