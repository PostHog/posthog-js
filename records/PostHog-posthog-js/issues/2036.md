# issue 2036: Surveys abruptly disappear on lose focus

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2036
- Conclusion: Valid selector-trigger survey lifecycle bug; keep open.
- Labels: feature/surveys
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28098329554
- Updated: 2026-06-24T12:28:12.275Z

```json
{
  "triage": {
    "conclusion": "Valid selector-trigger survey lifecycle bug; keep open.",
    "summary": "Selector-based feedback surveys can be torn down when the trigger element is unmounted, e.g. when the trigger lives inside a dropdown that closes as the user clicks into the survey.",
    "issueType": "bug",
    "confidence": 0.9,
    "labels": [
      "bug: Something isn't working",
      "feature/surveys"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clarification. This looks like a valid surveys bug rather than intended behavior. The selector widget cleanup currently removes the survey DOM when the configured trigger element disappears, so an open survey can be destroyed if the trigger lived inside a dropdown/menu that unmounts on blur. The expected behavior should be that an already-open survey remains visible until the user closes or submits it, even if the original trigger element is removed from the page.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/surveys.tsx",
        "packages/browser/src/extensions/surveys/surveys-extension-utils.tsx",
        "packages/browser/src/__tests__/extensions/surveys.test.ts"
      ],
      "findings": [
        "`SurveyManager._manageWidgetSelectorListener` checks `document.querySelector(selector)` on each evaluation cycle and calls `_removeWidgetSelectorListener(survey)` when the selector element is no longer found.",
        "`_removeWidgetSelectorListener` unconditionally calls `_removeSurveyFromDom(survey)` before detaching the trigger click listener, which removes the shadow DOM containing the widget and popup.",
        "`FeedbackWidget` stores whether the popup is open in local `showSurvey` state, so the manager cleanup path does not currently distinguish an open survey from a closed/hidden selector widget.",
        "`retrieveSurveyShadow` appends the survey container to `document.body` by default, so the popup does not naturally need to be removed just because the trigger element was unmounted.",
        "Existing survey tests cover selector listener invocation, but I did not find a regression test for opening a selector widget and then removing the trigger element."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The bug is localized, but the safe fix needs care because selector listener cleanup, survey DOM cleanup, and the widget's open/closed state are currently coupled across manager and component state. A small PR is likely possible, but it should include a regression test and avoid leaking stale hidden survey containers when selector elements permanently disappear.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "16b30d9b2ea8196b2fb3d0eb353cfd30fdca2b8b65bd44e39451c9efe622fdd8",
  "piCalls": 1,
  "runId": 28098329554,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28098329554"
}
```
