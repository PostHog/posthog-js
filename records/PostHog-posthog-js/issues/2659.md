# issue 2659: Feature Request: Add option to flush with pending promises

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2659
- Conclusion: Already fixed and released for posthog-node.
- Labels: enhancement, feature, node, feature/error-tracking, feature/flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/32584199153
- Updated: 2026-08-22T16:17:15.678Z

```json
{
  "triage": {
    "conclusion": "Already fixed and released for posthog-node.",
    "summary": "The request was to make flush() wait for asynchronous SDK work, such as captureException() event construction, without shutting down a warm serverless client.",
    "issueType": "feature",
    "confidence": 0.99,
    "labels": [
      "enhancement",
      "feature",
      "node",
      "feature/error-tracking",
      "feature/flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "This has been implemented by [#4028](https://github.com/PostHog/posthog-js/pull/4028) and released in `posthog-node` **5.39.2**.\n\n`posthog.flush()` now waits for pending asynchronous SDK work before draining the event queue. This covers events produced by helpers such as `captureException()` and avoids requiring `shutdown()` and client reinitialization for warm serverless containers.",
    "investigation": {
      "relevantFiles": [
        "packages/node/src/client.ts",
        "packages/core/src/posthog-core-stateless.ts",
        "packages/node/src/__tests__/posthog-node.spec.ts",
        "packages/core/src/__tests__/posthog.flush.spec.ts",
        "packages/node/CHANGELOG.md"
      ],
      "findings": [
        "`PostHog` in `packages/node/src/client.ts` overrides `flush()` to call `flushWithPendingPromises()`.",
        "`flushWithPendingPromises()` uses the core flush path with pending-work waiting enabled before `_flush()` reads and drains the event queue.",
        "The core implementation snapshots the pending-promise queue before registering the flush promise and excludes the flush promises from its wait, avoiding a self-wait/deadlock.",
        "Core tests cover waiting for pending work that enqueues an event, not waiting for work added after the flush begins, and continuing to flush queued events when pending work rejects.",
        "Node tests explicitly verify that an immediate `flush()` after `captureException()` sends the asynchronously prepared `$exception` event.",
        "`packages/node/CHANGELOG.md` records PR #4028 as released in `posthog-node` 5.39.2 on 2026-07-01."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "No new implementation is appropriate: the requested behavior is already implemented, regression-tested, and released. The minimal resolved design makes Node flush() wait for pending SDK work rather than adding a separate public option.",
      "suggestedApproach": "",
      "risk": "low"
    },
    "closeProposal": {
      "propose": true,
      "category": "already-fixed",
      "confidence": 0.99,
      "reason": "PR #4028 implemented the requested flush semantics, the current Node and core code contains the behavior and regression coverage, and the Node changelog confirms release in version 5.39.2.",
      "canonicalUrl": "https://github.com/PostHog/posthog-js/pull/4028"
    }
  },
  "relatedItems": [
    {
      "number": 4028,
      "type": "pull_request",
      "state": "closed",
      "title": "fix: make flush wait for pending async work",
      "url": "https://github.com/PostHog/posthog-js/pull/4028",
      "labels": [],
      "bodyExcerpt": "## Problem\n\nNode `flush()` only waited for in-flight network flushes, not pending async SDK work that can enqueue events later. This meant calls like `captureException()` could still be building the `$exception` event when `flush()` drained the queue, leaving the exception buffered until a later flush or shutdown.\n\n## Changes\n\n- Make Node `flush()` wait on a pending-work barrier before draining the event queue, so work already in flight when `flush()` starts can enqueue its events.\n- Use an all-settled pending wait so a rejected pending task does not prevent already queued events from flushing.\n- Register flushes in the promise queue synchronously so `shutdown()` cannot miss them, while excluding active flush promises from pending-work waits to avoid self-waits/deadlocks.\n- Keep the pending-promise wait out of React Native because RN exception capture is synchronous today.\n- Add regression coverage for pending work, rejected pending work, timeout fallback, and Node `captureException()`…",
      "createdAt": "2026-06-30T15:33:45Z",
      "reason": "explicit-reference"
    },
    {
      "number": 2220,
      "type": "issue",
      "state": "closed",
      "title": "`captureException` doesn't get flushed, which breaks exception autocapture on fatal errors",
      "url": "https://github.com/PostHog/posthog-js/issues/2220",
      "labels": [
        "bug",
        "feature/error-tracking"
      ],
      "bodyExcerpt": "## Bug description\nIn posthog-node, a call to `captureException` followed by `shutdown` or `flush` does not flush the captured exception. Which means that the `enableExceptionAutocapture` feature is broken for exceptions that cause the process to exit.\n\n## How to reproduce\nRun this script. The error should show up in posthog, but it does not.\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY, {enableExceptionAutocapture: true})\nthrow new Error(\"foo\")\n```\n\nThe same problem exists with this script, which essentially does the same thing, but explicitly via `captureException` and `shutdown` rather than `enableExceptionAutocapture`:\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY)\nposthog.captureException(new Error(\"foo\"))\nawait posthog.shutdown()\n```\n\n## Related sub-libraries\n- [x] posthog-node\n\n## Additional context\nI tracked the problem down to these lines: https://github.com/PostHog/posth…",
      "createdAt": "2025-08-15T05:37:18Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.1118421052631579,
    "canonical": {
      "number": 2220,
      "type": "issue",
      "state": "closed",
      "title": "`captureException` doesn't get flushed, which breaks exception autocapture on fatal errors",
      "url": "https://github.com/PostHog/posthog-js/issues/2220",
      "labels": [
        "bug",
        "feature/error-tracking"
      ],
      "bodyExcerpt": "## Bug description\nIn posthog-node, a call to `captureException` followed by `shutdown` or `flush` does not flush the captured exception. Which means that the `enableExceptionAutocapture` feature is broken for exceptions that cause the process to exit.\n\n## How to reproduce\nRun this script. The error should show up in posthog, but it does not.\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY, {enableExceptionAutocapture: true})\nthrow new Error(\"foo\")\n```\n\nThe same problem exists with this script, which essentially does the same thing, but explicitly via `captureException` and `shutdown` rather than `enableExceptionAutocapture`:\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY)\nposthog.captureException(new Error(\"foo\"))\nawait posthog.shutdown()\n```\n\n## Related sub-libraries\n- [x] posthog-node\n\n## Additional context\nI tracked the problem down to these lines: https://github.com/PostHog/posth…",
      "createdAt": "2025-08-15T05:37:18Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 11%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "Triage proposed this issue as already-fixed of https://github.com/PostHog/posthog-js/pull/4028; skipping a duplicate fix PR.",
  "snapshotHash": "5eab684e3d29e93088f4f231aa970ffdbfe70eba0c3dfe53c6ace37c4f6e2011",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/ac90fc38dd9a5f8b65b2661f450f9a0c",
    "gistId": "ac90fc38dd9a5f8b65b2661f450f9a0c",
    "files": [
      {
        "name": "call-1-1-2026-08-22T16-16-55-298Z_01a02a42-ca02-71c6-a143-a1b0d083c11b.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/ac90fc38dd9a5f8b65b2661f450f9a0c/raw/8619b35edcba650d5bdbcbe7f94cbe4578dde4fa/call-1-1-2026-08-22T16-16-55-298Z_01a02a42-ca02-71c6-a143-a1b0d083c11b.jsonl"
      }
    ]
  },
  "runId": 32584199153,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/32584199153"
}
```
