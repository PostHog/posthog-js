# issue 2659: Feature Request: Add option to flush with pending promises

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2659
- Conclusion: Valid feature request for posthog-node flush semantics
- Labels: enhancement, node, feature, feature/error-tracking, feature/flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28160600817
- Updated: 2026-06-25T09:33:23.158Z

```json
{
  "triage": {
    "conclusion": "Valid feature request for posthog-node flush semantics",
    "summary": "posthog-node async event preparation is tracked via addPendingPromise(), but flush() only sends already-queued events. This means captureException() and capture({ sendFeatureFlags: true }) can be missed by a per-request flush unless callers use shutdown() or internal promiseQueue.join().",
    "issueType": "feature",
    "confidence": 0.88,
    "labels": [
      "enhancement",
      "node",
      "feature",
      "feature/error-tracking",
      "feature/flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. I verified the behavior in the current code: `captureException()` and normal `capture()` both add async preparation work to the internal pending-promise queue, while `flush()` only drains the persisted event queue as it exists at flush time. `shutdown()` does wait on `promiseQueue.join()` first, which explains why it works but has the wrong lifecycle semantics for warm serverless containers.\n\nA minimal fix seems feasible, but it needs a small API decision: either extend `flush()` with an option such as `{ waitForPending: true }`, or add a dedicated method. The implementation should be careful to wait for pending work before creating/adding the flush promise itself, then flush any events that were enqueued by that pending work, without stopping pollers or error tracking.",
    "investigation": {
      "relevantFiles": [
        "packages/node/src/client.ts",
        "packages/core/src/posthog-core-stateless.ts",
        "packages/core/src/utils/promise-queue.ts",
        "packages/node/src/__tests__/context.spec.ts",
        "packages/node/src/__tests__/posthog-node.spec.ts"
      ],
      "findings": [
        "packages/node/src/client.ts:_capturePreparedEvent wraps prepareEventMessage(...).then(...captureStateless...) in addPendingPromise(), so even normal capture() has async pending work before an event is queued.",
        "packages/node/src/client.ts:captureException() wraps ErrorTracking.buildEventMessage(...).then(msg => this.capture(...)) in addPendingPromise(), matching the issue report.",
        "packages/node/src/client.ts:prepareEventMessage awaits feature-flag enrichment when sendFeatureFlags is set, so capture({ sendFeatureFlags: true }) can enqueue later than a same-tick flush.",
        "packages/core/src/posthog-core-stateless.ts:flush() chains only the current flushPromise and then calls _flush(); it does not join the promiseQueue before reading the event queue.",
        "packages/core/src/posthog-core-stateless.ts:_shutdown() explicitly calls await this.promiseQueue.join() before repeatedly flushing until the queue is empty.",
        "packages/core/src/utils/promise-queue.ts:join() waits until currently pending promises, including recursively added promises, are drained.",
        "packages/node/src/__tests__/context.spec.ts already uses an internal (posthog as any).promiseQueue.join() in tests to deterministically drain captureException async work before flushing/asserting, which is effectively the requested public behavior."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The behavior is well localized and the repository already has the needed primitive via promiseQueue.join(). A small PR can expose this safely as an explicit opt-in flush path, with tests for captureException and sendFeatureFlags. The main caution is avoiding a self-deadlock by joining pending promises before adding the new flush promise to the queue.",
      "suggestedApproach": "Add an explicit public API, preferably flush({ waitForPending: true }) or a dedicated flushWithPending() depending on maintainer preference. Implement it in the core/node flush path by joining pending promises before starting the actual flush, then calling the existing flush implementation. Add unit tests showing that captureException() followed immediately by the new API sends the exception event without shutdown(), and that capture({ sendFeatureFlags: true }) is also drained. Preserve existing flush() behavior by default.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 2220,
      "type": "issue",
      "state": "closed",
      "title": "`captureException` doesn't get flushed, which breaks exception autocapture on fatal errors",
      "url": "https://github.com/PostHog/posthog-js/issues/2220",
      "labels": [
        "bug",
        "feature/error-tracking"
      ],
      "bodyExcerpt": "## Bug description\nIn posthog-node, a call to `captureException` followed by `shutdown` or `flush` does not flush the captured exception. Which means that the `enableExceptionAutocapture` feature is broken for exceptions that cause the process to exit.\n\n## How to reproduce\nRun this script. The error should show up in posthog, but it does not.\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY, {enableExceptionAutocapture: true})\nthrow new Error(\"foo\")\n```\n\nThe same problem exists with this script, which essentially does the same thing, but explicitly via `captureException` and `shutdown` rather than `enableExceptionAutocapture`:\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY)\nposthog.captureException(new Error(\"foo\"))\nawait posthog.shutdown()\n```\n\n## Related sub-libraries\n- [x] posthog-node\n\n## Additional context\nI tracked the problem down to these lines: https://github.com/PostHog/posth…",
      "createdAt": "2025-08-15T05:37:18Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.11258278145695365,
    "canonical": {
      "number": 2220,
      "type": "issue",
      "state": "closed",
      "title": "`captureException` doesn't get flushed, which breaks exception autocapture on fatal errors",
      "url": "https://github.com/PostHog/posthog-js/issues/2220",
      "labels": [
        "bug",
        "feature/error-tracking"
      ],
      "bodyExcerpt": "## Bug description\nIn posthog-node, a call to `captureException` followed by `shutdown` or `flush` does not flush the captured exception. Which means that the `enableExceptionAutocapture` feature is broken for exceptions that cause the process to exit.\n\n## How to reproduce\nRun this script. The error should show up in posthog, but it does not.\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY, {enableExceptionAutocapture: true})\nthrow new Error(\"foo\")\n```\n\nThe same problem exists with this script, which essentially does the same thing, but explicitly via `captureException` and `shutdown` rather than `enableExceptionAutocapture`:\n```js\nimport {PostHog} from \"posthog-node\"\nconst posthog = new PostHog(process.env.POSTHOG_API_KEY)\nposthog.captureException(new Error(\"foo\"))\nawait posthog.shutdown()\n```\n\n## Related sub-libraries\n- [x] posthog-node\n\n## Additional context\nI tracked the problem down to these lines: https://github.com/PostHog/posth…",
      "createdAt": "2025-08-15T05:37:18Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 11%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "9c1c99736a2d61f851ca73f95deee7bad6a42b457a11842392065894ab0b4042",
  "piCalls": 2,
  "runId": 28160600817,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28160600817"
}
```
