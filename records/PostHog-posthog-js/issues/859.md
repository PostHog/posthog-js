# issue 859: Bootstrapped inactive feature flags are ignored

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/859
- Conclusion: Valid bug; bootstrapped false feature flag values are currently filtered out before they can be read.
- Labels: feature/flags, team/feature-flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28434455752
- Updated: 2026-06-30T09:38:37.586Z

```json
{
  "triage": {
    "conclusion": "Valid bug; bootstrapped false feature flag values are currently filtered out before they can be read.",
    "summary": "When `bootstrap.featureFlags` contains a key with value `false`, the browser SDK omits that key during bootstrap normalization, so `getFeatureFlag()` returns `undefined` until a remote flags response later stores the false value.",
    "issueType": "bug",
    "confidence": 0.94,
    "labels": [
      "bug: Something isn't working",
      "feature/flags",
      "team/feature-flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the current code path. `PostHogFeatureFlags.initialize()` filters bootstrapped flags with `!!bootstrapFlags[flag]`, so `false` values are dropped before `receivedFeatureFlags()` persists them. The public bootstrap type allows `Record<string, boolean | string>`, so `false` should be a valid immediate value. A small fix should preserve keys whose value is explicitly `false` while still ignoring invalid/undefined entries, then update the tests that currently assert disabled bootstrapped flags are `undefined`.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-featureflags.ts",
        "packages/browser/src/__tests__/posthog-core-also.test.ts",
        "packages/browser/src/__tests__/extension-classes.test.ts",
        "packages/types/src/posthog-config.ts",
        "packages/core/src/featureFlagUtils.ts",
        "packages/core/src/posthog-core.ts"
      ],
      "findings": [
        "`packages/browser/src/posthog-featureflags.ts` `initialize()` builds `activeFlags` with `.filter((flag) => !!bootstrapFlags[flag])`, which removes any bootstrapped flag whose value is `false`.",
        "`getFeatureFlagResult()` later checks whether the key exists in `getFlagVariants()`. Since the false bootstrapped key was never persisted, `getFeatureFlag()` returns `undefined` for that key.",
        "`packages/types/src/posthog-config.ts` documents `bootstrap.featureFlags` as `Record<string, boolean | string>`, so `false` is a valid feature flag value rather than an absent value.",
        "Existing browser tests currently encode the problematic behavior: `packages/browser/src/__tests__/posthog-core-also.test.ts` and `packages/browser/src/__tests__/extension-classes.test.ts` expect bootstrapped disabled flags to be `undefined`.",
        "There is a similar false-dropping helper in `packages/core/src/featureFlagUtils.ts`, and `packages/core/src/posthog-core.ts` has a comment noting `createFlagsResponseFromFlagsAndPayloads` drops false flags, so shared-core bootstrap behavior may need to be aligned as well."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The root cause is a simple truthiness filter in bootstrap normalization. A small PR can preserve explicit false values and update the affected expectations.",
      "suggestedApproach": "In the browser bootstrap path, replace the truthiness filter with an explicit undefined check so `{ flag: false }` is included in stored feature flag variants while active-flag callbacks still filter false values. Update the browser bootstrap tests to expect `getFeatureFlag('disabled') === false`. If maintaining parity for shared core SDKs, adjust `createFlagsResponseFromFlagsAndPayloads` similarly and update its bootstrap tests.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": true,
    "reasons": [
      "token"
    ]
  },
  "snapshotHash": "8ef84b7c6eddc7731ffd10d522518c753880f5e1246491e70cb5a4bfd8d3671f",
  "piCalls": 4,
  "runId": 28434455752,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28434455752"
}
```
