# issue 2012: Allow evaluating feature flags with groups

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2012
- Conclusion: Valid feature request, but the proposed browser and React APIs need an explicit asynchronous, isolated-evaluation contract before implementation.
- Labels: enhancement, feature, feature/flags, team/feature-flags, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/36230148323
- Updated: 2026-09-26T08:37:03.172Z

```json
{
  "triage": {
    "conclusion": "Valid feature request, but the proposed browser and React APIs need an explicit asynchronous, isolated-evaluation contract before implementation.",
    "summary": "Allow browser feature-flag evaluation with groups supplied per evaluation rather than mutating the SDK-wide group state through group().",
    "issueType": "feature",
    "confidence": 0.91,
    "labels": [
      "enhancement",
      "feature",
      "feature/flags",
      "team/feature-flags",
      "web"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "Thanks — this is a real capability gap in `posthog-js`.\n\nToday the browser SDK evaluates `/flags` using the client's persisted group state, and `group()` updates that state before reloading flags. The browser `getFeatureFlag()` APIs are synchronous reads of the shared cached flag snapshot, so adding `groups` to their options cannot provide a distinct evaluation without defining an isolated network-request and result lifecycle. The React hook's second argument is also currently a boolean fallback value, not an options object.\n\nBefore implementing a public API, please confirm the intended contract:\n\n- Should contextual groups be ephemeral, with no persistence, `$groupidentify`, or effect on later event/group attribution?\n- Should this be an async isolated evaluator/snapshot API, rather than a synchronous `getFeatureFlag()` overload?\n- Should contextual evaluations report `$feature_flag_called`, and if so, which group context should be attached?\n- Are group properties, person properties, flag-key filtering, and concurrent evaluations in scope?\n\nThe Node SDK has asynchronous contextual evaluation APIs, including `evaluateFlags(..., { groups })`; maintainers should verify the published sdk-spec contract before selecting the browser equivalent.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-featureflags.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/types/src/feature-flags.ts",
        "packages/types/src/posthog.ts",
        "packages/react/src/hooks/useFeatureFlagEnabled.ts",
        "packages/react/src/hooks/useFeatureFlagResult.ts",
        "packages/node/src/client.ts",
        "packages/core/src/posthog-core-stateless.ts"
      ],
      "findings": [
        "`FeatureFlagOptions` in `packages/types/src/feature-flags.ts` only supports `send_event` and `fresh`; the public browser `getFeatureFlag`, `getFeatureFlagResult`, and `isFeatureEnabled` signatures use those options.",
        "`PostHogFeatureFlags._callFlagsEndpoint()` sends `groups: client.groups` in the shared `/flags/?v=2` request, then persists the returned flag snapshot for later synchronous reads.",
        "`PostHog.group()` stores the group association in browser persistence and triggers a feature-flag reload when the group changes, so it is intentionally SDK-wide rather than evaluation-local.",
        "`getFeatureFlag()` and `getFeatureFlagResult()` only read the shared loaded/cached flag state; they do not issue a contextual evaluation request. `getFeatureFlagResult()` can also emit a deduplicated `$feature_flag_called` event from that shared result.",
        "`useFeatureFlagEnabled(flag, secondArgument)` is overloaded only for a boolean default value and calls `client.isFeatureEnabled(flag)` without evaluation options.",
        "The Node client supports asynchronous group-scoped evaluation through `getFeatureFlag(..., { groups })` and `evaluateFlags(..., { groups })`; its shared stateless flag transport sends supplied groups directly to `/flags`."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "This is a public API addition that cannot be safely implemented as a simple option on the current synchronous cached browser methods. It needs agreed semantics for asynchronous evaluation, isolation from persisted global flags and event attribution, React loading/concurrency behavior, and consistency with the SDK specification.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.98,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "53e1ad18a64e8c59f467980a5010dfeeb8ce9fe6803aca3e03fa68a7cc4c3f8a",
  "piCalls": 2,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/1459975b92c203506da97d32f0a06644",
    "gistId": "1459975b92c203506da97d32f0a06644",
    "files": [
      {
        "name": "call-2-1-2026-09-26T08-35-55-476Z_01a0dcdb-4fd4-71a4-9f3f-bb22dce7a4e5.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/1459975b92c203506da97d32f0a06644/raw/e49b28457d827b5d348d5497a765282ce27d2340/call-2-1-2026-09-26T08-35-55-476Z_01a0dcdb-4fd4-71a4-9f3f-bb22dce7a4e5.jsonl"
      }
    ]
  },
  "runId": 36230148323,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/36230148323"
}
```
