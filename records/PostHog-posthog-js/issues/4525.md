# issue 4525: perf(react-native): useFeatureFlags / useFeatureFlag re-read the full flag store on every render

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4525
- Conclusion: Confirmed React Native performance bug with a small, behavior-preserving fix.
- Labels: react-native, feature/flags, team/client-libraries
- PR: https://github.com/PostHog/posthog-js/pull/4526
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/31799902130
- Updated: 2026-08-14T12:22:48.751Z

```json
{
  "triage": {
    "conclusion": "Confirmed React Native performance bug with a small, behavior-preserving fix.",
    "summary": "The `useFeatureFlags` and `useFeatureFlag` hooks pass eagerly evaluated client reads to `useState`, so those reads run on every component render even though React only uses the value on mount.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug: Something isn't working",
      "react-native",
      "feature/flags",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report — confirmed from the current source.\n\nBoth reported React Native hooks eagerly call `posthog?.getFeatureFlags()` / `posthog?.getFeatureFlag(flag)` while rendering. Since the `useState` value is only used for initial state, parent re-renders discard those results. The effect already refreshes state when the client or flag changes and subscribes to flag updates, so changing only these initializers to lazy `useState(() => ...)` functions preserves the existing update behavior.\n\nA focused PR would be welcome. Please include regression tests that rerender a hook consumer without changing flags and assert the corresponding client getter is not called again after mount. The change should stay scoped to the reported hooks; no core caching or broader refactor is needed for this fix.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/hooks/useFeatureFlags.ts",
        "packages/react-native/src/hooks/useFeatureFlag.ts",
        "packages/react-native/src/hooks/useFeatureFlagResult.ts",
        "packages/react-native/test/useFeatureFlagResult.spec.ts",
        "packages/core/src/posthog-core.ts",
        "packages/react/src/hooks/useFeatureFlagEnabled.ts",
        "packages/react/src/hooks/useActiveFeatureFlags.ts"
      ],
      "findings": [
        "`useFeatureFlags` currently initializes state with `posthog?.getFeatureFlags()` directly, and `useFeatureFlag` does the same with `posthog?.getFeatureFlag(flag)`, confirming both getters are evaluated during every render.",
        "Each reported hook's existing effect explicitly rereads the getter when its dependencies change and subscribes through `onFeatureFlags`, so a lazy initializer changes only discarded rerender work, not the initial value or update paths.",
        "`PostHogCore.getFeatureFlags()` calls `getFeatureFlagDetails()`, and `getKnownFeatureFlagDetails()` calls `normalizeFlagsResponse()` whenever stored flag details exist. `getFeatureFlag()` similarly reads feature-flag details through `_getFeatureFlagResult`, so avoiding unnecessary hook calls avoids synchronous flag-store processing.",
        "`@posthog/react` already uses lazy state initialization in `useFeatureFlagEnabled` and `useActiveFeatureFlags`, matching the proposed React pattern.",
        "There are no focused tests for the two reported React Native hooks. `useFeatureFlagResult` has an established React hook test setup that can be used as a minimal precedent for adding rerender regression coverage.",
        "`useFeatureFlagResult` also has an eager getter state initializer, but it is outside the issue's explicitly requested scope and should not be changed incidentally without corresponding coverage and maintainer agreement."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "Wrapping the two existing initial reads in lazy `useState` initializer functions is a two-line behavioral no-op after mount. Existing effects retain client-change, flag-change, reload, and reset handling.",
      "suggestedApproach": "In `packages/react-native/src/hooks/useFeatureFlags.ts`, change the initializer to `useState<...>(() => posthog?.getFeatureFlags())`. In `packages/react-native/src/hooks/useFeatureFlag.ts`, change it to `useState<...>(() => posthog?.getFeatureFlag(flag))`. Add focused hook tests that mount, allow the existing effect read, rerender without dependency changes, and verify no additional getter call occurs.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 714,
      "type": "issue",
      "state": "closed",
      "title": "[react hooks] Unnecessary re-render when feature flag is already loaded",
      "url": "https://github.com/PostHog/posthog-js/issues/714",
      "labels": [
        "frameworks/next-js"
      ],
      "bodyExcerpt": "It seems that all react hooks initially render w/o the feature flag value and they will be populated after the internal useEffect has ran.\r\nSee for example the following hook coming from [`useFeatureFlagEnabled.ts`](https://github.com/PostHog/posthog-js/blob/0fa6e1d8aa26f141f66932d0ce83dfd91b8a3003/react/src/hooks/useFeatureFlagEnabled.ts#L4C17-L4C38):\r\n\r\n```tsx\r\nexport function useFeatureFlagEnabled(flag: string): boolean | undefined {\r\n    const client = usePostHog()\r\n\r\n    const [featureEnabled, setFeatureEnabled] = useState<boolean | undefined>()\r\n    // would be nice to have a default value above however it's not possible due\r\n    // to a hydration error when using nextjs\r\n\r\n    useEffect(() => {\r\n        return client.onFeatureFlags(() => {\r\n            setFeatureEnabled(client.isFeatureEnabled(flag))\r\n        })\r\n    }, [client, flag])\r\n\r\n    return featureEnabled\r\n}\r\n```\r\n\r\nEach and every time this hook is called, the initial (default) `featureEnabled` will be `undefined` and a…",
      "createdAt": "2023-06-27T09:30:46Z",
      "reason": "explicit-reference"
    },
    {
      "number": 1719,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(flags): set default state values in feature flag hooks",
      "url": "https://github.com/PostHog/posthog-js/pull/1719",
      "labels": [],
      "bodyExcerpt": "## Changes\r\n\r\nContext: https://posthog.slack.com/archives/C07F4BF6WLT/p1738270875752869\r\n\r\nTLDR:\r\nTo suppress a NextJS hydration error, we were previously forgoing default state values in our feature flags hooks. This was necessary because the `posthog` client methods like `isFeatureEnabled` use browser-only APIs like `localStorage` or `document`, which cannot run in SSR. \r\n\r\nCurrently, we \"fix\" this issue by using solution #1 from the [nextjs docs](https://nextjs.org/docs/messages/react-hydration-error#possible-ways-to-fix-it). However, this results in the first render on the client side returning `undefined` from our feature flag hooks, which is both potentially incorrect (if flags are already loaded) and can cause bugs. Further, this hydration error does not actually apply to anyone using our React library for CSR-only.\r\n\r\nHowever, `posthog-js` does not actually support SSR in any case - `usePosthog` uses the SSR-violating `useContext` hook, and our documentation even requires the `…",
      "createdAt": "2025-02-05T23:39:46Z",
      "reason": "explicit-reference"
    },
    {
      "number": 3255,
      "type": "pull_request",
      "state": "closed",
      "title": "perf(browser): reduce runtime memory footprint and GC pressure by ~54%",
      "url": "https://github.com/PostHog/posthog-js/pull/3255",
      "labels": [
        "stale"
      ],
      "bodyExcerpt": "## Problem\n\nThe posthog-js browser SDK creates significant transient memory allocations during every `capture()` call and SDK initialization. These allocations increase GC pressure, which can cause jank and frame drops in performance-sensitive web applications. For customers capturing many events per page session, the cumulative GC cost is substantial.\n\n## Changes\n\nThis PR systematically reduces transient memory allocations (GC pressure) by **~54%** through ~30 targeted runtime optimizations across 10 source files. All changes are internal — no public API changes, no new dependencies, no breaking changes.\n\n### High-impact optimizations\n\n| Optimization | Impact | Description |\n|---|---|---|\n| **Cache session props** | −44% | `getSessionProps()` was called every capture, parsing URLs and campaign params each time. Now cached with session-change invalidation. |\n| **Parse URL query once** | −14% | `_getCampaignParamsFromUrl` called `getQueryParam` 20+ times per URL, each splitting the URL/…",
      "createdAt": "2026-03-19T07:06:02Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.11764705882352941,
    "canonical": {
      "number": 714,
      "type": "issue",
      "state": "closed",
      "title": "[react hooks] Unnecessary re-render when feature flag is already loaded",
      "url": "https://github.com/PostHog/posthog-js/issues/714",
      "labels": [
        "frameworks/next-js"
      ],
      "bodyExcerpt": "It seems that all react hooks initially render w/o the feature flag value and they will be populated after the internal useEffect has ran.\r\nSee for example the following hook coming from [`useFeatureFlagEnabled.ts`](https://github.com/PostHog/posthog-js/blob/0fa6e1d8aa26f141f66932d0ce83dfd91b8a3003/react/src/hooks/useFeatureFlagEnabled.ts#L4C17-L4C38):\r\n\r\n```tsx\r\nexport function useFeatureFlagEnabled(flag: string): boolean | undefined {\r\n    const client = usePostHog()\r\n\r\n    const [featureEnabled, setFeatureEnabled] = useState<boolean | undefined>()\r\n    // would be nice to have a default value above however it's not possible due\r\n    // to a hydration error when using nextjs\r\n\r\n    useEffect(() => {\r\n        return client.onFeatureFlags(() => {\r\n            setFeatureEnabled(client.isFeatureEnabled(flag))\r\n        })\r\n    }, [client, flag])\r\n\r\n    return featureEnabled\r\n}\r\n```\r\n\r\nEach and every time this hook is called, the initial (default) `featureEnabled` will be `undefined` and a…",
      "createdAt": "2023-06-27T09:30:46Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 12%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "3ba57edf017ce0aec034edb82d9c521c0eb5ab8b188f143a9773c1e20dd655b0",
  "piCalls": 3,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/bf990210b5f2a226d40a503e40dcdd80",
    "gistId": "bf990210b5f2a226d40a503e40dcdd80",
    "files": [
      {
        "name": "call-1-1-2026-08-14T12-21-14-387Z_01a00038-2412-747d-9933-3dfffab58dd4.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/bf990210b5f2a226d40a503e40dcdd80/raw/086c6946b1dae791c38f989610cf0a2699137326/call-1-1-2026-08-14T12-21-14-387Z_01a00038-2412-747d-9933-3dfffab58dd4.jsonl"
      }
    ]
  },
  "runId": 31799902130,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/31799902130"
}
```
