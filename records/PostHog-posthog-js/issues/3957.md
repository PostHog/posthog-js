# issue 3957: @posthog/convex - POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS setting is not respected

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3957
- Conclusion: Valid bug: the Convex polling interval env var is read too early to affect the registered cron schedule.
- Labels: feature/flags, team/feature-flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28122358217
- Updated: 2026-06-24T19:00:06.732Z

```json
{
  "triage": {
    "conclusion": "Valid bug: the Convex polling interval env var is read too early to affect the registered cron schedule.",
    "summary": "@posthog/convex advertises POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS as configurable, but the cron interval is computed during module-level cron registration, while Convex component env vars are only available at runtime. That means the schedule likely falls back to 60s and bakes that into the deployment.",
    "issueType": "bug",
    "confidence": 0.9,
    "labels": [
      "bug: Something isn't working",
      "feature/flags",
      "team/feature-flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. This looks valid from the current code.\n\n`packages/convex/src/component/crons.ts` calls `readPollingIntervalSeconds()` directly inside the top-level `crons.interval(...)` registration. That value is therefore resolved during Convex's module analysis / cron registration phase. The nearby comment already notes the same lifecycle issue for component env vars and `POSTHOG_PERSONAL_API_KEY`, so a component env var-based interval override cannot reliably affect the registered cron cadence.\n\nI don't think this should be fixed by moving the read into the handler, because the cron period is fixed at registration time. The minimal product/API decision is whether to introduce an analysis-time/install-time config path for the interval, or to remove/document the env var as unsupported so users are not given a dead configuration knob.",
    "investigation": {
      "relevantFiles": [
        "packages/convex/src/component/crons.ts",
        "packages/convex/src/component/convex.config.ts",
        "packages/convex/src/component/crons.test.ts",
        "packages/convex/CHANGELOG.md"
      ],
      "findings": [
        "`packages/convex/src/component/crons.ts` registers `crons.interval('Refresh PostHog feature flag definitions', { seconds: readPollingIntervalSeconds() }, ...)` at module scope.",
        "`readPollingIntervalSeconds()` reads `env.POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS`, which is generated as `process.env` in `packages/convex/src/component/_generated/server.ts`.",
        "The comment immediately above the cron registration says Convex forwards component env vars only at runtime, and explains why load-time gating on `POSTHOG_PERSONAL_API_KEY` was incorrect.",
        "`packages/convex/src/component/convex.config.ts` declares `POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS` as a component env var and documents it as parsed at module load.",
        "`packages/convex/CHANGELOG.md` advertises the env var as a way to tune the cron cadence, which conflicts with the current lifecycle behavior.",
        "`packages/convex/src/component/crons.test.ts` tests parsing behavior by mutating `process.env`, but does not cover the Convex deploy-analysis case where component env vars are absent during cron registration."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The root cause is a Convex lifecycle/API mismatch rather than a local parsing bug. A real behavior fix likely needs a supported analysis-time/install-time configuration mechanism for component cron registration, while the simpler alternative is a documentation/API cleanup that removes the unsupported env var. That decision should be made by maintainers.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "14d50a069d7105d37e290c40547fe9ac3bc7282bdb7de49edcf10b02a9dbe091",
  "piCalls": 1,
  "runId": 28122358217,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28122358217"
}
```
