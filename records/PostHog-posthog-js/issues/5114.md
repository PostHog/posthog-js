# issue 5114: Replay player drops insertRule styles (emotion/MUI) after a large add batch into <head> (regression in 1.434.12, #5073)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/5114
- Conclusion: Confirmed replay-player regression introduced by the large-add detached-subtree optimization.
- Labels: feature/replay, javascript, bug
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/36168754291
- Updated: 2026-09-25T17:44:15.681Z

```json
{
  "triage": {
    "conclusion": "Confirmed replay-player regression introduced by the large-add detached-subtree optimization.",
    "summary": "Large mutation batches whose first add targets <head> detach and reattach the entire head, which can discard CSSOM-only rules previously applied through CSSStyleSheet.insertRule.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "javascript"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. This is confirmed by the current replay implementation: the large-add optimization selects `adds[0].parentId` as the root to detach and excludes only `<html>`, so a batch beginning in `<head>` removes and reinserts the complete head subtree. That is unsafe for stylesheets whose effective rules exist only in CSSOM via `insertRule`.\n\nThe narrow fix should be to keep `<head>` on the normal live-DOM path. Please add a regression fixture with a stylesheet that receives a `StyleSheetRule` event before a >=1,000-add batch rooted in `<head>`, then assert that the pre-existing rule is still present after the batch. This preserves the performance optimization for other large subtrees without attempting speculative stylesheet-state reconstruction.",
    "investigation": {
      "relevantFiles": [
        "packages/rrweb/rrweb/src/replay/index.ts",
        "packages/rrweb/rrweb/test/replayer.test.ts",
        "packages/rrweb/rrweb/test/events/huge-add-mutation.ts",
        "packages/rrweb/rrweb/test/events/style-sheet-text-mutation.ts",
        "packages/rrweb/README.md"
      ],
      "findings": [
        "`DETACH_ADDS_THRESHOLD` is 1,000 in `packages/rrweb/rrweb/src/replay/index.ts`.",
        "`detachRootForLargeAddBatch()` selects `d.adds[0].parentId`, removes that node from its parent, and reinserts it in `finally`; it only rejects the document element and does not reject `<head>`.",
        "The same file applies recorded `StyleSheetRule` mutations directly to the target style element's `sheet`, so rules that exist in CSSOM rather than text content are tied to that stylesheet instance.",
        "The existing huge-add fixture exercises detachment beneath a body `<div>` and sibling ordering, while the stylesheet-rule fixtures test CSSOM mutations separately. There is no regression test combining a CSSOM rule with a large batch rooted in `<head>`.",
        "The vendored rrweb README states that this fork is maintained for PostHog-specific changes; this report concerns the PostHog replay implementation."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The faulty behavior is localized to the root eligibility check for the detached-subtree optimization. Excluding `<head>` avoids replacing live stylesheet objects and requires no public API or recorder-format change.",
      "suggestedApproach": "Add a `<head>` exclusion in `detachRootForLargeAddBatch()`. Add a focused replay regression test with a pre-batch CSSOM rule, a large mutation batch whose first add has `<head>` as parent, and an assertion that the original rule remains applied/present after replaying the batch.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 5073,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(replay): apply huge add mutations against a detached subtree",
      "url": "https://github.com/PostHog/posthog-js/pull/5073",
      "labels": [
        "stamphog"
      ],
      "bodyExcerpt": "## Problem\n\n- Watching a session recording whose page inserts tens of thousands of DOM nodes in one mutation freezes the viewer's tab. The recording is unwatchable.\n- A customer session carried one mutation batch of 25,746 `<style>` elements (one `@font-face` rule each, 23 MB of the 23.5 MB recording). Seeking through it blocked the main thread for 92 seconds in headless Chromium.\n- The replayer inserts each added node into the live document one at a time. Each stylesheet insert makes the browser update style state that grows with the sheets already present, so the cost is quadratic in batch size.\n\n## Changes\n\n- The replayer now applies a mutation of 1,000 or more adds against a detached subtree. It detaches the batch's target ancestor, runs the unchanged add loop, and reattaches in a `try/finally`. The document pays the style and layout update once. Seek time through the batch above: 92.5 s to 1.5 s.\n- Batches under 1,000 adds take exactly the old code path. Nothing a viewer sees chan…",
      "createdAt": "2026-09-22T23:10:28Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "647410fccd803a37b6412c3bd88a404d0666dbed1939c2cf3e6b064ea24e086b",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/642b816145a80503cb20fdfcff1cfbba",
    "gistId": "642b816145a80503cb20fdfcff1cfbba",
    "files": [
      {
        "name": "call-1-1-2026-09-25T17-43-22-416Z_01a0d9aa-2830-7def-9aa7-92884b4c308a.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/642b816145a80503cb20fdfcff1cfbba/raw/b6b8e2cd88095edefb499294a83be21b42db951c/call-1-1-2026-09-25T17-43-22-416Z_01a0d9aa-2830-7def-9aa7-92884b4c308a.jsonl"
      }
    ]
  },
  "runId": 36168754291,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/36168754291"
}
```
