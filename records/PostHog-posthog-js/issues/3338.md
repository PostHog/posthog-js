# issue 3338: Source map uploads for non expo project - bare bones react native support

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3338
- Conclusion: Valid feature gap for bare React Native Hermes sourcemap support; current serializer code is consistent with the reported failure.
- Labels: enhancement, help wanted, react-native, feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28284865427
- Updated: 2026-06-27T09:24:19.545Z

```json
{
  "triage": {
    "conclusion": "Valid feature gap for bare React Native Hermes sourcemap support; current serializer code is consistent with the reported failure.",
    "summary": "Bare React Native apps using Hermes and `createPostHogMetroSerializer()` can fail to produce usable PostHog Error Tracking sourcemaps because the injected chunk ID placeholder/key handling is Expo-oriented and the Hermes clone/upload flow may not propagate the needed ID.",
    "issueType": "feature",
    "confidence": 0.86,
    "labels": [
      "enhancement",
      "help wanted",
      "react-native",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed repro and root-cause notes. I checked the current source and this looks actionable: `createDebugIdSnippet()` writes `_posthogChunkIds[n] = \"...\"`, while `determineDebugIdFromBundleSource()` only matches string/numeric bracket keys, so the reported `Chunk ID was not found in the bundle` failure is plausible. The non-Expo serializer path also injects `__POSTHOG_CHUNK_ID__` directly, while the Expo path receives a `debugId` from Expo's asset serialization plugin.\n\nI would keep this tracked as bare React Native Hermes sourcemap support for Error Tracking rather than close it as user error. A complete fix likely needs serializer changes plus verification of the `posthog-cli hermes clone/upload` metadata propagation, so it should be tested end-to-end against a bare RN Hermes bundle.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/tooling/utils.ts",
        "packages/react-native/src/tooling/posthogMetroSerializer.ts",
        "packages/react-native/src/tooling/metroconfig.ts",
        "packages/react-native/tooling/posthog.gradle",
        "packages/react-native/tooling/posthog-xcode.sh",
        "packages/core/src/error-tracking/chunk-ids.ts",
        "packages/react-native/package.json"
      ],
      "findings": [
        "`packages/react-native/src/tooling/utils.ts` defines `createDebugIdSnippet(debugId)` as code which stores the ID under `_posthogChunkIds[n]`, where `n` is a variable containing an Error stack string.",
        "`determineDebugIdFromBundleSource()` in the same file currently matches only `_posthogChunkIds[...]` keys that are string literals, single-quoted literals, or digits, so it will not match the generated `[n]` form.",
        "`packages/react-native/src/tooling/posthogMetroSerializer.ts` injects a module with `DEBUG_ID_PLACE_HOLDER = '__POSTHOG_CHUNK_ID__'`, calls `determineDebugIdFromBundleSource(bundleCode)`, throws `Chunk ID was not found in the bundle.` if no match is found, and writes the returned value into the sourcemap as `chunkId`.",
        "`packages/react-native/src/tooling/metroconfig.ts` shows the Expo path uses `unstableBeforeAssetSerializationDebugIdPlugin` with a `debugId` provided by Expo; that is separate from the bare/custom serializer path reported here.",
        "`packages/react-native/tooling/posthog.gradle` and `packages/react-native/tooling/posthog-xcode.sh` invoke `posthog-cli hermes clone` followed by `posthog-cli hermes upload`, but the implementation of the CLI command itself is not present in this repository, so the reported clone metadata behavior cannot be fully verified from these files alone.",
        "`packages/core/src/error-tracking/chunk-ids.ts` reads runtime IDs from `globalThis._posthogChunkIds` and maps them to parsed stack frame filenames, confirming the injected runtime map is part of the Error Tracking chunk ID flow."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The first serializer failure is likely a small targeted change, but the complete request spans bare RN Metro serialization, placeholder-to-real-ID generation, Hermes composed sourcemap metadata, and `posthog-cli hermes clone/upload` behavior. The CLI implementation is not in this repo, and the fix should be validated with a bare React Native Hermes bundle rather than a speculative regex-only patch.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "0e2a5e3026eba1373656dde0560023aa5eeef3f69cd70bf0c21c3b1df1a18c06",
  "piCalls": 7,
  "runId": 28284865427,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28284865427"
}
```
