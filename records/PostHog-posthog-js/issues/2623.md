# issue 2623: Feature flag value changes after identify, `Flag persistence` is enabled

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2623
- Conclusion: Valid feature flag persistence bug with a plausible SDK-side race around identify-triggered flag reloads.
- Labels: feature/flags, team/feature-flags, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28507814375
- Updated: 2026-07-01T09:35:42.542Z

```json
{
  "triage": {
    "conclusion": "Valid feature flag persistence bug with a plausible SDK-side race around identify-triggered flag reloads.",
    "summary": "After an anonymous user evaluates a persistent feature flag, calling identify can cause the client to receive/use a different flag variant, despite flag persistence being enabled. The SDK is intended to send the previous anonymous distinct ID on the identify-triggered /flags request, but the current reload/debounce flow can drop it in at least one race window.",
    "issueType": "bug",
    "confidence": 0.82,
    "labels": [
      "bug: Something isn't working",
      "feature/flags",
      "team/feature-flags",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed repro and confirmations. I inspected the browser SDK path around `identify()` and feature flag reloads. The SDK does set the previous anonymous distinct ID for flag consistency when identity changes, but there appears to be a race window: if an existing `/flags` request completes after `identify()` queues a debounced reload but before that reload actually starts, the successful older response can clear the stored `$anon_distinct_id`. The subsequent identify-triggered `/flags` request may then be sent without `$anon_distinct_id`, so server-side flag persistence cannot preserve the anonymous assignment.\n\nA focused regression test should cover: initial `/flags` request in flight → `identify()` called → initial response returns before the identify reload debounce fires → assert the next `/flags` request still includes the previous anonymous distinct ID.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-featureflags.ts",
        "packages/browser/src/__tests__/posthog-core.identify.test.ts",
        "packages/browser/src/__tests__/featureflags.test.ts",
        "packages/types/src/feature-flags.ts"
      ],
      "findings": [
        "`PostHog.identify()` stores the new user ID, captures `$identify`, calls `featureFlags.setAnonymousDistinctId(previous_distinct_id)`, and then calls `reloadFeatureFlags()` when the distinct ID changes.",
        "`PostHogFeatureFlags._callFlagsEndpoint()` includes `$anon_distinct_id: this.$anon_distinct_id` in the `/flags/?v=2` request payload, which is the SDK mechanism needed for persistence across anonymous-to-identified transitions.",
        "The `/flags` callback clears `this.$anon_distinct_id` after a 200 response when `_additionalReloadRequested` is false.",
        "`reloadFeatureFlags()` only sets a 5ms debouncer; `_additionalReloadRequested` is not set until the debounced `_callFlagsEndpoint()` runs and sees `_requestInFlight`.",
        "Therefore, if `identify()` queues a reload while another `/flags` request is in flight, and the older request completes before the 5ms debounce fires, the callback can clear `$anon_distinct_id` before the identify-triggered request is sent.",
        "Existing tests verify that identify calls `setAnonymousDistinctId()` and that requests include `$anon_distinct_id` in simple cases, but I did not find a regression test for the in-flight request/debounced reload ordering above."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small SDK PR is appropriate if scoped to preserving the anonymous distinct ID until the identify-triggered flags request has actually been sent. The likely change is localized to the feature flag reload/callback state machine plus one regression test.",
      "suggestedApproach": "Add a regression test for the race where an initial `/flags` response completes after `identify()` schedules a reload but before the debounce fires. Then adjust the clearing condition for `this.$anon_distinct_id` so it is not cleared while a reload debouncer is pending, or mark an additional reload as requested immediately when `reloadFeatureFlags()` is called during an in-flight request.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "fb3c5c2e4c8c60267da778dcc76f559ce49f80729f7b8859b310c670872e5ab9",
  "piCalls": 2,
  "runId": 28507814375,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28507814375"
}
```
