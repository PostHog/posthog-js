# issue 5084: Local flag evaluation keeps Durable Objects alive indefinitely

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/5084
- Conclusion: Confirmed posthog-node bug: local feature-flag evaluation always schedules a recurring timer, including when a shared cache says this instance should not fetch definitions.
- Labels: node, feature/flags, team/client-libraries, performance, bug
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/35945126886
- Updated: 2026-09-24T01:58:23.394Z

```json
{
  "triage": {
    "conclusion": "Confirmed posthog-node bug: local feature-flag evaluation always schedules a recurring timer, including when a shared cache says this instance should not fetch definitions.",
    "summary": "In Cloudflare Durable Objects, the local-evaluation poller’s recurring timeout can keep a long-lived object billable. A cache provider reduces definition fetches but does not prevent the SDK from scheduling the next poll.",
    "issueType": "bug",
    "confidence": 0.94,
    "labels": [
      "bug: Something isn't working",
      "node",
      "feature/flags",
      "team/client-libraries",
      "performance"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report — this is confirmed in the Node SDK.\n\n`posthog-node` creates the local-evaluation poller whenever a secret/personal API key is provided and `enableLocalEvaluation` is not explicitly disabled. After every definition-load attempt, including a cache-only load where `shouldFetchFlagDefinitions()` returns `false`, the poller schedules another `setTimeout`. That timer remains until `shutdown()` is called.\n\nA cache provider therefore avoids duplicate definition fetches, but does not avoid the Durable Object lifetime/billing effect. `shutdown()` clears the poller, but is not a suitable workaround for an object that intentionally remains alive for WebSockets.\n\nThe requested stale-while-revalidate/on-demand mode is plausible, but needs an explicit freshness and API decision before implementation: it would trade periodic definition freshness for refreshing when flags are evaluated. A timer-only workaround should not be assumed to work across Durable Object runtimes without verifying their timer/ref semantics.",
    "investigation": {
      "relevantFiles": [
        "packages/node/src/client.ts",
        "packages/node/src/extensions/feature-flags/feature-flags.ts",
        "packages/node/src/extensions/feature-flags/cache.ts",
        "packages/node/src/types.ts",
        "packages/node/src/__tests__/feature-flags.spec.ts",
        "packages/node/src/__tests__/posthog-node.spec.ts"
      ],
      "findings": [
        "`PostHogBackendClient` creates `FeatureFlagsPoller` when a secret/personal API key is configured and `enableLocalEvaluation !== false`; local evaluation defaults on in that situation.",
        "The Node client defaults `featureFlagsPollingInterval` to 30,000 ms and passes it to the poller.",
        "`FeatureFlagsPoller._loadFeatureFlags()` consults `cacheProvider.shouldFetchFlagDefinitions()`. When it returns false, the poller can load cached definitions and return without requesting PostHog.",
        "Regardless of whether definitions were fetched, cache-loaded, stale, or an error occurred, `_loadFeatureFlags()` schedules `setTimeout(() => this.loadFeatureFlags(true), this.getPollingInterval())` in its `finally` block unless stopped.",
        "`stopPoller()` clears that timeout, and client `_shutdown()` calls `featureFlagsPoller.stopPoller()`. The public shutdown contract documents that it clears the feature-flags poller.",
        "Existing tests verify that polling is rescheduled after a slow fetch and that stopping the poller prevents rescheduling, but do not cover a Durable Object-compatible cache-only/on-demand mode."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The immediate cause is localized, but a correct fix needs an agreed freshness policy. Disabling the recurring timer changes local-evaluation consistency, and exposing stale-while-revalidate behavior would be a public configuration/API decision. Using a Node-only timer `unref()` is not a safe assumed fix for Cloudflare Durable Objects.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "d521f553f5828d95d216d3ba7e1c09d4bad1f6c14880ff5c29c0448eaf5be041",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/8c28abfd53309106f44c9161d371ad71",
    "gistId": "8c28abfd53309106f44c9161d371ad71",
    "files": [
      {
        "name": "call-1-1-2026-09-24T01-57-45-460Z_01a0d122-0f74-74ae-9ac9-19e9ef80b5d9.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/8c28abfd53309106f44c9161d371ad71/raw/c871313094611c3baaa8bafeebdb16010ba079c3/call-1-1-2026-09-24T01-57-45-460Z_01a0d122-0f74-74ae-9ac9-19e9ef80b5d9.jsonl"
      }
    ]
  },
  "runId": 35945126886,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/35945126886"
}
```
