# issue 4132: jsonStringify throws on circular references (DOM nodes with React fibers); with capture_exceptions the throw is recaptured in a loop

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4132
- Conclusion: Valid browser SDK bug; circular event properties can break request serialization and cause retries/internal exception noise.
- Labels: feature/error-tracking, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29189622492
- Updated: 2026-07-12T10:46:41.640Z

```json
{
  "triage": {
    "conclusion": "Valid browser SDK bug; circular event properties can break request serialization and cause retries/internal exception noise.",
    "summary": "The browser SDK's shared jsonStringify helper only handles BigInt and still uses JSON.stringify directly, so cyclic values such as React DOM nodes throw during request body serialization. The request/retry/error-tracking paths can then turn that serialization failure into repeated retries or captured internal errors.",
    "issueType": "bug",
    "confidence": 0.92,
    "labels": [
      "bug: Something isn't working",
      "feature/error-tracking",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Valid bug. `packages/browser/src/request.ts` exports `jsonStringify`, and its replacer only converts `bigint`; it does not guard circular references. `capture()` forwards user properties into the request payload, so a DOM node with React fiber back-references can reach this serializer.\n\nA small, low-risk fix is appropriate: keep the current fast `JSON.stringify` path unchanged, and on failure retry with a circular-aware replacer that also preserves the existing BigInt-to-string behavior. Tests should cover circular request data, BigInt behavior, and ideally the shared-but-acyclic reference case so only genuine back-references become `\"[Circular]\"`.\n\nI would not broaden this into DOM-node filtering or general SDK-exception suppression in the same change; those are follow-ups.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/request.ts",
        "packages/browser/src/__tests__/request.test.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/retry-queue.ts",
        "packages/browser/src/extensions/exception-autocapture/index.ts",
        "packages/browser/src/posthog-exceptions.ts",
        "packages/browser/src/utils/property-utils.ts"
      ],
      "findings": [
        "`packages/browser/src/request.ts` defines `jsonStringify` as `JSON.stringify(data, (_, value) => (typeof value === 'bigint' ? value.toString() : value), space)`, which handles BigInt but not circular object graphs.",
        "`encodePostData`, `encodeToDataString`, gzip/base64 handling, and async gzip pre-encoding all route request bodies through `jsonStringify`, so this helper is on the shared browser request serialization path.",
        "`packages/browser/src/posthog-core.ts` builds capture payloads from user-supplied properties and sends them as `requestOptions.data`; there is no generic cycle removal before the request layer.",
        "`packages/browser/src/__tests__/request.test.ts` has coverage for BigInt conversion and for JSON serialization failures being reported through callbacks, but no coverage for circular data being serialized successfully.",
        "`packages/browser/src/retry-queue.ts` retries statusCode 0 failures up to the status-zero retry budget, so serialization failures reported as statusCode 0 can be retried rather than dropped immediately.",
        "`packages/browser/src/extensions/exception-autocapture/index.ts` can send captured exceptions through `sendExceptionEvent`; `packages/browser/src/posthog-exceptions.ts` has some SDK-exception suppression, but the root serialization failure still needs to be avoided."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The root cause is localized to the shared JSON serialization helper. A fallback-only circular-safe stringify preserves normal payloads and existing fast-path behavior while making cyclic payloads serializable.",
      "suggestedApproach": "Update `jsonStringify` in `packages/browser/src/request.ts` to first try the current BigInt-aware `JSON.stringify`. If it throws, retry with a circular-aware replacer that tracks the current ancestor stack and returns `\"[Circular]\"` only for true back-references, while still converting BigInt to strings. Add request tests for a self-referential object, a React-like element/fiber cycle, BigInt preservation, and shared-but-acyclic references.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4133,
      "type": "pull_request",
      "state": "open",
      "title": "fix(web): make jsonStringify circular-safe so event serialization never throws",
      "url": "https://github.com/PostHog/posthog-js/pull/4133",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`jsonStringify` in `packages/browser/src/request.ts` — the serializer every request body passes through — only guards `BigInt`. When a captured property holds a circular value, `JSON.stringify` throws `Converting circular structure to JSON`.\n\nThe common trigger on a React app is a DOM node: React attaches `__reactFiber$…` / `__reactProps$…` to elements, and a fiber's `stateNode` points back at the element, so any element reference that reaches event properties is circular. With `capture_exceptions` enabled, the `TypeError` the serializer throws is itself autocaptured as a new `$exception` — and when the condition recurs it turns into a burst of `handled` self-referential exceptions. We saw ~100 identical `$exception`s in a ~2 second window in production.\n\nFixes #4132.\n\n## Changes\n\n`jsonStringify` now falls back to a circular-safe replacer **only when the fast path throws**, so serialization always succeeds instead of throwing:\n\n- The common (non-circular) path is byte-for-b…",
      "createdAt": "2026-07-12T10:12:30Z",
      "reason": "closing-pr"
    }
  ],
  "duplicate": {
    "duplicate": true,
    "score": 1,
    "canonical": {
      "number": 4133,
      "type": "pull_request",
      "state": "open",
      "title": "fix(web): make jsonStringify circular-safe so event serialization never throws",
      "url": "https://github.com/PostHog/posthog-js/pull/4133",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`jsonStringify` in `packages/browser/src/request.ts` — the serializer every request body passes through — only guards `BigInt`. When a captured property holds a circular value, `JSON.stringify` throws `Converting circular structure to JSON`.\n\nThe common trigger on a React app is a DOM node: React attaches `__reactFiber$…` / `__reactProps$…` to elements, and a fiber's `stateNode` points back at the element, so any element reference that reaches event properties is circular. With `capture_exceptions` enabled, the `TypeError` the serializer throws is itself autocaptured as a new `$exception` — and when the condition recurs it turns into a burst of `handled` self-referential exceptions. We saw ~100 identical `$exception`s in a ~2 second window in production.\n\nFixes #4132.\n\n## Changes\n\n`jsonStringify` now falls back to a circular-safe replacer **only when the fast path throws**, so serialization always succeeds instead of throwing:\n\n- The common (non-circular) path is byte-for-b…",
      "createdAt": "2026-07-12T10:12:30Z",
      "reason": "closing-pr"
    },
    "blockingPr": {
      "number": 4133,
      "type": "pull_request",
      "state": "open",
      "title": "fix(web): make jsonStringify circular-safe so event serialization never throws",
      "url": "https://github.com/PostHog/posthog-js/pull/4133",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`jsonStringify` in `packages/browser/src/request.ts` — the serializer every request body passes through — only guards `BigInt`. When a captured property holds a circular value, `JSON.stringify` throws `Converting circular structure to JSON`.\n\nThe common trigger on a React app is a DOM node: React attaches `__reactFiber$…` / `__reactProps$…` to elements, and a fiber's `stateNode` points back at the element, so any element reference that reaches event properties is circular. With `capture_exceptions` enabled, the `TypeError` the serializer throws is itself autocaptured as a new `$exception` — and when the condition recurs it turns into a burst of `handled` self-referential exceptions. We saw ~100 identical `$exception`s in a ~2 second window in production.\n\nFixes #4132.\n\n## Changes\n\n`jsonStringify` now falls back to a circular-safe replacer **only when the fast path throws**, so serialization always succeeds instead of throwing:\n\n- The common (non-circular) path is byte-for-b…",
      "createdAt": "2026-07-12T10:12:30Z",
      "reason": "closing-pr"
    },
    "reason": "open related PR #4133 already appears to cover this report"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "open related PR [#4133](https://github.com/PostHog/posthog-js/pull/4133) already appears to cover this report",
  "snapshotHash": "9c0a4445b03498fe7c85142de6f21140d6b821e2f448398911a3c1cf9d831be3",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/baa75f30796207e37be802c6c3129332",
    "gistId": "baa75f30796207e37be802c6c3129332",
    "files": [
      {
        "name": "call-1-1-2026-07-12T10-45-02-999Z_019f55ee-3797-7076-8a13-76238f7e2bbb.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/baa75f30796207e37be802c6c3129332/raw/629b2cbbec5d277334da60b9b258798c71531902/call-1-1-2026-07-12T10-45-02-999Z_019f55ee-3797-7076-8a13-76238f7e2bbb.jsonl"
      }
    ]
  },
  "runId": 29189622492,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29189622492"
}
```
