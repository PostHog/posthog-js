# issue 3130: Session continuity across consent state changes

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3130
- Conclusion: Valid feature request, but part of the requested API already exists and same-instance persistence switching should already preserve the current session in this codebase.
- Labels: enhancement, feature, feature/web-analytics, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28317863973
- Updated: 2026-06-28T09:32:04.694Z

```json
{
  "triage": {
    "conclusion": "Valid feature request, but part of the requested API already exists and same-instance persistence switching should already preserve the current session in this codebase.",
    "summary": "The issue asks for session continuity when moving from pre-consent memory persistence to post-consent durable persistence. Current code already supports init-time bootstrap.sessionID and migrates in-memory persistence props when persistence changes, but it does not implement an automatic memory+session-cookie/sessionStorage mode, and runtime set_config bootstrap adoption is not clearly supported.",
    "issueType": "feature",
    "confidence": 0.78,
    "labels": [
      "enhancement",
      "feature",
      "feature/web-analytics",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. A couple of codebase findings:\n\n- `bootstrap.sessionID` already exists in the SDK config and is consumed by `SessionIdManager` during initialization, provided it is a valid UUIDv7.\n- For the specific flow `init({ persistence: 'memory' })` followed later by `set_config({ persistence: 'localStorage+cookie' })` on the same PostHog instance, the current persistence layer appears intended to migrate existing in-memory props, including `$session_id`, into the new backend rather than generating a new session.\n- The remaining product gap seems to be automatic pre-consent persistence across reloads/pages, e.g. a `memory+session_cookie` mode or a sessionStorage-backed session option. Those modes/configs do not appear to exist today.\n\nIf you are still seeing a new `$session_id` with the same-instance `set_config` flow on the latest SDK, please share the exact init/update sequence, especially whether GTM is reinitializing PostHog instead of updating the existing instance. Otherwise this looks like a feature request for an automatic consent-safe session continuity mode rather than a small bug fix.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/sessionid.ts",
        "packages/types/src/posthog-config.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-persistence.ts",
        "packages/browser/src/__tests__/sessionid.test.ts"
      ],
      "findings": [
        "packages/types/src/posthog-config.ts defines BootstrapConfig.sessionID and documents that it must be a valid UUIDv7 with timestamp constraints.",
        "packages/browser/src/sessionid.ts reads config.bootstrap?.sessionID in the SessionIdManager constructor, parses the UUIDv7 timestamp with uuid7ToTimestampMs, and calls _setSessionId to seed the session.",
        "packages/browser/src/__tests__/sessionid.test.ts has coverage for bootstrapping the session id.",
        "packages/browser/src/posthog-core.ts set_config updates the existing persistence object and does not recreate SessionIdManager.",
        "packages/browser/src/posthog-persistence.ts update_config preserves this.props and saves them into the new backend when persistence changes, so memory -> localStorage/localStorage+cookie should carry the existing SESSION_ID for the same instance.",
        "The supported persistence union is currently 'localStorage' | 'cookie' | 'memory' | 'localStorage+cookie' | 'sessionStorage'; searches found no memory+session_cookie, session_cookie_name, or preserve_session_across_pages config."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A narrow regression test for same-instance memory-to-durable persistence would be small, but the requested automatic pre-consent continuity mode involves new persistence semantics, consent/privacy product decisions, session timeout handling, and session-manager lifecycle behavior. Runtime adoption of bootstrap.sessionID via set_config would also need careful handling of validation and session-change callbacks.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 2095,
      "type": "issue",
      "state": "closed",
      "title": "Opt-Capturing persistence cookie is being ignored",
      "url": "https://github.com/PostHog/posthog-js/issues/2095",
      "labels": [],
      "bodyExcerpt": "Hi, I'm using posthog-js with a Vue/Nuxt setup, and I believe there's an issue with how opt_out_capturing_persistence_type is handled during initialization:\n\n```ts\nposthog.init('<ph_project_api_key>', {\n    opt_out_capturing_persistence_type: 'cookie',\n});\n```\nWhen I call `opt_out_capturing()`, it partially works — the value is set, but it still gets saved in localStorage instead of a cookie.\n\nFrom what I can tell (though I'm not a JavaScript expert — just trying to implement cross-subdomain opt-out in existing website), this might be caused by the following logic in consent.ts:\n\n```ts\n    private get _storage() {\n        if (!this._persistentStore) {\n            const persistenceType = this._config.opt_out_capturing_persistence_type\n            this._persistentStore = persistenceType === 'localStorage' ? localStore : cookieStore\n            const otherStorage = persistenceType === 'localStorage' ? cookieStore : localStore\n\n            if (otherStorage._get(this._storageKey)) {\n       …",
      "createdAt": "2025-07-17T14:20:09Z",
      "reason": "explicit-reference"
    },
    {
      "number": 1898,
      "type": "issue",
      "state": "closed",
      "title": "`ReferenceError: localStorage is not defined` with posthog.reset() and memory persistence",
      "url": "https://github.com/PostHog/posthog-js/issues/1898",
      "labels": [
        "bug",
        "feature/surveys"
      ],
      "bodyExcerpt": "I'm trying out posthog in a chrome extension and would like to capture some events in the service worker, which of course means, it has no access to localStorage, sessionStorage, etc.\n\nI've set persistence to \"memory\" but get an error on `posthog.reset()` on user logout that `ReferenceError: localStorage is not defined`\n\nIt seems to be coming from `posthog-surveys.ts`\n```\n    reset(): void {\n        localStorage.removeItem('lastSeenSurveyDate')\n        const surveyKeys = getSurveySeenStorageKeys()\n        surveyKeys.forEach((key) => localStorage.removeItem(key))\n    }\n```\n\nI initialize posthog with\n```\n        posthog.init(process.env.POSTHOG_API_KEY, {\n            api_host: process.env.POSTHOG_HOST,\n            persistence: \"memory\",\n            disable_session_recording: true,\n            autocapture: false,\n            capture_pageview: false,\n            capture_pageleave: false,\n            person_profiles: \"identified_only\"\n        });\n\n```",
      "createdAt": "2025-04-12T03:37:52Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.07142857142857142,
    "canonical": {
      "number": 2095,
      "type": "issue",
      "state": "closed",
      "title": "Opt-Capturing persistence cookie is being ignored",
      "url": "https://github.com/PostHog/posthog-js/issues/2095",
      "labels": [],
      "bodyExcerpt": "Hi, I'm using posthog-js with a Vue/Nuxt setup, and I believe there's an issue with how opt_out_capturing_persistence_type is handled during initialization:\n\n```ts\nposthog.init('<ph_project_api_key>', {\n    opt_out_capturing_persistence_type: 'cookie',\n});\n```\nWhen I call `opt_out_capturing()`, it partially works — the value is set, but it still gets saved in localStorage instead of a cookie.\n\nFrom what I can tell (though I'm not a JavaScript expert — just trying to implement cross-subdomain opt-out in existing website), this might be caused by the following logic in consent.ts:\n\n```ts\n    private get _storage() {\n        if (!this._persistentStore) {\n            const persistenceType = this._config.opt_out_capturing_persistence_type\n            this._persistentStore = persistenceType === 'localStorage' ? localStore : cookieStore\n            const otherStorage = persistenceType === 'localStorage' ? cookieStore : localStore\n\n            if (otherStorage._get(this._storageKey)) {\n       …",
      "createdAt": "2025-07-17T14:20:09Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 7%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "6110947aa2eee8851f9812d237127f4bbd1113c2ad9abf40913e2c89dc9e7cae",
  "piCalls": 2,
  "runId": 28317863973,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28317863973"
}
```
