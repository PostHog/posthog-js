# issue 4310: UMD build's react bindings pass unwrapped CJS namespace to setDefaultPostHogInstance since 1.407.3

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4310
- Conclusion: Confirmed likely UMD/CJS interop regression in the React bindings' default-instance fallback.
- Labels: web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/30455217900
- Updated: 2026-07-29T13:18:23.473Z

```json
{
  "triage": {
    "conclusion": "Confirmed likely UMD/CJS interop regression in the React bindings' default-instance fallback.",
    "summary": "The React package imports the default posthog-js instance and stores it as the global fallback, but its UMD build has no explicit external-module interop policy. In affected UMD output, CommonJS receives the posthog-js module namespace rather than its default instance.",
    "issueType": "bug",
    "confidence": 0.97,
    "labels": [
      "bug: Something isn't working",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report and published-bundle comparison. The source and package configuration support this: `@posthog/react` uses a default `posthog-js` import to initialize the fallback, while its package `main` points to the UMD bundle and it has no `exports` map. The UMD Rollup output currently has no explicit interop setting, so a Rollup interop behavior change can cause the CJS `require('posthog-js')` namespace to be stored instead of the default instance.\n\nA fix should explicitly preserve compatibility default interop for the UMD build and add a regression test that executes or otherwise validates the generated UMD bundle with a CommonJS-shaped `posthog-js` dependency. The test should verify that the default fallback exposes instance methods such as `capture` and `isFeatureEnabled`, not merely that the source-level import type-checks.",
    "investigation": {
      "relevantFiles": [
        "packages/react/src/index.ts",
        "packages/react/src/context/posthog-default.ts",
        "packages/react/src/context/PostHogContext.ts",
        "packages/react/src/hooks/usePostHog.ts",
        "packages/react/src/hooks/useFeatureFlagEnabled.ts",
        "packages/react/rollup.config.mjs",
        "packages/react/package.json",
        "packages/react/src/__tests__/slim-bundle.test.ts",
        "packages/browser/package.json"
      ],
      "findings": [
        "`packages/react/src/index.ts` default-imports `posthog-js` and immediately passes that value to `setDefaultPostHogInstance`.",
        "`setDefaultPostHogInstance` stores its argument without runtime normalization, so a CommonJS module namespace remains the context fallback.",
        "`PostHogContext` retrieves that stored fallback lazily, and `usePostHog` returns it directly.",
        "`useFeatureFlagEnabled` invokes `client.isFeatureEnabled(flag)` during its initial state calculation, which matches the reported failure when `client` is a module namespace rather than a PostHog instance.",
        "`packages/react/package.json` sets `main` to `dist/umd/index.js`, sets `module` to the ESM build, and has no `exports` field.",
        "`packages/react/rollup.config.mjs` marks `posthog-js` external for the UMD build but does not set `output.interop`; this leaves default-import handling dependent on Rollup's behavior.",
        "The existing generated-bundle test only asserts that the slim UMD build has no `posthog-js` runtime import; there is no regression coverage for the full UMD bundle's CommonJS default-import semantics.",
        "`packages/browser/package.json` packages the React distribution under `react/dist/**`, making this UMD binding path part of the published posthog-js package."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The failing boundary is narrowly identified: UMD external default-import interop for `posthog-js`. The source fallback mechanism itself need not be redesigned.",
      "suggestedApproach": "Set an explicit compatibility default-interoperability mode on the React UMD Rollup output so a CommonJS `posthog-js` module with a `default` export is unwrapped before `setDefaultPostHogInstance` is called. Add focused generated-UMD regression coverage using a CommonJS-shaped posthog-js dependency and assert the fallback supports `capture` and `isFeatureEnabled`. Verify the ESM and UMD React builds afterward.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "754c25293d945c389175ee6a3c27a183e90ba1a2a20c3ff10154f2d2569988c1",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/c322e0bc14bfb925ff08db75338c9534",
    "gistId": "c322e0bc14bfb925ff08db75338c9534",
    "files": [
      {
        "name": "call-1-1-2026-07-29T13-17-23-536Z_019fae05-ccd0-7e2c-a743-02a49c51bb14.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/c322e0bc14bfb925ff08db75338c9534/raw/61917b5a3ec8807235b7c3982d66fa8dbfa8a4f4/call-1-1-2026-07-29T13-17-23-536Z_019fae05-ccd0-7e2c-a743-02a49c51bb14.jsonl"
      }
    ]
  },
  "runId": 30455217900,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/30455217900"
}
```
