# issue 3594: Toolbar does not respect `ui_host` param when init-ing posthog-js with nextjs rewrites enabled

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3594
- Conclusion: Valid toolbar/proxy routing issue; current code only uses ui_host for toolbar API params, not for loading toolbar.js.
- Labels: feature/toolbar, frameworks/next-js, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28434455752
- Updated: 2026-06-30T09:36:45.252Z

```json
{
  "triage": {
    "conclusion": "Valid toolbar/proxy routing issue; current code only uses ui_host for toolbar API params, not for loading toolbar.js.",
    "summary": "With `api_host: \"/ingest\"` and `ui_host: \"https://us.posthog.com\"`, the SDK still resolves the toolbar bundle as `/ingest/static/toolbar.js...` because external dependency loading uses the asset endpoint derived from `api_host`.",
    "issueType": "bug",
    "confidence": 0.86,
    "labels": [
      "bug: Something isn't working",
      "feature/toolbar",
      "frameworks/next-js",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear repro. I checked the current SDK routing and this matches the reported behavior:\n\n- `ui_host` is respected when building the toolbar params passed to the loaded toolbar (`apiURL`).\n- The toolbar bundle itself is loaded by the external dependency loader via the `assets` route, not the `ui` route.\n- For a custom/relative `api_host` like `/ingest`, `RequestRouter.endpointFor('assets', '/static/toolbar.js...')` resolves to `/ingest/static/toolbar.js...`, which matches the failed request shown here.\n\nThere is now an `asset_host` config specifically for static dependencies such as `toolbar.js`, so a likely workaround is setting an asset host explicitly, e.g. the appropriate PostHog assets host for the project region, alongside `ui_host`. Maintainers should decide whether the intended fix is code behavior (`ui_host` should also steer toolbar bundle loading when set) or documentation/config guidance (`asset_host` is required for proxied static toolbar assets).",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/toolbar.ts",
        "packages/browser/src/entrypoints/external-scripts-loader.ts",
        "packages/browser/src/utils/request-router.ts",
        "packages/browser/src/__tests__/extensions/toolbar.test.ts",
        "packages/browser/src/__tests__/utils/external-scripts-loader.test.ts",
        "packages/browser/src/__tests__/utils/request-router.test.ts",
        "packages/types/src/posthog-config.ts",
        "packages/browser/CHANGELOG.md"
      ],
      "findings": [
        "`packages/browser/src/extensions/toolbar.ts` sets `apiURL: this.instance.requestRouter.endpointFor('ui')`, so `ui_host` affects the params passed into the toolbar after it loads.",
        "`packages/browser/src/entrypoints/external-scripts-loader.ts` loads `toolbar` through `posthog.requestRouter.endpointFor('assets', scriptUrlToLoad)`, so the toolbar JavaScript bundle source does not use the `ui` endpoint.",
        "`packages/browser/src/utils/request-router.ts` treats non-PostHog hosts, including relative `/ingest`, as `custom`; for custom regions, `endpointFor('assets', '/static/toolbar.js...')` returns `apiHost + path`, i.e. `/ingest/static/toolbar.js...`.",
        "`packages/browser/src/__tests__/utils/request-router.test.ts` already verifies that `ui_host` is only used for the `ui` target.",
        "`packages/types/src/posthog-config.ts` documents `asset_host` as an override for static assets including `toolbar.js`; `packages/browser/CHANGELOG.md` shows this became a supported config option in PR #3777."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The reproduction is clear, but the correct contract needs maintainer confirmation: either change toolbar bundle loading to prefer `ui_host` in this scenario, or document/use the newer `asset_host` option for proxied static dependencies. A code change would be small but could affect toolbar script loading, CSP, and custom host setups.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "cf92ba22c7e66444f33a2e6968e9155519b7ff3266b5599acef52a5af0c84274",
  "piCalls": 3,
  "runId": 28434455752,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28434455752"
}
```
