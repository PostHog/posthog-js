# issue 4208: save_referrer() overwrites posthog.register() values for $referrer and $referring_domain in SPA + iframe contexts

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4208
- Conclusion: Valid browser SDK bug: internally saved session referrer can override user-registered $referrer/$referring_domain.
- Labels: web, feature/web-analytics, feature/product-analytics, team/web-analytics
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29777147134
- Updated: 2026-07-20T20:43:56.300Z

```json
{
  "triage": {
    "conclusion": "Valid browser SDK bug: internally saved session referrer can override user-registered $referrer/$referring_domain.",
    "summary": "In the browser SDK, `posthog.register({ $referrer, $referring_domain })` stores regular super properties, but `capture()` saves referrer info into session persistence before calculating event properties. Because session persistence is merged after regular persistence, the SDK-derived `document.referrer` values take precedence over registered values unless the properties are passed directly on `capture()` or registered for the session.",
    "issueType": "bug",
    "confidence": 0.88,
    "labels": [
      "bug: Something isn't working",
      "web",
      "feature/web-analytics",
      "feature/product-analytics",
      "team/web-analytics"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. I was able to verify the relevant code path.\n\nCurrent behavior is that every `capture()` call runs the `save_referrer` path before event properties are calculated. That stores `document.referrer`/derived domain into `sessionPersistence` via `update_referrer_info()`. Later, `calculateEventProperties()` merges regular persisted super properties first and session persistence after that, so the internally saved session `$referrer` and `$referring_domain` override values set with `posthog.register()`.\n\nOne immediate workaround that is less brittle than re-registering after every pageview is to use `posthog.register_for_session({ $referrer, $referring_domain })` before captures in the iframe session, or disable automatic referrer saving with `save_referrer: false` if you are fully managing those properties yourself. The underlying SDK behavior still looks surprising because regular registered super properties do not win over this internal session referrer mechanism.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-persistence.ts",
        "packages/browser/src/utils/event-utils.ts",
        "packages/browser/src/__tests__/posthog-core.test.ts",
        "packages/browser/src/__tests__/posthog-persistence.test.ts",
        "packages/types/src/posthog-config.ts"
      ],
      "findings": [
        "`packages/browser/src/posthog-core.ts` defaults `save_referrer` to `true`.",
        "`packages/browser/src/posthog-core.ts` calls `this.sessionPersistence.update_referrer_info()` during `capture()` whenever `config.save_referrer` is enabled, before `calculateEventProperties()` is called.",
        "`packages/browser/src/posthog-persistence.ts` implements `update_referrer_info()` as `this.register_once(getReferrerInfo(), undefined)`, so the SDK-derived referrer is stored in the persistence object on which it is called; in this capture path that object is `sessionPersistence`.",
        "`packages/browser/src/utils/event-utils.ts` implements `getReferrerInfo()` using `document.referrer` and derives `$referring_domain` from that URL host, falling back to `$direct`.",
        "`packages/browser/src/posthog-core.ts` merges event properties in this order: browser/info properties, regular `this.persistence.properties()`, `this.sessionPersistence.properties()`, then direct event properties. This means session persistence overrides values set by `posthog.register()`, while direct `capture()` properties still win.",
        "`packages/browser/src/__tests__/posthog-core.test.ts` already asserts that referrer is stored in `sessionPersistence` and reused within a session, but there does not appear to be coverage for precedence between user-registered `$referrer` values and SDK-managed session referrer values.",
        "`packages/browser/src/__tests__/posthog-persistence.test.ts` covers `update_referrer_info()` deriving `$referrer`/`$referring_domain` from `document.referrer`."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The bug is localized, but the correct precedence rule needs care because `$referrer` and `$referring_domain` are used for attribution/session behavior and existing tests intentionally rely on session-scoped referrer values. A small PR is plausible once the desired semantics are confirmed, but changing merge order globally would be too broad and could break other session super-property behavior.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "54d098dd90aaaa9758823ce936879f8b5eee952ee7211b9e4c32c20e73a0a335",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/b6c45f9145f91e7697d34e9d51d95cdf",
    "gistId": "b6c45f9145f91e7697d34e9d51d95cdf",
    "files": [
      {
        "name": "call-1-1-2026-07-20T20-41-51-619Z_019f8143-7d03-763d-9614-d9e5644557fa.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/b6c45f9145f91e7697d34e9d51d95cdf/raw/55cd685da955612cc2517d0a555e00a52cac1d73/call-1-1-2026-07-20T20-41-51-619Z_019f8143-7d03-763d-9614-d9e5644557fa.jsonl"
      }
    ]
  },
  "runId": 29777147134,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29777147134"
}
```
