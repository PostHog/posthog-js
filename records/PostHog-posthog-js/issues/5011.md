# issue 5011: posthog.capture returns 200 but events are not indexed in current browser reproduction

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/5011
- Conclusion: Plausible browser SDK ingestion bug, but the report does not yet isolate an SDK-side difference from an ingestion-side treatment of the enriched SDK event.
- Labels: feature/product-analytics, javascript, web, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/35233989468
- Updated: 2026-09-17T14:33:55.754Z

```json
{
  "triage": {
    "conclusion": "Plausible browser SDK ingestion bug, but the report does not yet isolate an SDK-side difference from an ingestion-side treatment of the enriched SDK event.",
    "summary": "On a hosted Astro diagnostic page, unbatched `posthog.capture()` requests receive HTTP 200 but are reportedly absent from event queries, while several direct browser `/batch/` submissions are indexed.",
    "issueType": "bug",
    "confidence": 0.68,
    "labels": [
      "bug: Something isn't working",
      "feature/product-analytics",
      "javascript",
      "web",
      "team/client-libraries"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "Thanks for the detailed comparison. This is worth investigating, but we need one exact SDK request to distinguish an SDK serialization/routing issue from an ingestion or query-visibility issue.\n\nA note on terminology: `posthog.capture()` returns the locally constructed capture result synchronously; the `200` is the subsequent transport response and is only an acknowledgement from the collector, not an indexing receipt.\n\nCould you provide, with API key, cookies, identifiers, URLs, and sensitive property values redacted:\n\n1. The complete final SDK request URL and JSON body from DevTools for one missing uniquely named event, preserving every field name, value type, event UUID, and UTC timestamp.\n2. The corresponding final direct-fetch request body for an indexed event, plus the exact event-query time range and filters used to look for each event.\n3. The diagnostic page's complete `posthog.init` configuration, including any `before_send`, `sanitize_properties`, consent/opt-out calls, `fetch_options`, or later `set_config` calls.\n4. The SDK event UUID, capture timestamp, and project ID through a secure support channel if maintainers need ingestion-side tracing.\n\nThe stated direct-fetch envelope is structurally similar, but it is not yet equivalent to an SDK event: the SDK adds identity, session/window, page, library, and person-processing properties. A sanitized on-wire pair is the minimal next step before changing broad capture or transport behavior.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/request-dispatch.ts",
        "packages/browser/src/request.ts",
        "packages/browser/src/utils/request-router.ts",
        "packages/browser/src/__tests__/posthog-core.wire-envelope.test.ts",
        "packages/browser/src/__tests__/posthog-core-also.test.ts",
        "packages/browser/src/__tests__/request.test.ts"
      ],
      "findings": [
        "`PostHog.capture()` can intentionally stop before dispatch when capture is disabled, the browser is classified as a bot, client rate limiting applies, or `before_send` rejects/errors. Those paths cannot explain a request that actually reached the collector, but the full deployed configuration is needed to rule out transformations.",
        "With `request_batching: false`, `capture()` constructs a POST request and sends it through the retriable request path rather than waiting for the normal batch queue. Its default analytics target is `/e/`, or a remote-config analytics endpoint, and `rewriteRequestPath` is applied by `RequestRouter` to that API endpoint.",
        "For analytics requests with `timestampMode: 'capture-body'`, `request.ts` serializes the SDK event into `{ api_key, batch, sent_at }`; `api_key` is taken from the event's `properties.token`. Existing request tests explicitly cover this envelope for `/e/`, `/i/v0/e/`, and `/batch/`.",
        "The SDK event is not identical to the direct example even when the outer envelope matches. `calculateEventProperties()` adds the project token, SDK/page/device properties, session and window IDs, identity state, and `$process_person_profile`, among other persisted properties.",
        "Existing tests verify the browser-side request route and decoded wire envelope using mocked HTTP responses. They do not exercise a live ingestion/indexing assertion, so they cannot confirm or refute the reported collector-versus-indexing behavior."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "There is no identified faulty SDK branch or demonstrated on-wire mismatch. Changing capture enrichment, request framing, or transport behavior without an exact failing SDK payload would be speculative and could affect all browser event ingestion.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3663,
      "type": "issue",
      "state": "closed",
      "title": "posthog.capture() events silently dropped — 200 OK but never ingested (TanStack Start + Vite SSR)",
      "url": "https://github.com/PostHog/posthog-js/issues/3663",
      "labels": [
        "bug",
        "question",
        "feature/product-analytics",
        "web"
      ],
      "bodyExcerpt": "## Bug description\n\nEvents captured via `posthog.capture()` return `200 {\"status\":\"Ok\"}` but never appear in the PostHog dashboard. Manual `fetch()` to `/batch/` with the same token and payload format works immediately — events appear within seconds. This affects both the React provider pattern and direct `posthog.capture()` calls on the initialized singleton.\n\n**Environment:** \nposthog-js 1.310.1 and 1.376.0 (tested both), @posthog/react 1.9.1, TanStack Start (React 19) + Vite 7 + Cloudflare Workers, PostHog Cloud US region, Safari 26 + Chrome.\n\n**What works:**\n- `fetch()` from browser console to `/batch/` endpoint\n- `curl` to `/e/` and `/batch/` with same token and same payload format\n- `curl` through Vite dev proxy\n\n**What doesn't work (all produce debug logs and 200 responses but no events in dashboard):**\n- `posthog.capture()` via `usePostHog()` hook with `apiKey` prop on PostHogProvider\n- `posthog.capture()` via `usePostHog()` hook with `client` prop after manual `posthog.init()`…",
      "createdAt": "2026-05-24T05:11:48Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.11059907834101383,
    "canonical": {
      "number": 3663,
      "type": "issue",
      "state": "closed",
      "title": "posthog.capture() events silently dropped — 200 OK but never ingested (TanStack Start + Vite SSR)",
      "url": "https://github.com/PostHog/posthog-js/issues/3663",
      "labels": [
        "bug",
        "question",
        "feature/product-analytics",
        "web"
      ],
      "bodyExcerpt": "## Bug description\n\nEvents captured via `posthog.capture()` return `200 {\"status\":\"Ok\"}` but never appear in the PostHog dashboard. Manual `fetch()` to `/batch/` with the same token and payload format works immediately — events appear within seconds. This affects both the React provider pattern and direct `posthog.capture()` calls on the initialized singleton.\n\n**Environment:** \nposthog-js 1.310.1 and 1.376.0 (tested both), @posthog/react 1.9.1, TanStack Start (React 19) + Vite 7 + Cloudflare Workers, PostHog Cloud US region, Safari 26 + Chrome.\n\n**What works:**\n- `fetch()` from browser console to `/batch/` endpoint\n- `curl` to `/e/` and `/batch/` with same token and same payload format\n- `curl` through Vite dev proxy\n\n**What doesn't work (all produce debug logs and 200 responses but no events in dashboard):**\n- `posthog.capture()` via `usePostHog()` hook with `apiKey` prop on PostHogProvider\n- `posthog.capture()` via `usePostHog()` hook with `client` prop after manual `posthog.init()`…",
      "createdAt": "2026-05-24T05:11:48Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 11%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "e033fd77d692d69c79c860ce74f7f6a3e294e4e5ab24a304a4b2dba5d9f8ccb0",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/983288f6f1d6629525efe6f3ab13b3cb",
    "gistId": "983288f6f1d6629525efe6f3ab13b3cb",
    "files": [
      {
        "name": "call-1-1-2026-09-17T14-30-52-711Z_01a0afc7-0c27-7d12-add7-94d9c9fd55ad.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/983288f6f1d6629525efe6f3ab13b3cb/raw/9d6358ecf135e91679f3b6a71f7cba2647bce64a/call-1-1-2026-09-17T14-30-52-711Z_01a0afc7-0c27-7d12-add7-94d9c9fd55ad.jsonl"
      }
    ]
  },
  "runId": 35233989468,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/35233989468"
}
```
