# issue 3785: @posthog/next: allow capturing route template

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3785
- Conclusion: Valid @posthog/next feature request; current pageview helper only captures concrete URLs.
- Labels: enhancement, feature, frameworks/next-js, feature/web-analytics
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28701471416
- Updated: 2026-07-04T09:12:07.810Z

```json
{
  "triage": {
    "conclusion": "Valid @posthog/next feature request; current pageview helper only captures concrete URLs.",
    "summary": "Add an option for Next.js pageview capture to use a route template for dynamic routes, e.g. grouping /foo/5 as /foo/[id] or /foo/${id}.",
    "issueType": "feature",
    "confidence": 0.86,
    "labels": [
      "enhancement",
      "feature",
      "frameworks/next-js",
      "feature/web-analytics"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed this is not currently supported in `@posthog/next`. The App Router `PostHogPageView` builds `$current_url` from `usePathname()`/`useSearchParams()`, and the Pages Router version uses `router.asPath`, so both capture the concrete URL today.\n\nA small implementation is possible once the API semantics are agreed, but I would avoid changing defaults. Main decisions: whether the opt-in should rewrite `$current_url` or add a separate route-template property, and whether the template format should be Next-style (`/foo/[id]`) or the requested `/foo/${id}` style.",
    "investigation": {
      "relevantFiles": [
        "packages/next/src/client/PostHogPageView.tsx",
        "packages/next/src/pages/PostHogPageView.tsx",
        "packages/next/tests/PostHogPageView.test.tsx",
        "packages/next/tests/PagesPostHogPageView.test.tsx",
        "packages/next/package.json"
      ],
      "findings": [
        "App Router pageviews are captured in `packages/next/src/client/PostHogPageView.tsx` using `usePathname()` plus `useSearchParams()`, then `posthog.capture('$pageview', { $current_url: currentUrl })`; there is no use of route params or route-template option.",
        "Pages Router pageviews are captured in `packages/next/src/pages/PostHogPageView.tsx` using `router.asPath`, which is the concrete URL path/query/hash rather than the template path available via `router.pathname`.",
        "Existing tests in `packages/next/tests/PostHogPageView.test.tsx` and `packages/next/tests/PagesPostHogPageView.test.tsx` assert concrete `$current_url` values, confirming current behavior.",
        "`@posthog/next` supports both App Router and Pages Router entrypoints, so a complete feature likely needs behavior for both."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The relevant code is localized, but this should wait for a product/API decision because rewriting `$current_url` changes pageview semantics, while adding a separate property may not satisfy the reporter's aggregation goal. App Router template derivation also has dynamic/catch-all/encoded-param edge cases.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "d31a1b202fa193e84b1ed6e1d3aa85a9fe44ee0890b74dd6a88b05b7f701d0a9",
  "piCalls": 3,
  "runId": 28701471416,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28701471416"
}
```
