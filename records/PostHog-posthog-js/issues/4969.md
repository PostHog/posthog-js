# issue 4969: Session replay: `hookSetter` defers on the zone.js-patched `setTimeout`, driving Angular change detection into an endless loop (tab at 100 % CPU)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4969
- Conclusion: Confirmed Session Replay bug: rrweb's deferred input-setter callback can schedule zone.js-patched timers from Angular change detection, creating an unbounded change-detection loop.
- Labels: feature/replay, web, performance, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/34982656324
- Updated: 2026-09-15T14:37:55.882Z

```json
{
  "triage": {
    "conclusion": "Confirmed Session Replay bug: rrweb's deferred input-setter callback can schedule zone.js-patched timers from Angular change detection, creating an unbounded change-detection loop.",
    "summary": "When recording is active, programmatic writes to hooked input/select properties schedule a zero-delay timer. In Angular, zone.js associates that timer with NgZone; components that write a property on every change-detection pass can then repeatedly trigger new passes and peg the tab at 100% CPU.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "web",
      "performance",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks — this is a detailed, self-contained reproduction, and the current vendored rrweb source supports the reported cause.\n\n`initInputObserver()` installs setter hooks for all six listed DOM properties. `hookSetter()` then always uses a deferred `setTimeout(..., 0)` before the input observer can deduplicate or reject a blocked element. This means `blockSelector` cannot prevent the timer from being scheduled. The synchronous native setter forwarding remains separate, so moving only rrweb's deferred callback to an unpatched timer should preserve page-visible setter behavior.\n\nA fix should stay narrow: resolve zone.js's original `setTimeout` when it is safely available for the target window, retain the normal timer as the fallback, and use it only for this deferred hook callback. Please add a real-browser regression that simulates the zone task bookkeeping: repeated programmatic `checked` writes during recording must not schedule work through the patched timer, while the native setter remains synchronous and the resulting input update is still recorded.\n\nThe existing `@posthog/rrweb-utils` Zone lookup is relevant precedent, but it currently resolves unpatched DOM constructors/prototypes rather than timers, so it should not be assumed reusable without a focused timer resolver.",
    "investigation": {
      "relevantFiles": [
        "packages/rrweb/rrweb/src/utils.ts",
        "packages/rrweb/rrweb/src/record/observer.ts",
        "packages/rrweb/rrweb/test/util.test.ts",
        "packages/rrweb/rrweb/test/record.test.ts",
        "packages/rrweb/utils/src/index.ts",
        "packages/rrweb/record/src/index.ts",
        "packages/browser/src/entrypoints/recorder.ts",
        ".agents/skills/replay-incident-risk/INCIDENTS.md"
      ],
      "findings": [
        "`hookSetter()` in `packages/rrweb/rrweb/src/utils.ts` currently invokes an unqualified `setTimeout(..., 0)` for every hooked assignment before it forwards to the original setter synchronously.",
        "`initInputObserver()` in `packages/rrweb/rrweb/src/record/observer.ts` installs `hookSetter()` on `HTMLInputElement.value`, `HTMLInputElement.checked`, `HTMLSelectElement.value`, `HTMLTextAreaElement.value`, `HTMLSelectElement.selectedIndex`, and `HTMLOptionElement.selected`, matching the report.",
        "The input observer's `isBlocked()` check and `lastInputValueMap` deduplication occur inside the deferred mock-event handler, after `hookSetter()` has already scheduled the timer. This supports the reporter's finding that `blockSelector` does not prevent the loop.",
        "Existing `hookSetter()` tests cover deferred callback execution, preservation of native setter behavior, and invalid receivers, but do not test zone.js-patched timer avoidance.",
        "`packages/rrweb/utils/src/index.ts` already detects Zone's unpatched globals through `Zone.__symbol__(key)`, but its private helper is typed and used only for unpatched DOM constructor/prototype lookup.",
        "`@posthog/rrweb-record` re-exports `record` from `@posthog/rrweb`, and the browser recorder entrypoint imports it, so this vendored implementation is on the PostHog Session Replay delivery path.",
        "Replay incident-risk review: this is recorder-side code but not the known rrweb serialization failure mode. The relevant regression should assert real-browser input-capture behavior and task scheduling, since a capture-fidelity regression could otherwise be silent."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The code change is likely small and localized, but it modifies a globally installed recorder setter hook. It needs a browser-level regression test for Angular/zone timer semantics plus verification that programmatic input capture, synchronous native setter behavior, fallback behavior without Zone, and iframe/window handling remain intact.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4217,
      "type": "issue",
      "state": "closed",
      "title": "Session recording (rrweb) blocks the main thread for several seconds serializing a large  DOM",
      "url": "https://github.com/PostHog/posthog-js/issues/4217",
      "labels": [
        "bug",
        "feature/replay",
        "web",
        "performance"
      ],
      "bodyExcerpt": "## Bug description\n\nWhen session recording is enabled, `posthog-recorder.js` (rrweb) serializes a large DOM mutation batch / full snapshot **synchronously on the main thread**, freezing the tab for several seconds. It happens when a large DOM subtree (a data-dense table, ~10k DOM nodes) is rebuilt or re-snapshotted on navigation — the kind of DOM common in data-heavy UIs.\n\nA Chrome Performance profile of the affected interaction shows the recorder dominating the main thread:\n\n- **Grouped by 1st/3rd party:** `posthog.com` consumed **22,444.6 ms of main-thread time** over a 29.6 s capture (~76% of the profile) while transferring only **0.6 kB** — i.e. pure CPU, not network.\n- **Bottom-up (function level):** effectively all heavy self-time resolves to `rrweb-record.js`, dominated by synchronous DOM traversal during serialization.\n- The Frames track shows multi-second frozen frames during the interaction.\n\nDisabling session recording removes the freeze entirely, confirming the recorder is …",
      "createdAt": "2026-07-22T06:19:47Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.058695652173913045,
    "canonical": {
      "number": 4217,
      "type": "issue",
      "state": "closed",
      "title": "Session recording (rrweb) blocks the main thread for several seconds serializing a large  DOM",
      "url": "https://github.com/PostHog/posthog-js/issues/4217",
      "labels": [
        "bug",
        "feature/replay",
        "web",
        "performance"
      ],
      "bodyExcerpt": "## Bug description\n\nWhen session recording is enabled, `posthog-recorder.js` (rrweb) serializes a large DOM mutation batch / full snapshot **synchronously on the main thread**, freezing the tab for several seconds. It happens when a large DOM subtree (a data-dense table, ~10k DOM nodes) is rebuilt or re-snapshotted on navigation — the kind of DOM common in data-heavy UIs.\n\nA Chrome Performance profile of the affected interaction shows the recorder dominating the main thread:\n\n- **Grouped by 1st/3rd party:** `posthog.com` consumed **22,444.6 ms of main-thread time** over a 29.6 s capture (~76% of the profile) while transferring only **0.6 kB** — i.e. pure CPU, not network.\n- **Bottom-up (function level):** effectively all heavy self-time resolves to `rrweb-record.js`, dominated by synchronous DOM traversal during serialization.\n- The Frames track shows multi-second frozen frames during the interaction.\n\nDisabling session recording removes the freeze entirely, confirming the recorder is …",
      "createdAt": "2026-07-22T06:19:47Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 6%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "9389bc49ba315222b2d6db3eaaec3cb1ef2f1ba56eeeb4ade8819fcd6e6b02ec",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/92b105b624e0a219ffedf82f0f32347d",
    "gistId": "92b105b624e0a219ffedf82f0f32347d",
    "files": [
      {
        "name": "call-1-1-2026-09-15T14-36-15-947Z_01a0a57f-42cb-73c4-9b6e-70a266ff7fb8.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/92b105b624e0a219ffedf82f0f32347d/raw/ebda477e00a85b369e4fdd40f7d6cc91dc3543e3/call-1-1-2026-09-15T14-36-15-947Z_01a0a57f-42cb-73c4-9b6e-70a266ff7fb8.jsonl"
      }
    ]
  },
  "runId": 34982656324,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/34982656324"
}
```
