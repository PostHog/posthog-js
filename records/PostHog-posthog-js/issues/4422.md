# issue 4422: Node SDK: steady memory leak in capture() + flush() traced to native-gzip path in @posthog/core gzipCompress

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4422
- Conclusion: Likely Node SDK compression-path memory-retention bug; the reported workaround and current implementation make this credible, but a focused reproducer is needed before choosing a fix.
- Labels: node, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/30986627675
- Updated: 2026-08-05T07:52:39.858Z

```json
{
  "triage": {
    "conclusion": "Likely Node SDK compression-path memory-retention bug; the reported workaround and current implementation make this credible, but a focused reproducer is needed before choosing a fix.",
    "summary": "Frequent capture-and-flush calls in posthog-node on Node 24 reportedly cause linear JS heap growth only when native gzip compression is enabled.",
    "issueType": "bug",
    "confidence": 0.84,
    "labels": [
      "bug: Something isn't working",
      "node",
      "team/client-libraries"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "Thanks for the detailed measurements and for isolating this with `disableCompression: true`.\n\nThe current shared gzip implementation does create a `CompressionStream`, writer, `Response(...).blob()`, and gzip header/trailer validation blobs for every compression. `posthog-node` uses that shared compressor for its normal batch transport, so calling `flush()` for every inbound request does exercise this path at a high rate.\n\nThat said, the source alone cannot establish which runtime object is retained, nor whether this is specific to Node 24's web-stream implementation or the extra validation introduced for invalid native gzip output. Could you provide a minimal standalone reproduction that repeatedly captures and flushes against a stub/local endpoint, ideally including results on the latest `posthog-node` release? A pair of heap snapshots (compression enabled vs. disabled) or a test separating the base `CompressionStream` call from the validation reads would also help identify the correct scope of a fix.\n\n`disableCompression: true` remains the appropriate workaround while this is investigated.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/gzip.ts",
        "packages/core/src/posthog-core-stateless.ts",
        "packages/core/src/__tests__/gzip.spec.ts",
        "packages/node/src/client.ts",
        "packages/node/src/capture-v1/sender.ts",
        "packages/node/src/__tests__/capture-v1/sender.spec.ts",
        "packages/node/package.json"
      ],
      "findings": [
        "`packages/core/src/gzip.ts` implements gzip with the platform `CompressionStream`, obtains the result through `new Response(compressedStream.readable).blob()`, and validates it by creating two additional sliced Blobs and reading their ArrayBuffers.",
        "`gzipCompress()` allocates these stream and Blob objects on every call; it has no pooling or Node-specific implementation.",
        "`PostHogCoreStateless.sendBatch()` calls the shared `gzipCompress()` for each outgoing `/batch/` payload whenever `disableCompression` is false, so capture followed by flush per request can invoke it once per request.",
        "The Node Capture V1 sender also defaults its compression hook to the same `gzipCompress()` implementation and calls it once per send attempt when compression is enabled.",
        "The gzip validation is covered by core unit tests, including rejection of malformed stream output; the tests compare valid output with `node:zlib`, but `packages/node` has no runtime `zlib` compression path.",
        "Current Node package metadata supports Node `>=22.22.0`, which includes the reporter's Node 24 runtime family; no existing runtime test exercises sustained high-frequency compression or checks retained heap growth."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The evidence supports investigating the shared native gzip path, but does not yet isolate whether retention is caused by Node's CompressionStream implementation, Response/Blob materialization, or the validation reads. A Node-only zlib path would also need to preserve browser and edge compatibility and cover all Node send paths that currently call the shared core helper.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3568,
      "type": "pull_request",
      "state": "closed",
      "title": "fix: validate native gzip output",
      "url": "https://github.com/PostHog/posthog-js/pull/3568",
      "labels": [],
      "bodyExcerpt": "## Problem\n\nNative browser `CompressionStream(\"gzip\")` can fail by resolving malformed or partial gzip output without throwing. When that happens, posthog-js sends the bad payload with gzip compression enabled and capture drops the event instead of retrying safely.\n\n## Changes\n\n- Validate native gzip output before returning it from `@posthog/core` by checking the gzip header, CRC32 trailer, and uncompressed size trailer.\n- Throw a recognizable `NativeGzipValidationError` when native gzip output is malformed.\n- Disable native async gzip for the current page lifetime after validation failures and fall back to the existing synchronous `fflate` gzip path, preserving compression.\n- Add regression tests for malformed native gzip output and browser request fallback behavior.\n\n## Release info Sub-libraries affected\n\n### Libraries affected\n\n- [ ] All of them\n- [x] posthog-js (web)\n- [ ] posthog-js-lite (web lite)\n- [ ] posthog-node\n- [ ] posthog-react-native\n- [ ] @posthog/react\n- [ ] @posthog/…",
      "createdAt": "2026-05-11T20:00:14Z",
      "reason": "explicit-reference"
    },
    {
      "number": 3564,
      "type": "issue",
      "state": "closed",
      "title": "Native CompressionStream produces invalid/partial gzip payloads with no thrown error in 1.372.10 (post #3378 + #3464)",
      "url": "https://github.com/PostHog/posthog-js/issues/3564",
      "labels": [
        "bug",
        "web"
      ],
      "bodyExcerpt": "# Native CompressionStream produces invalid/partial gzip payloads with no thrown error in 1.372.10 (post #3378 + #3464)\n\n## Summary\n\nposthog-js 1.372.10 is producing invalid gzip request bodies on a non-trivial fraction of real-browser requests to our self-hosted capture (`POST /e/`), despite both PR #3378 (NotReadableError fallback) and PR #3464 (avoid `Blob.stream`) being present in the bundled SDK. The browser sends `Content-Encoding: gzip` (and `?compression=gzip` per `posthog-core.js`), capture-rs decodes, and logs one of:\n\n- `decompress_gzip_to_bytes: failed to read GZIP chunk: invalid gzip header`\n- `event: request payload parsing error: failed to decode request: invalid GZIP data`\n- `High GZIP compression ratio detected - potential GZIP bomb` (less frequent)\n\nThe event is dropped. There is no thrown error in the browser that triggers the SDK's no-compression retry path (line `request.ts` ~equivalent of bundle line 85636: `Ns=!0; retry uncompressed`), so the bad payload is sent …",
      "createdAt": "2026-05-11T09:42:09Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.06227106227106227,
    "canonical": {
      "number": 3564,
      "type": "issue",
      "state": "closed",
      "title": "Native CompressionStream produces invalid/partial gzip payloads with no thrown error in 1.372.10 (post #3378 + #3464)",
      "url": "https://github.com/PostHog/posthog-js/issues/3564",
      "labels": [
        "bug",
        "web"
      ],
      "bodyExcerpt": "# Native CompressionStream produces invalid/partial gzip payloads with no thrown error in 1.372.10 (post #3378 + #3464)\n\n## Summary\n\nposthog-js 1.372.10 is producing invalid gzip request bodies on a non-trivial fraction of real-browser requests to our self-hosted capture (`POST /e/`), despite both PR #3378 (NotReadableError fallback) and PR #3464 (avoid `Blob.stream`) being present in the bundled SDK. The browser sends `Content-Encoding: gzip` (and `?compression=gzip` per `posthog-core.js`), capture-rs decodes, and logs one of:\n\n- `decompress_gzip_to_bytes: failed to read GZIP chunk: invalid gzip header`\n- `event: request payload parsing error: failed to decode request: invalid GZIP data`\n- `High GZIP compression ratio detected - potential GZIP bomb` (less frequent)\n\nThe event is dropped. There is no thrown error in the browser that triggers the SDK's no-compression retry path (line `request.ts` ~equivalent of bundle line 85636: `Ns=!0; retry uncompressed`), so the bad payload is sent …",
      "createdAt": "2026-05-11T09:42:09Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 6%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "862e31c3c4932eb48d7bfd00dd94e79ddc8b8ec536384f3d4cb90d4d87e4a687",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/f613259913d8c1dec4f8be12198fcf8b",
    "gistId": "f613259913d8c1dec4f8be12198fcf8b",
    "files": [
      {
        "name": "call-1-1-2026-08-05T07-51-56-586Z_019fd0e8-5baa-7b32-b3e6-970bfcb3bac4.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/f613259913d8c1dec4f8be12198fcf8b/raw/fb3e5c09fe663ab9858d3557b45ea54b0273a9a7/call-1-1-2026-08-05T07-51-56-586Z_019fd0e8-5baa-7b32-b3e6-970bfcb3bac4.jsonl"
      }
    ]
  },
  "runId": 30986627675,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/30986627675"
}
```
