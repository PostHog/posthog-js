# issue 2292: Session recording input masking is flaky

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2292
- Conclusion: Plausible web replay textarea masking bug, but not yet reproducible enough for a safe code change.
- Labels: bug, feature/replay, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28097081022
- Updated: 2026-06-24T12:06:08.661Z

```json
{
  "triage": {
    "conclusion": "Plausible web replay textarea masking bug, but not yet reproducible enough for a safe code change.",
    "summary": "A textarea in some session recordings becomes visible after a React preset/config update, then appears masked again after a later full snapshot.",
    "issueType": "bug",
    "confidence": 0.78,
    "labels": [
      "bug",
      "feature/replay",
      "web"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "This still looks like a plausible replay masking/privacy bug, but I don't think a speculative fix is safe yet.\n\nI rechecked the current recorder paths: PostHog defaults session recording to `maskAllInputs: true`, rrweb expands that to include `textarea: true`, and the obvious full snapshot, input/change, `HTMLTextAreaElement.value` setter, and textarea mutation paths all call `maskInputValue`. That matches the observation that a later full snapshot masks the textarea again, but it does not yet identify which incremental event carried the plaintext.\n\nMost useful next step: inspect/export the raw rrweb events around one unmasked timestamp and confirm whether the plaintext appears in an `IncrementalSnapshot` with source `Input` or source `Mutation` (`attributes.value`, `texts`, or `adds`). It would also help to have the exact `posthog-js` version and full `session_recording` masking config used in the Chrome extension sidepanel.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/extensions/replay/external/mutation-throttler.ts",
        "packages/rrweb/rrweb/src/record/index.ts",
        "packages/rrweb/rrweb/src/record/observer.ts",
        "packages/rrweb/rrweb/src/record/mutation.ts",
        "packages/rrweb/rrweb-snapshot/src/snapshot.ts",
        "packages/rrweb/rrweb-snapshot/src/utils.ts",
        "packages/rrweb/rrweb/test/integration.test.ts"
      ],
      "findings": [
        "PostHog initializes rrweb session recording with `maskAllInputs: true` by default in `lazy-loaded-session-recorder.ts`.",
        "rrweb expands `maskAllInputs === true` to a mask options object that includes `textarea: true` in `packages/rrweb/rrweb/src/record/index.ts`.",
        "Full snapshots serialize `input`, `textarea`, and `select` values through `maskInputValue` and skip textarea child nodes when a value attribute is recorded, which is consistent with full snapshots restoring masked output.",
        "The input observer listens for `input`/`change` events and hooks `HTMLTextAreaElement.prototype.value`; when textarea masking is enabled it masks before emitting input snapshots.",
        "The mutation recorder masks `value` attribute changes and has textarea-specific handling via `genTextAreaValueMutation`; the PostHog mutation throttler only filters mutation events and does not transform masked values back to plaintext.",
        "There is existing integration coverage for dynamically added textareas and textarea value/setAttribute/innerText updates under `maskAllInputs: true`, so the reported flake likely needs the raw affected event payload or a narrower reproduction in the Chrome extension/React sidepanel context."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The report is privacy-sensitive and plausible, but the inspected current code paths already mask textarea values. Without knowing which raw rrweb incremental event contains plaintext, a small PR would be speculative and could miss the leak or affect replay behavior incorrectly.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "42df15d24e6b94b50dd421ee936f535cc4e2c295bc6b8385d692a0a61f99cc05",
  "piCalls": 1,
  "runId": 28097081022,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28097081022"
}
```
