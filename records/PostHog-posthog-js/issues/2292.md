# issue 2292: Session recording input masking is flaky

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2292
- Conclusion: Likely a real web replay masking bug, but not yet reproducible/actionable.
- Labels: feature/replay, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28095535073
- Updated: 2026-06-24T11:36:42.514Z

```json
{
  "triage": {
    "conclusion": "Likely a real web replay masking bug, but not yet reproducible/actionable.",
    "summary": "A React-controlled textarea in a Chrome extension sidepanel is sometimes shown unmasked in session recordings after preset/config loads, then becomes masked again after a full snapshot.",
    "issueType": "bug",
    "confidence": 0.78,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "web",
      "question: Further information is requested"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "This looks worth keeping as a replay masking/privacy bug, but I don't think it is safe to make a speculative code change yet.\n\nI checked the current recorder paths: PostHog sets `maskAllInputs: true` by default, rrweb expands that to include `textarea: true`, full snapshots mask textarea values, and the input observer also patches `HTMLTextAreaElement.value` and masks before emitting. That matches the observation that a later full snapshot returns the textarea to a masked state, but it does not yet explain which incremental event is carrying plaintext.\n\nUseful next debugging step: inspect/export the raw rrweb events around one unmasked timestamp and identify whether the plaintext is in an `IncrementalSnapshot` with source `Input` or source `Mutation` (`attributes.value`, `texts`, or `adds`). Also useful from the reporter: exact `posthog-js` version and full `session_recording` masking config, plus whether this only occurs in the Chrome extension sidepanel context.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/extensions/replay/external/mutation-throttler.ts",
        "packages/rrweb/rrweb/src/record/index.ts",
        "packages/rrweb/rrweb/src/record/observer.ts",
        "packages/rrweb/rrweb/src/record/mutation.ts",
        "packages/rrweb/rrweb-snapshot/src/snapshot.ts",
        "packages/rrweb/rrweb-snapshot/src/utils.ts",
        "packages/rrweb/rrweb/test/integration.test.ts"
      ],
      "findings": [
        "PostHog's lazy session recorder initializes rrweb with `maskAllInputs: true` and `maskInputOptions: { password: true }` by default in `lazy-loaded-session-recorder.ts`.",
        "rrweb's `record/index.ts` expands `maskAllInputs === true` to a `MaskInputOptions` object that includes `textarea: true`.",
        "Full snapshots in `rrweb-snapshot/src/snapshot.ts` serialize `input`, `textarea`, and `select` values through `maskInputValue`, and textarea child nodes are skipped when a value attribute is recorded. This is consistent with the reporter/maintainer observation that full snapshots restore masking.",
        "Incremental input recording in `rrweb/src/record/observer.ts` listens for `input`/`change` events and hooks `HTMLTextAreaElement.prototype.value`; it masks through `maskInputValue` before emitting input events.",
        "Mutation recording in `rrweb/src/record/mutation.ts` has textarea-specific handling for child/text mutations via `genTextAreaValueMutation`, which also uses `maskInputValue`.",
        "PostHog's `MutationThrottler` only filters mutation attributes for rate limiting and does not transform masked values back to plaintext.",
        "Because the obvious full-snapshot, input, setter, and textarea mutation paths all mask, this needs either a raw event payload from an affected replay or a deterministic minimal reproduction before a focused fix can be identified."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The report is plausible and privacy-sensitive, but there is no deterministic reproduction and the inspected code paths already mask textareas. A small PR would be speculative until we know which rrweb incremental event path leaks the plaintext.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "42df15d24e6b94b50dd421ee936f535cc4e2c295bc6b8385d692a0a61f99cc05",
  "piCalls": 1,
  "runId": 28095535073,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28095535073"
}
```
