# issue 3618: Feature: allow disabling `_=timestamp` on `/e` and `/s` requests

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3618
- Conclusion: Valid feature request; the browser SDK currently adds the `_` cache-buster unconditionally and there is no opt-out.
- Labels: enhancement, web, feature
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28579791283
- Updated: 2026-07-02T09:34:08.676Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; the browser SDK currently adds the `_` cache-buster unconditionally and there is no opt-out.",
    "summary": "The reporter wants a way to disable the per-request `_=timestamp` query parameter on web SDK `/e/` event and `/s/` session replay requests because unique URLs prevent reuse of CORS preflight results in their setup.",
    "issueType": "feature",
    "confidence": 0.88,
    "labels": [
      "enhancement",
      "web",
      "feature"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the code path: `packages/browser/src/request.ts` unconditionally adds `_: new Date().getTime().toString()` in `buildRequestURL()`, and `request()` applies that before choosing `fetch`, `XHR`, or `sendBeacon`. Event captures use this path for `/e/`, and replay snapshots use the same capture/send path for the configured replay endpoint, typically `/s/`.\n\nThere does not appear to be an existing config option to disable this or a general outgoing-request transform hook. `before_send` can mutate event payloads, but not the final request URL. This overlaps with #2289 for broader custom transports, but this issue is a narrower opt-out for the cache-busting query param.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/request.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/types.ts",
        "packages/types/src/posthog-config.ts",
        "packages/browser/src/__tests__/request.test.ts"
      ],
      "findings": [
        "`packages/browser/src/request.ts` has `buildRequestURL()` adding `_: new Date().getTime().toString()` along with `ver` and `compression` for every request.",
        "`request()` in `packages/browser/src/request.ts` always calls `buildRequestURL(options.url, options.compression)` before selecting the transport, so this applies to fetch, XHR, and sendBeacon.",
        "`packages/browser/src/posthog-core.ts` sends normal captured events to `this.requestRouter.endpointFor('api', this.analyticsDefaultEndpoint)`, with the default analytics endpoint set to `/e/`.",
        "`packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts` sends `$snapshot` events through `capture()` with `_url` set to the configured replay endpoint, typically `/s/`, so replay traffic also goes through the same request URL builder.",
        "No existing `cache_bust`, request transform, or custom transport option was found in the inspected browser source; public config currently exposes related but insufficient knobs like `request_headers` and `fetch_options`."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The timestamp is centralized in one request helper, so an opt-in config flag could be added without changing default behavior. The main decision is public API naming and whether the opt-out should apply globally or only to `/e/` and `/s/`.",
      "suggestedApproach": "Add a default-false advanced config option, thread it from `_send_request()` into `request()`, and make `buildRequestURL()` omit `_` when the option is enabled. Add unit coverage in `request.test.ts` showing default behavior is preserved and the cache-buster is omitted when opted out.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 2289,
      "type": "issue",
      "state": "open",
      "title": "Feature Request: Custom Transports",
      "url": "https://github.com/PostHog/posthog-js/issues/2289",
      "labels": [
        "enhancement",
        "web"
      ],
      "bodyExcerpt": "## Is your feature request related to a problem?\n\nWhen integrating with custom solutions around PostHog SDK, it would be great to be able to create custom transports (instead of currently available `fetch` and `xhr`) to have full control on how events are sent to PostHog.\n\nFor example, I'm currently developing a Tauri plugin for integrating PostHog into my project. I want to proxy all the frontend event through the Tauri backend. That is, all event will be passed from the frontend to the Tauri backend and actually sent to PostHog using the Rust SDK.\n\n## Describe the solution you'd like\n\nAllow passing a function to the `transport` option of the client settings.\n\n```ts\nposthog.init(\"api_key\", {\n  // ...\n  transport: /* \"fetch\" | \"xhr\" | Transport */ (/* arguments (e.g. the event) */) => {\n    // Send the event in a custom way.\n  },\n  // ...\n});\n```\n\n## Describe alternatives you've considered\n\nCurrently, it is possible to emulate custom transports using the `before_send` option. However, …",
      "createdAt": "2025-09-05T14:26:49Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.06930693069306931,
    "canonical": {
      "number": 2289,
      "type": "issue",
      "state": "open",
      "title": "Feature Request: Custom Transports",
      "url": "https://github.com/PostHog/posthog-js/issues/2289",
      "labels": [
        "enhancement",
        "web"
      ],
      "bodyExcerpt": "## Is your feature request related to a problem?\n\nWhen integrating with custom solutions around PostHog SDK, it would be great to be able to create custom transports (instead of currently available `fetch` and `xhr`) to have full control on how events are sent to PostHog.\n\nFor example, I'm currently developing a Tauri plugin for integrating PostHog into my project. I want to proxy all the frontend event through the Tauri backend. That is, all event will be passed from the frontend to the Tauri backend and actually sent to PostHog using the Rust SDK.\n\n## Describe the solution you'd like\n\nAllow passing a function to the `transport` option of the client settings.\n\n```ts\nposthog.init(\"api_key\", {\n  // ...\n  transport: /* \"fetch\" | \"xhr\" | Transport */ (/* arguments (e.g. the event) */) => {\n    // Send the event in a custom way.\n  },\n  // ...\n});\n```\n\n## Describe alternatives you've considered\n\nCurrently, it is possible to emulate custom transports using the `before_send` option. However, …",
      "createdAt": "2025-09-05T14:26:49Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 7%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "95c13bf8c0338e9a9240d50ce11696ff7b3125c31f8139a76b52f649189554ce",
  "piCalls": 3,
  "runId": 28579791283,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28579791283"
}
```
