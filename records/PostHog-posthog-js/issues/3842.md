# issue 3842: AI observability: support Firebase Genkit (Vertex AI)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3842
- Conclusion: Valid feature request; current @posthog/ai OTel filtering excludes Genkit spans.
- Labels: enhancement, team/llm-analytics
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28701471416
- Updated: 2026-07-04T09:13:34.379Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; current @posthog/ai OTel filtering excludes Genkit spans.",
    "summary": "Firebase Genkit emits OpenTelemetry spans using a `genkit:` attribute namespace, but @posthog/ai currently only forwards spans with `gen_ai.`, `llm.`, `ai.`, or `traceloop.` names/attributes, so Genkit traces are dropped before reaching PostHog AI observability.",
    "issueType": "feature",
    "confidence": 0.86,
    "labels": [
      "enhancement",
      "team/llm-analytics"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the repo that this is not currently supported in `@posthog/ai`: `isAISpan` only recognizes `gen_ai.`, `llm.`, `ai.`, and `traceloop.` prefixes, and there are no existing Genkit integration files/references.\n\nThe smallest client-side change would be to include `genkit:` in the OTel AI span detection and add tests in the OTel exporter/processor test coverage. However, that only fully solves the issue if the PostHog `/i/v0/ai/otel` ingestion path already understands Genkit's `genkit:*` attributes. If it does not, this likely needs either server-side Genkit parsing or a dedicated `@posthog/ai/genkit` processor that maps Genkit spans to PostHog AI events directly.",
    "investigation": {
      "relevantFiles": [
        "packages/ai/src/otel/spans.ts",
        "packages/ai/src/otel/processor.ts",
        "packages/ai/src/otel/exporter.ts",
        "packages/ai/tests/otel.test.ts",
        "packages/ai/tests/processor.test.ts",
        "packages/ai/package.json",
        "packages/ai/rollup.config.mjs",
        "packages/ai/src/openai-agents/processor.ts",
        "packages/ai/src/openai-agents/index.ts"
      ],
      "findings": [
        "`packages/ai/src/otel/spans.ts` defines `AI_SPAN_PREFIXES` as `['gen_ai.', 'llm.', 'ai.', 'traceloop.']`; `genkit:` is not included.",
        "`isAISpan` checks only the span name and attribute keys against those prefixes, so a Genkit span named after a flow/action with attributes like `genkit:type` or `genkit:input` would be classified as non-AI.",
        "`PostHogSpanProcessor.onEnd` in `packages/ai/src/otel/processor.ts` forwards spans only when `isAISpan(span)` is true.",
        "`PostHogTraceExporter.export` in `packages/ai/src/otel/exporter.ts` filters spans with `spans.filter(isAISpan)` before exporting to `/i/v0/ai/otel`.",
        "A repo search found no existing `genkit` references under `packages/ai`, confirming there is no dedicated Genkit integration today.",
        "`packages/ai/tests/otel.test.ts` and `packages/ai/tests/processor.test.ts` already cover prefix-based filtering and would be the natural place to add regression tests for `genkit:` detection.",
        "`packages/ai/src/openai-agents/processor.ts` shows an existing pattern for a dedicated framework processor that captures PostHog AI events directly, but implementing an equivalent Genkit processor would be larger than just forwarding OTel spans.",
        "Adding a new dedicated subpath integration would also require package export/build updates in `packages/ai/package.json` and `packages/ai/rollup.config.mjs`."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The client-side filtering gap is small and well localized, but the correct product fix depends on whether PostHog's OTel ingestion already parses Genkit's `genkit:*` attributes. Without that confirmation, simply forwarding Genkit spans may not make flows/generations appear correctly, while a dedicated Genkit processor is a larger integration.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "71bb520250c9c476333d7c0bbbca727dbb5c4f6e39cbb6ac884bee68b8ffed6f",
  "piCalls": 4,
  "runId": 28701471416,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28701471416"
}
```
