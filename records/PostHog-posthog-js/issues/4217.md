# issue 4217: Session recording (rrweb) blocks the main thread for several seconds serializing a large  DOM

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4217
- Conclusion: Valid replay performance bug; not a small/safe fix.
- Labels: feature/replay, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29896428489
- Updated: 2026-07-22T06:22:51.495Z

```json
{
  "triage": {
    "conclusion": "Valid replay performance bug; not a small/safe fix.",
    "summary": "Session replay can freeze data-heavy pages because rrweb synchronously snapshots/serializes large DOM additions and full snapshots on the main thread.",
    "issueType": "bug",
    "confidence": 0.86,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "web",
      "needs-to-be-contributed-back: rrweb fork change that should be contributed back to upstream rrweb-io/rrweb"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report and profile. The code paths match the described behavior: full snapshots call rrweb's DOM snapshotter synchronously, and large structural mutation batches are walked/serialized synchronously before PostHog's replay buffering/compression code sees the event.\n\nA couple of notes from triage:\n- The current browser-side mutation throttler only filters high-frequency attribute mutations after rrweb emits an event; it does not prevent rrweb from traversing/serializing large added subtrees or full snapshots.\n- `full_snapshot_interval_millis: 0` can reduce periodic full snapshots, but it won't address initial/forced full snapshots or structural add/remove batches.\n- `blockSelector`/`ph-no-capture` can be used as a mitigation for known very-heavy subtrees, but that is not a general fix.\n- If the runtime recorder asset changing independently is a concern, `strict_script_versioning: true` makes external dependencies load from semver-qualified `/static/<sdk-version>/...` paths instead of the default unversioned asset path with a `?v=` query.\n\nA real fix likely needs rrweb-level work such as chunking/yielding traversal, budgets/backpressure for huge mutation batches/full snapshots, or another replay-safe way to avoid serializing unbounded DOM synchronously.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/replay/session-recording.ts",
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/extensions/replay/external/mutation-throttler.ts",
        "packages/browser/src/entrypoints/external-scripts-loader.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/types/src/posthog-config.ts",
        "packages/rrweb/rrweb/src/record/index.ts",
        "packages/rrweb/rrweb/src/record/mutation.ts",
        "packages/rrweb/rrweb-snapshot/src/snapshot.ts"
      ],
      "findings": [
        "`SessionRecording._lazyLoadAndStart()` lazy-loads an external recorder script when rrweb/initSessionRecording are not already present, using a script kind from remote config or defaulting to `lazy-recorder`.",
        "`external-scripts-loader.ts` loads `/static/${kind}.js?v=${posthog.version}` by default, while `strict_script_versioning` switches to `/static/${posthog.version}/${kind}.js`; `posthog-core.ts` defaults `strict_script_versioning` to `false`.",
        "`LazyLoadedSessionRecording._startRecorder()` passes allowlisted rrweb options to `rrwebRecord(...)`, then handles emitted events; there is no node-count/time budget before rrweb emits.",
        "`MutationThrottler` in `packages/browser/src/extensions/replay/external/mutation-throttler.ts` only filters attribute mutations by node rate after an rrweb mutation event exists, so it does not reduce rrweb's synchronous work for structural `adds`/`removes` or full snapshots.",
        "`packages/rrweb/rrweb/src/record/index.ts` implements `takeFullSnapshot` by locking mutation buffers and calling `snapshot(document, ...)` synchronously before emitting the full snapshot event.",
        "`packages/rrweb/rrweb-snapshot/src/snapshot.ts` recursively iterates `dom.childNodes(n)` in `serializeNodeWithId`; it has a max-depth guard but no max-node or time-slicing/yielding guard, so broad DOMs can still be expensive.",
        "`packages/rrweb/rrweb/src/record/mutation.ts` handles `MutationObserver` batches synchronously: `processMutations` calls `genAdds` recursively over added subtrees, and `emit` serializes additions with `serializeNodeWithId` in the same processing flow."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small browser-SDK patch would only add mitigations or knobs. The freezing comes from rrweb core snapshot/mutation serialization, where changing traversal scheduling, node budgets, or event semantics risks replay correctness, masking/blocking behavior, iframe/shadow DOM handling, and ingestion compatibility. This needs a targeted rrweb performance design plus regression/performance tests rather than a speculative one-line throttle.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3273,
      "type": "issue",
      "state": "closed",
      "title": "Session recorder blocks main thread",
      "url": "https://github.com/PostHog/posthog-js/issues/3273",
      "labels": [
        "bug",
        "feature/replay"
      ],
      "bodyExcerpt": "## Bug description\nAround the morning of March 20th, I noticed a performance degradation in our Nextjs 16 apps more resource intensive views (our WebGL maps went from smooth 120 FPS to ~25 FPS). Doing a performance profile in Chrome reveals that rrweb consumes ~55% of main thread time (see screenshot). Over ~9 seconds of the profile, there's 443 calls to posthog-recorder.js and 76 of those exceed the 16ms frame budget, averaging ~60ms each (peak 93ms). Hence it's blocking the main thread from other work.\n\nI tried the latest released version (1.363.2) without success and then downgraded to an older version (1.360.2) with the same result. Hence, I hashed the contents of the call to posthog-recorder.js?v=1.360.2 and posthog-recorder.js?v=1.362.0 and realized they're the same which makes me wonder if the version is only used for cache control and I cannot actually fix the issue myself.\n\nMD5 (posthog-recorder.js?v=1.360.2) = 9b63344f134220932e12f151b9faad6c\nMD5 (posthog-recorder.js?v=1.362.…",
      "createdAt": "2026-03-23T11:31:27Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.08267716535433071,
    "canonical": {
      "number": 3273,
      "type": "issue",
      "state": "closed",
      "title": "Session recorder blocks main thread",
      "url": "https://github.com/PostHog/posthog-js/issues/3273",
      "labels": [
        "bug",
        "feature/replay"
      ],
      "bodyExcerpt": "## Bug description\nAround the morning of March 20th, I noticed a performance degradation in our Nextjs 16 apps more resource intensive views (our WebGL maps went from smooth 120 FPS to ~25 FPS). Doing a performance profile in Chrome reveals that rrweb consumes ~55% of main thread time (see screenshot). Over ~9 seconds of the profile, there's 443 calls to posthog-recorder.js and 76 of those exceed the 16ms frame budget, averaging ~60ms each (peak 93ms). Hence it's blocking the main thread from other work.\n\nI tried the latest released version (1.363.2) without success and then downgraded to an older version (1.360.2) with the same result. Hence, I hashed the contents of the call to posthog-recorder.js?v=1.360.2 and posthog-recorder.js?v=1.362.0 and realized they're the same which makes me wonder if the version is only used for cache control and I cannot actually fix the issue myself.\n\nMD5 (posthog-recorder.js?v=1.360.2) = 9b63344f134220932e12f151b9faad6c\nMD5 (posthog-recorder.js?v=1.362.…",
      "createdAt": "2026-03-23T11:31:27Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 8%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "80dd42ad3a60e30747acc4cfb4118ee44a21ac2de8567e0355a6c96ff8db6c32",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/3120fa51c840872d49a60af982f51a92",
    "gistId": "3120fa51c840872d49a60af982f51a92",
    "files": [
      {
        "name": "call-1-1-2026-07-22T06-20-47-004Z_019f887b-de1c-7299-a37e-840c4b9227d0.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/3120fa51c840872d49a60af982f51a92/raw/ecdb890e80193d1172482a3230e8d76c0a350728/call-1-1-2026-07-22T06-20-47-004Z_019f887b-de1c-7299-a37e-840c4b9227d0.jsonl"
      }
    ]
  },
  "runId": 29896428489,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29896428489"
}
```
