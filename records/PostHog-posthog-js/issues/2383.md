# issue 2383: @posthog/nextjs-config keeps `scss` files sourcemap when delete-after enabled

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2383
- Conclusion: Likely already fixed in current @posthog/nextjs-config / webpack sourcemap handling
- Labels: frameworks/next-js, feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28098569629
- Updated: 2026-06-24T12:43:58.819Z

```json
{
  "triage": {
    "conclusion": "Likely already fixed in current @posthog/nextjs-config / webpack sourcemap handling",
    "summary": "The report says Next.js CSS/SCSS source maps under .next/static/css were not deleted after sourcemap upload because only uploaded files were deleted. Current code now routes Next.js webpack builds through the PostHog webpack plugin, which sends all webpack chunk files rather than JS-only files to the sourcemap CLI and forwards delete-after.",
    "issueType": "bug",
    "confidence": 0.86,
    "labels": [
      "bug: Something isn't working",
      "frameworks/next-js",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. This appears to be fixed in the current implementation.\n\nIn current `@posthog/nextjs-config`, non-Turbopack builds install `PosthogWebpackPlugin` when sourcemaps are enabled. The webpack plugin now iterates every emitted webpack `chunk.file` and passes that list to the sourcemap CLI, rather than filtering to JS-only assets. The CLI invocation also forwards `deleteAfterUpload` as `--delete-after`, and `deleteAfterUpload` defaults to `true` unless overridden.\n\nThere is also a directly related closed PR, #2440, titled `fix: process CSS sourcemaps in addition to JS sourcemaps (#2383)`, which matches this issue.\n\nI’d recommend closing this as already fixed unless it still reproduces on the latest `@posthog/nextjs-config`. If it does still reproduce, please share the exact package version and a minimal Next.js project or `.next/static/css` output tree so we can treat it as a regression.",
    "investigation": {
      "relevantFiles": [
        "packages/nextjs-config/src/config.ts",
        "packages/nextjs-config/src/utils.ts",
        "packages/nextjs-config/package.json",
        "packages/webpack-plugin/src/index.ts",
        "packages/plugin-utils/src/cli.ts",
        "packages/plugin-utils/src/config.ts"
      ],
      "findings": [
        "`packages/nextjs-config/src/config.ts` delegates non-Turbopack sourcemap processing to `PosthogWebpackPlugin` when sourcemaps are enabled.",
        "`packages/nextjs-config/src/config.ts` only disables `deleteAfterUpload` for server builds; browser/static assets keep the configured delete behavior.",
        "`packages/webpack-plugin/src/index.ts` collects files by iterating `compilation.chunks` and every `chunk.files` entry, with no `.js`-only filter visible in current code.",
        "`packages/plugin-utils/src/cli.ts` adds `--delete-after` when `config.sourcemaps.deleteAfterUpload` is true and passes explicit file paths to the CLI via stdin.",
        "`packages/plugin-utils/src/config.ts` defaults `sourcemaps.deleteAfterUpload` to `true` when the option is not provided.",
        "`packages/nextjs-config/package.json` shows the current package version is `1.9.68` and depends on the workspace `@posthog/webpack-plugin`, so the current Next.js wrapper uses the current webpack sourcemap behavior.",
        "The provided same-repo related context identifies closed PR #2440 as `fix: process CSS sourcemaps in addition to JS sourcemaps (#2383)`, directly matching this report."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "A new small PR does not appear appropriate without a fresh reproduction because the current code path no longer appears to exclude CSS assets and the directly related fix PR is already closed.",
      "suggestedApproach": "",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "already-fixed",
      "confidence": 0.84,
      "reason": "Current repository code sends all webpack chunk files to the sourcemap CLI with `--delete-after`, and the related closed PR #2440 explicitly references fixing CSS sourcemaps for this issue.",
      "canonicalUrl": "https://github.com/PostHog/posthog-js/pull/2440"
    }
  },
  "relatedItems": [
    {
      "number": 2440,
      "type": "pull_request",
      "state": "closed",
      "title": "fix: process CSS sourcemaps in addition to JS sourcemaps (#2383)",
      "url": "https://github.com/PostHog/posthog-js/pull/2440",
      "labels": [],
      "bodyExcerpt": "## Summary\n\nThis PR fixes issue #2383 by ensuring CSS sourcemaps are processed and cleaned up alongside JS sourcemaps.\n\n**Original Issue**: https://github.com/PostHog/posthog-js/issues/2383\n\n## Problem\n\nThe `@posthog/nextjs-config` sourcemap upload and cleanup process was only scanning `.next/static/chunks` for sourcemaps, completely missing the CSS sourcemaps located in `.next/static/css`. This caused:\n\n1. SCSS source files to remain exposed in production builds\n2. Developer folder structure to be visible in browser DevTools via `webpack://_N_E/...` paths\n3. `deleteAfterUpload` option to be ineffective for CSS/SCSS sourcemaps\n\nUsers had to work around this with manual cleanup scripts:\n```js\n\"clean-css-maps\": \"find .next/static/css -type f -name '*.css.map' -delete\"\n```\n\n## Solution\n\nModified the webpack plugin to also process the `.next/static/css` directory for client-side builds. Now when sourcemaps are uploaded and `deleteAfterUpload` is enabled, both JS and CSS sourcemaps are prop…",
      "createdAt": "2025-10-15T14:43:47Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "6832704e70f10a3118d841799930a73d25a13d4746549c6e29b477d26027073d",
  "piCalls": 5,
  "runId": 28098569629,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28098569629"
}
```
