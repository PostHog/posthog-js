# issue 2342: fetching from https://eu.posthog.com/decide shouldn't throw multiple errors with invalid posthog.init

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2342
- Conclusion: Needs maintainer guidance/repro; current SDK has no awaitable init and will log repeated request failures for invalid tokens/config.
- Labels: feature/flags, team/feature-flags, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28434455752
- Updated: 2026-06-30T09:40:56.111Z

```json
{
  "triage": {
    "conclusion": "Needs maintainer guidance/repro; current SDK has no awaitable init and will log repeated request failures for invalid tokens/config.",
    "summary": "Reporter initializes browser PostHog with invalid configuration/token, sees repeated errors from remote config/flags/capture requests, and asks whether init can be awaited or status-checked.",
    "issueType": "bug",
    "confidence": 0.72,
    "labels": [
      "bug: Something isn't working",
      "feature/flags",
      "team/feature-flags",
      "web",
      "question: Further information is requested"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "Thanks for the report. From the current browser SDK code, `posthog.init()` is intentionally synchronous and returns the PostHog instance, not a promise. The `loaded` callback only indicates local SDK setup has completed; it does not validate the project token/server config.\n\nFor feature flags, the closest async signal is `posthog.onFeatureFlags((flags, variants, { errorsLoading }) => { ... })`, where `errorsLoading` indicates whether the flags request failed. Request failures can also be handled/suppressed with the `on_request_error` config callback. If you want to disable the initial remote config/flags calls entirely, `advanced_disable_flags: true` is the current config option; `advanced_disable_decide` is deprecated.\n\nCould you share the SDK version, the exact `posthog.init(...)` options with sensitive values redacted, and the exact errors/status codes being logged? That would help determine whether this is expected request-error logging for an invalid token or an SDK bug causing unnecessary repeated flag/config requests.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/remote-config.ts",
        "packages/browser/src/posthog-featureflags.ts",
        "packages/types/src/posthog-config.ts",
        "packages/browser/src/utils/request-router.ts",
        "packages/browser/src/__tests__/remote-config.test.ts"
      ],
      "findings": [
        "`posthog.init()` returns a `PostHog` instance synchronously; `_init()` sets `__loaded` and calls `_loaded()`, and the code comment notes async work is callback-based rather than promise-based.",
        "The default `on_request_error` in `packages/browser/src/posthog-core.ts` logs `Bad HTTP status: ...`, and `_send_request()` invokes it for every response with status `>= 400`.",
        "Current code no longer uses `/decide` for the main browser SDK path inspected here: remote config loads from `/array/{token}/config`, and feature flags POST to `/flags/?v=2`.",
        "`advanced_disable_decide` still exists only as a deprecated alias/migration path; `advanced_disable_flags` is the current option for disabling external remote-config/flags calls.",
        "`RemoteConfigLoader` intentionally still initializes extensions and calls `featureFlags.ensureFlagsLoaded()` when remote config fetch returns no config, so an invalid token/config can produce multiple initial request errors.",
        "Feature flag loading exposes `posthog.onFeatureFlags(..., { errorsLoading })` as an async failure signal, but there is no public API that awaits overall init plus token validation.",
        "`capture()` continues sending events whenever the SDK is loaded and `is_capturing()` is true, so an invalid project token can cause request-error logging for each captured event unless capture is skipped/opted out or request errors are handled."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small safe code change is not obvious without a concrete reproduction and desired product behavior. Making `init()` awaitable would be a public API change, and silently disabling capture/flags after a 4xx could hide transient/proxy errors or change existing error handling semantics.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": true,
    "reasons": [
      "token"
    ]
  },
  "snapshotHash": "1912a6bdd45eb06bf467dfc7cc32cb8fe21815e49bb6bca52ee012dc5143270b",
  "piCalls": 5,
  "runId": 28434455752,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28434455752"
}
```
