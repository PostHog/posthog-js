# issue 2241: Make update to default verbosity levels

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2241
- Conclusion: Valid enhancement request for quieter @posthog/nextjs-config sourcemap upload output, but likely needs CLI/logging behavior design rather than a tiny config-only tweak.
- Labels: enhancement, frameworks/next-js, feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28095535073
- Updated: 2026-06-24T11:57:52.619Z

```json
{
  "triage": {
    "conclusion": "Valid enhancement request for quieter @posthog/nextjs-config sourcemap upload output, but likely needs CLI/logging behavior design rather than a tiny config-only tweak.",
    "summary": "The Next.js config package currently routes sourcemap processing through posthog-cli with inherited stdio and a default logLevel of info, which can produce noisy build output, especially with Turbopack/chunk-heavy builds.",
    "issueType": "feature",
    "confidence": 0.82,
    "labels": [
      "enhancement",
      "frameworks/next-js",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the report. In the current codebase `@posthog/nextjs-config` no longer appears to expose a `verbose` flag directly; sourcemap upload verbosity is controlled through the shared `logLevel` option, which defaults to `info` and is forwarded to `posthog-cli` via `RUST_LOG`. The CLI is spawned with inherited stdio, so per-file/progress output comes from the CLI itself.\n\nA workaround today is likely setting `logLevel` to `warn`, `error`, or `silent`, but that also reduces status visibility. The requested behavior — quiet-by-default output while still showing concise upload progress — looks valid, but probably needs either a posthog-cli logging/progress mode or a deliberate change to the default log level plus CLI-side progress semantics.",
    "investigation": {
      "relevantFiles": [
        "packages/nextjs-config/src/config.ts",
        "packages/nextjs-config/src/utils.ts",
        "packages/plugin-utils/src/config.ts",
        "packages/plugin-utils/src/cli.ts",
        "packages/webpack-plugin/src/config.ts",
        "packages/webpack-plugin/src/index.ts",
        "packages/nextjs-config/CHANGELOG.md"
      ],
      "findings": [
        "`packages/nextjs-config/src/config.ts` resolves PostHog config through `@posthog/webpack-plugin` and, for Turbopack, calls `processSourceMaps(posthogConfig, config.distDir)` from the compiler hook.",
        "`packages/nextjs-config/src/utils.ts` delegates sourcemap processing directly to `runSourcemapCli(posthogOptions, { directory })`.",
        "`packages/plugin-utils/src/config.ts` defines `logLevel?: 'debug' | 'info' | 'warn' | 'error' | 'silent'` and currently defaults `logLevel` to `'info'`.",
        "`packages/plugin-utils/src/cli.ts` forwards that log level to the CLI with `RUST_LOG: posthog_cli=${config.logLevel}` and spawns the CLI with `stdio: 'inherit'`, so CLI output is shown directly in the build logs.",
        "No `verbose` option was found in `packages/nextjs-config`; changelog entry for #2552 notes that `cliBinaryPath` and `logLevel` options were later exposed, which partially addresses user control but does not implement quiet-by-default progress output."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A simple default change from `info` to `warn`/`silent` would reduce noise, but would also remove the status updates the reporter explicitly wants. Concise non-verbose progress likely requires changes or support in posthog-cli output behavior, not just a local Next.js wrapper change.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "8f40d30ad6b23517f3ff88a08d3172c9ea523672d2488251f6f1bce3932a22de",
  "piCalls": 9,
  "runId": 28095535073,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28095535073"
}
```
