# issue 3583: posthog_trace_id is not captured by openai responses.stream

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3583
- Conclusion: Valid bug: OpenAI Responses API streaming helper is not wrapped for PostHog params.
- Labels: bug, team/llm-analytics
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28363800378
- Updated: 2026-06-29T10:04:37.269Z

```json
{
  "triage": {
    "conclusion": "Valid bug: OpenAI Responses API streaming helper is not wrapped for PostHog params.",
    "summary": "`@posthog/ai` wraps `responses.create()` including `stream: true`, but there is no wrapper for the separate `responses.stream()` helper, so PostHog monitoring params are not extracted/captured there. The sample also uses unsupported snake_case `posthog_trace_id`; current SDK params are camelCase like `posthogTraceId`.",
    "issueType": "bug",
    "confidence": 0.86,
    "labels": [
      "bug",
      "team/llm-analytics"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the report. I can reproduce the likely code-path issue from the SDK source: `WrappedResponses` instruments `responses.create()` and `responses.parse()`, but does not override the separate `responses.stream()` helper, so PostHog params are not extracted/captured for that API surface.\n\nOne clarification: the SDK currently recognizes camelCase monitoring params such as `posthogTraceId`, not snake_case `posthog_trace_id`. Putting trace IDs inside OpenAI `metadata` also will not map them to PostHog `$ai_trace_id` today.\n\nA fix should add explicit `responses.stream()` support while preserving the OpenAI SDK stream helper behavior.",
    "investigation": {
      "relevantFiles": [
        "packages/ai/src/openai/index.ts",
        "packages/ai/src/openai/azure.ts",
        "packages/ai/src/utils.ts",
        "packages/ai/tests/openai.test.ts"
      ],
      "findings": [
        "`packages/ai/src/openai/index.ts` defines `WrappedResponses extends Responses` with overrides for `create()` and `parse()`, but no `stream()` override.",
        "`WrappedResponses.create()` already handles `openAIParams.stream` and captures `$ai_generation` with `...posthogParams`, so `responses.create({ stream: true, posthogTraceId })` is the instrumented path.",
        "`packages/ai/src/utils.ts` maps `posthogTraceId` to internal `traceId`; it does not recognize `posthog_trace_id`, so the sample's snake_case parameter is not a supported monitoring param.",
        "Search found no `responses.stream` implementation or tests under `packages/ai`, so this helper path appears uncovered.",
        "`packages/ai/src/openai/azure.ts` has the same pattern for Azure Responses: `create()` and `parse()` are wrapped, but `stream()` is not."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The affected area is localized, but `responses.stream()` has distinct OpenAI SDK helper semantics from `responses.create({ stream: true })`; the fix must avoid changing the returned stream/helper API while adding capture, so it needs careful review rather than a trivial one-line change.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3856,
      "type": "pull_request",
      "state": "open",
      "title": "fix(ai): wrap responses.stream() so posthog params are tracked (#3583)",
      "url": "https://github.com/PostHog/posthog-js/pull/3856",
      "labels": [
        "stale"
      ],
      "bodyExcerpt": "## Problem\n\nFixes #3583.\n\n`PostHogOpenAI` / `PostHogAzureOpenAI` wrap `responses.create` (and `responses.parse`) but **not `responses.stream()`**. Because the streaming helper was unwrapped:\n\n- a top-level `posthogTraceId` was forwarded straight to the API → `400 Unknown parameter: 'posthog_trace_id'`, and\n- the streamed generation was never captured, so it couldn't be associated with a trace.\n\n## Fix\n\nAdd a `stream()` override to `WrappedResponses` in both `src/openai/index.ts` and `src/openai/azure.ts`. It mirrors the existing `create` path:\n\n1. `extractPosthogParams(body)` strips the `posthog*` params before the body reaches OpenAI (fixes the 400).\n2. The generation is captured via `captureAiGeneration`, reusing the same formatting/usage helpers `create` uses.\n\n### Design note: `ResponseStream` vs `Stream`\n\n`responses.stream()` does not return a plain `Stream` like `create({ stream: true })` does — it returns OpenAI's `ResponseStream` helper, so the `.tee()` approach `create` uses d…",
      "createdAt": "2026-06-16T02:35:08Z",
      "reason": "closing-pr"
    }
  ],
  "duplicate": {
    "duplicate": true,
    "score": 1,
    "canonical": {
      "number": 3856,
      "type": "pull_request",
      "state": "open",
      "title": "fix(ai): wrap responses.stream() so posthog params are tracked (#3583)",
      "url": "https://github.com/PostHog/posthog-js/pull/3856",
      "labels": [
        "stale"
      ],
      "bodyExcerpt": "## Problem\n\nFixes #3583.\n\n`PostHogOpenAI` / `PostHogAzureOpenAI` wrap `responses.create` (and `responses.parse`) but **not `responses.stream()`**. Because the streaming helper was unwrapped:\n\n- a top-level `posthogTraceId` was forwarded straight to the API → `400 Unknown parameter: 'posthog_trace_id'`, and\n- the streamed generation was never captured, so it couldn't be associated with a trace.\n\n## Fix\n\nAdd a `stream()` override to `WrappedResponses` in both `src/openai/index.ts` and `src/openai/azure.ts`. It mirrors the existing `create` path:\n\n1. `extractPosthogParams(body)` strips the `posthog*` params before the body reaches OpenAI (fixes the 400).\n2. The generation is captured via `captureAiGeneration`, reusing the same formatting/usage helpers `create` uses.\n\n### Design note: `ResponseStream` vs `Stream`\n\n`responses.stream()` does not return a plain `Stream` like `create({ stream: true })` does — it returns OpenAI's `ResponseStream` helper, so the `.tee()` approach `create` uses d…",
      "createdAt": "2026-06-16T02:35:08Z",
      "reason": "closing-pr"
    },
    "blockingPr": {
      "number": 3856,
      "type": "pull_request",
      "state": "open",
      "title": "fix(ai): wrap responses.stream() so posthog params are tracked (#3583)",
      "url": "https://github.com/PostHog/posthog-js/pull/3856",
      "labels": [
        "stale"
      ],
      "bodyExcerpt": "## Problem\n\nFixes #3583.\n\n`PostHogOpenAI` / `PostHogAzureOpenAI` wrap `responses.create` (and `responses.parse`) but **not `responses.stream()`**. Because the streaming helper was unwrapped:\n\n- a top-level `posthogTraceId` was forwarded straight to the API → `400 Unknown parameter: 'posthog_trace_id'`, and\n- the streamed generation was never captured, so it couldn't be associated with a trace.\n\n## Fix\n\nAdd a `stream()` override to `WrappedResponses` in both `src/openai/index.ts` and `src/openai/azure.ts`. It mirrors the existing `create` path:\n\n1. `extractPosthogParams(body)` strips the `posthog*` params before the body reaches OpenAI (fixes the 400).\n2. The generation is captured via `captureAiGeneration`, reusing the same formatting/usage helpers `create` uses.\n\n### Design note: `ResponseStream` vs `Stream`\n\n`responses.stream()` does not return a plain `Stream` like `create({ stream: true })` does — it returns OpenAI's `ResponseStream` helper, so the `.tee()` approach `create` uses d…",
      "createdAt": "2026-06-16T02:35:08Z",
      "reason": "closing-pr"
    },
    "reason": "open related PR #3856 already appears to cover this report"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "open related PR #3856 already appears to cover this report: #3856 https://github.com/PostHog/posthog-js/pull/3856",
  "snapshotHash": "613c129f7a59d400c469419b783b35766df79b4151036834e37a54635a60f94a",
  "piCalls": 4,
  "runId": 28363800378,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28363800378"
}
```
