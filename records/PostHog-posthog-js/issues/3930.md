# issue 3930: React Native: align isFeatureEnabled missing-flag fallback with sdk-specs

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3930
- Conclusion: Valid RN/core feature-flag spec-parity issue; current code can return undefined for the boolean helper.
- Labels: feature/flags, team/feature-flags, react-native, feature/mobile
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28736108285
- Updated: 2026-07-05T09:26:45.734Z

```json
{
  "triage": {
    "conclusion": "Valid RN/core feature-flag spec-parity issue; current code can return undefined for the boolean helper.",
    "summary": "React Native delegates isFeatureEnabled() to core, and core currently returns undefined whenever getFeatureFlag() returns undefined, which conflicts with the requested SDK-spec behavior for missing cached flags.",
    "issueType": "bug",
    "confidence": 0.91,
    "labels": [
      "bug: Something isn't working",
      "feature/flags",
      "team/feature-flags",
      "react-native",
      "feature/mobile"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the current tree. `packages/react-native/src/posthog-rn.ts` just delegates `isFeatureEnabled()` to core, and `packages/core/src/posthog-core.ts` returns `undefined` whenever `getFeatureFlag()` returns `undefined`. Existing core tests also assert `undefined` for not-loaded / errored-empty flag state, while missing flags with a non-empty cached flag set already return `false`.\n\nA minimal fix should keep the change scoped to the boolean convenience API: update core `isFeatureEnabled()` behavior and matching tests/types/docs as needed, while avoiding behavior changes to `getFeatureFlag()` / `getFeatureFlagResult()` unless the spec explicitly requires it. There is also an open related PR (#3959) that appears intended to address this.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/posthog-core.ts",
        "packages/react-native/src/posthog-rn.ts",
        "packages/core/src/__tests__/posthog.featureflags.spec.ts",
        "packages/core/src/__tests__/posthog.featureflags.v1.spec.ts",
        "packages/core/src/types.ts",
        "packages/types/src/posthog.ts"
      ],
      "findings": [
        "`packages/react-native/src/posthog-rn.ts` implements `isFeatureEnabled(key, options)` as `return super.isFeatureEnabled(key, options)`, so RN inherits core behavior directly.",
        "`packages/core/src/posthog-core.ts` implements `isFeatureEnabled()` by calling `getFeatureFlag()` and returning `undefined` if that result is `undefined`; otherwise it coerces with `!!response`.",
        "`_getFeatureFlagResult()` currently has legacy missing-flag behavior for `getFeatureFlag`: it only fabricates `{ enabled: false }` when flag details exist and `details.flags` is non-empty; otherwise the value remains `undefined`.",
        "`packages/core/src/__tests__/posthog.featureflags.spec.ts` currently asserts `isFeatureEnabled()` returns `undefined` before flags are loaded and for an errored/empty cached state, so tests would need to be updated alongside the behavior change.",
        "The public typings currently expose `isFeatureEnabled(...): boolean | undefined`, so a full parity fix may need a deliberate type/docs decision depending on whether unloaded/error states can still be represented as `undefined`."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The code path is localized because RN delegates to core, but the behavior is public API and shared by core consumers, so tests and typings/docs should be updated deliberately.",
      "suggestedApproach": "Add/adjust core tests for the SDK-spec cases, then change only the boolean `isFeatureEnabled()` path to return `false` for missing cached flags while preserving `getFeatureFlag()` and `getFeatureFlagResult()` semantics. If the spec requires all undefined boolean results to become false, update the return type/docs accordingly; otherwise preserve undefined for genuinely not-loaded/error states.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3959,
      "type": "pull_request",
      "state": "open",
      "title": "fix(core): isFeatureEnabled returns false instead of undefined",
      "url": "https://github.com/PostHog/posthog-js/pull/3959",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`isFeatureEnabled` in `@posthog/core` declares a return type of `boolean | undefined` and returns `undefined` when the underlying `getFeatureFlag` call finds no cached flag value. This third state is unnecessary: the function's semantics are \\\"is this flag enabled?\\\", and the answer to that question when a flag is absent or not yet loaded is `false`, not an unknown.\n\nCloses #3930\n\n## Changes\n\n`isFeatureEnabled` in `packages/core/src/posthog-core.ts` now returns `false` instead of `undefined` when `getFeatureFlag` returns `undefined`. The return type is narrowed from `boolean | undefined` to `boolean`. The interface in `packages/types/src/posthog.ts` is updated to match.\n\nTests in `posthog.featureflags.spec.ts` and `posthog.featureflags.v1.spec.ts` that previously asserted `undefined` for the not-loaded and error-state cases are updated to assert `false`.\n\nNote: `posthog-featureflags.ts` in the browser package has its own `isFeatureEnabled` that retains `undefined` for the \\…",
      "createdAt": "2026-06-25T00:42:02Z",
      "reason": "closing-pr"
    }
  ],
  "duplicate": {
    "duplicate": true,
    "score": 1,
    "canonical": {
      "number": 3959,
      "type": "pull_request",
      "state": "open",
      "title": "fix(core): isFeatureEnabled returns false instead of undefined",
      "url": "https://github.com/PostHog/posthog-js/pull/3959",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`isFeatureEnabled` in `@posthog/core` declares a return type of `boolean | undefined` and returns `undefined` when the underlying `getFeatureFlag` call finds no cached flag value. This third state is unnecessary: the function's semantics are \\\"is this flag enabled?\\\", and the answer to that question when a flag is absent or not yet loaded is `false`, not an unknown.\n\nCloses #3930\n\n## Changes\n\n`isFeatureEnabled` in `packages/core/src/posthog-core.ts` now returns `false` instead of `undefined` when `getFeatureFlag` returns `undefined`. The return type is narrowed from `boolean | undefined` to `boolean`. The interface in `packages/types/src/posthog.ts` is updated to match.\n\nTests in `posthog.featureflags.spec.ts` and `posthog.featureflags.v1.spec.ts` that previously asserted `undefined` for the not-loaded and error-state cases are updated to assert `false`.\n\nNote: `posthog-featureflags.ts` in the browser package has its own `isFeatureEnabled` that retains `undefined` for the \\…",
      "createdAt": "2026-06-25T00:42:02Z",
      "reason": "closing-pr"
    },
    "blockingPr": {
      "number": 3959,
      "type": "pull_request",
      "state": "open",
      "title": "fix(core): isFeatureEnabled returns false instead of undefined",
      "url": "https://github.com/PostHog/posthog-js/pull/3959",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`isFeatureEnabled` in `@posthog/core` declares a return type of `boolean | undefined` and returns `undefined` when the underlying `getFeatureFlag` call finds no cached flag value. This third state is unnecessary: the function's semantics are \\\"is this flag enabled?\\\", and the answer to that question when a flag is absent or not yet loaded is `false`, not an unknown.\n\nCloses #3930\n\n## Changes\n\n`isFeatureEnabled` in `packages/core/src/posthog-core.ts` now returns `false` instead of `undefined` when `getFeatureFlag` returns `undefined`. The return type is narrowed from `boolean | undefined` to `boolean`. The interface in `packages/types/src/posthog.ts` is updated to match.\n\nTests in `posthog.featureflags.spec.ts` and `posthog.featureflags.v1.spec.ts` that previously asserted `undefined` for the not-loaded and error-state cases are updated to assert `false`.\n\nNote: `posthog-featureflags.ts` in the browser package has its own `isFeatureEnabled` that retains `undefined` for the \\…",
      "createdAt": "2026-06-25T00:42:02Z",
      "reason": "closing-pr"
    },
    "reason": "open related PR #3959 already appears to cover this report"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "open related PR #3959 already appears to cover this report: #3959 https://github.com/PostHog/posthog-js/pull/3959",
  "snapshotHash": "9b7925b3c7b7358bcfc1f53217476d2f45dbe4e00895097eb5594c8f0990b9ce",
  "piCalls": 2,
  "runId": 28736108285,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28736108285"
}
```
