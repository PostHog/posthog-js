# issue 2159: Implement `element_allowlist`/`element_denylist`

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2159
- Conclusion: Valid feature request; partially supported in browser SDK but still missing in core-derived SDKs/RN and no element denylist exists.
- Labels: enhancement, react-native, feature/mobile, feature/autocapture, web-lite
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28229591102
- Updated: 2026-06-26T09:34:06.886Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; partially supported in browser SDK but still missing in core-derived SDKs/RN and no element denylist exists.",
    "summary": "Request to add element-level allow/deny list filtering for autocapture in core-derived SDKs, especially web-lite and React Native, to avoid requiring `ph-no-capture` throughout apps.",
    "issueType": "feature",
    "confidence": 0.86,
    "labels": [
      "enhancement",
      "react-native",
      "feature/mobile",
      "feature/autocapture",
      "web-lite"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Triaged as an autocapture feature request for React Native / web-lite.\n\nCurrent state on `main`:\n- `posthog-js` browser autocapture already supports `autocapture.element_allowlist`.\n- I did not find any `element_denylist` support in the package sources.\n- `@posthog/core` only exposes the low-level `autocapture(eventType, elements, ...)` enqueue path; it does not own filtering/config semantics.\n- React Native autocapture supports `noCaptureProp` and `ignoreLabels`, but not `element_allowlist` / `element_denylist`.\n- web-lite currently exposes `autocapture?: boolean` in its options, with no element allow/deny config in `packages/web/src/types.ts`.\n\nThe request is clear, but the implementation should first settle SDK-specific semantics, especially for React Native where captured “elements” are component labels/tag names rather than DOM tag names.",
    "investigation": {
      "relevantFiles": [
        "packages/types/src/posthog-config.ts",
        "packages/browser/src/autocapture-utils.ts",
        "packages/react-native/src/types.ts",
        "packages/react-native/src/autocapture.tsx",
        "packages/react-native/test/autocapture.spec.ts",
        "packages/web/src/types.ts",
        "packages/web/src/posthog-web.ts",
        "packages/core/src/posthog-core.ts",
        "packages/core/src/types.ts"
      ],
      "findings": [
        "`packages/types/src/posthog-config.ts` defines `AutocaptureConfig.element_allowlist?: AutocaptureCompatibleElement[]` for DOM autocapture, but no `element_denylist` was found in package sources.",
        "`packages/browser/src/autocapture-utils.ts` implements allowlist filtering via `checkIfElementTreePassesElementAllowList`, checking DOM `tagName` against `autocaptureConfig?.element_allowlist`.",
        "`packages/core/src/posthog-core.ts` has `PostHogCore.autocapture(...)`, but it only builds and enqueues a `$autocapture` payload; it does not filter elements based on config.",
        "`packages/react-native/src/types.ts` defines `PostHogAutocaptureOptions` with `captureTouches`, `customLabelProp`, `noCaptureProp`, `maxElementsCaptured`, `ignoreLabels`, and `propsToCapture`, but no element allow/deny list options.",
        "`packages/react-native/src/autocapture.tsx` returns early for `noCaptureProp` and skips labels listed in `ignoreLabels`, then captures when any elements remain; there is no allowlist/denylist filtering.",
        "`packages/web/src/types.ts` exposes only `autocapture?: boolean` for web-lite options, and `packages/web/src/posthog-web.ts` does not add element-level autocapture filtering."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A narrow React Native-only filter would be small, but the issue asks for core/shared behavior across SDKs and includes `element_denylist`, which is not currently present even in the browser autocapture config. React Native also needs an API/semantics decision because its captured element names are component labels rather than DOM element names.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "7a646a8d3c2a91c20b361a2db201b971fdd6a8275b642bd39947e470926ee1ad",
  "piCalls": 2,
  "runId": 28229591102,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28229591102"
}
```
