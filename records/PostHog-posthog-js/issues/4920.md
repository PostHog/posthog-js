# issue 4920: immediate delivery resolves after failed requests and terminal redirects

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4920
- Conclusion: Valid Node delivery-semantics bug; the terminal-3xx portion is already fixed in shared core, but immediate calls still suppress transport failures.
- Labels: node, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/34821416005
- Updated: 2026-09-14T08:12:44.242Z

```json
{
  "triage": {
    "conclusion": "Valid Node delivery-semantics bug; the terminal-3xx portion is already fixed in shared core, but immediate calls still suppress transport failures.",
    "summary": "Awaited posthog-node immediate capture methods emit/log failed delivery but resolve instead of rejecting, preventing callers from using the returned promise as a delivery acknowledgement.",
    "issueType": "bug",
    "confidence": 0.97,
    "labels": [
      "bug: Something isn't working",
      "node",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the focused reproduction. The terminal-3xx portion has since been addressed in shared core by [#4922](https://github.com/PostHog/posthog-js/pull/4922): successful writes now require a final 2xx response, while required-response reads retain their existing handling.\n\nThe remaining immediate-delivery behavior is confirmed in the current Node source: core's `sendImmediate()` emits an `error` event after `sendBatch()` fails but resolves, and Node's `_sendPreparedEvent()` has a final catch that logs and resolves. The existing Node regression test explicitly expects 300/302/304/503 immediate writes to resolve after emitting an error.\n\nChanging this needs a deliberate compatibility decision rather than a speculative patch: immediate methods are documented as promises, but callers may currently rely on their suppression behavior. If rejection is adopted, update the focused immediate-delivery tests for HTTP, network, and timeout failures; retain queued/background capture error handling; and exercise shutdown/pending-promise behavior because immediate work is registered in the shared promise queue.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/posthog-core-stateless.ts",
        "packages/core/src/__tests__/posthog.flush.spec.ts",
        "packages/core/src/__tests__/posthog.shutdown.spec.ts",
        "packages/core/src/utils/promise-queue.ts",
        "packages/core/CHANGELOG.md",
        "packages/node/src/client.ts",
        "packages/node/src/__tests__/immediate-delivery.spec.ts",
        "packages/node/src/__tests__/posthog-node.spec.ts",
        "packages/node/CHANGELOG.md"
      ],
      "findings": [
        "Shared core now rejects successful-write responses with status >= 300; core flush tests explicitly cover 300, 302, and 304 as failures while preserving 302 handling for required feature-flag reads.",
        "@posthog/core 1.53.3 changelog attributes the final-2xx successful-write change to PR #4922 and explicitly says immediate-call error handling was preserved.",
        "PostHogCoreStateless.sendImmediate() awaits sendBatch(), emits an error in its catch block, and does not rethrow.",
        "PostHog._sendPreparedEvent() catches errors from both preparation and sending, logs them, and resolves; this is a second suppression layer for Node immediate methods.",
        "The existing Node immediate-delivery test currently asserts that HTTP 300, 302, 304, and 503 emit an error without rejecting captureImmediate().",
        "Immediate work is added to the shared PromiseQueue; shutdown joins that queue and separately catches PostHog fetch errors, so changing rejection propagation requires regression coverage for shutdown rather than only changing the two catches."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The code change could be small, but it changes established observable promise behavior and interacts with the pending-work/shutdown path. Maintainer agreement is needed on whether all immediate methods should reject transport failures and on the intended behavior for callers that do not handle those rejections.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.96,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3819,
      "type": "issue",
      "state": "closed",
      "title": "[posthog-node] captureImmediate() returns before the HTTP request completes — events dropped in AWS Lambda / Workers / Vercel",
      "url": "https://github.com/PostHog/posthog-js/issues/3819",
      "labels": [
        "node"
      ],
      "bodyExcerpt": "## Summary\n\n`captureImmediate()` returns from `await` before the underlying HTTP request actually lands. In short-lived environments (AWS Lambda, Cloudflare Workers, Vercel functions without `waitUntil`) the runtime freezes the container the moment the request handler returns its response, dropping any in-flight HTTP request — including the one `captureImmediate` is supposed to guarantee.\n\nThis contradicts the documented serverless guidance ([posthog.com/docs/libraries/node — \"Short-lived processes like serverless environments\"](https://posthog.com/docs/libraries/node)):\n\n> we recommend using the `captureImmediate` method instead of `capture` to ensure the event is captured before the function shuts down\n\n## Root cause\n\nThe bug is a missing `return` in the final `.then` callback of the `captureImmediate` promise chain.\n\n[`posthog-node/src/client.ts` on `main`, lines 222–226](https://github.com/PostHog/posthog-js-lite/blob/main/posthog-node/src/client.ts#L222-L226):\n\n```ts\n.then((additi…",
      "createdAt": "2026-05-13T16:30:15Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.07964601769911504,
    "canonical": {
      "number": 3819,
      "type": "issue",
      "state": "closed",
      "title": "[posthog-node] captureImmediate() returns before the HTTP request completes — events dropped in AWS Lambda / Workers / Vercel",
      "url": "https://github.com/PostHog/posthog-js/issues/3819",
      "labels": [
        "node"
      ],
      "bodyExcerpt": "## Summary\n\n`captureImmediate()` returns from `await` before the underlying HTTP request actually lands. In short-lived environments (AWS Lambda, Cloudflare Workers, Vercel functions without `waitUntil`) the runtime freezes the container the moment the request handler returns its response, dropping any in-flight HTTP request — including the one `captureImmediate` is supposed to guarantee.\n\nThis contradicts the documented serverless guidance ([posthog.com/docs/libraries/node — \"Short-lived processes like serverless environments\"](https://posthog.com/docs/libraries/node)):\n\n> we recommend using the `captureImmediate` method instead of `capture` to ensure the event is captured before the function shuts down\n\n## Root cause\n\nThe bug is a missing `return` in the final `.then` callback of the `captureImmediate` promise chain.\n\n[`posthog-node/src/client.ts` on `main`, lines 222–226](https://github.com/PostHog/posthog-js-lite/blob/main/posthog-node/src/client.ts#L222-L226):\n\n```ts\n.then((additi…",
      "createdAt": "2026-05-13T16:30:15Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 8%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "ec6fe0c61f787e56c8fb1f6fbaf5412787151173c0a456ef4e76c2c83c050815",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/e3dcb761aaf9566e33432d45a1ed1d1f",
    "gistId": "e3dcb761aaf9566e33432d45a1ed1d1f",
    "files": [
      {
        "name": "call-1-1-2026-09-14T08-11-55-432Z_01a09ef9-06a8-7c1f-8029-9ea989ccdf20.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/e3dcb761aaf9566e33432d45a1ed1d1f/raw/07b83ef4968215920e9fe3eaf158da2136fec99a/call-1-1-2026-09-14T08-11-55-432Z_01a09ef9-06a8-7c1f-8029-9ea989ccdf20.jsonl"
      }
    ]
  },
  "runId": 34821416005,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/34821416005"
}
```
