# issue 3826: posthog-react-native: support Kotlin DSL (build.gradle.kts) in the Expo Android symbol-upload config plugin

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3826
- Conclusion: Valid enhancement: Expo Android config plugin lacks explicit Kotlin DSL support for posthog-react-native Gradle edits.
- Labels: enhancement, react-native, Android, feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28651575993
- Updated: 2026-07-03T09:35:06.092Z

```json
{
  "triage": {
    "conclusion": "Valid enhancement: Expo Android config plugin lacks explicit Kotlin DSL support for posthog-react-native Gradle edits.",
    "summary": "The React Native Expo config plugin currently only handles the native-symbol Android Gradle plugin path for Groovy build.gradle files, so build.gradle.kts projects do not get the PostHog Android Gradle plugin wiring needed for R8 mapping uploads.",
    "issueType": "feature",
    "confidence": 0.88,
    "labels": [
      "enhancement",
      "react-native",
      "Android",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the current config-plugin code. `withAndroidNativeSymbolsPlugin` returns early when `config.modResults.language !== 'groovy'`, so Kotlin DSL projects skip both the `com.posthog.android` classpath and app plugin apply step. The lower-level apply helper also only recognizes/emits Groovy `apply plugin:` syntax. Separately, the JS sourcemap `withAndroidPlugin` path warns for non-Groovy but still emits Groovy `apply from:` / `project.ext` syntax, so KTS handling should be made explicit there too.\n\nA focused fix should add language-aware Gradle mutations plus unit tests for representative `build.gradle.kts` project/app files, while preserving the existing classpath/apply coupling so we never apply `com.posthog.android` unless the classpath was successfully inserted or already present.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/tooling/expoconfig.ts",
        "packages/react-native/test/expoconfig.spec.ts",
        "packages/react-native/CHANGELOG.md"
      ],
      "findings": [
        "`packages/react-native/src/tooling/expoconfig.ts` has `withAndroidNativeSymbolsPlugin` returning early for non-Groovy project and app Gradle mods before calling the classpath/apply helpers.",
        "`addPostHogAndroidGradlePluginClasspath` already bounds insertion to `buildscript { dependencies { ... } }`, but it is only reached from the Groovy-only mod path today.",
        "`applyPostHogAndroidGradlePlugin` only detects and inserts Groovy `apply plugin: \"com.posthog.android\"`; it does not handle Kotlin DSL `plugins { id(...) }` or `apply(plugin = ...)` syntax.",
        "`withAndroidPlugin` emits Groovy `apply from: new File(...)` and `project.ext.posthogReactNativeSkipOnConflict = true`; it warns on non-Groovy language but does not return, so KTS app Gradle files need an explicit Kotlin DSL variant there as well.",
        "`packages/react-native/test/expoconfig.spec.ts` covers the Groovy-style classpath/apply helpers but has no Kotlin DSL cases."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The change is localized to the React Native Expo config plugin and its unit tests, with no public API change. The main caution is verifying exact Expo `language` values and valid Gradle Kotlin DSL syntax.",
      "suggestedApproach": "Add language-aware helper paths for Groovy vs Kotlin DSL. For KTS, insert `classpath(\"com.posthog:posthog-android-gradle-plugin:...\")` inside the buildscript dependencies block, apply the native-symbol plugin using Kotlin DSL syntax, and add a KTS equivalent for the `posthog.gradle` apply-from plus skip-on-conflict property. Keep the existing buildscript-bounded search and only apply the app plugin when the classpath helper reports present. Add unit tests for representative project `build.gradle.kts` and app `build.gradle.kts` contents, including idempotency.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3677,
      "type": "pull_request",
      "state": "closed",
      "title": "feat(react-native): add native crash capture and debug symbol upload",
      "url": "https://github.com/PostHog/posthog-js/pull/3677",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nThe React Native SDK only autocaptures JS-side errors today. Native iOS and Android crashes — which happen below the JS layer — never reach PostHog. This PR lets users opt in to native crash capture through the optional native plugin, alongside session replay.\r\n\r\nThe companion package `posthog-react-native-session-replay` is superseded by **`@posthog/react-native-plugin`** (now in this monorepo, published as `2.0.1`): it keeps the same session-replay surface and adds native error tracking behind a unified `setup()` entry point. This PR wires the SDK to the new plugin while keeping the legacy package supported as a fallback.\r\n\r\n## Changes\r\n\r\n### Runtime — native crash capture\r\n- New `errorTracking.autocapture.nativeCrashes?: boolean` (on `AutocaptureOptions`). When `true`, the SDK forwards native crash autocapture to the iOS/Android PostHog SDKs via the optional plugin. Off by default, and additionally gated server-side by the project's Error tracking **Autocapture** setti…",
      "createdAt": "2026-05-27T14:23:42Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "f988d6ae190f36429f2ebb8499f2d4d0e1bec5af0254069c3666ffb1edf59e83",
  "piCalls": 4,
  "runId": 28651575993,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28651575993"
}
```
