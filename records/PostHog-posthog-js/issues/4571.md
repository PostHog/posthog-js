# issue 4571: Logs OTLP encoder emits int64 as a JSON number instead of a string

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4571
- Conclusion: Confirmed OTLP/HTTP JSON encoding bug affecting log and metric attribute integers.
- Labels: team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/32303605131
- Updated: 2026-08-19T21:31:50.193Z

```json
{
  "triage": {
    "conclusion": "Confirmed OTLP/HTTP JSON encoding bug affecting log and metric attribute integers.",
    "summary": "The shared logs encoder emits OTLP int64 AnyValue fields as JSON numbers. Proto3 JSON mapping requires int64 values to be strings, and metrics reuse this encoder for data-point attributes.",
    "issueType": "bug",
    "confidence": 0.94,
    "labels": [
      "bug: Something isn't working",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed from the current source: `toOtlpAnyValue()` returns `{ intValue: value }` for every finite integer, while `toOtlpKeyValueList()` is shared by both logs and metric data-point attributes. The current unit tests explicitly assert the numeric wire shape in both places.\n\nA minimal correction should make `intValue` a string in the shared OTLP type and encoder, update the affected tests (including nested arrays), and encode integers outside signed int64 range as `stringValue` rather than an invalid `intValue`. Because this changes the emitted OTLP JSON for two pipelines, it should be verified against ingestion with a representative logs and metrics payload before release.\n\nOne caveat: the issue's cited `packages/core/src/traces/otlp.ts` reference is not present in this checkout, so that proposed implementation could not be compared directly here.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/logs/logs-utils.ts",
        "packages/core/src/logs/logs-utils.spec.ts",
        "packages/core/src/metrics/index.ts",
        "packages/core/src/metrics/index.spec.ts",
        "packages/types/src/capture-log.ts"
      ],
      "findings": [
        "`packages/core/src/logs/logs-utils.ts` handles every finite `Number.isInteger(value)` as `{ intValue: value }`, so the serialized OTLP JSON contains a numeric int64 field.",
        "`packages/core/src/metrics/index.ts` imports and uses `toOtlpKeyValueList()` when creating metric data-point attributes, so the same encoding affects metric attributes but not `asDouble` metric values.",
        "`packages/types/src/capture-log.ts` currently declares `OtlpAnyValue.intValue?: number`, which must be changed along with the encoder for the corrected wire shape.",
        "`packages/core/src/logs/logs-utils.spec.ts` asserts numeric `intValue` for scalar integers, arrays, and key-value lists; `packages/core/src/metrics/index.spec.ts` asserts `{ intValue: 2 }` for a metric attribute.",
        "The existing encoder already correctly represents non-finite numbers as string values, and its object-to-JSON-string behavior is explicitly documented as deliberate; neither needs to be broadened as part of this fix.",
        "No `packages/core/src/traces/otlp.ts` or core traces encoder exists in the inspected checkout, so the issue's claimed traces precedent could not be verified."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The faulty behavior is centralized in one shared encoder and has focused unit-test coverage. The change is small, but it alters a shipped OTLP JSON contract for logs and metric attributes, so an ingestion-level verification is needed.",
      "suggestedApproach": "Change `OtlpAnyValue.intValue` to `string`; encode signed-int64 integers as decimal strings; compare an integer converted to `BigInt` against signed int64 bounds and emit an exact `stringValue` when out of range. Update focused logs scalar/array/key-value tests and the metrics attribute assertion, then verify representative logs and metrics requests are accepted by ingestion.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4570,
      "type": "issue",
      "state": "open",
      "title": "Extract the three duplicated OTLP senders; logs drops batches on 5xx where metrics and traces retry",
      "url": "https://github.com/PostHog/posthog-js/issues/4570",
      "labels": [
        "team/client-libraries"
      ],
      "bodyExcerpt": "`PostHogCoreStateless` has three near-identical OTLP senders — `_sendLogsBatch`, `_sendMetricsBatch`, and `_sendTracesBatch`. Each is ~50 lines and they differ only in the endpoint path and auth style. Nothing keeps them in sync, and they have already drifted in a way that loses data.\n\n## The drift\n\nAll three classify a send failure into the same tagged outcome, but not by the same rule:\n\n| sender | retry-later when |\n|---|---|\n| `_sendLogsBatch` | `err instanceof PostHogFetchNetworkError` |\n| `_sendMetricsBatch` | `isPostHogFetchRetryableError(err)` |\n| `_sendTracesBatch` | `isPostHogFetchRetryableError(err)` |\n\n`isPostHogFetchRetryableError` returns true for HTTP **408, 429, and ≥500**, *or* a network error. `PostHogFetchNetworkError` is network-only.\n\nSo once `fetchWithRetry` exhausts its internal retries against a struggling ingestion endpoint:\n\n- **logs** classifies the 429/5xx as `fatal` and **drops the batch**\n- **metrics** and **traces** classify it as `retry-later` and keep th…",
      "createdAt": "2026-08-19T17:59:55Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.04672897196261682,
    "canonical": {
      "number": 4570,
      "type": "issue",
      "state": "open",
      "title": "Extract the three duplicated OTLP senders; logs drops batches on 5xx where metrics and traces retry",
      "url": "https://github.com/PostHog/posthog-js/issues/4570",
      "labels": [
        "team/client-libraries"
      ],
      "bodyExcerpt": "`PostHogCoreStateless` has three near-identical OTLP senders — `_sendLogsBatch`, `_sendMetricsBatch`, and `_sendTracesBatch`. Each is ~50 lines and they differ only in the endpoint path and auth style. Nothing keeps them in sync, and they have already drifted in a way that loses data.\n\n## The drift\n\nAll three classify a send failure into the same tagged outcome, but not by the same rule:\n\n| sender | retry-later when |\n|---|---|\n| `_sendLogsBatch` | `err instanceof PostHogFetchNetworkError` |\n| `_sendMetricsBatch` | `isPostHogFetchRetryableError(err)` |\n| `_sendTracesBatch` | `isPostHogFetchRetryableError(err)` |\n\n`isPostHogFetchRetryableError` returns true for HTTP **408, 429, and ≥500**, *or* a network error. `PostHogFetchNetworkError` is network-only.\n\nSo once `fetchWithRetry` exhausts its internal retries against a struggling ingestion endpoint:\n\n- **logs** classifies the 429/5xx as `fatal` and **drops the batch**\n- **metrics** and **traces** classify it as `retry-later` and keep th…",
      "createdAt": "2026-08-19T17:59:55Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 5%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "3666c37afa7a5689f46fa345b5178c5199c53997114887e882b2d2e4c35d6e21",
  "piCalls": 4,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/13c063feaaf6360c16c2db63ff847d25",
    "gistId": "13c063feaaf6360c16c2db63ff847d25",
    "files": [
      {
        "name": "call-4-1-2026-08-19T21-30-33-213Z_01a01bee-d97d-717a-96e8-2773faac1264.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/13c063feaaf6360c16c2db63ff847d25/raw/ab6be48d0ad9744bd4e85a5298a7d61b90ed29d8/call-4-1-2026-08-19T21-30-33-213Z_01a01bee-d97d-717a-96e8-2773faac1264.jsonl"
      }
    ]
  },
  "runId": 32303605131,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/32303605131"
}
```
