# issue 4285: [React Native Expo] Combining PostHog and Sentry config plugins can silently ship iOS without main.jsbundle

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4285
- Conclusion: Confirmed React Native iOS build-plugin composition bug: independent PostHog and Sentry wrappers can replace the same Expo bundle phase and produce a successful build without running the React Native bundler.
- Labels: react-native, iOS
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/30323418200
- Updated: 2026-07-28T02:34:27.554Z

```json
{
  "triage": {
    "conclusion": "Confirmed React Native iOS build-plugin composition bug: independent PostHog and Sentry wrappers can replace the same Expo bundle phase and produce a successful build without running the React Native bundler.",
    "summary": "Combining the PostHog and Sentry Expo config plugins can nest their iOS bundle-phase wrappers such that a wrapper receives `/bin/sh` instead of the React Native bundle command. The build exits successfully but omits `main.jsbundle`, leaving release apps stuck before JavaScript starts.",
    "issueType": "bug",
    "confidence": 0.91,
    "labels": [
      "bug: Something isn't working",
      "react-native",
      "iOS"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed reproduction and IPA-level verification. This is a valid config-plugin composition problem, not a PostHog runtime or native-replay issue.\n\nThe PostHog Expo plugin currently replaces the React Native bundle invocation with its own wrapper and only recognizes an existing PostHog wrapper as idempotent. It does not model another tool owning that same bundle phase. That permits positional-argument nesting with another wrapper such as Sentry, where a successful `/bin/sh` invocation can prevent the actual React Native bundle script from running.\n\nA safe resolution needs an explicit ownership strategy for the bundle phase rather than another wrapper-order workaround. At minimum, the plugin should prevent this configuration from silently producing an unbundled release app; a supported opt-out for PostHog's iOS source-map wrapper is a plausible short-term escape hatch.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/tooling/expoconfig.ts",
        "packages/react-native/tooling/posthog-xcode.sh",
        "packages/react-native/test/expoconfig.spec.ts",
        "packages/react-native/test/posthog-xcode-parse.spec.ts"
      ],
      "findings": [
        "`modifyExistingXcodeBuildScript` identifies the React Native bundle phase by the presence of `react-native-xcode.sh`, then replaces that invocation with `/bin/sh <posthog-xcode.sh> <original command>`.",
        "The Expo plugin only treats scripts containing `posthog-xcode.sh` or `posthog-react-native` as already handled. It has no detection or compatibility path for a third-party bundle wrapper.",
        "`posthog-xcode.sh` accepts a single script-path argument and subsequently runs it using `/bin/sh -c \"$REACT_NATIVE_XCODE\"`; it does not preserve or compose an arbitrary wrapper command and its remaining positional arguments.",
        "Existing regression coverage for #3682 verifies that PostHog does not mistake a direct `/bin/sh` prefix for the React Native script, but does not cover composition with a second config-plugin wrapper such as Sentry.",
        "The current source comments and tests confirm the prior Expo `/bin/sh` argument failure mode, but no repository test fixture currently validates the reported Sentry-generated bundle-phase command."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A narrowly targeted option to skip PostHog's iOS bundle/source-map wrapper would be localized, but an automatic fix must work regardless of Expo plugin execution order and must ensure React Native bundling runs exactly once while preserving each vendor's upload behavior. Detecting only one textual Sentry wrapper would be incomplete and brittle.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3682,
      "type": "issue",
      "state": "closed",
      "title": "iOS Release: posthog-xcode.sh greps $REACT_NATIVE_XCODE (=/bin/sh) so its PACKAGER_SOURCEMAP_FILE preserve-patch no-ops; posthog-cli fails 'Failed to load minified map'",
      "url": "https://github.com/PostHog/posthog-js/issues/3682",
      "labels": [
        "bug",
        "react-native"
      ],
      "bodyExcerpt": "# iOS Release build fails: posthog-xcode.sh greps `/bin/sh` instead of react-native-xcode.sh, so its \"preserve PACKAGER_SOURCEMAP_FILE\" patch no-ops and posthog-cli can't find the minified map\n\n**Repo:** PostHog/posthog-js-lite (`posthog-react-native`)\n\n## Summary\n\nOn iOS Release builds (Hermes, React Native ≥ 0.81, Expo config plugin), the\n\"Bundle React Native code and images\" phase fails with:\n\n```\nERROR posthog_cli::commands: msg=\"Oops! Failed to load minified map at\n'.../Build/.../BuildProductsPath/Release-iphoneos/main.jsbundle.map':\nNo such file or directory (os error 2)\"\n```\n\n`tooling/posthog-xcode.sh` already knows RN deletes the intermediate packager\nsourcemap and tries to patch `react-native-xcode.sh` to keep it — but the patch\ntargets the wrong variable, so it never runs. RN deletes the packager map, and\n`posthog-cli hermes clone --minified-map-path $SOURCEMAP_PACKAGER_FILE` then\naborts the whole build.\n\n## Environment\n\n| | |\n|---|---|\n| posthog-react-native | 4.45.5 (also r…",
      "createdAt": "2026-05-27T18:58:14Z",
      "reason": "explicit-reference"
    },
    {
      "number": 3689,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(react-native): fix posthog-xcode.sh receiving /bin/sh as $1 in Expo bundle phase",
      "url": "https://github.com/PostHog/posthog-js/pull/3689",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nCloses #3682\r\n\r\niOS Release builds using the Expo config plugin (`posthog-react-native/expo`) fail on Hermes + RN ≥ 0.81 with:\r\n\r\n```\r\nERROR posthog_cli::commands: msg=\"Oops! Failed to load minified map at '...main.jsbundle.map': No such file or directory (os error 2)\"\r\n```\r\n\r\nWhen the Expo bundle phase already starts with `/bin/sh`, the plugin wraps it as:\r\n\r\n```sh\r\n/bin/sh posthog-xcode.sh /bin/sh react-native-xcode.sh ...\r\n```\r\n\r\nSo `$1` inside `posthog-xcode.sh` resolves to `/bin/sh` instead of `react-native-xcode.sh`. The `grep`/`sed` patch that preserves `PACKAGER_SOURCEMAP_FILE` silently no-ops (grepping a binary), RN deletes the packager map, and `posthog-cli hermes clone` hard-fails trying to read it.\r\n\r\n## Changes\r\n\r\n**`src/tooling/expoconfig.ts`** — updated the regex in `addPostHogWithBundledScriptsToBundleShellScript` to capture only the RN script path (stripping any leading `/bin/sh` prefix) before passing it as `$1` to `posthog-xcode.sh`.\r\n\r\n**`tooling/posth…",
      "createdAt": "2026-05-28T12:01:01Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.125,
    "canonical": {
      "number": 3682,
      "type": "issue",
      "state": "closed",
      "title": "iOS Release: posthog-xcode.sh greps $REACT_NATIVE_XCODE (=/bin/sh) so its PACKAGER_SOURCEMAP_FILE preserve-patch no-ops; posthog-cli fails 'Failed to load minified map'",
      "url": "https://github.com/PostHog/posthog-js/issues/3682",
      "labels": [
        "bug",
        "react-native"
      ],
      "bodyExcerpt": "# iOS Release build fails: posthog-xcode.sh greps `/bin/sh` instead of react-native-xcode.sh, so its \"preserve PACKAGER_SOURCEMAP_FILE\" patch no-ops and posthog-cli can't find the minified map\n\n**Repo:** PostHog/posthog-js-lite (`posthog-react-native`)\n\n## Summary\n\nOn iOS Release builds (Hermes, React Native ≥ 0.81, Expo config plugin), the\n\"Bundle React Native code and images\" phase fails with:\n\n```\nERROR posthog_cli::commands: msg=\"Oops! Failed to load minified map at\n'.../Build/.../BuildProductsPath/Release-iphoneos/main.jsbundle.map':\nNo such file or directory (os error 2)\"\n```\n\n`tooling/posthog-xcode.sh` already knows RN deletes the intermediate packager\nsourcemap and tries to patch `react-native-xcode.sh` to keep it — but the patch\ntargets the wrong variable, so it never runs. RN deletes the packager map, and\n`posthog-cli hermes clone --minified-map-path $SOURCEMAP_PACKAGER_FILE` then\naborts the whole build.\n\n## Environment\n\n| | |\n|---|---|\n| posthog-react-native | 4.45.5 (also r…",
      "createdAt": "2026-05-27T18:58:14Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 13%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "2fa00964211a750decaf010b2c1a48aa0b1b44a54bf1a6175bb6e53197743a1d",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/f231e699c3a6d041595b7c1dfb289d34",
    "gistId": "f231e699c3a6d041595b7c1dfb289d34",
    "files": [
      {
        "name": "call-1-1-2026-07-28T02-33-48-270Z_019fa692-37ed-769f-8694-7cee38e70ed0.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/f231e699c3a6d041595b7c1dfb289d34/raw/cad83cbc692441521d7f801470ff71f7af3b1722/call-1-1-2026-07-28T02-33-48-270Z_019fa692-37ed-769f-8694-7cee38e70ed0.jsonl"
      }
    ]
  },
  "runId": 30323418200,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/30323418200"
}
```
