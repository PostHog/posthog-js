# issue 3928: React Native: align app lifecycle events with sdk-specs canonical behavior

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3928
- Conclusion: Valid React Native spec-parity enhancement; current code matches the reported behavior gap.
- Labels: enhancement, react-native, feature/mobile
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28736108285
- Updated: 2026-07-05T09:28:09.383Z

```json
{
  "triage": {
    "conclusion": "Valid React Native spec-parity enhancement; current code matches the reported behavior gap.",
    "summary": "React Native lifecycle capture currently emits `Application Became Active` on foregrounding and duplicate `Application Backgrounded` events, rather than canonical `Application Opened` with `from_background: true` and lifecycle `version`/`build` props.",
    "issueType": "feature",
    "confidence": 0.88,
    "labels": [
      "enhancement",
      "react-native",
      "feature/mobile"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "I verified the reported behavior in the RN SDK code.\n\nEvidence:\n- `captureAppLifecycleEvents()` captures `Application Opened` only during initialization.\n- Its `AppState` listener captures `Application Became Active` for every `active` transition and `Application Backgrounded` for every `background` transition.\n- There is no foreground-from-background `from_background: true` path and no duplicate-background guard in that lifecycle listener.\n- `$app_version` / `$app_build` are included via common event properties, but the lifecycle event-specific payload does not add canonical `version` / `build` fields.\n\nA focused fix looks feasible, but note that `packages/react-native/CHANGELOG.md` says the current `Application Became Active` behavior was intentionally introduced in 3.1.0, so this should be treated as an intentional spec-parity behavior change rather than an accidental regression.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/posthog-rn.ts",
        "packages/react-native/test/posthog.spec.ts",
        "packages/react-native/src/native-deps.tsx",
        "packages/react-native/CHANGELOG.md"
      ],
      "findings": [
        "`packages/react-native/src/posthog-rn.ts` calls `captureAppLifecycleEvents()` by default unless `captureAppLifecycleEvents` is explicitly `false`.",
        "`captureAppLifecycleEvents()` captures `Application Opened` once after `Linking.getInitialURL()`, then registers an `AppState` listener.",
        "That listener currently captures `Application Became Active` when state is `active` and `Application Backgrounded` whenever state is `background`; it does not track previous lifecycle state.",
        "Because the listener does not track previous state, repeated `background` callbacks would produce repeated `Application Backgrounded` events.",
        "`getCommonEventProperties()` merges `_appProperties`, and `getAppProperties()` populates `$app_build` and `$app_version`, but lifecycle-specific `version` / `build` fields are not added in `captureAppLifecycleEvents()`.",
        "`packages/react-native/test/posthog.spec.ts` has tests asserting the current background/foreground behavior, so tests would need updating for spec parity.",
        "`packages/react-native/CHANGELOG.md` documents that moving foreground-from-background from `Application Opened` with `from_background: true` to `Application Became Active` was intentional in version 3.1.0."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The code path is localized to RN lifecycle capture and existing tests already cover the current behavior, making a focused PR practical. The main caveat is product/backward-compatibility risk because event names/properties would change for existing users.",
      "suggestedApproach": "In `captureAppLifecycleEvents()`, create shared lifecycle props with `version` and `build` from `$app_version` / `$app_build` when available. Track whether the app is currently backgrounded in the AppState listener. On first `background`, capture `Application Backgrounded` with lifecycle props and mark backgrounded. On `active` after background, capture `Application Opened` with `from_background: true` and lifecycle props instead of `Application Became Active`, then clear backgrounded. Update RN lifecycle unit tests to cover foreground-from-background and duplicate-background suppression.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "6ad0b62c0ed8ba05d5ef99402ae3bacf0c58ce42100b0074640b8ddc52371815",
  "piCalls": 3,
  "runId": 28736108285,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28736108285"
}
```
