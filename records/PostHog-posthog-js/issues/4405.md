# issue 4405: @posthog/ai: $ai_http_status is always 500 for Vercel AI SDK provider errors

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4405
- Conclusion: Confirmed bug in the shared AI generation error-status fallback.
- Labels: team/llm-analytics
- PR: https://github.com/PostHog/posthog-js/pull/4406
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/30846052723
- Updated: 2026-08-03T19:32:15.947Z

```json
{
  "triage": {
    "conclusion": "Confirmed bug in the shared AI generation error-status fallback.",
    "summary": "`captureAiGeneration` only reads `error.status`, so Vercel AI SDK `APICallError` values that expose `statusCode` are recorded as HTTP 500.",
    "issueType": "bug",
    "confidence": 0.99,
    "labels": [
      "bug: Something isn't working",
      "team/llm-analytics"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed reproduction — this is confirmed against the current source.\n\n`packages/ai/src/captureAiGeneration.ts` derives an error status only from a numeric `error.status`, then falls back to `500`. Both Vercel `doGenerate` and `doStream` error paths pass the caught error to this shared helper without an explicit `httpStatus`, so an AI SDK `APICallError` with only `statusCode` will take that fallback.\n\nA small additive change to recognize a numeric `statusCode` after preserving the existing `status` precedence should address the confirmed Vercel and direct-helper cases. The existing serialization fixture already uses `statusCode: 502`, but it should additionally assert `$ai_http_status`; a focused shared-helper regression test is sufficient. The LangChain path sets its status independently and is worth evaluating separately, but this report does not need that speculative scope expansion.",
    "investigation": {
      "relevantFiles": [
        "packages/ai/src/captureAiGeneration.ts",
        "packages/ai/src/vercel/middleware.ts",
        "packages/ai/tests/captureAiGeneration.test.ts",
        "packages/ai/src/langchain/callbacks.ts",
        "packages/ai/package.json"
      ],
      "findings": [
        "`captureAiGeneration` initializes `httpStatus` from the caller, then for errors only accepts a numeric `options.error.status`; all other errors are assigned status 500 before the final defaulting logic.",
        "`withTracing` in `packages/ai/src/vercel/middleware.ts` supplies `httpStatus: 200` on successful `doGenerate` and `doStream` captures, but its two catch paths pass only `error`, leaving status derivation to `captureAiGeneration`.",
        "The current shared-helper tests cover `error.status`, no status, and an explicitly supplied status. A later serialization test constructs an error with `statusCode: 502`, but does not assert the generated `$ai_http_status`, so it currently permits the reported regression.",
        "`@posthog/ai` currently declares optional `@ai-sdk/provider` peer support for `^2.0.0 || ^3.0.0`, matching the affected integration range described in the report.",
        "LangChain has a separate implementation that assigns `(output as any).status || 500` directly rather than calling `captureAiGeneration`; no evidence in this report or the inspected code establishes a LangChain `statusCode`-only provider error, so it should not be bundled into this focused fix."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The faulty decision point is a single shared status-extraction branch. Adding a numeric `statusCode` fallback after the established `status` check preserves existing integration behavior and directly covers the Vercel wrapper's error paths.",
      "suggestedApproach": "In `captureAiGeneration`, when an error is present and no explicit `httpStatus` was supplied, keep numeric `error.status` as the first choice, then use numeric `error.statusCode`, otherwise use 500. Add a table-driven `statusCode` case asserting `$ai_http_status` (and optionally retain the existing serialization fixture unchanged).",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3584,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(ai): preserve Error message, stack, and cause chain on $ai_error",
      "url": "https://github.com/PostHog/posthog-js/pull/3584",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`JSON.stringify` on a JavaScript `Error` instance returns `{}` because\n`name`, `message`, `stack`, and `cause` are non-enumerable. The\n`captureAiGeneration` path stored `JSON.stringify(options.error)` directly\non ``, so events captured via `withTracing` ended up with a\nstripped-down blob — no message, no stack, no usable cause chain — making\n`GatewayResponseError` and friends nearly impossible to debug from the\ndashboard.\n\nCloses #3556\n\n## Fix\n\n- New `serializeError` helper in `packages/ai/src/utils.ts` that\n  normalizes `Error` instances to plain objects (`name` / `message` /\n  `stack` / `cause` plus own enumerable properties such as `statusCode`,\n  `response`, `validationError`), recurses through the cause chain and\n  through nested plain objects, and guards against circular cause graphs\n  with a `WeakSet`.\n- `captureAiGeneration` now passes the error through `serializeError`\n  before `JSON.stringify`, so `` keeps the same string shape\n  (backwards compatible with anythin…",
      "createdAt": "2026-05-12T11:46:08Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "0c09817b8b1b08450b396cb2aed76b5e35860d74a10427df6da6a6a8ba633e94",
  "piCalls": 3,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/2dd4cfbd00acdd366fda7564a5ce3238",
    "gistId": "2dd4cfbd00acdd366fda7564a5ce3238",
    "files": [
      {
        "name": "call-1-1-2026-08-03T19-31-07-645Z_019fc91b-c2fd-734e-9f2c-22c6a4024fa6.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/2dd4cfbd00acdd366fda7564a5ce3238/raw/1c9bd3b6544a78fbcad8893ecbdbaeb9c4f99f84/call-1-1-2026-08-03T19-31-07-645Z_019fc91b-c2fd-734e-9f2c-22c6a4024fa6.jsonl"
      },
      {
        "name": "call-2-2-2026-08-03T19-31-37-768Z_019fc91c-38a8-7b2d-9208-c42101c290aa.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/2dd4cfbd00acdd366fda7564a5ce3238/raw/715a67c67e0152b06ba1657755af62cea00beb75/call-2-2-2026-08-03T19-31-37-768Z_019fc91c-38a8-7b2d-9208-c42101c290aa.jsonl"
      },
      {
        "name": "call-3-3-2026-08-03T19-32-05-225Z_019fc91c-a3e9-7aa6-a800-4a2527019f74.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/2dd4cfbd00acdd366fda7564a5ce3238/raw/234b106818119623985a954c6103d56b79fb7ce7/call-3-3-2026-08-03T19-32-05-225Z_019fc91c-a3e9-7aa6-a800-4a2527019f74.jsonl"
      }
    ]
  },
  "runId": 30846052723,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/30846052723"
}
```
