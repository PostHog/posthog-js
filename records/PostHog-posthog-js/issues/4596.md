# issue 4596: @posthog/rollup-plugin 1.4.9+ silently skips Vite 8 client sourcemaps after Oxc strips chunkId comments

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4596
- Conclusion: Confirmed regression in the Rollup plugin’s post-1.4.8 in-memory chunk-ID flow when a downstream minifier removes the injected comment.
- Labels: feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/32482154265
- Updated: 2026-08-21T12:31:42.152Z

```json
{
  "triage": {
    "conclusion": "Confirmed regression in the Rollup plugin’s post-1.4.8 in-memory chunk-ID flow when a downstream minifier removes the injected comment.",
    "summary": "Vite 8/Oxc removes the trailing chunkId comment injected by renderChunk; writeBundle then excludes every minified client chunk and skips sourcemap upload.",
    "issueType": "bug",
    "confidence": 0.95,
    "labels": [
      "bug: Something isn't working",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed reproduction. The current implementation confirms the reported failure mode: `renderChunk` injects both an executable runtime snippet and a trailing `//# chunkId=` comment, while `writeBundle` only selects chunks whose final code still has that comment. The shared detector explicitly refuses to treat the runtime IIFE as proof of injection. Therefore a minifier that preserves executable code but removes comments can make the plugin report `No chunks found` and skip every client upload.\n\nA fix should not simply upload every JavaScript chunk: the existing exclusion protects prebuilt chunks that never passed through `renderChunk`. It also needs to verify the post-minification identifier protocol understood by `posthog-cli`, since the current source documents the trailing comment as the CLI's upload-time chunk-ID marker. A regression test must cover the comment being removed between `renderChunk` and `writeBundle`; a real minified Vite 8 build would provide the strongest coverage.",
    "investigation": {
      "relevantFiles": [
        "packages/rollup-plugin/src/index.ts",
        "packages/rollup-plugin/src/index.spec.ts",
        "packages/plugin-utils/src/chunk-ids.ts",
        "packages/plugin-utils/src/chunk-ids.spec.ts",
        "packages/rollup-plugin/CHANGELOG.md"
      ],
      "findings": [
        "`packages/rollup-plugin/src/index.ts` injects `createChunkIdSnippet(...)` and `createChunkIdComment(...)` in `renderChunk`, but `writeBundle` skips a JavaScript chunk unless `determineChunkIdFromSource(chunk.code)` succeeds.",
        "`packages/plugin-utils/src/chunk-ids.ts` only recognizes a line-anchored `//# chunkId=` comment. Its documentation and test explicitly state that the executable chunk-ID IIFE alone must return `undefined`.",
        "The `writeBundle` empty-selection path emits the same `No chunks found, skipping sourcemap processing` message reported in the issue.",
        "Existing Rollup-plugin tests invoke `renderChunk` and `writeBundle` directly using code that still contains the comment; they do not simulate downstream output minification or run a Vite build.",
        "`packages/rollup-plugin/CHANGELOG.md` records version 1.4.9 / PR #4512 as the release that moved chunk-ID injection into `renderChunk`, matching the reported regression boundary.",
        "The chunk-ID helper documents the comment as the marker posthog-cli discovers at upload time. This means changing only the Rollup-plugin selection condition may be insufficient unless the CLI upload protocol can also derive the ID from the surviving runtime snippet."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The immediate gate is localized, but the removed comment is documented as the CLI upload-time chunk-ID protocol and the current filter intentionally avoids including untouched prebuilt chunks. A safe repair requires verifying or extending the shared plugin/CLI identification protocol, rather than broadly uploading all JavaScript chunks or making an unverified IIFE matcher.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4512,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(rollup): inject chunk ids in-memory in renderChunk for SRI compatibility",
      "url": "https://github.com/PostHog/posthog-js/pull/4512",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\n`@posthog/rollup-plugin` shells out to `posthog-cli sourcemap process` in `writeBundle`, which rewrites the emitted `.js` files **on disk** (prepending the `_posthogChunkIds` IIFE). SRI plugins like `vite-plugin-sri3` compute integrity hashes earlier, in `generateBundle`, from the in-memory chunks — so the served files no longer match their hashes and the browser blocks every script:\r\n\r\n```\r\nFailed to find a valid digest in the 'integrity' attribute for resource '…/assets/index-*.js'\r\n```\r\n\r\nReported by a customer using Vite + `vite-plugin-sri3`; the site broke on every attempt to add the plugin. Plugin ordering cannot fix this — `generateBundle` always completes before any `writeBundle` runs (the previous ordering fix in #2943 only helps plugins that read `chunk.code` in a later `writeBundle`).\r\n\r\n## Fix\r\n\r\nI basically moved part of a CLI into our plugin utils so we are able to inject chunk ID during chunk rendering. Hash computes after that so we are good here.\r\n\r\nRollu…",
      "createdAt": "2026-08-12T18:38:32Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "875f0e8efdfacedb610a4ff314678a1544478504025cff3e5947944ce351d50c",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/984d9d3b71a85ed7c04adea359b6bae7",
    "gistId": "984d9d3b71a85ed7c04adea359b6bae7",
    "files": [
      {
        "name": "call-1-1-2026-08-21T12-30-35-944Z_01a0244d-39a8-7e95-af7b-1ca92f9d912a.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/984d9d3b71a85ed7c04adea359b6bae7/raw/b470c88126b98470cf54cd78ca0da0e3a061a062/call-1-1-2026-08-21T12-30-35-944Z_01a0244d-39a8-7e95-af7b-1ca92f9d912a.jsonl"
      }
    ]
  },
  "runId": 32482154265,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/32482154265"
}
```
