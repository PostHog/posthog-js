# issue 4000: Google mobile rendering bot (Googlebot) bypasses bot detection by using real Android device user agents

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4000
- Conclusion: Valid bot-filtering bug, but a safe fix likely needs server-side IP/Googlebot verification rather than an SDK-only UA blocklist change.
- Labels: web, feature/web-analytics, team/web-analytics
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28316199744
- Updated: 2026-06-28T08:17:16.021Z

```json
{
  "triage": {
    "conclusion": "Valid bot-filtering bug, but a safe fix likely needs server-side IP/Googlebot verification rather than an SDK-only UA blocklist change.",
    "summary": "Google mobile rendering traffic can use a normal Android device user agent from Google-owned IP ranges, so PostHog's current user-agent-based bot detection does not filter it and it inflates web analytics pageviews.",
    "issueType": "bug",
    "confidence": 0.84,
    "labels": [
      "bug: Something isn't working",
      "web",
      "feature/web-analytics",
      "team/web-analytics"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. I checked the SDK bot-detection path and the current default detection is user-agent substring based, plus `navigator.webdriver` / `userAgentData.brands`. The provided Android UA has no stable bot-identifying substring, so it would not be caught today.\n\nI would avoid adding a broad device-model substring like `moto g power` to the default SDK blocklist because that could block real users on that device. Since browser-side JS cannot see the source IP, the safer fix is probably in server-side ingestion / `$virt_is_bot` classification using Google's documented verification method or maintained Google IP ranges. As a temporary customer-side workaround, `custom_blocked_useragents` could target the exact observed UA, but that carries false-positive risk.",
    "investigation": {
      "relevantFiles": [
        "packages/core/src/utils/bot-detection.ts",
        "packages/browser/src/utils/blocked-uas.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/__tests__/utils.test.ts",
        "packages/node/src/client.ts",
        "packages/types/src/posthog-config.ts"
      ],
      "findings": [
        "`packages/core/src/utils/bot-detection.ts` defines `DEFAULT_BLOCKED_UA_STRS` and `isBlockedUA`; matching is a case-insensitive substring check against known bot strings such as `googlebot`, `adsbot-google`, `googleother`, etc.",
        "`packages/browser/src/utils/blocked-uas.ts` reuses the shared UA list and `isLikelyBot` only checks `navigator.userAgent`, `navigator.userAgentData.brands`, and `navigator.webdriver`; it has no access to source IP or ASN.",
        "`packages/browser/src/posthog-core.ts` drops bot events or routes pageviews to `$bot_pageview` based on `_is_bot()`, which delegates to `isLikelyBot`, so a real-looking Android UA will be treated as browser traffic.",
        "`packages/node/src/client.ts` has similar preview bot-pageview handling based only on `$raw_user_agent` via `isBlockedUA`; no IP-based detection is present in the SDK code inspected.",
        "`packages/types/src/posthog-config.ts` documents `custom_blocked_useragents`, which can extend the list, but using a consumer device model as a default blocked UA would risk false positives."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small SDK PR adding `moto g power` or a similar Android-device substring would likely create false positives for real users, while the reliable signal described in the issue is source IP/Google ownership, which browser-side posthog-js cannot observe. The appropriate fix likely belongs in backend/web-analytics bot classification rather than this SDK blocklist.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "15cee0e64a1ce8a943b62bd4519b6a7672816aeea7895a8e15f7b271693c8f8b",
  "piCalls": 1,
  "runId": 28316199744,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28316199744"
}
```
