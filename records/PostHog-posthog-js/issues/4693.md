# issue 4693: recordCrossOriginIframes: child contributes no events when the iframe redirects through the parent's origin (LTI/OIDC)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4693
- Conclusion: Confirmed session-replay bug in cross-origin iframe relay after an iframe temporarily becomes same-origin during navigation.
- Labels: feature/replay, javascript, team/client-libraries, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/33336937412
- Updated: 2026-08-30T21:39:07.479Z

```json
{
  "triage": {
    "conclusion": "Confirmed session-replay bug in cross-origin iframe relay after an iframe temporarily becomes same-origin during navigation.",
    "summary": "A cross-origin child iframe that redirects through the parent origin loses its parent-side relay mapping when the intermediate same-origin document unloads, so the final child document's relayed snapshots are ignored.",
    "issueType": "bug",
    "confidence": 0.93,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "javascript",
      "team/client-libraries",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed reproduction and event-level evidence. This appears to be a real recorder bug, distinct from the full-snapshot reattachment issue you ruled out.\n\nThe likely failure path is:\n\n1. During the parent full snapshot, rrweb registers the iframe's `contentWindow` in `crossOriginIframeMap`, which is how later `postMessage` relay events are associated with the iframe element.\n2. While the iframe is briefly same-origin, rrweb serializes and attaches that intermediate document. `IframeManager.attachIframe()` installs a `pagehide` handler for it.\n3. On navigation away from that intermediate page, the handler deletes the iframe's `contentWindow` mapping.\n4. The final child page is cross-origin again, so its recorder correctly chooses the relay path and posts its full snapshot to the parent. However, the parent can no longer find the iframe for that message and silently drops it.\n\nThe iframe element remains in the parent DOM, and its browsing-context `WindowProxy` is retained across navigation, so removing the relay mapping on the intermediate document's `pagehide` is inconsistent with the existing cross-origin relay mechanism. Removal of the iframe element itself already has separate cleanup in `removeIframeById()`.\n\nA fix should include a browser regression test for cross-origin → parent-origin → cross-origin navigation and assert that the final child full snapshot becomes an `isAttachIframe` mutation in the parent stream. Until then, disabling relay in the child remains a reasonable workaround.",
    "investigation": {
      "relevantFiles": [
        "packages/rrweb/rrweb/src/record/iframe-manager.ts",
        "packages/rrweb/rrweb/src/record/index.ts",
        "packages/rrweb/rrweb-snapshot/src/snapshot.ts",
        "packages/rrweb/rrweb/test/record/cross-origin-iframes.test.ts",
        "packages/browser/src/entrypoints/lazy-recorder.ts",
        "packages/browser/src/entrypoints/posthog-recorder.ts"
      ],
      "findings": [
        "`record()` sets `passEmitsToParent` when an iframe cannot access `window.parent.document`; `wrappedEmit()` then sends recorder events to the parent with `window.parent.postMessage(...)` in `packages/rrweb/rrweb/src/record/index.ts`.",
        "During serialization, `IframeManager.addIframe()` stores `iframeEl.contentWindow` in `crossOriginIframeMap`. `handleMessage()` depends on this map to associate a relayed event's `message.source` with the iframe; if no mapping is found, it returns without emitting the event.",
        "When a same-origin iframe document is attached, `IframeManager.attachIframe()` registers a `pagehide` handler. That handler removes the intermediate document from the mirror and calls `crossOriginIframeMap.delete(iframeEl.contentWindow)`.",
        "The iframe is only re-added to `crossOriginIframeMap` when the iframe element is serialized via `addIframe()`. A later cross-origin navigation cannot be serialized through the same-origin iframe path, so it does not restore the mapping before the final child relays its full snapshot.",
        "`removeIframeById()` already removes the window mapping when the iframe element is actually detached, making that the appropriate lifecycle point for disposal rather than an intermediate document navigation.",
        "Existing `cross-origin-iframes.test.ts` covers initial cross-origin relay, same-origin iframe navigation, and full-snapshot reattachment, but does not cover one iframe navigating cross-origin → same-origin → cross-origin."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The loss is localized to the pagehide cleanup of a relay mapping that remains necessary for the iframe element after navigation. The public API and cross-bundle contract need not change, but this is recorder serialization code, so a real-browser regression test is required to prevent silent replay corruption.",
      "suggestedApproach": "Retain `crossOriginIframeMap` association across pagehide/navigation for an iframe that remains attached, while preserving cleanup in `removeIframeById()`. Add a Puppeteer/Playwright regression that hosts parent and child on distinct origins, navigates one iframe through a parent-origin intermediate document, starts the final child recorder with `recordCrossOriginIframes: true`, and verifies its relayed full snapshot is emitted by the parent as an `isAttachIframe` mutation.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3073,
      "type": "issue",
      "state": "closed",
      "title": "Session replay not working with cookieless_mode on_reject and recordCrossOriginIframes is not showin",
      "url": "https://github.com/PostHog/posthog-js/issues/3073",
      "labels": [
        "bug",
        "feature/replay",
        "web"
      ],
      "bodyExcerpt": "## Bug description\n\nSession replays with recorded on iframes are not being recorded if the page embedded as an iframe has `cookieless_mode: \"on_reject\",`\n\n## How to reproduce\n\n1. Create a React appliacation and add Posthog\n2. Create another React appliacation and add Posthog\n3. Embed second application on the first one as iframe\n4. Add the following Posthog configuration:\n```\n{\n\n\t\t\t\tdefaults: \"2026-01-30\",\n\t\t\t\tcapture_exceptions: true,\n\t\t\t\tcookieless_mode: \"on_reject\",\n\t\t\t\tsession_recording: {\n\t\t\t\t\tmaskAllInputs: true,\n\t\t\t\t\tmaskTextSelector: \"*\",\n\t\t\t\t\trecordCrossOriginIframes: true,\n\t\t\t\t},\n}\n```\n\n## Related sub-libraries\n\n- [ ] All of them\n- [ ] posthog-js (web)\n- [ ] posthog-js-lite (web lite)\n- [ ] posthog-node\n- [ ] posthog-react-native\n- [x] @posthog/react\n- [ ] @posthog/ai\n- [ ] @posthog/nextjs-config\n- [ ] @posthog/nuxt\n- [ ] @posthog/rollup-plugin\n- [ ] @posthog/webpack-plugin\n\n## Additional context\n\n#### _Thank you_ for your bug report – we love squashing them!\n",
      "createdAt": "2026-02-11T09:13:05Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.06324110671936758,
    "canonical": {
      "number": 3073,
      "type": "issue",
      "state": "closed",
      "title": "Session replay not working with cookieless_mode on_reject and recordCrossOriginIframes is not showin",
      "url": "https://github.com/PostHog/posthog-js/issues/3073",
      "labels": [
        "bug",
        "feature/replay",
        "web"
      ],
      "bodyExcerpt": "## Bug description\n\nSession replays with recorded on iframes are not being recorded if the page embedded as an iframe has `cookieless_mode: \"on_reject\",`\n\n## How to reproduce\n\n1. Create a React appliacation and add Posthog\n2. Create another React appliacation and add Posthog\n3. Embed second application on the first one as iframe\n4. Add the following Posthog configuration:\n```\n{\n\n\t\t\t\tdefaults: \"2026-01-30\",\n\t\t\t\tcapture_exceptions: true,\n\t\t\t\tcookieless_mode: \"on_reject\",\n\t\t\t\tsession_recording: {\n\t\t\t\t\tmaskAllInputs: true,\n\t\t\t\t\tmaskTextSelector: \"*\",\n\t\t\t\t\trecordCrossOriginIframes: true,\n\t\t\t\t},\n}\n```\n\n## Related sub-libraries\n\n- [ ] All of them\n- [ ] posthog-js (web)\n- [ ] posthog-js-lite (web lite)\n- [ ] posthog-node\n- [ ] posthog-react-native\n- [x] @posthog/react\n- [ ] @posthog/ai\n- [ ] @posthog/nextjs-config\n- [ ] @posthog/nuxt\n- [ ] @posthog/rollup-plugin\n- [ ] @posthog/webpack-plugin\n\n## Additional context\n\n#### _Thank you_ for your bug report – we love squashing them!\n",
      "createdAt": "2026-02-11T09:13:05Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 6%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "843aa031791228ba1a64200e23d7a89111cf9033a3e92643b55a7abb2c91a72b",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/050166a99d8735c7f1f8508c5d230570",
    "gistId": "050166a99d8735c7f1f8508c5d230570",
    "files": [
      {
        "name": "call-1-1-2026-08-30T21-38-11-027Z_01a0549b-c9d2-7311-b82d-1b225b7b61a1.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/050166a99d8735c7f1f8508c5d230570/raw/66559f2242e8991f9fa2178e608b9ecb1effedab/call-1-1-2026-08-30T21-38-11-027Z_01a0549b-c9d2-7311-b82d-1b225b7b61a1.jsonl"
      }
    ]
  },
  "runId": 33336937412,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/33336937412"
}
```
