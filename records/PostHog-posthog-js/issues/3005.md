# issue 3005: Nuxt module fails to upload source map when ssr is false

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3005
- Conclusion: Valid Nuxt sourcemap upload bug in client-only builds
- Labels: nuxt, feature/error-tracking, team/error-tracking
- PR: https://github.com/PostHog/posthog-js/pull/3997
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28284865427
- Updated: 2026-06-27T09:18:58.277Z

```json
{
  "triage": {
    "conclusion": "Valid Nuxt sourcemap upload bug in client-only builds",
    "summary": "@posthog/nuxt unconditionally injects server sourcemaps before uploading build assets; with Nuxt `ssr: false`, the server output directory/source maps may not exist, so the CLI fails and upload is skipped.",
    "issueType": "bug",
    "confidence": 0.88,
    "labels": [
      "bug: Something isn't working",
      "nuxt",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. The current Nuxt module does appear to assume server sourcemaps exist when sourcemap upload is enabled. In `packages/nuxt/src/module.ts`, the `close` hook injects server sourcemaps before running the upload step, so a missing/empty server output in `ssr: false` builds can prevent the final upload.\n\nA small PR should be appropriate here: skip only the server sourcemap injection when Nuxt SSR is disabled or no server output is available, while still uploading the build output so client/public sourcemaps are processed.",
    "investigation": {
      "relevantFiles": [
        "packages/nuxt/src/module.ts",
        "packages/nuxt/tests/vue-plugin.test.mjs"
      ],
      "findings": [
        "`packages/nuxt/src/module.ts` enables sourcemap processing when `options.sourcemaps.enabled` is true and not in dev mode.",
        "`nitro:init` stores `publicDir`, `serverDir`, and `outputDir`; `nitro:build:public-assets` injects public sourcemaps.",
        "The `close` hook currently returns unless `serverDir` and `outputDir` are set, then unconditionally runs `cliRunner(getInjectArgs(serverDir, sourcemapsConfig))` before `cliRunner(getUploadArgs(outputDir, sourcemapsConfig))`.",
        "Because server injection and upload are in the same `try` block, a CLI failure for a missing or empty server directory prevents the upload step from running.",
        "The Nuxt package tests currently inspected only cover the Vue plugin behavior; there does not appear to be a sourcemap/module hook regression test in `packages/nuxt/tests`."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The failure is localized to one Nuxt module hook and the report includes clear reproduction steps. A small conditional change can avoid invoking server sourcemap injection for client-only builds without changing normal SSR behavior.",
      "suggestedApproach": "In `packages/nuxt/src/module.ts`, change the `close` hook so it only requires `outputDir` for the upload step. Run server sourcemap injection only when SSR is enabled and `serverDir` is available, then always run `getUploadArgs(outputDir, sourcemapsConfig)` for build uploads. Add a focused module-hook test if feasible.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "6a8ffe9ec83f92fd98c6fcec975d45972ec7c2b4c26d041a45c6ed7e486a120c",
  "piCalls": 3,
  "runId": 28284865427,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28284865427"
}
```
