# issue 5020: Browser opt_out_capturing still permits pending batch and failed-request retry dispatch (1.396.6 / 1.434.0)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/5020
- Conclusion: Confirmed browser SDK bug: `opt_out_capturing()` blocks new capture admission but does not discard already queued events or stop retry scheduling after the opt-out boundary.
- Labels: javascript, web, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/35339369962
- Updated: 2026-09-18T11:25:27.719Z

```json
{
  "triage": {
    "conclusion": "Confirmed browser SDK bug: `opt_out_capturing()` blocks new capture admission but does not discard already queued events or stop retry scheduling after the opt-out boundary.",
    "summary": "Pending batched events can flush and failed event requests can retry after explicit opt-out, despite `has_opted_out_capturing()` being true. The report provides a detailed local reproduction for both paths.",
    "issueType": "bug",
    "confidence": 0.96,
    "labels": [
      "bug: Something isn't working",
      "javascript",
      "web",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed local-only reproduction. The current browser implementation supports the reported mechanism:\n\n- `opt_out_capturing()` updates consent and persistence, but does not clear the normal request or retry queues.\n- `RequestQueue` retains its pending timer and queued requests, then dispatches them through the retry path.\n- `RetryQueue` schedules retries after a failed response without re-checking whether capturing is still enabled, so a response arriving after opt-out can enqueue a later retry.\n- `before_send` runs during event construction, rather than at later queue/retry dispatch, which explains why it is not invoked again for the retry.\n\n`shutdown()` is not a cancellation API: it explicitly unloads both queues using `sendBeacon`, so it is not an appropriate workaround. There is no public queue-discard/cancel API exposed by the current browser SDK surface.\n\nAssuming the intended contract is that ordinary non-cookieless `opt_out_capturing()` forms a strict boundary for future request starts (while not attempting to abort an already in-flight browser request), this should be fixed internally without adding a public API. The regression coverage should specifically prove that a pending batch is dropped and that a pre-opt-out request which fails after opt-out cannot enqueue or start a retry. Cookieless `on_reject` behavior must remain unchanged because it intentionally continues capturing in cookieless mode after rejection.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/request-queue.ts",
        "packages/browser/src/retry-queue.ts",
        "packages/browser/src/request-dispatch.ts",
        "packages/browser/src/__tests__/consent.test.ts",
        "packages/browser/src/__tests__/request-queue.test.ts",
        "packages/browser/src/__tests__/retry-queue.test.ts",
        "packages/types/src/posthog.ts"
      ],
      "findings": [
        "`PostHog.opt_out_capturing()` in `packages/browser/src/posthog-core.ts` records denied consent and synchronizes persistence, but does not clear `_requestQueue` or `_retryQueue` for the ordinary browser mode.",
        "`RequestQueue.enqueue()` retains queued requests and schedules a flush timer; its timer callback formats and sends the queue without consulting consent or `is_capturing()`.",
        "`RetryQueue.retriableRequest()` enqueues retryable failed responses and its polling flush re-dispatches them without checking the instance's current capture state.",
        "`capture()` checks `is_capturing()` before constructing an event and invokes `before_send` at that time. It then places eligible requests in the request/retry paths, so later retries reuse the accepted payload and do not rerun `before_send`.",
        "`shutdown()` and page-unload handling call `RequestQueue.unload()` and `RetryQueue.unload()`, which intentionally send retained work with `sendBeacon`; this confirms shutdown is flush-oriented rather than cancellation-oriented.",
        "The public `PostHog` interface exposes opt-in/out and consent-status methods but no public request-queue discard or cancellation method.",
        "Existing opt-out tests verify that events captured after opt-out are not sent, but do not cover an event already pending in a batch or a failed in-flight request whose failure callback occurs after opt-out."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The defect is localized to the browser request and retry lifecycle and can be addressed without a new public API. Care is required because `cookieless_mode: 'on_reject'` deliberately continues capturing after an opt-out decision, and in-flight requests cannot reliably be recalled.",
      "suggestedApproach": "Add internal non-sending discard/reset operations for `RequestQueue` and `RetryQueue`, invoke them from ordinary non-cookieless opt-out, and gate retry enqueue/dispatch on the current capture state so a failure callback that runs after opt-out cannot recreate retry work. Add focused regression tests for a queued batch and for a delayed failed response after opt-out; retain the existing cookieless-on-reject behavior.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "90de21f92b738f43d2b5168947b6577df2463d61d84b9eb8599e5175664e1047",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/df890e906ddedace8e0b333533ef7ce7",
    "gistId": "df890e906ddedace8e0b333533ef7ce7",
    "files": [
      {
        "name": "call-1-1-2026-09-18T11-24-04-402Z_01a0b442-61b2-7474-b74b-fc29b045da43.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/df890e906ddedace8e0b333533ef7ce7/raw/ec8aefe1ebc95fca6f489ec59e5955f0784fb006/call-1-1-2026-09-18T11-24-04-402Z_01a0b442-61b2-7474-b74b-fc29b045da43.jsonl"
      }
    ]
  },
  "runId": 35339369962,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/35339369962"
}
```
