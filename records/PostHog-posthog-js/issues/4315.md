# issue 4315: Dead click autocapture: false positives — detector's MutationObserver sees no mutations while rrweb records instant DOM updates

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4315
- Conclusion: Confirmed likely dead-click false-positive bug: the dead-click observer uses the same detached-iframe MutationObserver fallback that rrweb has already identified as unsafe on WebKit.
- Labels: feature/dead-clicks
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/30488647853
- Updated: 2026-07-29T20:31:20.386Z

```json
{
  "triage": {
    "conclusion": "Confirmed likely dead-click false-positive bug: the dead-click observer uses the same detached-iframe MutationObserver fallback that rrweb has already identified as unsafe on WebKit.",
    "summary": "Dead-click detection can stop receiving DOM-mutation callbacks after selecting an untainted MutationObserver constructor from an iframe that is immediately detached, causing responsive controls to be reported as $dead_click.",
    "issueType": "bug",
    "confidence": 0.88,
    "labels": [
      "bug: Something isn't working",
      "feature/dead-clicks"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed event and replay correlation. The reported signature matches a concrete issue in the current implementation: dead-click autocapture obtains its observer through `getNativeMutationObserverImplementation`, whose iframe fallback removes the iframe before using its `MutationObserver` constructor.\n\nThis is particularly credible for Mobile Safari and Chrome on iOS: the rrweb utilities in this repository already carry a WebKit-specific fix for this exact lifecycle problem (`webkit.org/b/179224`), keeping the fallback iframe attached on Safari because a detached iframe can cause MutationObserver callbacks to be silently dropped. That rrweb fix does not cover the separate browser-common helper used by dead-click detection.\n\nA narrow fix should mirror the established rrweb behavior in the browser-common fallback, with regression coverage for the forced fallback path. The Safari/iOS portion is well explained by this; the Brave and Edge reports may be the same patched-global fallback path, but should be verified separately rather than assumed.\n\nAs a temporary mitigation for the affected controls, `.ph-no-deadclick` or `capture_dead_clicks.css_selector_ignorelist` can exclude those elements from dead-click detection without disabling replay or general autocapture.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/entrypoints/dead-clicks-autocapture.ts",
        "packages/browser-common/src/utils/prototype-utils.ts",
        "packages/rrweb/utils/src/index.ts",
        "packages/rrweb/rrweb/test/untainted-prototype.test.ts",
        "packages/rrweb/utils/CHANGELOG.md",
        "packages/browser/src/__tests__/entrypoints/lazy-loaded-dead-clicks-autocapture.test.ts",
        "packages/browser/playwright/mocked/dead-clicks.spec.ts"
      ],
      "findings": [
        "Dead-click autocapture creates its observer with `getNativeMutationObserverImplementation(assignableWindow)` and updates `_lastMutation` only when that observer callback runs. If callbacks cease, the timeout logic can emit `$dead_click` with no post-click mutation, matching the report's timestamp pattern.",
        "`packages/browser-common/src/utils/prototype-utils.ts` falls back when MutationObserver does not appear native or Angular Zone is present: it creates an iframe, takes `contentWindow.MutationObserver`, removes the iframe, and caches a bound constructor.",
        "The fallback helper is the only browser-common usage path found for this native MutationObserver helper, so a lifecycle correction is localized to the dead-click observer's dependency.",
        "`packages/rrweb/utils/src/index.ts` explicitly documents that WebKit tears down a detached iframe's ScriptExecutionContext and MutationObserver callbacks can silently stop as a result. Its Safari path retains, hides, and blocks the iframe from recording.",
        "`packages/rrweb/utils/CHANGELOG.md` records this as PR #4128, but the browser-common helper used by dead clicks still removes its fallback iframe; therefore the existing rrweb fix does not resolve this issue.",
        "Existing dead-click unit coverage has a skipped mutation-observer test due to jsdom delivery behavior, and the Playwright test notes that the last-mutation assertion flakes. There is no focused regression test for a retained fallback iframe or a forced patched-global fallback."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The unsafe detached-iframe pattern is directly present in the dead-click dependency, and the repository already contains a narrowly scoped, documented Safari solution for the same WebKit behavior in rrweb. The implementation should remain limited to this fallback lifecycle rather than changing dead-click timing heuristics.",
      "suggestedApproach": "Update `getNativeImplementation`'s MutationObserver iframe fallback to retain the hidden iframe on Safari/WebKit when it supplies the constructor, mark it with `ph-no-capture` and `rr-block` so it cannot appear in autocapture or replay, and retain the existing cleanup behavior elsewhere. Add focused forced-fallback tests for retained-versus-removed iframe behavior and observer usability; use a browser-level WebKit test if callback delivery cannot be verified in jsdom.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "f24f9cfb035dca5388674386f409c59e35953df64d2d49d0c5a87f29a3f6ea18",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/a04622328d1287b48a52ba1a9375830e",
    "gistId": "a04622328d1287b48a52ba1a9375830e",
    "files": [
      {
        "name": "call-1-1-2026-07-29T20-30-07-563Z_019faf91-facb-7f4c-9401-dacca214dc12.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/a04622328d1287b48a52ba1a9375830e/raw/e3477329049f00c931619a42890e4dcdd0fde1ed/call-1-1-2026-07-29T20-30-07-563Z_019faf91-facb-7f4c-9401-dacca214dc12.jsonl"
      }
    ]
  },
  "runId": 30488647853,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/30488647853"
}
```
