# issue 4046: @posthog/convex: make feature flag refresh opt-in instead of running on every install

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4046
- Conclusion: Valid feature request; current code confirms the Convex flag refresh loop is not gated before scheduling.
- Labels: enhancement, feature, feature/flags, team/feature-flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28521543603
- Updated: 2026-07-01T13:38:01.970Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; current code confirms the Convex flag refresh loop is not gated before scheduling.",
    "summary": "@posthog/convex starts and maintains the local feature-flag refresh loop even when local evaluation cannot run because POSTHOG_PERSONAL_API_KEY is missing, causing scheduled mutations/actions to execute and then skip.",
    "issueType": "feature",
    "confidence": 0.93,
    "labels": [
      "enhancement",
      "feature",
      "feature/flags",
      "team/feature-flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. I checked the current Convex component code and the described behavior matches the implementation:\n\n- `packages/convex/src/component/crons.ts` always registers the 5-minute supervisor cron.\n- `ensureRefreshLoop` schedules `refreshLoop` without checking whether local flag refresh is configured.\n- `refreshLoop` always schedules `refreshFlagDefinitions` and its successor tick.\n- The only runtime missing-key gate is inside `refreshFlagDefinitions`, after the scheduled mutation/action work has already happened.\n\nA low-surface fix looks feasible: treat a configured `POSTHOG_PERSONAL_API_KEY` as the existing opt-in for local evaluation, add a runtime helper for that check, and no-op early in both `ensureRefreshLoop` and `refreshLoop` when it is absent. That would preserve current behavior for installs that have configured local evaluation while stopping the per-minute skipped work for event-only / remote-flag users.",
    "investigation": {
      "relevantFiles": [
        "packages/convex/src/component/crons.ts",
        "packages/convex/src/component/lib.ts",
        "packages/convex/src/component/convex.config.ts",
        "packages/convex/src/component/refresh-loop.test.ts",
        "packages/convex/src/component/crons.test.ts"
      ],
      "findings": [
        "crons.ts registers `Ensure PostHog flag refresh loop is running` unconditionally every 5 minutes.",
        "lib.ts `ensureRefreshLoop` checks only the recorded scheduled job state, then schedules `internal.lib.refreshLoop`; it does not check `POSTHOG_PERSONAL_API_KEY` or another opt-in.",
        "lib.ts `refreshLoop` always schedules `api.lib.refreshFlagDefinitions` immediately and schedules the next `internal.lib.refreshLoop` using `readPollingIntervalSeconds()`.",
        "lib.ts `refreshFlagDefinitions` is where missing `projectToken` or `personalApiKey` returns `{ status: 'skipped', reason: 'missing-keys' }`, so the scheduled function/action invocation has already occurred.",
        "convex.config.ts comments already describe `POSTHOG_PERSONAL_API_KEY` as gating local evaluation refresh, but the current runtime scheduling code does not enforce that before starting/rescheduling the loop.",
        "Existing tests cover unconditional cron registration and loop scheduling with a personal API key configured; they do not assert a no-op path when the key is missing."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "A small PR is appropriate because the issue is localized to the Convex component refresh-loop scheduling code and can be addressed with runtime checks plus focused tests, without changing remote flag evaluation or event capture paths.",
      "suggestedApproach": "Add a small runtime helper in `packages/convex/src/component/lib.ts` that returns whether local flag refresh is enabled, likely by checking trimmed `env.POSTHOG_PERSONAL_API_KEY` as the existing local-eval opt-in. Call it at the top of `ensureRefreshLoop` before DB/scheduler work and at the top of `refreshLoop` before scheduling `refreshFlagDefinitions` or a successor tick. Update refresh-loop tests to assert missing PAK causes `ensureRefreshLoop` and direct `refreshLoop` invocations to schedule nothing, while PAK-present behavior remains unchanged. Adjust misleading comments if needed.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3957,
      "type": "issue",
      "state": "closed",
      "title": "@posthog/convex - POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS setting is not respected",
      "url": "https://github.com/PostHog/posthog-js/issues/3957",
      "labels": [
        "bug",
        "feature/flags",
        "team/feature-flags"
      ],
      "bodyExcerpt": "## Bug description\n\nPOSTHOG_FLAGS_POLLING_INTERVAL_SECONDS has no effect on the feature-flag refresh cron — the schedule is always registered at the 60s default, regardless of what the env var is set to.\n\nRoot cause is a lifecycle mismatch in component/crons.ts. The interval is computed by a top-level call at module-import time:\n\ncrons.interval(\n  'Refresh PostHog feature flag definitions',\n  { seconds: readPollingIntervalSeconds() },   // runs during the deploy-time analyze pass\n  api.lib.refreshFlagDefinitions,\n  {}\n)\n\nreadPollingIntervalSeconds() reads process.env.POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS. But as the very comment above this call notes, Convex forwards component env vars only at runtime, not during the deploy-time module analysis where crons are registered. So at the moment the interval is evaluated, the env var is empty, readPollingIntervalSeconds() falls back to DEFAULT_INTERVAL_SECONDS = 60, and that 60 is baked into the registered schedule. The configured value only…",
      "createdAt": "2026-06-24T18:56:46Z",
      "reason": "explicit-reference"
    },
    {
      "number": 4021,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(convex): honour POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS",
      "url": "https://github.com/PostHog/posthog-js/pull/4021",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n`POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS` was a dead knob for `@posthog/convex` users (#3957). The flag-refresh cron read the interval at **registration time**, but Convex forwards component env vars only at **runtime** — so the value was always empty during deploy-time module analysis, `readPollingIntervalSeconds()` fell back to `DEFAULT_INTERVAL_SECONDS = 60`, and `60` was baked into the schedule. No way to change the cadence from an installing app, and no warning either (the value was absent, not malformed, so the existing validation warning never fired). This is the same runtime-only-forwarding constraint that bit the `POSTHOG_PERSONAL_API_KEY` gate in #3683 — but a cron's period can't be deferred to runtime the way that gate was.\n\n## Changes\n\nReplaced the fixed-interval cron with a **self-rescheduling loop** that reads the interval where the forwarded value is actually visible — at runtime.\n\n- `refreshLoop` (internal mutation): one tick kicks a refresh now (`runAfter(0,…",
      "createdAt": "2026-06-30T06:59:17Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.08902077151335312,
    "canonical": {
      "number": 3957,
      "type": "issue",
      "state": "closed",
      "title": "@posthog/convex - POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS setting is not respected",
      "url": "https://github.com/PostHog/posthog-js/issues/3957",
      "labels": [
        "bug",
        "feature/flags",
        "team/feature-flags"
      ],
      "bodyExcerpt": "## Bug description\n\nPOSTHOG_FLAGS_POLLING_INTERVAL_SECONDS has no effect on the feature-flag refresh cron — the schedule is always registered at the 60s default, regardless of what the env var is set to.\n\nRoot cause is a lifecycle mismatch in component/crons.ts. The interval is computed by a top-level call at module-import time:\n\ncrons.interval(\n  'Refresh PostHog feature flag definitions',\n  { seconds: readPollingIntervalSeconds() },   // runs during the deploy-time analyze pass\n  api.lib.refreshFlagDefinitions,\n  {}\n)\n\nreadPollingIntervalSeconds() reads process.env.POSTHOG_FLAGS_POLLING_INTERVAL_SECONDS. But as the very comment above this call notes, Convex forwards component env vars only at runtime, not during the deploy-time module analysis where crons are registered. So at the moment the interval is evaluated, the env var is empty, readPollingIntervalSeconds() falls back to DEFAULT_INTERVAL_SECONDS = 60, and that 60 is baked into the registered schedule. The configured value only…",
      "createdAt": "2026-06-24T18:56:46Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 9%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "5bbffa332363998230365145e257a8041d5608dd950d57f29ab72efa2f9c9a68",
  "piCalls": 1,
  "runId": 28521543603,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28521543603"
}
```
