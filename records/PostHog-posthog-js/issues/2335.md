# issue 2335: Remote configs should be locally evaluated and cached like feature flags

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2335
- Conclusion: Valid feature request; current Node local-evaluation cache does not cover remote config payloads.
- Labels: enhancement, feature/flags, team/feature-flags, node
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28507814375
- Updated: 2026-07-01T09:33:43.968Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; current Node local-evaluation cache does not cover remote config payloads.",
    "summary": "Remote config payloads are fetched per key via a separate authenticated endpoint instead of being loaded and cached by the feature flag definitions poller/local evaluation path.",
    "issueType": "feature",
    "confidence": 0.88,
    "labels": [
      "enhancement",
      "feature/flags",
      "team/feature-flags",
      "node"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I confirmed the current Node SDK path treats remote config payloads separately from feature flag local evaluation: `getRemoteConfigPayload()` calls the per-flag remote config endpoint directly, while the local-evaluation poller only loads/caches flag definitions, group mappings, and cohorts.\n\nThis looks like a valid feature request rather than a missing reproduction. The main design question is likely the backend/API contract: whether remote config payloads should be included in `/flags/definitions`, exposed through a separate batched/polled endpoint, and whether SDK APIs like `getAllFlags*` should include them or whether only `getRemoteConfigPayload()` should become cache-backed.",
    "investigation": {
      "relevantFiles": [
        "packages/node/src/client.ts",
        "packages/node/src/types.ts",
        "packages/node/src/extensions/feature-flags/feature-flags.ts",
        "packages/node/src/extensions/feature-flags/cache.ts",
        "packages/node/src/__tests__/posthog-node.spec.ts"
      ],
      "findings": [
        "`packages/node/src/client.ts` implements `getRemoteConfigPayload(flagKey)` by requiring `personalApiKey`, calling `_requestRemoteConfigPayload(flagKey)`, then parsing the response. There is no local poller/cache lookup in this method.",
        "`_requestRemoteConfigPayload()` in `packages/node/src/client.ts` performs a GET to `/api/projects/@current/feature_flags/{flagKey}/remote_config?token=...` with the personal API key bearer token, so each uncached caller-level lookup can become a network request.",
        "`packages/node/src/__tests__/posthog-node.spec.ts` has tests asserting that `getRemoteConfigPayload()` calls `_requestRemoteConfigPayload()` and that the fetch URL is the per-flag `remote_config` endpoint.",
        "`packages/node/src/extensions/feature-flags/feature-flags.ts` loads local evaluation data from `/flags/definitions?token=...&send_cohorts` and builds `FlagDefinitionCacheData` from `flags`, `group_type_mapping`, and `cohorts` only.",
        "`packages/node/src/extensions/feature-flags/cache.ts` defines the cache provider schema as only feature flag definitions, group type mappings, and cohort property groups; it has no remote config payload field.",
        "`packages/node/src/types.ts` explicitly documents `enableLocalEvaluation` separately from evaluating remote config payloads via `getRemoteConfigPayload`, which supports the reporter's observation that remote configs are not currently part of local evaluation."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small SDK-only change would at most add an in-process cache around individual remote config fetches. Fully matching the request requires a product/API decision and probably backend support to include remote config payloads in the polling/local-evaluation data model, plus cache schema/API behavior updates.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "65fcc5cde279e8f8cf57ee35909268714b5b34559c3712e7786658bd1854ee27",
  "piCalls": 1,
  "runId": 28507814375,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28507814375"
}
```
