# issue 4507: Dead clicks: treat an in-flight network request as a liveness signal

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4507
- Conclusion: Valid enhancement proposal, but the requested signal is not available through the existing recorder hook for all dead-click users and safely adding it needs a deliberate instrumentation design.
- Labels: enhancement, feature/dead-clicks, team/analytics-platform
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/31603850919
- Updated: 2026-08-12T13:55:29.854Z

```json
{
  "triage": {
    "conclusion": "Valid enhancement proposal, but the requested signal is not available through the existing recorder hook for all dead-click users and safely adding it needs a deliberate instrumentation design.",
    "summary": "Add a tightly time-bounded fetch/XHR request-start signal that suppresses dead-click capture when a click plausibly initiated asynchronous work.",
    "issueType": "feature",
    "confidence": 0.87,
    "labels": [
      "enhancement",
      "feature/dead-clicks",
      "team/analytics-platform"
    ],
    "needsMoreInfo": true,
    "maintainerComment": "Thanks — this addresses a credible false-positive class: the current detector only observes DOM/scroll/selection/visibility activity, so a click whose visible response arrives after the mutation threshold can be emitted as `$dead_click`.\n\nA small clarification is needed before implementation: please define the intended request-start window (including whether it is configurable) and whether suppression should apply to dead swipes as well as clicks.\n\nThe existing replay network capture hook cannot directly provide this signal universally: it only runs when session recording is active and network payload/performance capture is enabled, and its `PerformanceObserver` path receives resource timing after a request has completed. A request-start signal therefore likely requires carefully shared fetch/XHR instrumentation or a new lightweight subscription mechanism. That must exclude PostHog ingestion via `requestRouter.isIngestionEndpoint` where available, avoid broad suppression from background traffic, and preserve compatibility with older cores loading the lazy dead-click bundle.\n\nPlease treat this as suppress-only. Required coverage should include a click followed by in-window fetch and XHR starts, stale/pre-click and out-of-window requests, PostHog ingestion requests, and teardown/restart behavior. If fetch/XHR wrappers are changed, add real-browser coverage including WebKit and another page-level fetch wrapper before landing.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/entrypoints/dead-clicks-autocapture.ts",
        "packages/browser/src/__tests__/entrypoints/lazy-loaded-dead-clicks-autocapture.test.ts",
        "packages/types/src/posthog-config.ts",
        "packages/browser/src/extensions/replay/external/network-plugin.ts",
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/extensions/replay/external/config.ts",
        "packages/browser/src/utils/request-router.ts",
        "packages/browser/src/extensions/replay/rrweb-plugins/patch.ts"
      ],
      "findings": [
        "`LazyLoadedDeadClicksAutocapture` currently stores timestamps only for mutations, scrolls, selection changes, and visibility changes. `_checkClicks` suppresses candidates with an in-window signal, otherwise requeues them until the mutation threshold and ultimately captures on an absolute timeout of `mutation_threshold_ms * 1.1`.",
        "The dead-click configuration defaults the mutation threshold to 2500 ms, so an asynchronous UI whose DOM update occurs later can indeed be classified as dead despite a successful request.",
        "The existing replay network plugin is not a general request-start feed. It is initialized only when replay is active and network capture is enabled; its `PerformanceObserver` callback observes resource entries after timing is available, while fetch/XHR wrapping is enabled only when request headers or bodies are being recorded.",
        "The replay XHR instrumentation records its start timestamp in a `send` wrapper; the fetch/XHR code uses shared patch-chain handling because tracing headers and replay can both wrap the same primitives. A separate dead-click wrapper would re-enter this high-risk compatibility area.",
        "Replay network capture already excludes PostHog ingestion requests using `requestRouter.isIngestionEndpoint`, with a path-based fallback for `/s/`, `/e/`, and `/i/`. `RequestRouter` accounts for rewritten ingestion URLs, but a new lazy bundle must feature-detect any newer-core API it calls.",
        "The dead-click detector is itself a lazy-loaded entrypoint. The incident guidance requires old-core/new-bundle compatibility when adding cross-boundary APIs or assumptions.",
        "Existing unit tests already exercise suppression and timeout behavior for mutation, scroll, selection, and visibility signals, providing a focused place to add the proposed signal tests."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The behavioral change is narrow, but request-start observation cannot simply reuse the existing replay hook without missing users and requests. Adding or altering fetch/XHR instrumentation has a documented high compatibility risk, while changing the lazy entrypoint must remain compatible with older SDK cores. The required timing window and scope are also not specified.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4503,
      "type": "pull_request",
      "state": "open",
      "title": "fix(dead-clicks): treat visibility and focus changes as liveness signals, not dead-click evidence",
      "url": "https://github.com/PostHog/posthog-js/pull/4503",
      "labels": [],
      "bodyExcerpt": "## Problem\n\nThe dead-click detector was flagging a large share of ordinary clicks as `$dead_click`. The dominant source was the visibility branch: it measured the gap between a click and the most recent `visibilitychange` with `Math.abs(...)` and, once that gap exceeded a threshold, timed the click out as **dead**. Because the handler only recorded the tab becoming *visible*, any click in a session where the tab had ever been backgrounded looked like it had a multi-second \"response\" and was flagged — the reported delays were far too large to be real reactions to the click.\n\nThat logic is also backwards: a visibility or focus change near a click is evidence the click **did something** (it woke/focused the tab, opened a new tab, or opened a new window/popup), not evidence that it was dead. And the liveness detection was incomplete — a click that opens a new window/popup often leaves the tab visible, so its only trace is the current window losing focus, which wasn't observed at all.\n\n**Wh…",
      "createdAt": "2026-08-12T11:54:31Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "20df644d35e96b680c08cec7f65c6ee6caccb369af1704ad9197c347083bd678",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/2095e8028c19d1fc4ddd15d0151febc3",
    "gistId": "2095e8028c19d1fc4ddd15d0151febc3",
    "files": [
      {
        "name": "call-1-1-2026-08-12T13-54-42-039Z_019ff640-fcf7-73c3-adf2-ff3ba0aaa9db.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/2095e8028c19d1fc4ddd15d0151febc3/raw/11b6ffdd3e51520e6c3b47839deadfe0c02a615e/call-1-1-2026-08-12T13-54-42-039Z_019ff640-fcf7-73c3-adf2-ff3ba0aaa9db.jsonl"
      }
    ]
  },
  "runId": 31603850919,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/31603850919"
}
```
