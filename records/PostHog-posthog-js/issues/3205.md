# issue 3205: Remove source maps from distribution

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3205
- Conclusion: Valid distribution-size enhancement: posthog-js currently publishes generated source maps, which can substantially inflate node_modules and production container images even if they are not included in the client bundle.
- Labels: enhancement, web, feature, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/32116700720
- Updated: 2026-08-18T08:31:34.349Z

```json
{
  "triage": {
    "conclusion": "Valid distribution-size enhancement: posthog-js currently publishes generated source maps, which can substantially inflate node_modules and production container images even if they are not included in the client bundle.",
    "summary": "The reporter requests that posthog-js source maps be excluded from the npm package to reduce installed-package and Docker image size.",
    "issueType": "feature",
    "confidence": 0.94,
    "labels": [
      "enhancement",
      "web",
      "feature",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed that this affects the installed `posthog-js` package size: the browser build generates `.js.map` artifacts and the package manifest currently includes all of `dist/*` in the published tarball. These maps generally should not increase a consumer's compiled client bundle, but they do increase `node_modules` and therefore Docker images that retain production dependencies.\n\nA small packaging-only change should address this while retaining source maps for local build validation and SDK debugging before packaging. The related open PR #4520 appears to target this behavior; this issue should remain open until that change is merged and released.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/package.json",
        "packages/browser/rollup.config.mjs",
        "packages/browser/scripts/check-sourcemap-ignore-list.js",
        "packages/browser/scripts/check-mangled-property-consistency.js"
      ],
      "findings": [
        "`packages/browser/rollup.config.mjs` sets `sourcemap: true` for every generated browser entrypoint, so the build emits source-map artifacts into `dist/`.",
        "`packages/browser/package.json` publishes `dist/*`, which includes `.js.map` files alongside JavaScript and declaration outputs.",
        "The browser package's post-build checks deliberately consume generated maps: `check-sourcemap-ignore-list.js` requires source maps to exist, and `check-mangled-property-consistency.js` reads maps to validate cross-bundle property mangling.",
        "Therefore, source maps should be retained through build and validation, then excluded only from the npm tarball rather than disabled globally."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The requested behavior is isolated to npm package contents. It does not require changing runtime code or disabling source-map generation used by existing build checks.",
      "suggestedApproach": "Adjust the browser package's publish-file selection or packaging step to exclude `dist/**/*.map`, and add a tarball-content check confirming maps are absent while the normal build and post-build source-map checks still pass.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.9,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4520,
      "type": "pull_request",
      "state": "open",
      "title": "chore(browser): stop publishing dist source maps to npm",
      "url": "https://github.com/PostHog/posthog-js/pull/4520",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nCloses #3205.\r\n\r\n`posthog-js` unpacks to ~41 MB, and 28 MB of that is `dist/*.js.map` — 41 bundles' worth of maps with their sources inlined. Nothing loads them at runtime, but every install pays for them: CI caches, Docker images that ship production `node_modules`, disk on every machine with the SDK in a lockfile.\r\n\r\nTo answer the question on the issue: no, they don't reach anyone's bundle output. That's exactly why this is cheap to fix and still worth fixing.\r\n\r\n## Changes\r\n\r\nOne line in `files`: `\"!dist/*.map\"`.\r\n\r\nThe maps are still built and the CDN still gets them — the `upload-s3` job reads `packages/browser/dist/*.js.map` from its own build, never from the npm tarball. So the `x_google_ignoreList` console attribution from #4299 is untouched for snippet users, and `check-sourcemap-ignore-list.js` still guards it.\r\n\r\nLeft alone on purpose: the 2.4 MB of `lib/**/*.js.map` (negation in `files` doesn't reach files pulled in by a directory match, so `\"!lib/**/*.map\"` i…",
      "createdAt": "2026-08-14T06:13:00Z",
      "reason": "closing-pr"
    }
  ],
  "duplicate": {
    "duplicate": true,
    "score": 1,
    "canonical": {
      "number": 4520,
      "type": "pull_request",
      "state": "open",
      "title": "chore(browser): stop publishing dist source maps to npm",
      "url": "https://github.com/PostHog/posthog-js/pull/4520",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nCloses #3205.\r\n\r\n`posthog-js` unpacks to ~41 MB, and 28 MB of that is `dist/*.js.map` — 41 bundles' worth of maps with their sources inlined. Nothing loads them at runtime, but every install pays for them: CI caches, Docker images that ship production `node_modules`, disk on every machine with the SDK in a lockfile.\r\n\r\nTo answer the question on the issue: no, they don't reach anyone's bundle output. That's exactly why this is cheap to fix and still worth fixing.\r\n\r\n## Changes\r\n\r\nOne line in `files`: `\"!dist/*.map\"`.\r\n\r\nThe maps are still built and the CDN still gets them — the `upload-s3` job reads `packages/browser/dist/*.js.map` from its own build, never from the npm tarball. So the `x_google_ignoreList` console attribution from #4299 is untouched for snippet users, and `check-sourcemap-ignore-list.js` still guards it.\r\n\r\nLeft alone on purpose: the 2.4 MB of `lib/**/*.js.map` (negation in `files` doesn't reach files pulled in by a directory match, so `\"!lib/**/*.map\"` i…",
      "createdAt": "2026-08-14T06:13:00Z",
      "reason": "closing-pr"
    },
    "blockingPr": {
      "number": 4520,
      "type": "pull_request",
      "state": "open",
      "title": "chore(browser): stop publishing dist source maps to npm",
      "url": "https://github.com/PostHog/posthog-js/pull/4520",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nCloses #3205.\r\n\r\n`posthog-js` unpacks to ~41 MB, and 28 MB of that is `dist/*.js.map` — 41 bundles' worth of maps with their sources inlined. Nothing loads them at runtime, but every install pays for them: CI caches, Docker images that ship production `node_modules`, disk on every machine with the SDK in a lockfile.\r\n\r\nTo answer the question on the issue: no, they don't reach anyone's bundle output. That's exactly why this is cheap to fix and still worth fixing.\r\n\r\n## Changes\r\n\r\nOne line in `files`: `\"!dist/*.map\"`.\r\n\r\nThe maps are still built and the CDN still gets them — the `upload-s3` job reads `packages/browser/dist/*.js.map` from its own build, never from the npm tarball. So the `x_google_ignoreList` console attribution from #4299 is untouched for snippet users, and `check-sourcemap-ignore-list.js` still guards it.\r\n\r\nLeft alone on purpose: the 2.4 MB of `lib/**/*.js.map` (negation in `files` doesn't reach files pulled in by a directory match, so `\"!lib/**/*.map\"` i…",
      "createdAt": "2026-08-14T06:13:00Z",
      "reason": "closing-pr"
    },
    "reason": "open related PR #4520 already appears to cover this report"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "open related PR [#4520](https://github.com/PostHog/posthog-js/pull/4520) already appears to cover this report",
  "snapshotHash": "0e3ce89ea2267e7282d46f89ac2752a48607e66fec4cd23af9e9527da21ba528",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/c21fe7884a089099cd8fbedf4c252c65",
    "gistId": "c21fe7884a089099cd8fbedf4c252c65",
    "files": [
      {
        "name": "call-1-1-2026-08-18T08-30-54-470Z_01a013fe-b406-7a9e-abcb-49a613aad9ec.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/c21fe7884a089099cd8fbedf4c252c65/raw/198fb464f69f9ca6b27766408595a6f9691f4e3c/call-1-1-2026-08-18T08-30-54-470Z_01a013fe-b406-7a9e-abcb-49a613aad9ec.jsonl"
      }
    ]
  },
  "runId": 32116700720,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/32116700720"
}
```
