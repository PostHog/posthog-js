# issue 4202: Replay full snapshot remains on previous session after idle rotation in 1.404.1

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4202
- Conclusion: Likely valid replay bug: an idle activity-timeout rotation can still leave the new replay session without a flushed FullSnapshot in 1.404.1.
- Labels: feature/replay, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29739996098
- Updated: 2026-07-20T11:54:27.847Z

```json
{
  "triage": {
    "conclusion": "Likely valid replay bug: an idle activity-timeout rotation can still leave the new replay session without a flushed FullSnapshot in 1.404.1.",
    "summary": "A background tab rotated the analytics session after activity timeout, but session replay stayed effectively unplayable for the new session because no rrweb FullSnapshot was flushed under the new session ID.",
    "issueType": "bug",
    "confidence": 0.82,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed diagnostics. This looks valid and appears to be an uncovered edge in the same area as #4104 rather than a straight duplicate: #4104 is already in the changelog before 1.404.1, but the reported 1.404.1 session still ended up with no playable new-session FullSnapshot.\n\nA few code paths line up with the symptoms:\n\n- `SessionIdManager.checkAndGetSessionAndWindowId()` rotates on `activityTimeout` and notifies session-id handlers.\n- `LazyLoadedSessionRecorder._onSessionIdCallback()` restarts recording with `start('session_id_changed')` when the recorder is idle, matching the reported `$session_recording_start_reason`.\n- After an idle restart the recorder can keep `_isIdle` as `'unknown'`. `_scheduleFullSnapshot()` still allows periodic full snapshots in that state, but `_captureSnapshotBuffered()` only schedules flushes when `!this._isIdle`, so background/non-interactive replay data can accumulate without a flush timer.\n- The existing #4104 tests cover post-rotation attribution by manually setting the rotated session id, but I did not find an end-to-end test for activity-timeout callback + idle/background + no user interaction + FullSnapshot flush.\n\nThe exact EU Cloud session would still be useful privately to confirm whether the remaining issue is stale session attribution, missing flush scheduling while `_isIdle === 'unknown'`, or both.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts",
        "packages/browser/src/sessionid.ts",
        "packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts",
        "packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording-compression.test.ts",
        "packages/browser/CHANGELOG.md"
      ],
      "findings": [
        "`packages/browser/CHANGELOG.md` shows #4104 shipped in 1.398.4 with the stated goal of fixing missing initial FullSnapshots after idle session-id rotation, so a 1.404.1 report is likely an incomplete edge rather than the already-fixed original issue.",
        "`packages/browser/src/sessionid.ts` rotates the session when `activityTimeout` is true and calls registered session-id change handlers with the change reason.",
        "`LazyLoadedSessionRecorder._onSessionIdCallback()` handles `activityTimeout` by emitting linking events and, when `_isIdle === true`, setting `_isIdle = 'unknown'`, then calling `stop()` and `start('session_id_changed')`.",
        "`LazyLoadedSessionRecorder._scheduleFullSnapshot()` only suppresses periodic full snapshots when `_isIdle === true`, so snapshots can still be requested/emitted while `_isIdle` is `'unknown'`.",
        "`LazyLoadedSessionRecorder._captureSnapshotBuffered()` only schedules the normal flush timer when `!this._isIdle`; because the string `'unknown'` is truthy, a restarted background recorder can buffer replay events without scheduling a flush.",
        "`lazy-sessionrecording-compression.test.ts` has tests for post-rotation FullSnapshot attribution, but they manually set `_isIdle = 'unknown'` and `_sessionId = 'rotated-session-id'`; they do not fully exercise the production path where `activityTimeout` triggers `_onSessionIdCallback()` in an idle/background tab with no later user interaction."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The affected code is localized, but replay idle state, session rotation, compression, and buffer flushing interact in subtle ways. A safe fix needs a reproducing test for the full activity-timeout callback path before changing `_isIdle` or flush scheduling semantics.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4104,
      "type": "pull_request",
      "state": "closed",
      "title": "fix(replay): attribute post-rotation snapshots to the new session while idle",
      "url": "https://github.com/PostHog/posthog-js/pull/4104",
      "labels": [
        "stamphog"
      ],
      "bodyExcerpt": "## Problem\n\nWhen the session id rotates while the recorder is idle, the restarted recorder's Meta and FullSnapshot are appended to the previous session's buffer and shipped under the old session id. The new recording has only incremental events until the next periodic full snapshot, so the player shows a multi-minute leading hole (\"the first Xm of this recording can't be shown\").\n\nCause: `_captureSnapshotBuffered` only flushed and rebound the buffer's session id when not idle, and an idle rotation restarts the recorder before any user interaction clears the idle state. Active rotations were unaffected, so only a fraction of sessions hit this.\n\n## Changes\n\n- flush and rebind the snapshot buffer on any session-id change, regardless of idle state; the idle guard now only applies to same-session size-based flushes\n- safety net: request a full snapshot when an incremental is about to ship for a session that has not produced one while an earlier session did (the rotation signature), once per…",
      "createdAt": "2026-07-07T19:59:54Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "0aa818b0aa1b4a8efbfdc3abb65f0e4aa506d2453121e8daa60e17cbbc5c861d",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/38ff10b8498ed9a419dbb89d6cdf812d",
    "gistId": "38ff10b8498ed9a419dbb89d6cdf812d",
    "files": [
      {
        "name": "call-1-1-2026-07-20T11-51-58-850Z_019f7f5e-5e82-7da2-9272-719455b9952f.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/38ff10b8498ed9a419dbb89d6cdf812d/raw/ae225aaefbd7067340902e6400acf4be9fc8e154/call-1-1-2026-07-20T11-51-58-850Z_019f7f5e-5e82-7da2-9272-719455b9952f.jsonl"
      }
    ]
  },
  "runId": 29739996098,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29739996098"
}
```
