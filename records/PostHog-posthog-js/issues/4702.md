# issue 4702: feat(browser): support reuseAnonymousId during identify

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4702
- Conclusion: Valid browser SDK feature request; the current identify flow always merges the previous anonymous ID and also forwards it to the feature-flags request.
- Labels: enhancement, feature/product-analytics, web, team/client-libraries
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/33403890505
- Updated: 2026-08-31T14:40:42.678Z

```json
{
  "triage": {
    "conclusion": "Valid browser SDK feature request; the current identify flow always merges the previous anonymous ID and also forwards it to the feature-flags request.",
    "summary": "Add a default-off `reuseAnonymousId` browser configuration option so `identify()` retains its normal local identity transition but does not send `$anon_distinct_id` in the `$identify` event or subsequent feature-flag reload.",
    "issueType": "feature",
    "confidence": 0.94,
    "labels": [
      "enhancement",
      "feature/product-analytics",
      "web",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Confirmed the current browser flow unconditionally includes the prior anonymous ID in both places relevant to this request:\n\n- `identify()` captures `$identify` with `$anon_distinct_id: previous_distinct_id` when transitioning from an anonymous identity.\n- It then passes that same ID to the feature-flags extension, which includes it in the next `/flags/?v=2` request.\n\nA small default-off config option can preserve existing behavior while conditionally omitting both payload fields. The local transition to the identified state, person-property handling, distinct ID update, device ID, and flag reload should remain unchanged. Focused regression tests should cover the default merge behavior and the enabled no-merge behavior for both `$identify` and `/flags/`.",
    "investigation": {
      "relevantFiles": [
        "packages/types/src/posthog-config.ts",
        "packages/browser/src/types.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-featureflags.ts",
        "packages/browser/src/__tests__/posthog-core.identify.test.ts",
        "packages/browser/src/__tests__/featureflags.test.ts",
        "packages/browser/functional_tests/identify.test.ts",
        "packages/browser/functional_tests/feature-flags.test.ts"
      ],
      "findings": [
        "`packages/browser/src/types.ts` defines the browser config as an extension of the shared `@posthog/types` `PostHogConfig`; neither inspected config type currently defines `reuseAnonymousId`.",
        "`defaultConfig` in `packages/browser/src/posthog-core.ts` has no reuse-anonymous-ID setting, so an explicit default of `false` is needed to preserve the current behavior.",
        "During an anonymous-to-identified transition, `PostHog.identify()` captures `$identify` with `distinct_id: new_distinct_id` and `$anon_distinct_id: previous_distinct_id`, then calls `featureFlags.setAnonymousDistinctId(previous_distinct_id)`.",
        "`PostHogFeatureFlags._callFlagsEndpoint()` serializes its stored anonymous ID as `$anon_distinct_id` in the `/flags/?v=2` request, so only changing the `$identify` event would leave feature-flag evaluation with merge semantics.",
        "The identity transition, user-state update, person-property handling, and feature-flag reload are separate from setting `$anon_distinct_id`; they can remain intact when the option is enabled.",
        "Existing unit and functional tests already assert the default `$identify` payload and the anonymous ID forwarded to feature-flag requests, providing direct locations for enabled and default-off regression coverage."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The behavior is localized to the identify transition and its feature-flag handoff. A default-off boolean preserves existing users, and existing focused tests cover both affected payloads.",
      "suggestedApproach": "Add documented `reuseAnonymousId?: boolean` configuration with a `false` browser default. In the anonymous-to-identified branch of `identify()`, conditionally omit `$anon_distinct_id` from the `$identify` capture and skip `featureFlags.setAnonymousDistinctId(...)` when enabled. Do not change distinct-ID persistence, user state, `$device_id`, person properties, or the feature-flag reload. Add unit/functional assertions for enabled omission and retain explicit coverage that the default still sends both fields.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4662,
      "type": "pull_request",
      "state": "open",
      "title": "fix(browser): warn when memory/sessionStorage persistence has no bootstrap.distinctID",
      "url": "https://github.com/PostHog/posthog-js/pull/4662",
      "labels": [],
      "bodyExcerpt": "## Problem\n\n- Customers on `persistence: 'memory'` (or `'sessionStorage'`) who also call `identify()` silently accumulate thousands of distinct IDs on one person, until that person's events vanish from person pages and the session tab — while ingestion stays fine, so the loss is invisible.\n- `memoryStore` lives only in the page's JS heap, so every page load hits the `if (!this.get_distinct_id())` branch and mints a fresh `uuidv7()` as both `distinct_id` and `$device_id`. `identify()` then sends `$identify` with that throwaway ID as `$anon_distinct_id`, merging one more distinct ID onto the same person each load.\n- Past the distinct-ID display limit, person-scoped views load only a truncated slice. The reporting team saw a 13-month apparent data gap for two users, each with over 10k distinct IDs — every occurrence costs a full support investigation.\n- The trigger is narrow (the default is `localStorage+cookie`), but the failure mode is silent and severe.\n\n## Changes\n\n- Warn once at init…",
      "createdAt": "2026-08-27T09:16:14Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "8c837ec08e98d11cb5c36c7eef6b28d985fe5fe6c3dc9105c53a0ca1cffe0e44",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/b8f1037d7c3ec99261ad4b1d06a2b0de",
    "gistId": "b8f1037d7c3ec99261ad4b1d06a2b0de",
    "files": [
      {
        "name": "call-1-1-2026-08-31T14-39-54-025Z_01a05843-32a8-75e4-b911-eb8fd3a6fa1d.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/b8f1037d7c3ec99261ad4b1d06a2b0de/raw/35c794e64575d26e7d20ed0fb9c2f96729995b77/call-1-1-2026-08-31T14-39-54-025Z_01a05843-32a8-75e4-b911-eb8fd3a6fa1d.jsonl"
      }
    ]
  },
  "runId": 33403890505,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/33403890505"
}
```
