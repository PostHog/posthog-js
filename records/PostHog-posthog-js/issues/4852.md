# issue 4852: posthog-react-native Expo plugin silently skips `apply plugin: "com.posthog.android"` when an earlier plugin registers an appBuildGradle mod

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4852
- Conclusion: Confirmed React Native Expo config-plugin bug: an earlier appBuildGradle mod can cause Android native-symbol setup to silently omit the PostHog Gradle plugin.
- Labels: react-native, Android, feature/error-tracking, team/error-tracking
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/34285347874
- Updated: 2026-09-08T22:21:25.846Z

```json
{
  "triage": {
    "conclusion": "Confirmed React Native Expo config-plugin bug: an earlier appBuildGradle mod can cause Android native-symbol setup to silently omit the PostHog Gradle plugin.",
    "summary": "When uploadNativeSymbols is enabled, the Expo plugin shares a classpath-present flag across separate Gradle mods. Expo mod-kind ordering can run the app Gradle mod before the project Gradle mod, leaving the flag false and skipping the required apply plugin line.",
    "issueType": "bug",
    "confidence": 0.97,
    "labels": [
      "bug: Something isn't working",
      "react-native",
      "Android",
      "feature/error-tracking",
      "team/error-tracking"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed reproduction. This is confirmed by the current Expo config-plugin implementation: `withAndroidNativeSymbolsPlugin` initializes `classpathPresent` to `false`, sets it only in the `projectBuildGradle` callback, and returns from the `appBuildGradle` callback without applying `com.posthog.android` while it remains false.\n\nThe implementation comments also acknowledge that it relies on registration/key insertion order. Registering PostHog's own project mod first does not protect against a plugin listed earlier which has already inserted the `appBuildGradle` mod kind. The current tests cover the individual Gradle text transformations, but do not cover a config where another plugin has registered `appBuildGradle` first.\n\nA targeted fix should remove this cross-mod execution-order dependency while preserving the existing safety behavior for project Gradle files where the PostHog classpath cannot be configured. Please add a regression test that registers an earlier no-op `withAppBuildGradle` mod and verifies that a prebuild/config-mod evaluation writes both the classpath and `apply plugin: \"com.posthog.android\"`.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/tooling/expoconfig.ts",
        "packages/react-native/test/expoconfig.spec.ts",
        "packages/react-native/CHANGELOG.md",
        "packages/react-native/package.json",
        "packages/react-native/CONTRIBUTING.md"
      ],
      "findings": [
        "`withAndroidNativeSymbolsPlugin` in `packages/react-native/src/tooling/expoconfig.ts` initializes `let classpathPresent = false`; its `withProjectBuildGradle` callback updates that local, while its `withAppBuildGradle` callback returns early if the local is false.",
        "The same source explicitly documents the ordering constraint twice: the native-symbol helper says it must register before anything else touches `appBuildGradle`, and `withPostHogPlugin` says an existing app mod could make the app callback run before the project callback.",
        "The app callback is responsible for calling `applyPostHogAndroidGradlePlugin`, which inserts `apply plugin: \"com.posthog.android\"`; returning early therefore leaves the project with the classpath but without the plugin that uploads R8 mappings.",
        "`addPostHogAndroidGradlePluginClasspath` has an intentional failure path for project Gradle files without a `buildscript.dependencies` block: it warns and reports `classpathPresent: false`. A fix should not accidentally turn that currently non-breaking unsupported layout into an invalid Gradle build without explicitly deciding that behavior.",
        "`packages/react-native/test/expoconfig.spec.ts` tests the classpath and app-plugin text helpers independently, but contains no Android config-mod ordering regression test involving an earlier `withAppBuildGradle` registration.",
        "`packages/react-native/CHANGELOG.md` records #4219 as fixing a prior case where mod ordering skipped the app plugin, but the current implementation still has the documented limitation when another plugin pre-registers the app Gradle mod kind."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The fault is localized to one Expo config-plugin helper and has a deterministic reproduction. The implementation should be kept surgical: eliminate reliance on mutable state shared between separately ordered mods, and add a regression test for the earlier-appBuildGradle-mod case. Care is needed to retain the existing behavior when no compatible project-level classpath location exists.",
      "suggestedApproach": "Refactor `withAndroidNativeSymbolsPlugin` so the app Gradle decision does not depend on `classpathPresent` being set by the project Gradle callback. Use an order-independent source of truth or a deliberately coordinated mod arrangement, then add a config-mod evaluation test with a preceding no-op `withAppBuildGradle` plugin that asserts both Gradle edits are present. Keep the existing warning/skip semantics for unsupported project Gradle layouts unless intentionally changed and tested.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 3677,
      "type": "pull_request",
      "state": "closed",
      "title": "feat(react-native): add native crash capture and debug symbol upload",
      "url": "https://github.com/PostHog/posthog-js/pull/3677",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nThe React Native SDK only autocaptures JS-side errors today. Native iOS and Android crashes — which happen below the JS layer — never reach PostHog. This PR lets users opt in to native crash capture through the optional native plugin, alongside session replay.\r\n\r\nThe companion package `posthog-react-native-session-replay` is superseded by **`@posthog/react-native-plugin`** (now in this monorepo, published as `2.0.1`): it keeps the same session-replay surface and adds native error tracking behind a unified `setup()` entry point. This PR wires the SDK to the new plugin while keeping the legacy package supported as a fallback.\r\n\r\n## Changes\r\n\r\n### Runtime — native crash capture\r\n- New `errorTracking.autocapture.nativeCrashes?: boolean` (on `AutocaptureOptions`). When `true`, the SDK forwards native crash autocapture to the iOS/Android PostHog SDKs via the optional plugin. Off by default, and additionally gated server-side by the project's Error tracking **Autocapture** setti…",
      "createdAt": "2026-05-27T14:23:42Z",
      "reason": "explicit-reference"
    },
    {
      "number": 3826,
      "type": "issue",
      "state": "open",
      "title": "posthog-react-native: support Kotlin DSL (build.gradle.kts) in the Expo Android symbol-upload config plugin",
      "url": "https://github.com/PostHog/posthog-js/issues/3826",
      "labels": [
        "enhancement",
        "react-native",
        "feature/error-tracking",
        "team/error-tracking",
        "Android"
      ],
      "bodyExcerpt": "### Context\n\nFrom review on #3677 (native crash capture + symbol upload for `posthog-react-native`):\nhttps://github.com/PostHog/posthog-js/pull/3677#discussion_r3394499737\n\n> we will probably need kotlin (kts) support very soon — @marandaneto\n\n### Problem\n\nThe Expo config plugin's Android gradle edits in `packages/react-native/src/tooling/expoconfig.ts` only handle **Groovy** `build.gradle`. Both `withProjectBuildGradle` and `withAppBuildGradle` mods bail when `config.modResults.language !== 'groovy'`:\n\n```ts\nif (config.modResults.language !== 'groovy') {\n  console.warn('Cannot configure the PostHog Android Gradle plugin because the ... build.gradle is not groovy')\n  return config\n}\n```\n\nNewer RN/Expo templates are moving toward the Kotlin DSL (`build.gradle.kts`). On a `.kts` project the `com.posthog.android` classpath + `apply plugin` wiring is silently skipped, so native symbols (R8 mappings) won't be uploaded.\n\nToday this fails safe — because the classpath insertion is skipped, the…",
      "createdAt": "2026-06-12T09:53:55Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.14285714285714285,
    "canonical": {
      "number": 3826,
      "type": "issue",
      "state": "open",
      "title": "posthog-react-native: support Kotlin DSL (build.gradle.kts) in the Expo Android symbol-upload config plugin",
      "url": "https://github.com/PostHog/posthog-js/issues/3826",
      "labels": [
        "enhancement",
        "react-native",
        "feature/error-tracking",
        "team/error-tracking",
        "Android"
      ],
      "bodyExcerpt": "### Context\n\nFrom review on #3677 (native crash capture + symbol upload for `posthog-react-native`):\nhttps://github.com/PostHog/posthog-js/pull/3677#discussion_r3394499737\n\n> we will probably need kotlin (kts) support very soon — @marandaneto\n\n### Problem\n\nThe Expo config plugin's Android gradle edits in `packages/react-native/src/tooling/expoconfig.ts` only handle **Groovy** `build.gradle`. Both `withProjectBuildGradle` and `withAppBuildGradle` mods bail when `config.modResults.language !== 'groovy'`:\n\n```ts\nif (config.modResults.language !== 'groovy') {\n  console.warn('Cannot configure the PostHog Android Gradle plugin because the ... build.gradle is not groovy')\n  return config\n}\n```\n\nNewer RN/Expo templates are moving toward the Kotlin DSL (`build.gradle.kts`). On a `.kts` project the `com.posthog.android` classpath + `apply plugin` wiring is silently skipped, so native symbols (R8 mappings) won't be uploaded.\n\nToday this fails safe — because the classpath insertion is skipped, the…",
      "createdAt": "2026-06-12T09:53:55Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 14%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "39af544b3454ae637ae41698ca516195fa854c6be225c267dfe847039fb0fdaf",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/6016b9972f2d74576f6e3fb27a65ac2a",
    "gistId": "6016b9972f2d74576f6e3fb27a65ac2a",
    "files": [
      {
        "name": "call-1-1-2026-09-08T22-20-34-640Z_01a0831b-d5d0-771c-9252-a49dd1dbbadb.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/6016b9972f2d74576f6e3fb27a65ac2a/raw/fd94c2deb9e8253043b9214a273cb2450415ad94/call-1-1-2026-09-08T22-20-34-640Z_01a0831b-d5d0-771c-9252-a49dd1dbbadb.jsonl"
      }
    ]
  },
  "runId": 34285347874,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/34285347874"
}
```
