# issue 3954: posthog.loadToolbar() makes unauthorized load_web_vitals request immediately after loading

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3954
- Conclusion: Valid toolbar bug; the premature authenticated requests appear to come from the loaded toolbar bundle, not the SDK loader.
- Labels: feature/toolbar, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28178910960
- Updated: 2026-06-25T14:53:18.247Z

```json
{
  "triage": {
    "conclusion": "Valid toolbar bug; the premature authenticated requests appear to come from the loaded toolbar bundle, not the SDK loader.",
    "summary": "Calling posthog.loadToolbar() before toolbar authentication loads the toolbar script and triggers toolbar API calls for web vitals, experiments, and product tours that return 401 warnings until the toolbar is authenticated.",
    "issueType": "bug",
    "confidence": 0.84,
    "labels": [
      "bug: Something isn't working",
      "feature/toolbar",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed repro. I checked the `posthog-js` side and `posthog.loadToolbar()` itself only delegates to the toolbar extension, persists toolbar params, loads the external `/static/toolbar.js` dependency, and then calls the global `ph_load_toolbar(params, instance)` function provided by that bundle.\n\nI could not find `load_web_vitals`, `load_experiments`, `load_product_tours`, or the reported `/api/environments/@current/web_vitals` endpoint in this repo. Given the log prefix is `[PostHog Toolbar][api]`, this looks like a real toolbar bug, likely in the toolbar runtime/bundle: it should either defer these authenticated feature-loading requests until toolbar authorization is complete, or treat expected pre-auth 401s silently rather than warning in the console.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/extensions/toolbar.ts",
        "packages/browser/src/entrypoints/external-scripts-loader.ts",
        "packages/types/src/toolbar.ts",
        "packages/browser/src/utils/globals.ts"
      ],
      "findings": [
        "`packages/browser/src/posthog-core.ts` exposes `loadToolbar(params)` and delegates directly to `this.toolbar?.loadToolbar(params)`.",
        "`packages/browser/src/extensions/toolbar.ts` stores toolbar params in localStorage, loads the `toolbar` external dependency, and calls `window.ph_load_toolbar`/`ph_load_editor`; it does not directly call the reported API contexts.",
        "`packages/browser/src/entrypoints/external-scripts-loader.ts` maps the `toolbar` dependency to `/static/toolbar.js?v=...` with a five-minute cache-busting timestamp.",
        "`ph_load_toolbar` is only declared on the global window type and mocked in tests in this repo, indicating the implementation is supplied by the externally loaded toolbar script.",
        "Searches found no matches for `load_web_vitals`, `load_experiments`, `load_product_tours`, or `/api/environments/@current/web_vitals` in the repository."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small SDK-loader PR is not clearly appropriate because the unauthorized requests appear to originate inside the externally loaded toolbar bundle after `ph_load_toolbar` runs. Changing `posthog-js` loading semantics could break the toolbar authorization flow.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": true,
    "reasons": [
      "token"
    ]
  },
  "snapshotHash": "663eb97333723c44618c10b17bbaba0e667661e57c42685cd3286957c0158aae",
  "piCalls": 1,
  "runId": 28178910960,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28178910960"
}
```
