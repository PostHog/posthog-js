# issue 3954: posthog.loadToolbar() makes unauthorized load_web_vitals request immediately after loading

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3954
- Conclusion: Valid toolbar bug, likely originating in the external toolbar bundle rather than the SDK loader
- Labels: feature/toolbar, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28114618873
- Updated: 2026-06-24T16:48:10.947Z

```json
{
  "triage": {
    "conclusion": "Valid toolbar bug, likely originating in the external toolbar bundle rather than the SDK loader",
    "summary": "Calling `posthog.loadToolbar()` loads the external toolbar immediately; that toolbar appears to make authenticated web vitals/experiments/product tours API requests before auth, producing 401 console warnings.",
    "issueType": "bug",
    "confidence": 0.82,
    "labels": [
      "bug: Something isn't working",
      "feature/toolbar",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear repro. I inspected the `posthog-js` side and `posthog.loadToolbar()` itself only delegates to the toolbar extension, persists toolbar params, loads `/static/toolbar.js`, and then calls `window.ph_load_toolbar(params, instance)`. I don't see `load_web_vitals`, `load_experiments`, or `load_product_tours` in this repo, and the reported log prefix is `[PostHog Toolbar][api]`, so the premature authenticated requests likely come from the external toolbar bundle rather than the SDK loader.\n\nThis looks valid and reproducible. The likely fix is for the toolbar bundle to defer those feature-loading API calls until toolbar authorization is complete, or to treat unauthenticated pre-auth 401s as expected and avoid warning in the console.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/extensions/toolbar.ts",
        "packages/browser/src/entrypoints/external-scripts-loader.ts",
        "packages/types/src/toolbar.ts"
      ],
      "findings": [
        "`packages/browser/src/posthog-core.ts` exposes `loadToolbar(params)` and delegates directly to `this.toolbar?.loadToolbar(params)`.",
        "`packages/browser/src/extensions/toolbar.ts` builds toolbar params, stores `_postHogToolbarParams` in localStorage, loads the external `toolbar` dependency, and calls `window.ph_load_toolbar(params, instance)`; it does not call the reported API contexts directly.",
        "`packages/browser/src/entrypoints/external-scripts-loader.ts` maps the `toolbar` dependency to `/static/toolbar.js?v=...` with cache-busting, so the toolbar UI/runtime behavior is in an externally loaded script.",
        "Searches did not find `load_web_vitals`, `load_experiments`, or `load_product_tours` in the repo; related SDK features use different internal code paths/endpoints."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small `posthog-js` PR is not clearly appropriate because the unauthorized requests appear to be made by the externally loaded toolbar bundle, not by the SDK loader code inspected here.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": true,
    "reasons": [
      "token"
    ]
  },
  "snapshotHash": "f6542aea9433d1a1f140d0b5403e294fe245260dd43a00b7c71f5e103d7774f4",
  "piCalls": 1,
  "runId": 28114618873,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28114618873"
}
```
