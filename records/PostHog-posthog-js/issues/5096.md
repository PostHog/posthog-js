# issue 5096: `useThumbSurvey` from `@posthog/react/surveys` never receives the PostHog client (no-op since 1.9.0)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/5096
- Conclusion: Confirmed regression: the separately built surveys entrypoint uses a different React context from the main entrypoint, so useThumbSurvey cannot receive the client supplied by PostHogProvider.
- Labels: javascript, web, team/client-libraries, bug
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/36048354120
- Updated: 2026-09-24T19:34:57.036Z

```json
{
  "triage": {
    "conclusion": "Confirmed regression: the separately built surveys entrypoint uses a different React context from the main entrypoint, so useThumbSurvey cannot receive the client supplied by PostHogProvider.",
    "summary": "Since @posthog/react 1.9.0, importing useThumbSurvey from @posthog/react/surveys produces a separately bundled context whose default client is unset and whose provider is not the main PostHogProvider context.",
    "issueType": "bug",
    "confidence": 0.98,
    "labels": [
      "bug: Something isn't working",
      "javascript",
      "web",
      "team/client-libraries"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report — this is confirmed from the current package source.\n\n`src/surveys/index.ts` reaches `useThumbSurvey` through a separate tsdown entrypoint. That hook imports `usePostHog`, which imports `PostHogContext`. The build creates one output file per entrypoint, so the surveys output has its own context/default-instance module rather than sharing the one initialized by `src/index.ts` and populated by the main `PostHogProvider`.\n\nThe default client getter in `PostHogContext` reads `getDefaultPostHogInstance()`, but only the main entrypoint calls `setDefaultPostHogInstance(posthogJs)`. More importantly, an explicit `client` passed to the main `PostHogProvider` is provided through its own context object and cannot be read by the context in the surveys bundle.\n\nThe existing hook tests exercise the hook and provider from the same source module graph, so they do not cover importing the hook from the published `@posthog/react/surveys` subpath alongside `PostHogProvider` from `@posthog/react`.",
    "investigation": {
      "relevantFiles": [
        "packages/react/src/surveys/index.ts",
        "packages/react/src/hooks/useThumbSurvey.ts",
        "packages/react/src/hooks/usePostHog.ts",
        "packages/react/src/context/PostHogContext.ts",
        "packages/react/src/context/posthog-default.ts",
        "packages/react/src/index.ts",
        "packages/react/tsdown.config.mts",
        "packages/react/src/hooks/__tests__/useThumbSurvey.test.tsx",
        "packages/react/scripts/check-build-output.mjs"
      ],
      "findings": [
        "`packages/react/src/surveys/index.ts` exports the hook through a dedicated build entrypoint, while `tsdown.config.mts` invokes separate runtime builds for `src/index.ts` and `src/surveys/index.ts`.",
        "`useThumbSurvey` obtains its client through `usePostHog`; `usePostHog` reads `PostHogContext`.",
        "`PostHogContext` defaults its client to `getDefaultPostHogInstance()`, whose module-local value is only set by `setDefaultPostHogInstance(posthogJs)` in the main `src/index.ts` entrypoint.",
        "The build-output contract expects a single JavaScript output for each entrypoint and no shared chunk, supporting that local context/default modules are duplicated across the independently built main and surveys bundles.",
        "The current hook tests wrap the directly imported hook in a directly imported provider, so both use the same source `PostHogContext`; they do not reproduce the two-entrypoint context split."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "This is a localized packaging/entrypoint bug with no new public API required. The main entrypoint already exports useThumbSurvey; the surveys subpath needs to consume that main export as an external dependency rather than bundle a second copy of the hook/context graph.",
      "suggestedApproach": "Make the surveys subpath re-export `useThumbSurvey` from the main `@posthog/react` package and configure the surveys build to keep that self-import external in ESM and UMD output, including the UMD global mapping. Update the build-output assertions for the changed dependency shape, and add a regression test that loads the main provider and the surveys entry separately, then verifies shown tracking and `displaySurvey` use the provider's client.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "4c7f03a4fe8197d561723f53d9daba12741b75ab63dc515a8eef6294843b7073",
  "piCalls": 6,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/30f3a855e46b0426ff1b5ab6fef8bea4",
    "gistId": "30f3a855e46b0426ff1b5ab6fef8bea4",
    "files": [
      {
        "name": "call-1-1-2026-09-24T19-29-07-878Z_01a0d4e4-9f26-711e-af15-b610fa434cd0.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/30f3a855e46b0426ff1b5ab6fef8bea4/raw/8babc982512b6f955d96a5fc1a08d0d7d536be7b/call-1-1-2026-09-24T19-29-07-878Z_01a0d4e4-9f26-711e-af15-b610fa434cd0.jsonl"
      }
    ]
  },
  "runId": 36048354120,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/36048354120"
}
```
