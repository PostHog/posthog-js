# issue 4142: Expo skipOnConflict does not apply to native iOS dSYM uploads

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4142
- Conclusion: Valid bug: Expo skipOnConflict is not wired into the generated iOS dSYM upload phase.
- Labels: react-native, iOS, feature/error-tracking, feature/mobile
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/29323333336
- Updated: 2026-07-14T09:55:25.111Z

```json
{
  "triage": {
    "conclusion": "Valid bug: Expo skipOnConflict is not wired into the generated iOS dSYM upload phase.",
    "summary": "The Expo config plugin forwards skipOnConflict to React Native/Hermes sourcemap uploads, but the separate iOS native symbol upload build phase calls posthog-ios upload-symbols.sh without any conflict-skip argument.",
    "issueType": "bug",
    "confidence": 0.92,
    "labels": [
      "bug: Something isn't working",
      "react-native",
      "iOS",
      "feature/error-tracking",
      "feature/mobile"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the clear report. I checked the current Expo config plugin path and this looks valid.\n\n`skipOnConflict` is currently only passed into the wrapped React Native bundle phase, where `posthog-xcode.sh` maps it to `posthog-cli hermes upload --skip-on-conflict`. The separate native iOS dSYM phase generated by `uploadNativeSymbols` calls posthog-ios's `upload-symbols.sh` without forwarding any extra args, so dSYM `content_hash_mismatch` conflicts can still fail the build.\n\nA minimal fix would be to thread `props.skipOnConflict === true` into the dSYM build phase generation and append/pass `--skip-on-conflict` when invoking the Pods/SPM `upload-symbols.sh` script, with unit coverage for the generated shell script. One thing to verify while implementing: whether the current posthog-ios `upload-symbols.sh` forwards arbitrary args to `posthog-cli dsym upload`; if not, the matching support may need to be added/confirmed there as well.",
    "investigation": {
      "relevantFiles": [
        "packages/react-native/src/tooling/expoconfig.ts",
        "packages/react-native/tooling/posthog-xcode.sh",
        "packages/react-native/tooling/posthog.gradle",
        "packages/react-native/test/expoconfig.spec.ts",
        "packages/react-native/test/posthog-xcode-parse.spec.ts",
        "packages/react-native/CHANGELOG.md"
      ],
      "findings": [
        "`PostHogPluginProps.skipOnConflict` in `packages/react-native/src/tooling/expoconfig.ts` is documented as appending `--skip-on-conflict` to `posthog-cli hermes upload` on iOS and Android, not native dSYM uploads.",
        "`withIosPlugin()` passes `props.skipOnConflict === true` only to `modifyExistingXcodeBuildScript()`, which wraps the `Bundle React Native code and images` phase for sourcemap upload.",
        "`buildDsymUploadShellScript(includeSource)` generates a separate dSYM upload phase that runs `/bin/sh \"$PODS_SCRIPT\"` or `/bin/sh \"$SPM_SCRIPT\"` with no additional arguments; the only native-symbol option currently forwarded is `POSTHOG_INCLUDE_SOURCE=1`.",
        "`addDsymUploadBuildPhase(xcodeProject, includeSource)` accepts only `includeSource`, and `withIosPlugin()` calls it as `addDsymUploadBuildPhase(xcodeProject, nativeSymbols.includeSource)`, so `skipOnConflict` cannot affect the generated dSYM phase.",
        "`packages/react-native/tooling/posthog-xcode.sh` maps `--posthog-skip-on-conflict` to `POSTHOG_UPLOAD_ARGS` and uses that only in the `posthog-cli hermes upload` command.",
        "`packages/react-native/test/posthog-xcode-parse.spec.ts` explicitly tests the current behavior as `passes --skip-on-conflict only to hermes upload`, confirming that native dSYM upload is outside the existing skip-on-conflict path.",
        "`packages/react-native/test/expoconfig.spec.ts` has dSYM build phase tests for shell validity, `upload-symbols.sh`, and `POSTHOG_INCLUDE_SOURCE`, but no test for conflict skipping."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The repository-side change appears small and localized to the Expo config plugin's dSYM shell-script generation and tests. The only caveat is verifying the external posthog-ios upload-symbols.sh argument-forwarding contract.",
      "suggestedApproach": "Add a `skipOnConflict` parameter to `buildDsymUploadShellScript()` and `addDsymUploadBuildPhase()`, pass `props.skipOnConflict === true` from `withIosPlugin()`, and include `--skip-on-conflict` when invoking both the Pods and SwiftPM upload-symbols.sh paths. Update tests to assert the generated dSYM script includes the flag only when requested and remains valid shell.",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "9ed500ecfed5b2b81649d7f98728643f9dbfb8c2819f7bc994e0e852591268e1",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/762aa0070d14e1150889515267bcbcf7",
    "gistId": "762aa0070d14e1150889515267bcbcf7",
    "files": [
      {
        "name": "call-1-1-2026-07-14T09-53-49-943Z_019f600c-0b76-7cb4-a436-e143bf1cfaaa.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/762aa0070d14e1150889515267bcbcf7/raw/b058b38a63eabaed9b990265aa47d25508cdbc4a/call-1-1-2026-07-14T09-53-49-943Z_019f600c-0b76-7cb4-a436-e143bf1cfaaa.jsonl"
      }
    ]
  },
  "runId": 29323333336,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/29323333336"
}
```
