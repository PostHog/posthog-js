# issue 4239: replayer: pausing exactly at a FullSnapshot's timestamp skips the rebuild

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4239
- Conclusion: Confirmed replay seek-boundary bug with a narrow but stateful fix required.
- Labels: feature/replay, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/30030683312
- Updated: 2026-07-23T17:45:34.901Z

```json
{
  "triage": {
    "conclusion": "Confirmed replay seek-boundary bug with a narrow but stateful fix required.",
    "summary": "Pausing a replay exactly at a FullSnapshot timestamp queues that snapshot as a future timer action, then immediately clears it. The snapshot rebuild is skipped, leaving the previously rendered DOM and imperative fullscreen marker state visible.",
    "issueType": "bug",
    "confidence": 0.93,
    "labels": [
      "bug: Something isn't working",
      "feature/replay",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks — the reported control flow matches the current replay machine. `pause(offset)` calls `play(offset)` and immediately sends `PAUSE`; `play` only synchronously applies events where `event.timestamp < baselineTime`, so an equal-timestamp FullSnapshot is placed on the timer and then discarded by `timer.clear()`.\n\nThis should be fixed with a focused regression test, but changing the comparison globally to `<=` is not safe without preserving/resetting `lastPlayedEvent` semantics. The reported regression in the related work is therefore relevant: the fix should explicitly cover the paused-seek FullSnapshot boundary while retaining correct rewind and delta-replay bookkeeping.",
    "investigation": {
      "relevantFiles": [
        "packages/rrweb/rrweb/src/replay/machine.ts",
        "packages/rrweb/rrweb/src/replay/index.ts",
        "packages/rrweb/rrweb/src/replay/timer.ts",
        "packages/rrweb/rrweb/test/replayer.test.ts",
        "packages/rrweb/rrweb/test/fullscreen.test.ts"
      ],
      "findings": [
        "`packages/rrweb/rrweb/src/replay/machine.ts` classifies only `event.timestamp < baselineTime` as synchronous in the `play` action; equal-timestamp events are added to the timer.",
        "`Replayer.pause(timeOffset)` in `packages/rrweb/rrweb/src/replay/index.ts` calls `play(timeOffset)` and immediately sends `PAUSE`, whose machine action calls `timer.clear()`. Thus a timer-queued equal-timestamp FullSnapshot cannot run during a paused seek.",
        "A FullSnapshot cast calls `clearFullscreen()` and `rebuildFullSnapshot()`. Skipping that cast explains both the stale DOM and leaked `rr_fullscreen` marker described in the report.",
        "The playback path conditionally emits `ReplayerEvents.PlayBack` when seeking backward; its handler resets the mirror and related replay state. `lastPlayedEvent` is not reset in that handler/action path, so broad boundary-classification changes can interact with stale delta bookkeeping.",
        "`packages/rrweb/rrweb/test/fullscreen.test.ts` currently uses `Math.max(enterOffset - 1, 0)` for its pre-fullscreen checkpoint, consistent with avoiding an exact event-timestamp boundary."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The failure mechanism is clear and the code change should be small, but the obvious global `<` to `<=` change has documented replay-state regressions. The fix needs a focused boundary rule and regression coverage that verifies both paused FullSnapshot seeks and existing rewind/lastPlayedEvent behavior.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4226,
      "type": "pull_request",
      "state": "open",
      "title": "feat(rrweb): opt-in event loop yielding during seek fast-forward rebuilds",
      "url": "https://github.com/PostHog/posthog-js/pull/4226",
      "labels": [
        "stamphog"
      ],
      "bodyExcerpt": "## Problem\n\nSeeking in a long recording can freeze the tab hard enough that Chrome offers to kill the page. The cause is old and documented in the player code: a seek fast-forwards through every event between the last full snapshot and the target in one synchronous main-thread pass (`applyEventsSynchronously`). Scrubbing is the worst case — every jump pays a full rebuild, back to back.\n\nWe reproduced this on a ~6 hour production recording from a customer report, driving the real player with Playwright and watching `longtask` entries plus a CDP CPU profile:\n\n- single seeks blocked the main thread for 0.4–3.7s each\n- a burst of 6 quick seekbar jumps blocked it for ~4.8s straight — the clicks themselves queued for 4.8s waiting for the main thread, which is exactly the input starvation that triggers Chrome's \"Page Unresponsive\" dialog\n- the largest share of non-idle CPU across the seek session landed in the rrweb chunk (~6s self time, plus most of ~7s of GC from rebuild allocation churn) —…",
      "createdAt": "2026-07-22T22:48:59Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "3759cd20209f48d0307339db00890f3411697ab656c06c2fc493c84b2c0d0483",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/86efa7eb6f097a5e354419fb2c3dc1a3",
    "gistId": "86efa7eb6f097a5e354419fb2c3dc1a3",
    "files": [
      {
        "name": "call-1-1-2026-07-23T17-45-01-669Z_019f9014-abe5-7498-b140-1d2fb31944ee.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/86efa7eb6f097a5e354419fb2c3dc1a3/raw/be65db6d6f5f42376e28465dc3d02de59cc57883/call-1-1-2026-07-23T17-45-01-669Z_019f9014-abe5-7498-b140-1d2fb31944ee.jsonl"
      }
    ]
  },
  "runId": 30030683312,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/30030683312"
}
```
