# issue 3980: Feature Request: Better client/server integration for Next.js [bootstrapped] flags

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3980
- Conclusion: Valid feature request; current code has bootstrap/update primitives but lacks automatic Next.js navigation/middleware-to-client flag refresh.
- Labels: enhancement, frameworks/next-js, feature/flags, team/feature-flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28183435637
- Updated: 2026-06-25T16:06:23.021Z

```json
{
  "triage": {
    "conclusion": "Valid feature request; current code has bootstrap/update primitives but lacks automatic Next.js navigation/middleware-to-client flag refresh.",
    "summary": "The request asks for a first-class Next.js flow where server/middleware-evaluated bootstrap flags are refreshed on subsequent navigations and automatically reflected in the client SDK and React hooks.",
    "issueType": "feature",
    "confidence": 0.86,
    "labels": [
      "enhancement",
      "frameworks/next-js",
      "feature/flags",
      "team/feature-flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks — this still looks like a valid feature request.\n\nCurrent code has some of the necessary pieces: `posthog-js` exposes `updateFlags()`, and `@posthog/next` can evaluate bootstrap flags server-side via `PostHogProvider`. However, I don’t see an automatic transport/navigation layer that updates the already-loaded browser SDK when new server-evaluated flags are produced after the initial load. `ClientPostHogProvider` currently passes bootstrap data into `posthog.init()` only when the client is not already loaded, and the React hooks only fall back to bootstrap values before flags have loaded.\n\nSo this should remain open as a Next.js + feature flags enhancement rather than being closed as covered by `updateFlags()` alone. A full implementation likely needs design across App Router/Pages Router navigation, middleware/RSC transport, persistence, consent, and local-evaluation semantics.",
    "investigation": {
      "relevantFiles": [
        "packages/next/src/middleware/postHogMiddleware.ts",
        "packages/next/src/app/PostHogProvider.tsx",
        "packages/next/src/client/ClientPostHogProvider.tsx",
        "packages/next/src/client/hooks.ts",
        "packages/react/src/hooks/useFeatureFlagResult.ts",
        "packages/react/src/hooks/useActiveFeatureFlags.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-featureflags.ts",
        "packages/next/tests/PostHogProvider.test.tsx",
        "packages/next/tests/ClientPostHogProvider.test.tsx"
      ],
      "findings": [
        "`packages/browser/src/posthog-core.ts` exposes `posthog.updateFlags(flags, payloads?, options?)` for injecting server/externally evaluated flags without a network request.",
        "`packages/browser/src/posthog-featureflags.ts` implements `updateFlags()` by converting supplied values into feature flag details and passing them through `receivedFeatureFlags()`.",
        "`packages/next/src/app/PostHogProvider.tsx` supports `bootstrapFlags`; it reads the PostHog cookie, evaluates flags with `posthog-node`, and passes a `bootstrap` object to the client provider.",
        "`packages/next/src/app/PostHogProvider.tsx` sets `advanced_disable_feature_flags_on_first_load` when bootstrap succeeds, which helps initial load but does not itself provide later navigation updates.",
        "`packages/next/src/client/ClientPostHogProvider.tsx` merges bootstrap into init options and calls `posthogJs.init()` only when `posthogJs.__loaded` is false; it does not call `updateFlags()` when a new bootstrap prop arrives after the SDK is already loaded.",
        "`packages/react/src/hooks/useFeatureFlagResult.ts` and `packages/react/src/hooks/useActiveFeatureFlags.ts` only use context bootstrap as a fallback while `client.featureFlags.hasLoadedFlags` is false; after flags are loaded they rely on SDK feature flag state and `onFeatureFlags` updates.",
        "`packages/next/src/middleware/postHogMiddleware.ts` currently seeds the identity cookie and optionally proxies ingest requests, but it does not evaluate flags or carry updated flag data back to the client."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "A small primitive already exists (`updateFlags()`), but the requested product behavior is broader: a reliable Next.js transport/navigation mechanism from middleware or server rendering into the loaded browser SDK and React hooks. That crosses App Router/Pages Router behavior, RSC/client boundaries, cookies or headers, persistence, consent, and flag freshness semantics, so a surgical low-risk PR is unlikely without a design decision.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 2813,
      "type": "pull_request",
      "state": "closed",
      "title": "feat(flags): add updateFlags() method for injecting flags without network request",
      "url": "https://github.com/PostHog/posthog-js/pull/2813",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nWhen using `instrumentation-client.ts` in Next.js, PostHog initializes once per app launch. This means feature flag values don't update on subsequent client-side navigations, even if a middleware evaluates fresh flags server-side. Users need a way to inject server-evaluated flag values into the client SDK without making additional network requests. This is applicable to other single-page applications with similar initialization methods.\r\n\r\nRelated: https://github.com/PostHog/posthog-js/issues/3980\r\n\r\n## Changes\r\n\r\nAdds a new public method `posthog.updateFlags(flags, payloads?, options?)` that:\r\n\r\n- Injects feature flag values from an external source (e.g., server-side evaluation, edge middleware)\r\n- Fires `onFeatureFlags` callbacks so React hooks auto-update\r\n- Works with `advanced_disable_flags: true` for server-only evaluation patterns\r\n- Supports `{ merge: true }` option to merge with existing flags instead of replacing\r\n\r\nThis is the foundational primitive: the transp…",
      "createdAt": "2025-12-24T19:31:02Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "20c5f11112bddd4221bd59669c0a83b7abfde84de99d59d695896809e79f7cfd",
  "piCalls": 2,
  "runId": 28183435637,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28183435637"
}
```
