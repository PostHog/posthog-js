# issue 3980: Feature Request: Better client/server integration for Next.js [bootstrapped] flags

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3980
- Conclusion: Valid Next.js feature enhancement, but automatic server-to-client flag synchronization needs an explicit design and is not a safe small change.
- Labels: enhancement, feature/flags, frameworks/next-js, team/feature-flags
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/32585328183
- Updated: 2026-08-22T16:40:32.204Z

```json
{
  "triage": {
    "conclusion": "Valid Next.js feature enhancement, but automatic server-to-client flag synchronization needs an explicit design and is not a safe small change.",
    "summary": "The Next.js provider supports initial SSR flag bootstrapping, while the browser SDK owns flag state after hydration. It does not automatically apply newly server-evaluated flags during later navigation or middleware execution.",
    "issueType": "feature",
    "confidence": 0.92,
    "labels": [
      "enhancement",
      "feature/flags",
      "frameworks/next-js",
      "team/feature-flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks — this remains a valid enhancement request, though the desired default behavior needs a product/design decision.\n\n`@posthog/next` now supports SSR bootstrap values for the initial render, so server and client can begin with consistent flag values and avoid hydration flicker. After hydration, however, `posthog-js` is intentionally the source of truth: the current Next.js provider only initializes the SDK when it has not already loaded, and does not apply later server bootstrap values to an existing browser client.\n\nThe SDK has primitives for explicit refreshes (`reloadFeatureFlags()`) and for injecting externally evaluated values (`updateFlags()`), and React hooks already update when the SDK emits a flag update. What is not provided is the transport and lifecycle policy requested here: evaluating flags in middleware/server rendering, delivering those values through App Router or Pages Router navigations, and deciding when they should replace browser state.\n\nA default continuous middleware-to-browser synchronization mechanism could have important semantics around identity/cookie synchronization, consent, client persistence, local evaluation, router behavior, and stale or out-of-order responses. A narrower opt-in refresh policy on navigation, focus, or an interval may be a better direction than making middleware evaluations continuously authoritative by default.",
    "investigation": {
      "relevantFiles": [
        "packages/next/src/app/PostHogProvider.tsx",
        "packages/next/src/client/ClientPostHogProvider.tsx",
        "packages/next/src/middleware/postHogMiddleware.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-featureflags.ts",
        "packages/react/src/hooks/useFeatureFlagResult.ts",
        "packages/react/src/hooks/useActiveFeatureFlags.ts",
        "packages/next/tests/ClientPostHogProvider.test.tsx",
        "packages/next/tests/PostHogProvider.test.tsx"
      ],
      "findings": [
        "`PostHogProvider` can evaluate flags from the PostHog identity cookie when `bootstrapFlags` is enabled, pass them as browser bootstrap data, and disable the initial browser flag request only when server evaluation succeeded.",
        "`ClientPostHogProvider` calls `posthogJs.init()` only when `posthogJs.__loaded` is false. On later renders it exposes new bootstrap data through React context but does not call `posthogJs.updateFlags()` or `reloadFeatureFlags()`.",
        "`postHogMiddleware` currently seeds the identity cookie and can proxy ingest traffic, but it neither evaluates flags nor transports evaluated flag data to the browser.",
        "`posthog.updateFlags()` replaces or merges supplied flag and payload values, and its implementation processes them through the normal feature-flag callback path; the React hooks subscribe to those SDK callbacks.",
        "`reloadFeatureFlags()` is publicly available for an explicit client-side refresh, and identity or group changes already trigger client SDK refreshes. There is no Next.js navigation, focus, or interval-based refresh policy in the inspected integration.",
        "The React hooks use context bootstrap only while SDK flags are not loaded; once loaded, they rely on browser SDK feature-flag state and its update callbacks."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The missing behavior is not merely forwarding a new bootstrap prop. It requires defining the authoritative flag source and an opt-in lifecycle across App Router and Pages Router navigation, server/client transport, identity and consent changes, persistence, local evaluation, and response ordering. Implementing continuous middleware synchronization without those decisions would be speculative.",
      "suggestedApproach": "",
      "risk": "high"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 2813,
      "type": "pull_request",
      "state": "closed",
      "title": "feat(flags): add updateFlags() method for injecting flags without network request",
      "url": "https://github.com/PostHog/posthog-js/pull/2813",
      "labels": [],
      "bodyExcerpt": "## Problem\r\n\r\nWhen using `instrumentation-client.ts` in Next.js, PostHog initializes once per app launch. This means feature flag values don't update on subsequent client-side navigations, even if a middleware evaluates fresh flags server-side. Users need a way to inject server-evaluated flag values into the client SDK without making additional network requests. This is applicable to other single-page applications with similar initialization methods.\r\n\r\nRelated: https://github.com/PostHog/posthog-js/issues/3980\r\n\r\n## Changes\r\n\r\nAdds a new public method `posthog.updateFlags(flags, payloads?, options?)` that:\r\n\r\n- Injects feature flag values from an external source (e.g., server-side evaluation, edge middleware)\r\n- Fires `onFeatureFlags` callbacks so React hooks auto-update\r\n- Works with `advanced_disable_flags: true` for server-only evaluation patterns\r\n- Supports `{ merge: true }` option to merge with existing flags instead of replacing\r\n\r\nThis is the foundational primitive: the transp…",
      "createdAt": "2025-12-24T19:31:02Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "fixBlocker": "feature requests require an explicit trusted fix command or mode: fix before opening a draft PR",
  "snapshotHash": "8b670573fd09e65836f907aa4edcedf8fef31d0b0167d657f0b7e3db90d0b145",
  "piCalls": 2,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/dd7fe697e3470eb7b00d77010869989e",
    "gistId": "dd7fe697e3470eb7b00d77010869989e",
    "files": [
      {
        "name": "call-2-1-2026-08-22T16-39-56-976Z_01a02a57-df30-7dc2-a8d7-1380b43bc0bd.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/dd7fe697e3470eb7b00d77010869989e/raw/37e6ea11ede4b6af8ccfc04dac34b8a1ad690ca9/call-2-1-2026-08-22T16-39-56-976Z_01a02a57-df30-7dc2-a8d7-1380b43bc0bd.jsonl"
      }
    ]
  },
  "runId": 32585328183,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/32585328183"
}
```
