# issue 4931: Dead-click false positives when DOM updates precede the window bubble listener

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/4931
- Conclusion: Valid dead-click false-positive bug with a narrowly scoped, evidence-backed fix direction.
- Labels: feature/dead-clicks, javascript, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/34724966087
- Updated: 2026-09-12T23:17:50.047Z

```json
{
  "triage": {
    "conclusion": "Valid dead-click false-positive bug with a narrowly scoped, evidence-backed fix direction.",
    "summary": "The detector creates click candidates from a window bubble listener, while MutationObserver delivery can record a synchronous target-handler DOM change before that listener runs. The later timestamp comparison then discards the valid mutation and reports a false dead click.",
    "issueType": "bug",
    "confidence": 0.94,
    "labels": [
      "bug: Something isn't working",
      "feature/dead-clicks",
      "javascript",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks — this is a clear, self-contained reproduction of a listener-ordering false positive.\n\nThe current implementation does register the click listener on `window` without capture, creates the candidate with `Date.now()`, and only accepts a mutation when its timestamp is at or after the candidate timestamp. That matches the reported failure mode when a target or bubbling application handler synchronously mutates the DOM and MutationObserver delivery happens before the detector's window bubble listener.\n\nRegistering the click listener in capture phase, with the same capture option on removal, is the smallest plausible fix. Before landing, this should have a real-browser regression test that exercises the ordering: a target click handler mutates the DOM, an earlier window bubble listener makes ordering deterministic, the mutating click emits no `$dead_click`, and an inert control still does. The test should cover the native observer path; jsdom's mutation-observer coverage is currently explicitly skipped.\n\nThis change should stay limited to the target/bubble-handler ordering bug. In particular, an application listener registered earlier on `window` in capture phase can still run first, and capture also observes clicks that later stop propagation, so behavior and event-volume effects should be reviewed rather than expanding the change speculatively.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/entrypoints/dead-clicks-autocapture.ts",
        "packages/browser/src/__tests__/entrypoints/lazy-loaded-dead-clicks-autocapture.test.ts",
        "packages/browser/playwright/mocked/dead-clicks.spec.ts",
        "packages/browser-common/src/utils/general-utils.ts",
        "packages/browser/src/extensions/dead-clicks-autocapture.ts",
        ".agents/skills/replay-incident-risk/INCIDENTS.md"
      ],
      "findings": [
        "`_startClickObserver()` registers `click` on `assignableWindow` with no capture option, so the helper defaults it to `{ capture: false, passive: true }`; `stop()` likewise removes that listener without a capture option.",
        "`asCandidate()` stamps the click candidate with `Date.now()`, while `_onMutation()` separately stamps `_lastMutation` with `Date.now()`.",
        "`_checkClicks()` computes a mutation delay only when `click.timestamp <= this._lastMutation`; a mutation callback delivered before the window bubble click handler is therefore ignored, exactly as described.",
        "The existing unit suite explicitly skips direct mutation-observer behavior due to jsdom limitations, while the Playwright suite already has a real-browser test asserting that a synchronous DOM update observed through the fallback MutationObserver prevents a dead click. It is the appropriate layer for this ordering regression.",
        "The change would affect the lazy-loaded dead-click entrypoint, but it does not require a core/extension API, persisted-state, or configuration-shape change. The lazy-load compatibility failure mode documented in the incident guidance is therefore not directly present.",
        "Moving to capture means the detector will also see clicks that application code later stops from bubbling. This can change dead-click candidate and event volume, so the behavioral change merits real-browser validation."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The implementation itself is likely a two-line listener-options change, but it changes globally observed click semantics in a lazily delivered analytics extension and can affect dead-click volume. A focused real-browser regression test is needed before treating it as a routine low-risk patch.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0.99,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [
    {
      "number": 4315,
      "type": "issue",
      "state": "closed",
      "title": "Dead click autocapture: false positives — detector's MutationObserver sees no mutations while rrweb records instant DOM updates",
      "url": "https://github.com/PostHog/posthog-js/issues/4315",
      "labels": [
        "feature/dead-clicks"
      ],
      "bodyExcerpt": "## Bug: dead click autocapture reports false positives — detector's MutationObserver sees no mutations while rrweb (session replay) records instant DOM updates\n\n### Environment\n\n- posthog-js **1.404.1**, EU Cloud\n- Vue 3 SPA (Vite build), init config: `defaults: '2025-05-24'`, `capture_exceptions: true`, `session_recording: { maskAllInputs: true }`, dead click autocapture enabled via project settings\n- Affected browsers in our data: **Mobile Safari, Chrome iOS, Brave (desktop), Edge (desktop)**\n\n### Summary\n\nClicks on date/week navigation buttons in our app are captured as `$dead_click` even though session replay of the *same clicks* shows the DOM updating instantly (a date label text change plus a full list re-render within ~100 ms). Roughly 3% of clicks on these buttons are flagged dead (16 of 535 over 3 days on one page); the affected clicks cluster on the browsers above.\n\n### Evidence from event properties\n\nGrouping all our `$dead_click` events by the timeout flags the SDK attaches…",
      "createdAt": "2026-07-29T20:29:16Z",
      "reason": "explicit-reference"
    }
  ],
  "duplicate": {
    "duplicate": false,
    "score": 0.10256410256410256,
    "canonical": {
      "number": 4315,
      "type": "issue",
      "state": "closed",
      "title": "Dead click autocapture: false positives — detector's MutationObserver sees no mutations while rrweb records instant DOM updates",
      "url": "https://github.com/PostHog/posthog-js/issues/4315",
      "labels": [
        "feature/dead-clicks"
      ],
      "bodyExcerpt": "## Bug: dead click autocapture reports false positives — detector's MutationObserver sees no mutations while rrweb (session replay) records instant DOM updates\n\n### Environment\n\n- posthog-js **1.404.1**, EU Cloud\n- Vue 3 SPA (Vite build), init config: `defaults: '2025-05-24'`, `capture_exceptions: true`, `session_recording: { maskAllInputs: true }`, dead click autocapture enabled via project settings\n- Affected browsers in our data: **Mobile Safari, Chrome iOS, Brave (desktop), Edge (desktop)**\n\n### Summary\n\nClicks on date/week navigation buttons in our app are captured as `$dead_click` even though session replay of the *same clicks* shows the DOM updating instantly (a date label text change plus a full list re-render within ~100 ms). Roughly 3% of clicks on these buttons are flagged dead (16 of 535 over 3 days on one page); the affected clicks cluster on the browsers above.\n\n### Evidence from event properties\n\nGrouping all our `$dead_click` events by the timeout flags the SDK attaches…",
      "createdAt": "2026-07-29T20:29:16Z",
      "reason": "explicit-reference"
    },
    "reason": "best duplicate score was 10%"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "b91e13f8c68ef5b264f88e73123c0628c6115afa275d461adb0c216b2c05b039",
  "piCalls": 1,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/7ccfaaea6fbc669027d4095751526690",
    "gistId": "7ccfaaea6fbc669027d4095751526690",
    "files": [
      {
        "name": "call-1-1-2026-09-12T23-17-06-703Z_01a097e9-080f-7b27-a556-ae8a0d5c8fec.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/7ccfaaea6fbc669027d4095751526690/raw/e89928afc9a8dbc967959abf9e51489dfcaed86b/call-1-1-2026-09-12T23-17-06-703Z_01a097e9-080f-7b27-a556-ae8a0d5c8fec.jsonl"
      }
    ]
  },
  "runId": 34724966087,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/34724966087"
}
```
