# issue 2488: Clarify the reason for always using the React provided client

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/2488
- Conclusion: Valid documentation clarification request: importing the default client is safe outside React only when it is the same initialized instance supplied to the provider.
- Labels: documentation, web
- PR: https://github.com/PostHog/posthog-js/pull/5104
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/36113916584
- Updated: 2026-09-25T08:41:34.096Z

```json
{
  "triage": {
    "conclusion": "Valid documentation clarification request: importing the default client is safe outside React only when it is the same initialized instance supplied to the provider.",
    "summary": "Clarify when React applications may import and use the global posthog-js client outside React, and why React components should use usePostHog().",
    "issueType": "docs",
    "confidence": 0.96,
    "labels": [
      "documentation",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "This is a useful clarification. In the standard `@posthog/react` entrypoint, the provider's `apiKey` path uses the default `posthog-js` export, so code outside React can import that same client and use it after initialization.\n\nThe important exception is that `PostHogProvider` can receive any `client` instance. `usePostHog()` reads that React-context instance, including custom or nested clients, while a direct `import posthog from 'posthog-js'` always addresses the default module singleton. The slim React entrypoint also requires callers to provide a client explicitly.\n\nA small docs update should recommend `usePostHog()` within React components so they respect the provider's client, and document that non-React code may use a direct import when the application initializes and passes that exact same instance to the provider. It should caution against mixing the default import with a provider configured with a different client instance.",
    "investigation": {
      "relevantFiles": [
        "packages/react/src/index.ts",
        "packages/react/src/context/PostHogProvider.tsx",
        "packages/react/src/context/PostHogContext.ts",
        "packages/react/src/context/PostHogProviderSlim.tsx",
        "packages/react/src/hooks/usePostHog.ts",
        "packages/browser/src/entrypoints/module.no-external.es.ts",
        "packages/browser/src/posthog-core.ts",
        "packages/browser/playground/error-tracking/next-ts-app/src/app/provider.tsx",
        "packages/browser/playground/error-tracking/next-ts-app/src/app/page.tsx"
      ],
      "findings": [
        "`@posthog/react` imports the default `posthog-js` export and registers it as its default instance in `packages/react/src/index.ts`.",
        "When `PostHogProvider` receives an `apiKey`, it returns and initializes that registered default instance; when it receives `client`, it instead provides that caller-supplied instance unchanged.",
        "`usePostHog()` only returns `PostHogContext.client`, so it deliberately honors a custom client supplied to the nearest provider rather than always selecting the module singleton.",
        "The `@posthog/react/slim` provider accepts only a supplied client and has no runtime `posthog-js` dependency, confirming that a provider is not inherently tied to the default import.",
        "`posthog-js` creates its default module singleton through `init_as_module()`. The repository includes a Next playground that initializes `posthog-js` in one component and imports the same default export elsewhere to capture an exception."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The requested behavior is already supported in the normal shared-singleton setup; the needed change is a narrowly scoped documentation clarification, not an SDK API or behavioral change.",
      "suggestedApproach": "Update the React documentation with a short explanation and example: create/import one `posthog-js` client, initialize it, pass it as `client` to `PostHogProvider`, use `usePostHog()` inside React, and use the same imported client in non-React modules. Explicitly state that direct imports must not be mixed with a provider using a different custom client.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "bb772442b8668c828f20bf2e8674749357a519084efc932f480997969aa045ac",
  "piCalls": 6,
  "piSessionReference": {
    "kind": "gist",
    "gistUrl": "https://gist.github.com/marandaneto/74803e9318d95d8fbaabcf1e6fa73f27",
    "gistId": "74803e9318d95d8fbaabcf1e6fa73f27",
    "files": [
      {
        "name": "call-2-1-2026-09-25T08-39-07-937Z_01a0d7b7-e3a0-77a1-8270-870ebf42b2ec.jsonl",
        "url": "https://gist.githubusercontent.com/marandaneto/74803e9318d95d8fbaabcf1e6fa73f27/raw/eaaf4f2a34c9ffae662b7c1e6e6ad908379d0103/call-2-1-2026-09-25T08-39-07-937Z_01a0d7b7-e3a0-77a1-8270-870ebf42b2ec.jsonl"
      }
    ]
  },
  "runId": 36113916584,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/36113916584"
}
```
