# issue 3578: JS SDK: reset() should work across subdomains

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3578
- Conclusion: Valid bug: reset cannot clear localStorage on other subdomains, and current default merge behavior can let stale subdomain localStorage override the cross-subdomain cookie.
- Labels: feature/product-analytics, web
- PR: (none)
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28363800378
- Updated: 2026-06-29T10:01:54.272Z

```json
{
  "triage": {
    "conclusion": "Valid bug: reset cannot clear localStorage on other subdomains, and current default merge behavior can let stale subdomain localStorage override the cross-subdomain cookie.",
    "summary": "With default browser persistence, `reset()` clears the current origin's storage and the shared cookie, then writes new reset state, but old localStorage on another subdomain remains and can win when that subdomain is revisited.",
    "issueType": "bug",
    "confidence": 0.86,
    "labels": [
      "bug: Something isn't working",
      "feature/product-analytics",
      "web"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the report. This matches the current browser persistence behavior: `reset()` can only clear storage for the current origin plus the shared cookie; it cannot directly delete `localStorage` for another subdomain. The risky part is that the default `localStorage+cookie` merge currently lets localStorage win on conflicts, so stale data from `a.example.com` can override the reset cookie state when that subdomain is revisited. There is already an internal preview config (`__preview_cookie_wins_on_conflict`) noted in code as addressing the cross-subdomain stale-localStorage case, so the likely fix is around promoting/changing that precedence with regression coverage rather than trying to delete cross-origin localStorage.",
    "investigation": {
      "relevantFiles": [
        "packages/browser/src/posthog-core.ts",
        "packages/browser/src/posthog-persistence.ts",
        "packages/browser/src/storage.ts",
        "packages/browser/src/__tests__/posthog-persistence.test.ts"
      ],
      "findings": [
        "`posthog-core.ts` default config uses `persistence: 'localStorage+cookie'` and `cross_subdomain_cookie` defaults based on the document location.",
        "`posthog-core.ts` `reset()` calls `this.persistence?.clear()` and `this.sessionPersistence?.clear()`, then registers a new anonymous distinct id/device id state.",
        "`posthog-persistence.ts` `remove()` removes both domain and subdomain cookies via `_remove(..., false)` and `_remove(..., true)`, but storage removal is scoped to the active origin's storage backend.",
        "`storage.ts` `createLocalPlusCookieStore._remove()` removes `window.localStorage.removeItem(name)` for the current origin and then removes the cookie; browser APIs cannot remove localStorage for sibling subdomains.",
        "`storage.ts` `createLocalPlusCookieStore._parse()` defaults to `extend(cookieProperties, localStorageData)`, meaning localStorage wins on key conflicts.",
        "`posthog-core.ts` includes `__preview_cookie_wins_on_conflict: false` with the comment `Opt-in: fixes cross-subdomain stale-localStorage bug`, and tests document that enabling it makes cookie values win over localStorage for conflicting keys."
      ]
    },
    "fix": {
      "straightforward": false,
      "reason": "The mechanical cause is clear, but a safe fix changes identity persistence precedence/defaults. Directly deleting localStorage across subdomains is impossible in the browser, so the practical fix is to make the shared cookie authoritative in this scenario, which has backwards-compatibility risk for existing localStorage+cookie users.",
      "suggestedApproach": "",
      "risk": "medium"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "96107a0720e3751d4f6bb3e535cd1577d2b7c9b182e8f83748d681d66b4dfa98",
  "piCalls": 3,
  "runId": 28363800378,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28363800378"
}
```
