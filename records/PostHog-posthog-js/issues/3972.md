# issue 3972: Local evaluation ignores the in/not_in operator on a flag-level cohort condition (not in cohort evaluated as in cohort)

- Repo: PostHog/posthog-js
- URL: https://github.com/PostHog/posthog-js/issues/3972
- Conclusion: Valid posthog-node local evaluation bug; flag-level cohort not_in is ignored.
- Labels: node, feature/flags, team/feature-flags
- PR: https://github.com/PostHog/posthog-js/pull/3973
- Closed: no
- Run: https://github.com/PostHog/posthog-js/actions/runs/28163619464
- Updated: 2026-06-25T10:29:43.279Z

```json
{
  "triage": {
    "conclusion": "Valid posthog-node local evaluation bug; flag-level cohort not_in is ignored.",
    "summary": "Local feature flag evaluation treats a flag condition of type cohort as raw cohort membership and does not apply the condition operator, so a flag-level cohort `not_in` condition can evaluate like `in`.",
    "issueType": "bug",
    "confidence": 0.95,
    "labels": [
      "bug: Something isn't working",
      "node",
      "feature/flags",
      "team/feature-flags"
    ],
    "needsMoreInfo": false,
    "maintainerComment": "Thanks for the detailed report. I verified the relevant local-evaluation path on `main`: `isConditionMatch` calls `matchCohort(...)` for `type: 'cohort'` and then checks the returned boolean directly, while `matchCohort` returns raw cohort membership without reading `operator`. That means a flag-level cohort property carrying `operator: 'not_in'` is not inverted locally.\n\nA small fix should be appropriate: add a regression test for a locally evaluated flag with a flag-condition-level cohort using `operator: 'not_in'`, then invert the cohort membership result before the `!matches` check for that operator. If the local-evaluation payload can also encode this as `negation: true`, include that case intentionally rather than broad refactoring.",
    "investigation": {
      "relevantFiles": [
        "packages/node/src/extensions/feature-flags/feature-flags.ts",
        "packages/node/src/types.ts",
        "packages/node/src/__tests__/feature-flags.spec.ts"
      ],
      "findings": [
        "`packages/node/src/extensions/feature-flags/feature-flags.ts` `isConditionMatch` branches on `propertyType === 'cohort'`, assigns `matches = await matchCohort(...)`, and then only checks `if (!matches) return 'no_match'`; it does not read the cohort property's `operator` before deciding whether the condition matched.",
        "`matchCohort` in the same file converts `property.value` to a cohort id, checks that the cohort exists, and returns `matchPropertyGroup(...)`; it does not inspect `property.operator` or `property.negation`, so it returns raw cohort membership.",
        "`matchPropertyGroup` does apply `prop.negation` for properties inside cohort groups, and existing tests cover complex cohorts with internal negation, but this does not cover the flag-condition-level cohort operator described in the issue.",
        "`packages/node/src/types.ts` defines `FlagProperty` with optional `operator` and `negation`, so the local-evaluation payload shape has fields that can represent this condition."
      ]
    },
    "fix": {
      "straightforward": true,
      "reason": "The bug is isolated to the posthog-node local feature-flag evaluator's handling of cohort properties at the flag condition level. A small targeted change plus regression tests should address it without changing unrelated property matching.",
      "suggestedApproach": "Add a regression test in `packages/node/src/__tests__/feature-flags.spec.ts` for a locally evaluated flag whose condition is `{ type: 'cohort', operator: 'not_in' }`, covering both a user in the cohort and a user outside it. Then, in `isConditionMatch`, after `matchCohort(...)` returns raw membership for a flag-level cohort property, invert `matches` when `prop.operator === 'not_in'` before the existing `if (!matches)` check. Optionally handle `prop.negation === true` if confirmed as a valid top-level encoding.",
      "risk": "low"
    },
    "closeProposal": {
      "propose": false,
      "category": "none",
      "confidence": 0,
      "reason": "",
      "canonicalUrl": ""
    }
  },
  "relatedItems": [],
  "duplicate": {
    "duplicate": false,
    "score": 0,
    "reason": "no related duplicate candidate found"
  },
  "security": {
    "sensitive": false,
    "reasons": []
  },
  "snapshotHash": "6bf48c3957b7c67efdc2391ff7aa6e12c95971d13f4b9a70c7c814af6cdb1ae8",
  "piCalls": 3,
  "runId": 28163619464,
  "runUrl": "https://github.com/PostHog/posthog-js/actions/runs/28163619464"
}
```
