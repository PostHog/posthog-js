# PostHog Configuration for Local Testing
# Copy this file to .env and fill in your actual values

# Your PostHog project API key (starts with phc_)
POSTHOG_PROJECT_KEY=phc_your_project_key_here

# Your PostHog personal API token (starts with phx_)
POSTHOG_PERSONAL_TOKEN=phx_your_personal_token_here

# PostHog host (default: http://localhost:8010)
POSTHOG_HOST=http://localhost:8010

# Test user ID (optional, will generate random if not provided)
TEST_USER_ID=test-user-123

# Flag key to test for dependencies demo (optional, defaults to test-dependent-flag)
FLAG_KEY=test-dependent-flag

# Flag key for remote config demo (optional, defaults to unencrypted-remote-config-setting)
REMOTE_CONFIG_FLAG_KEY=unencrypted-remote-config-setting