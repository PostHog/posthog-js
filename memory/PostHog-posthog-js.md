# PostHog Watcher memory

...<older entries truncated>

ts__/posthog-node.spec.ts`, `packages/node/CHANGELOG.md`
- Findings: Shared core now rejects successful-write responses with status >= 300; core flush tests explicitly cover 300, 302, and 304 as failures while preserving 302 handling for required feature-flag reads.; @posthog/core 1.53.3 changelog attributes the final-2xx successful-write change to PR #4922 and explicitly says immediate-call error handling was preserved.; PostHogCoreStateless.sendImmediate() awaits sendBatch(), emits an error in its catch block, and does not rethrow.; PostHog._sendPreparedEvent() catches errors from both preparation and sending, logs them, and resolves; this is a second suppression layer for Node immediate methods.; The existing Node immediate-delivery test currently asserts that HTTP 300, 302, 304, and 503 emit an error without rejecting captureImmediate().; Immediate work is added to the shared PromiseQueue; shutdown joins that queue and separately catches PostHog fetch errors, so changing rejection propagation requires regression coverage for shutdown rather than only changing the two catches.
- Fix assessment: The code change could be small, but it changes established observable promise behavior and interacts with the pending-work/shutdown path. Maintainer agreement is needed on whether all immediate methods should reject transport failures and on the intended behavior for callers that do not handle those rejections.

## 2026-09-15T12:38:25.285Z
- Item: issue #4968 — Canvas recording enabled, but rrweb emits zero canvas mutations (posthog-js 1.428.6)
- Conclusion: Likely a canvas FPS capture compatibility/failure-path issue, distinct from a playback-only canvas replacement fix.
- Labels: feature/replay, javascript
- URL: https://github.com/PostHog/posthog-js/issues/4968
- Relevant files: `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/rrweb/rrweb/src/record/observers/canvas/canvas-manager.ts`, `packages/rrweb/rrweb/src/record/observers/canvas/2d.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/types/src/index.ts`, `packages/browser/playwright/mocked/session-recording/canvas-resolution.spec.ts`, `packages/browser/CHANGELOG.md`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: The SDK converts enabled remote canvas configuration into `recordCanvas: true` and `sampling.canvas: canvasFps`; the reported `canvasFps: 3` therefore selects rrweb's numeric FPS snapshot path.; The numeric FPS path returns immediately, with no canvas mutation observer or frame loop, when `OffscreenCanvas` is absent. rrweb's public type documentation explicitly says numeric canvas sampling is supported only where `OffscreenCanvas` is supported.; The same FPS path creates an inline encoding worker and silently disables canvas snapshotting if worker construction fails or its error handler fires. DOM recording is independent of this path.; The browser Playwright canvas-resolution test documents a pre-existing case where the FPS pipeline produces no canvas mutations under headless WebKit even though `OffscreenCanvas`, image-bitmap operations, and the worker are present; the test is skipped there.; The repository changelog records an intentional behavior change to skip FPS canvas recording on browsers without `OffscreenCanvas` support, specifically Safari before 16.4.; Initial full snapshots can serialize a nonblank 2D canvas as `rr_dataURL`; source-9 incremental events are not the only possible recorded canvas pixels.; The direct rrweb control is not yet independently reproducible from the issue because its complete options, browser, timing, and CSP are absent. If it used numeric sampling, it exercises the same FPS path; if it truly used default sampling, it should instead exercise the 2D method-patching observer.
- Fix assessment: A safe fix depends on distinguishing unsupported WebKit behavior, a blocked/failed inline worker, and a regression in the default per-call observer. A fallback between capture modes would change fleet-wide replay CPU, payload volume, and canvas-pixel capture behavior, so it needs a real-browser reproduction and regression test first.

## 2026-09-15T14:38:03.012Z
- Item: issue #4969 — Session replay: `hookSetter` defers on the zone.js-patched `setTimeout`, driving Angular change detection into an endless loop (tab at 100 % CPU)
- Conclusion: Confirmed Session Replay bug: rrweb's deferred input-setter callback can schedule zone.js-patched timers from Angular change detection, creating an unbounded change-detection loop.
- Labels: feature/replay, web, performance, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4969
- Relevant files: `packages/rrweb/rrweb/src/utils.ts`, `packages/rrweb/rrweb/src/record/observer.ts`, `packages/rrweb/rrweb/test/util.test.ts`, `packages/rrweb/rrweb/test/record.test.ts`, `packages/rrweb/utils/src/index.ts`, `packages/rrweb/record/src/index.ts`, `packages/browser/src/entrypoints/recorder.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `hookSetter()` in `packages/rrweb/rrweb/src/utils.ts` currently invokes an unqualified `setTimeout(..., 0)` for every hooked assignment before it forwards to the original setter synchronously.; `initInputObserver()` in `packages/rrweb/rrweb/src/record/observer.ts` installs `hookSetter()` on `HTMLInputElement.value`, `HTMLInputElement.checked`, `HTMLSelectElement.value`, `HTMLTextAreaElement.value`, `HTMLSelectElement.selectedIndex`, and `HTMLOptionElement.selected`, matching the report.; The input observer's `isBlocked()` check and `lastInputValueMap` deduplication occur inside the deferred mock-event handler, after `hookSetter()` has already scheduled the timer. This supports the reporter's finding that `blockSelector` does not prevent the loop.; Existing `hookSetter()` tests cover deferred callback execution, preservation of native setter behavior, and invalid receivers, but do not test zone.js-patched timer avoidance.; `packages/rrweb/utils/src/index.ts` already detects Zone's unpatched globals through `Zone.__symbol__(key)`, but its private helper is typed and used only for unpatched DOM constructor/prototype lookup.; `@posthog/rrweb-record` re-exports `record` from `@posthog/rrweb`, and the browser recorder entrypoint imports it, so this vendored implementation is on the PostHog Session Replay delivery path.; Replay incident-risk review: this is recorder-side code but not the known rrweb serialization failure mode. The relevant regression should assert real-browser input-capture behavior and task scheduling, since a capture-fidelity regression could otherwise be silent.
- Fix assessment: The code change is likely small and localized, but it modifies a globally installed recorder setter hook. It needs a browser-level regression test for Angular/zone timer semantics plus verification that programmatic input capture, synchronous native setter behavior, fallback behavior without Zone, and iframe/window handling remain intact.

## 2026-09-15T16:58:41.683Z
- Item: issue #4974 — Session replay upload rate declined across 1.428 to 1.433 while a pinned 1.404.1 surface in the same project was unaffected
- Conclusion: Likely explained by the intentional interaction-gated replay flush behavior, but affected-session debug properties are needed to confirm whether this is expected suppression or an iOS interaction-detection regression.
- Labels: feature/replay, javascript, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4974
- Relevant files: `packages/browser/CHANGELOG.md`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/extensions/replay/session-recording.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/entrypoints/external-scripts-loader.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: The changelog records replay flush-hold diagnostics in the affected release range: an epoch without user interaction retains snapshots and uploads nothing while `$recording_status` remains `active`; it exposes `$sdk_debug_replay_flush_hold_reason` specifically to distinguish this state.; `LazyLoadedSessionRecording.get sdkDebugProperties()` reports both `$recording_status` and `$sdk_debug_replay_flush_hold_reason`; its comments explicitly state that `active` does not mean uploading.; `_setFlushHold()` prevents uploads until interaction for `no_interaction_since_recording_started` or `no_interaction_since_session_rotated`; `_releaseHoldAndFlush()` releases it on a qualifying interaction, event-trigger match, or explicit override.; `posthog.startSessionRecording()` with no argument only sets `disable_session_recording: false`; only explicit override options invoke recorder override methods. `startSessionRecording({ sampling: true })` reaches `overrideSampling()`, which releases the hold.; The replay tests explicitly cover an active fresh-start recorder that does not upload until interaction and assert that the hold reason is reported; they also assert that explicit overrides release the hold.; The lazy-load incident guidance identifies version skew and replay flush semantics as high-risk areas. In particular, changing this hold can move recording volume fleet-wide and must retain rotation/idle volume invariants rather than receiving a speculative suppression-removal fix.; The external script loader supports strict versioned recorder paths and legacy/fallback paths. The reporter's `strict_script_versioning: true` workaround is consistent with keeping the lazy-loaded recorder aligned to the pinned core, but it does not by itself identify the failing recorder behavior.
- Fix assessment: The available evidence strongly suggests a deliberately held buffer rather than a confirmed transport failure. If that is not the explanation, the remaining hypothesis is an interaction-detection failure in Mobile Safari or Facebook's in-app browser. Either path affects recording volume and lazy-loaded recorder behavior, so a broad or speculative fix would be unsafe.

## 2026-09-15T23:42:04.613Z
- Item: issue #4978 — React Native: support bootstrapping an existing session ID
- Conclusion: Valid React Native feature gap; initial bootstrap support is feasible, but reset and runtime replacement require separate public-API decisions.
- Labels: enhancement, react-native, feature/mobile, feature, feature/replay, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4978
- Relevant files: `packages/core/src/types.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/vendor/uuidv7.ts`, `packages/core/src/__tests__/posthog.sessions.spec.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/test/posthog.spec.ts`, `packages/react-native/test/session-replay-request-headers.spec.ts`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/browser/src/sessionid.ts`, `packages/browser/src/__tests__/sessionid.test.ts`, `packages/browser/src/__tests__/posthog-core.reset.test.ts`, `packages/types/src/posthog-config.ts`
- Findings: `PostHogCoreOptions.bootstrap` declares only `distinctId`, `isIdentifiedId`, feature flags, and feature-flag payloads; `PostHogCore.setupBootstrap()` implements only those fields.; `PostHogCore.getSessionId()` generates and persists a UUIDv7 when no current session exists, while `enrichProperties()` assigns `$session_id` after caller properties. This confirms that passing `$session_id` to `capture()` cannot work.; React Native invokes `setupBootstrap(options)` during initialization after applying its restart-session clearing behavior and before setting `_isInitialized = true`; there is currently no session bootstrap handling in that path.; React Native's native initialization calls `getSessionId()` and passes the result to its plugin. Android parses it as a UUID and calls `PostHogSessionManager.setSessionId`; iOS calls `PostHogSessionManager.shared.setSessionId`. Existing replay bridge tests already assert that native setup receives the JS session ID.; The web `SessionIdManager` already invokes `setBootstrapSessionId()` from `config.bootstrap.sessionID`. It derives the UUIDv7 timestamp, rejects IDs more than one minute in the future, and rejects a bootstrapped session beyond the 24-hour maximum when it is checked.; Core vendors a UUID implementation that can parse UUIDs and report their UUID version, so validation primitives exist in the shared package, though core does not currently expose a timestamp parser equivalent to browser's `uuid7ToTimestampMs`.; React Native's public `reset()` accepts `PostHogPersistedProperty[]` and delegates to core reset; it then performs native identity/reset work. This does not match the browser's `ResetOptions` API, so reset bootstrap parity is not a one-line extension.; `resetSessionId()` only clears the JS session state and ends the native session when applicable; it offers no supplied-ID replacement path. Its lifecycle behavior is relevant to the replay-rotation risk noted in #3359.
- Fix assessment: The initialization-only mechanism is localized, but this issue also requests decisions on two additional public APIs. Per contributor guidance, the API shape must be agreed before implementation. Session timestamp validation, persisted-session precedence, and native replay synchronization need focused tests to avoid session/replay divergence.

## 2026-09-15T23:46:39.751Z
- Item: issue #4978 — Feature Request: React Native - support bootstrapping an existing session ID
- Conclusion: Confirmed React Native feature gap; initialization-time session bootstrap is plausible, but reset parity and runtime session replacement require separate public-API and replay-lifecycle decisions.
- Labels: enhancement, feature, feature/replay, feature/mobile, react-native, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4978
- Relevant files: `packages/core/src/types.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/vendor/uuidv7.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/browser/src/sessionid.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/__tests__/sessionid.test.ts`, `packages/types/src/posthog-config.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `PostHogCoreOptions.bootstrap` currently contains only identity and feature-flag fields; `PostHogCore.setupBootstrap()` likewise has no session-ID handling. Consequently, `PostHogOptions`, which extends core options, does not type or implement `bootstrap.sessionID`.; `PostHogCore.enrichProperties()` assigns `$session_id: this.getSessionId()` after caller properties, confirming that passing `$session_id` to `capture()` cannot override the managed ID.; `PostHogCore.getSessionId()` generates a UUIDv7 when the stored session is absent, idle-expired, or older than the 24-hour maximum, and `resetSessionId()` only clears stored session values. Core's vendored UUID implementation can parse UUIDs and report their version, but it does not expose the browser timestamp helper.; React Native clears persisted session state at initialization unless `enablePersistSessionIdAcrossRestart` is enabled, then calls `setupBootstrap(options)` before marking the client initialized. A new bootstrap path therefore needs an explicit persisted-session precedence rule when restart persistence is enabled.; React Native's native-plugin initialization calls `getSessionId()` and passes it into native setup. Android parses it as a UUID and calls `PostHogSessionManager.setSessionId`; iOS calls `PostHogSessionManager.shared.setSessionId`, so a correctly bootstrapped JS session can be propagated during initial native setup.; React Native's `getSessionId()` detects a changed JS session ID and calls native `endSession()` followed by `startSession(sessionId)`. This makes post-initialization replacement materially different from bootstrap and relevant to replay continuity.; The browser `SessionIdManager` already supports `bootstrap.sessionID`: it parses the UUIDv7 timestamp, rejects IDs more than one minute in the future, stores the UUID timestamp as the session start, and rejects sessions beyond the 24-hour maximum at session evaluation. Browser `reset()` also accepts `ResetOptions.bootstrap`; React Native `reset()` instead accepts a persisted-properties array.; The replay incident guidance identifies session rotation as a medium-risk area because rotation can affect recording continuity and recording volume. This initialization-only request does not itself establish the idle-tab over-recording failure mode, but tests must prove native replay does not rotate independently or use a different ID.
- Fix assessment: The narrow initialization mechanism is localized, but it introduces a public configuration option and requires an agreed precedence policy for persisted sessions. Reset parity and runtime replacement have distinct API shapes and replay/native lifecycle effects, so they should not be bundled into a speculative small PR.

## 2026-09-17T14:34:02.846Z
- Item: issue #5011 — posthog.capture returns 200 but events are not indexed in current browser reproduction
- Conclusion: Plausible browser SDK ingestion bug, but the report does not yet isolate an SDK-side difference from an ingestion-side treatment of the enriched SDK event.
- Labels: feature/product-analytics, javascript, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/5011
- Relevant files: `packages/browser/src/posthog-core.ts`, `packages/browser/src/request-dispatch.ts`, `packages/browser/src/request.ts`, `packages/browser/src/utils/request-router.ts`, `packages/browser/src/__tests__/posthog-core.wire-envelope.test.ts`, `packages/browser/src/__tests__/posthog-core-also.test.ts`, `packages/browser/src/__tests__/request.test.ts`
- Findings: `PostHog.capture()` can intentionally stop before dispatch when capture is disabled, the browser is classified as a bot, client rate limiting applies, or `before_send` rejects/errors. Those paths cannot explain a request that actually reached the collector, but the full deployed configuration is needed to rule out transformations.; With `request_batching: false`, `capture()` constructs a POST request and sends it through the retriable request path rather than waiting for the normal batch queue. Its default analytics target is `/e/`, or a remote-config analytics endpoint, and `rewriteRequestPath` is applied by `RequestRouter` to that API endpoint.; For analytics requests with `timestampMode: 'capture-body'`, `request.ts` serializes the SDK event into `{ api_key, batch, sent_at }`; `api_key` is taken from the event's `properties.token`. Existing request tests explicitly cover this envelope for `/e/`, `/i/v0/e/`, and `/batch/`.; The SDK event is not identical to the direct example even when the outer envelope matches. `calculateEventProperties()` adds the project token, SDK/page/device properties, session and window IDs, identity state, and `$process_person_profile`, among other persisted properties.; Existing tests verify the browser-side request route and decoded wire envelope using mocked HTTP responses. They do not exercise a live ingestion/indexing assertion, so they cannot confirm or refute the reported collector-versus-indexing behavior.
- Fix assessment: There is no identified faulty SDK branch or demonstrated on-wire mismatch. Changing capture enrichment, request framing, or transport behavior without an exact failing SDK payload would be speculative and could affect all browser event ingestion.
