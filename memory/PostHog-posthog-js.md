# PostHog Watcher memory

...<older entries truncated>

the generated Android Gradle files are necessary to confirm which plugin version the affected project actually resolves.
- Fix assessment: A dependency-scope change in the PostHog Android Gradle plugin cannot be verified from this repository, and blindly changing or downgrading the injected plugin could break the Android native-symbol and release-mode behavior introduced in 4.66.0. First verify the generated classpath and the upstream plugin's transitive dependency metadata.

## 2026-08-29T14:34:32.398Z
- Item: issue #4688 — Session Replay leaks DOM nodes with certain payloads
- Conclusion: Confirmed Session Replay memory-leak bug in the vendored rrweb MutationBuffer cleanup path.
- Labels: feature/replay, needs-to-be-contributed-back, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4688
- Relevant files: `packages/rrweb/rrweb/src/record/mutation.ts`, `packages/rrweb/rrweb/test/record/memory-leaks.test.ts`, `packages/browser/src/entrypoints/posthog-recorder.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`
- Findings: `MutationBuffer.processBufferedMutations()` constructs normalized `texts`, `attributes`, `removes`, and `adds`, then returns when all four are empty.; The existing cleanup is located after that early return and clears `texts`, `attributes`, `removes`, `addedSet`, `movedSet`, `droppedSet`, `removesSubTreeCache`, and `movedMap`—several of these collections can strongly retain DOM nodes.; `mapRemoves` is drained before payload construction, but the early return leaves the other node-containing collections intact.; The existing rrweb memory-leak suite covers recorder and iframe-buffer teardown, but no inspected test covers an otherwise-normal MutationBuffer batch that normalizes to an empty payload.; The browser replay recorder entrypoint imports `@posthog/rrweb-record`, so this rrweb recorder path is used by the Session Replay bundle.
- Fix assessment: The fault is localized and deterministic: cleanup is skipped solely because it is placed after an empty-payload return. Moving the existing cleanup before the return preserves the rule that empty payloads are not emitted while releasing references from the completed batch.
- PR: https://github.com/PostHog/posthog-js/pull/4689

## 2026-08-30T21:39:12.736Z
- Item: issue #4693 — recordCrossOriginIframes: child contributes no events when the iframe redirects through the parent's origin (LTI/OIDC)
- Conclusion: Confirmed session-replay bug in cross-origin iframe relay after an iframe temporarily becomes same-origin during navigation.
- Labels: feature/replay, javascript, team/client-libraries, web
- URL: https://github.com/PostHog/posthog-js/issues/4693
- Relevant files: `packages/rrweb/rrweb/src/record/iframe-manager.ts`, `packages/rrweb/rrweb/src/record/index.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/rrweb/test/record/cross-origin-iframes.test.ts`, `packages/browser/src/entrypoints/lazy-recorder.ts`, `packages/browser/src/entrypoints/posthog-recorder.ts`
- Findings: `record()` sets `passEmitsToParent` when an iframe cannot access `window.parent.document`; `wrappedEmit()` then sends recorder events to the parent with `window.parent.postMessage(...)` in `packages/rrweb/rrweb/src/record/index.ts`.; During serialization, `IframeManager.addIframe()` stores `iframeEl.contentWindow` in `crossOriginIframeMap`. `handleMessage()` depends on this map to associate a relayed event's `message.source` with the iframe; if no mapping is found, it returns without emitting the event.; When a same-origin iframe document is attached, `IframeManager.attachIframe()` registers a `pagehide` handler. That handler removes the intermediate document from the mirror and calls `crossOriginIframeMap.delete(iframeEl.contentWindow)`.; The iframe is only re-added to `crossOriginIframeMap` when the iframe element is serialized via `addIframe()`. A later cross-origin navigation cannot be serialized through the same-origin iframe path, so it does not restore the mapping before the final child relays its full snapshot.; `removeIframeById()` already removes the window mapping when the iframe element is actually detached, making that the appropriate lifecycle point for disposal rather than an intermediate document navigation.; Existing `cross-origin-iframes.test.ts` covers initial cross-origin relay, same-origin iframe navigation, and full-snapshot reattachment, but does not cover one iframe navigating cross-origin → same-origin → cross-origin.
- Fix assessment: The loss is localized to the pagehide cleanup of a relay mapping that remains necessary for the iframe element after navigation. The public API and cross-bundle contract need not change, but this is recorder serialization code, so a real-browser regression test is required to prevent silent replay corruption.

## 2026-08-30T22:52:19.276Z
- Item: issue #4694 — Global Privacy Control support as part of Do Not Track
- Conclusion: Valid, narrowly scoped enhancement: `respect_dnt` currently ignores Global Privacy Control, and the requested behavior fits the existing consent mechanism.
- Labels: enhancement, feature, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4694
- Relevant files: `packages/browser/src/consent.ts`, `packages/browser/src/__tests__/consent.test.ts`, `packages/browser-common/src/utils/globals.ts`, `packages/types/src/posthog-config.ts`, `packages/browser/src/posthog-core.ts`
- Findings: `ConsentManager._getDnt()` returns `false` unless `respect_dnt` is enabled, so the requested behavior remains explicitly opt-in.; When enabled, `_getDnt()` currently checks `navigator.doNotTrack`, legacy `navigator.msDoNotTrack`, and `window.doNotTrack`, but no inspected source references `navigator.globalPrivacyControl`.; The DNT result is used by the `consent` getter to return `DENIED`, which in turn makes the SDK report capture as opted out.; `isYesLike` already recognizes both boolean `true` and string/number truthy DNT representations; GPC's boolean `true` is compatible with the existing evaluation helper.; Existing consent tests already establish that `navigator.doNotTrack = '1'` is respected only with `respect_dnt: true`, providing the appropriate test location and pattern.
- Fix assessment: The behavior is localized to the browser consent manager and can reuse its existing DNT evaluation semantics. No new configuration, persistence behavior, or cross-package API is required.

## 2026-08-31T02:16:20.581Z
- Item: issue #4695 — Capture attrs don't come from the nearest parent
- Conclusion: Confirmed autocapture precedence bug: an ancestor's capture attribute overwrites the same attribute on the clicked element or its closest attributed ancestor.
- Labels: feature/autocapture, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4695
- Relevant files: `packages/browser/src/autocapture.ts`, `packages/browser/src/__tests__/autocapture.test.ts`, `packages/browser-common/src/utils/general-utils.ts`, `packages/react-native/src/autocapture.tsx`
- Findings: `autocapturePropertiesForElement` builds `targetElementList` in target-to-ancestor order.; For every element in that order, `getAugmentPropertiesFromElement` extracts `data-ph-capture-attribute-*` attributes and `extend(autocaptureAugmentProperties, augmentProperties)` merges them.; `extend` assigns every defined source value over the destination's current value. Consequently, an ancestor's value for a duplicate key overwrites the closer target/descendant value.; The existing browser tests verify capture attributes from a target and parent only when their keys differ; there is no test defining the same key at multiple DOM levels.; The React Native implementation currently also overwrites accumulated capture-property keys while walking from target to ancestors, but this report is specifically for the browser SDK.
- Fix assessment: The faulty precedence is localized to the browser autocapture attribute merge. Preserving the first valid value for each key changes only duplicate-key behavior and can be protected by a small regression test.
- PR: https://github.com/PostHog/posthog-js/pull/4696

## 2026-08-31T14:40:48.271Z
- Item: issue #4702 — feat(browser): support reuseAnonymousId during identify
- Conclusion: Valid browser SDK feature request; the current identify flow always merges the previous anonymous ID and also forwards it to the feature-flags request.
- Labels: enhancement, feature/product-analytics, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4702
- Relevant files: `packages/types/src/posthog-config.ts`, `packages/browser/src/types.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/posthog-featureflags.ts`, `packages/browser/src/__tests__/posthog-core.identify.test.ts`, `packages/browser/src/__tests__/featureflags.test.ts`, `packages/browser/functional_tests/identify.test.ts`, `packages/browser/functional_tests/feature-flags.test.ts`
- Findings: `packages/browser/src/types.ts` defines the browser config as an extension of the shared `@posthog/types` `PostHogConfig`; neither inspected config type currently defines `reuseAnonymousId`.; `defaultConfig` in `packages/browser/src/posthog-core.ts` has no reuse-anonymous-ID setting, so an explicit default of `false` is needed to preserve the current behavior.; During an anonymous-to-identified transition, `PostHog.identify()` captures `$identify` with `distinct_id: new_distinct_id` and `$anon_distinct_id: previous_distinct_id`, then calls `featureFlags.setAnonymousDistinctId(previous_distinct_id)`.; `PostHogFeatureFlags._callFlagsEndpoint()` serializes its stored anonymous ID as `$anon_distinct_id` in the `/flags/?v=2` request, so only changing the `$identify` event would leave feature-flag evaluation with merge semantics.; The identity transition, user-state update, person-property handling, and feature-flag reload are separate from setting `$anon_distinct_id`; they can remain intact when the option is enabled.; Existing unit and functional tests already assert the default `$identify` payload and the anonymous ID forwarded to feature-flag requests, providing direct locations for enabled and default-off regression coverage.
- Fix assessment: The behavior is localized to the identify transition and its feature-flag handoff. A default-off boolean preserves existing users, and existing focused tests cover both affected payloads.

## 2026-09-01T12:02:49.182Z
- Item: issue #4718 — `IPostHog` interface is missing `captureException` methods
- Conclusion: Confirmed TypeScript API-surface bug: request-scoped @posthog/next clients expose exception capture at runtime, but IPostHog omits both exception-capture methods.
- Labels: node, frameworks/next-js, feature/error-tracking, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4718
- Relevant files: `packages/node/src/types.ts`, `packages/node/src/client.ts`, `packages/next/src/server/createPostHog.ts`, `packages/next/src/server/getPostHog.ts`, `packages/next/src/server/captureRequestError.ts`, `packages/node/src/__tests__/metrics.spec.ts`
- Findings: `IPostHog` in `packages/node/src/types.ts` declares `capture` and `captureImmediate`, but declares neither `captureException` nor `captureExceptionImmediate`.; `PostHogBackendClient` explicitly implements `IPostHog` and defines `captureException(error, distinctId?, additionalProperties?, uuid?, flags?): void` and `captureExceptionImmediate(error, distinctId?, additionalProperties?, flags?): Promise<void>` in `packages/node/src/client.ts`.; `createPostHog().getPostHog` is explicitly typed as `() => Promise<IPostHog>` in `packages/next/src/server/createPostHog.ts`, which produces the reported TS2339 error for consumers.; `withRequestContext` in `packages/next/src/server/getPostHog.ts` proxies every callable member other than `withContext`, so both concrete exception-capture methods are available and request-context-wrapped at runtime.; `packages/next/src/server/captureRequestError.ts` defines a local `PostHogExceptionCaptureClient` solely to type its call to `captureExceptionImmediate`, corroborating that the shared interface is incomplete.; `packages/node/src/__tests__/metrics.spec.ts` already demonstrates the repository's pattern for verifying that a concrete client API is reachable through `IPostHog`.
- Fix assessment: The mismatch is localized to the public interface and the exact concrete signatures already exist. No runtime proxy or event-capture behavior needs to change.
- PR: https://github.com/PostHog/posthog-js/pull/4719

## 2026-09-01T14:07:00.601Z
- Item: issue #4724 — Issues from removal of source map content
- Conclusion: Confirmed compatibility regression for consumers whose Webpack configuration processes dependency source maps.
- Labels: javascript, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4724
- Relevant files: `packages/browser/package.json`, `packages/browser/scripts/strip-sourcemap-sources-content.js`, `packages/browser/src/__tests__/strip-sourcemap-sources-content.test.ts`, `packages/browser/rollup.config.mjs`, `packages/browser/CHANGELOG.md`
- Findings: `packages/browser/package.json` runs `scripts/strip-sourcemap-sources-content.js` during `prepack`, so every npm package removes `sourcesContent` from maps immediately before publishing.; The stripping script deliberately preserves the `.js.map` files and their `sources` entries, but deletes `sourcesContent` in place. Its own comment states that downstream bundlers chain through the maps.; The package's published files are limited to built outputs and selected subdirectories; `src/` is not included. Consequently, source-map consumers that resolve a map's source paths from the installed package cannot find PostHog TypeScript source files.; Rollup produces source maps for browser entrypoints (`sourcemap: true`), so this affects normal distributed browser bundles rather than an unused artifact.; The current changelog records #4520 as intentionally dropping npm `sourcesContent`, reducing the unpacked package from 40.8 MB to 17.4 MB. This confirms the report is a consequence of an active packaging decision, not a stale artifact.
- Fix assessment: The immediate cause is clear, but no one-line change preserves both the package-size reduction and valid chained maps. Restoring all embedded contents increases package size; publishing/rebasing source paths must also account for bundled dependency sources and avoid exposing build-machine absolute paths. Disabling maps would avoid the resolution failures but regress source-map support.

## 2026-09-01T21:05:06.956Z
- Item: issue #4734 — RN Android session replay: expose gesture-aware capture deferral (Android counterpart of screenshotModeBackgroundCapture)
- Conclusion: Valid React Native Android session-replay enhancement, but it is blocked on a concrete posthog-android API for gesture-aware/background screenshot capture.
- Labels: react-native, Android, feature/replay, enhancement, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4734
- Relevant files: `packages/react-native/src/types.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/android/build.gradle`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/test/java/com/posthogreactnativeplugin/PosthogReactNativePluginModuleTest.kt`, `packages/react-native/test/session-replay-request-headers.spec.ts`, `packages/react-native-plugin/CHANGELOG.md`
- Findings: `PostHogSessionReplayConfig` exposes `screenshotModeBackgroundCapture`, explicitly documents it as iOS-only, and already exposes an Android-specific `verifyScreenshotMaskAlignment` option.; `packages/react-native/src/posthog-rn.ts` destructures replay settings and sends `sdkReplayConfig` in the native setup envelope, so the JS-to-native configuration path already exists.; The Android bridge unconditionally assigns `sessionReplayConfig.screenshot = true`; it maps masking, throttle delay, sample rate, log capture, and `verifyScreenshotMaskAlignment`, but contains no gesture-deferral or background-capture mapping.; The iOS bridge reads `sdkReplayConfig["screenshotModeBackgroundCapture"]` and assigns it to `config.sessionReplayConfig.screenshotModeBackgroundCapture`, verifying the #3552-style precedent.; The RN plugin currently compiles against `com.posthog:posthog-android:3.61.0`. No inspected repository source identifies an Android gesture-deferral/background-capture property available in that dependency, so an exact bridge assignment cannot be safely specified yet.; Existing tests snapshot the full JS native setup envelope and Android has focused mapping tests for `verifyScreenshotMaskAlignment`; these are the appropriate regression-test locations for a future concrete option.
- Fix assessment: The RN bridge work will be localized once the upstream Android option, semantics, and minimum supported posthog-android version are known, but implementing it now would require inventing an API and behavior. Capture deferral also changes screenshot timing, so replay completeness must be tested rather than assumed.

## 2026-09-07T13:35:46.469Z
- Item: issue #4825 — Session replay: idle tabs still produce 2-3 day recordings under one session id on 1.425.1
- Conclusion: Likely confirmed Session Replay idle/rotation bug with a specific event-ordering gap; it should not be closed as already fixed.
- Labels: feature/replay, javascript, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4825
- Relevant files: `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/sessionid.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording-rotation-invariants.test.ts`, `packages/browser/CHANGELOG.md`
- Findings: `SessionIdManager.checkAndGetSessionAndWindowId()` enforces `SESSION_LENGTH_LIMIT_MILLISECONDS` even for read-only calls, but it only runs when called; there is no separate 24-hour timer.; The session manager does schedule an idle timeout at `sessionTimeoutMs * 1.1`, but browser tab suspension can delay that callback. Its forced-idle path resets the session and waits for a subsequent session-ID check before the recorder restarts.; In `_updateWindowAndSessionIds()`, a non-interactive event observed after the recorder idle threshold sets `_isIdle`, emits `sessionIdle`, and calls `_flushBuffer()` before calling `checkAndGetSessionAndWindowId(!isUserInteraction, event.timestamp)`.; The current test titled `rotates a confirmed-idle session at the 24 hour session cap, into a held epoch` verifies that the recorder adopts a new session ID after a later non-interactive event, but does not verify that no old-session buffer was flushed before that check.; The repository's replay incident guidance identifies this exact rotation/idle/flush area as a prior over-recording and multi-day-recording incident class; changes must preserve zero-upload behavior for untouched tabs and prevent flushing events beyond the session age cap.; `posthog-js` 1.427.3 includes #4788, a related fix that resets the replay idle clock on session-ID rotation so a rotation-born recording does not start hours early. The reported version, 1.425.1, predates that release, but the changelog does not establish that #4788 fixes this flush-before-cap-check path.
- Fix assessment: The likely fault is localized, but changing the ordering of idle detection, lifecycle-marker routing, flushing, and session rotation is replay billing-sensitive. A fix must account for synchronous rrweb re-entry and preserve both old-session ending markers and held rotation-born buffers.
