# PostHog Watcher memory

...<older entries truncated>

o not appear to implement feedback widget/user feedback support.
- Labels: enhancement, react-native, feature/mobile, feature/surveys
- URL: https://github.com/PostHog/posthog-js/issues/3814
- Relevant files: `packages/react-native/src/surveys/PostHogSurveyProvider.tsx`, `packages/react-native/src/surveys/components/Surveys.tsx`, `packages/react-native/src/surveys/getActiveMatchingSurveys.ts`, `packages/react-native/src/surveys/index.ts`, `packages/react-native/src/index.ts`, `packages/browser/src/extensions/surveys.tsx`, `packages/core/src/types.ts`, `packages/react-native/CHANGELOG.md`
- Findings: `packages/core/src/types.ts` defines `SurveyType.Widget`, so the shared survey model includes feedback/widget-style surveys.; `packages/browser/src/extensions/surveys.tsx` implements `FeedbackWidget` and handles `SurveyType.Widget` display logic for tab and selector widgets.; `packages/react-native/src/surveys/PostHogSurveyProvider.tsx` loads surveys and renders `SurveyModal`, but `shouldShowModal` currently only allows `SurveyType.Popover`.; `PostHogSurveyProvider.tsx` contains a commented-out `useFeedbackSurvey` hook that looks intended to find `SurveyType.Widget` surveys by `appearance.widgetSelector`, but it is not active or exported.; `packages/react-native/src/surveys/index.ts` exports `PostHogSurveyProvider`, `SurveyModal`, and `Questions`, but does not export a feedback-specific hook/component.; `packages/react-native/CHANGELOG.md` shows RN survey support exists, but I did not find evidence that feedback widget/user feedback support specifically is implemented.
- Fix assessment: A small code path is hinted at by the commented hook, but the issue does not define the intended React Native UX/API. Web widget semantics like tab/selector do not translate directly to native apps, so implementing without product direction risks adding the wrong public API.

## 2026-07-06T09:44:17.490Z
- Item: issue #3813 — Stop all integrations like session replay and autocapture on opt-out
- Conclusion: Valid React Native enhancement/privacy issue; opt-out currently persists consent and drops events, but does not stop integration work like session replay or autocapture hooks.
- Labels: enhancement, react-native, feature/mobile, feature/replay, feature/autocapture
- URL: https://github.com/PostHog/posthog-js/issues/3813
- Relevant files: `packages/react-native/src/posthog-rn.ts`, `packages/core/src/posthog-core-stateless.ts`, `packages/react-native/src/PostHogProvider.tsx`, `packages/react-native/src/autocapture.tsx`, `packages/react-native/src/hooks/useNavigationTracker.ts`, `packages/react-native/src/frameworks/wix-navigation.ts`, `packages/react-native/src/error-tracking/index.ts`, `packages/react-native/src/error-tracking/utils.ts`, `packages/react-native/src/optional/OptionalPlugin.ts`, `packages/react-native/test/posthog.spec.ts`, `packages/react-native/test/session-replay-rearm.spec.ts`
- Findings: `packages/react-native/src/posthog-rn.ts` overrides `optIn()` and `optOut()`, but both only call the core implementation and wait for storage persistence; there are no session replay/autocapture start or stop calls.; `packages/core/src/posthog-core-stateless.ts` stores `PostHogPersistedProperty.OptedOut` and drops queued/immediate events when `optedOut` is true, so integration-generated events are not sent while opted out.; RN session replay enablement uses `_isEnableSessionReplay()` as `!this.isDisabled && enableSessionReplay`; it does not check `optedOut`. Startup calls `startSessionReplay()` after initialization without an opt-out gate.; Native error tracking enablement similarly checks `isDisabled` but not `optedOut` in `_isAutocaptureNativeErrors()`.; Existing replay controls are available: `_stopSessionRecording()` calls `OptionalReactNativePlugin.stopRecording()`, linked-flag re-evaluation already pauses/resumes recording, and `startSessionRecording(false)` creates a fresh session by resetting the session id.; `PostHogProvider.tsx`, `autocapture.tsx`, and `useNavigationTracker.ts` gate touch/screen autocapture on provider options, not `posthog.optedOut`; capture calls are later dropped by core.; `captureAppLifecycleEvents()` registers an `AppState` listener that continues calling `capture()` for lifecycle events; it does not store an unsubscribe or check `optedOut` before calling capture.; `frameworks/wix-navigation.ts` registers a screen listener without retaining an unsubscribe and does not check `optedOut` before calling `posthog.screen()`.; Error-tracking JS wrappers in `error-tracking/utils.ts` do not return unsubscribe handles, and `OptionalPlugin.ts` exposes replay stop/start methods but not a general native plugin stop method.
- Fix assessment: A small replay-only change would be possible, but the issue asks for all integrations. RN currently has no shared integration lifecycle manager, several JS listeners are not removable, and native crash/error autocapture may need product/API decisions because the optional native plugin has no general stop method.

## 2026-07-06T09:45:52.794Z
- Item: issue #3811 — Rage touch detection
- Conclusion: Valid React Native feature request; RN touch autocapture exists but there is no rage-touch/rageclick detection today.
- Labels: enhancement, react-native, feature/mobile, feature/autocapture, feature/product-analytics
- URL: https://github.com/PostHog/posthog-js/issues/3811
- Relevant files: `packages/react-native/src/PostHogProvider.tsx`, `packages/react-native/src/autocapture.tsx`, `packages/react-native/src/types.ts`, `packages/react-native/test/autocapture.spec.ts`, `packages/browser/src/extensions/rageclick.ts`, `packages/browser/src/autocapture.ts`, `packages/core/src/posthog-core.ts`, `packages/types/src/posthog-config.ts`
- Findings: `packages/react-native/src/PostHogProvider.tsx` only wires touch autocapture through `onTouchEndCapture` when `captureTouches` is enabled, and contains a TODO noting it currently captures touch ends rather than confirmed presses.; `packages/react-native/src/autocapture.tsx` builds the RN element chain and sends `posthog.autocapture('touch', ...)` with `$touch_x` and `$touch_y`; it has no stateful repeated-touch/rage detection.; `packages/react-native/src/types.ts` defines `PostHogAutocaptureOptions` for touch capture, labels, no-capture props, screens, and navigation, but no rage-touch/rageclick option.; `packages/core/src/posthog-core.ts` shows `autocapture()` always enqueues event `$autocapture` with `$event_type`, so RN cannot emit `$rageclick` through that helper as-is.; `packages/browser/src/extensions/rageclick.ts` implements web rage click detection using default thresholds of 30px, 3 clicks, and 1000ms.; `packages/browser/src/autocapture.ts` uses the web `RageClick` detector to emit `$rageclick` during click autocapture, then continues with normal `$autocapture`.; `packages/react-native/test/autocapture.spec.ts` covers normal touch autocapture and no-capture behavior, but there are no tests for rage touch detection.
- Fix assessment: A small detector could be added, but this is a new mobile autocapture behavior and likely public API/event contract. The current RN code only captures touch-end events as `$autocapture`, while web emits a separate `$rageclick`; matching web semantics, thresholds, filtering, and gesture false-positive behavior needs product/API alignment first.

## 2026-07-06T09:48:11.525Z
- Item: issue #3810 — Network bodies for network logs
- Conclusion: Valid React Native mobile feature request; RN currently only wires network timing/telemetry, not network request/response bodies.
- Labels: enhancement, react-native, feature/mobile, team/logs
- URL: https://github.com/PostHog/posthog-js/issues/3810
- Relevant files: `packages/react-native/src/types.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/browser/src/extensions/replay/external/config.ts`, `packages/browser/src/extensions/replay/external/network-plugin.ts`, `packages/types/src/session-recording.ts`, `packages/react-native-plugin/posthog-react-native-plugin.podspec`, `packages/react-native-plugin/android/build.gradle`
- Findings: `packages/react-native/src/types.ts` defines `PostHogSessionReplayConfig.captureNetworkTelemetry?: boolean` as iOS-only, but does not expose request/response body or header capture options.; `packages/react-native/src/posthog-rn.ts` builds `sdkReplayConfig` with masking, log capture, `captureNetworkTelemetry`, screenshot, sample-rate, and throttle settings; it gates only `capturePerformance.network_timing` from remote config and does not read `networkPayloadCapture` body/header settings.; `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift` applies `config.sessionReplayConfig.captureNetworkTelemetry`, `captureLogs`, masking, sample rate, and screenshot settings, but has no request/response body or header capture wiring.; `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt` configures Android session replay screenshots, logcat, masking, throttle delay, and sample rate, but has no network body/header capture wiring in the RN plugin.; The browser SDK has the relevant model: `NetworkRecordOptions` includes `recordHeaders`, `recordBody`, `maskRequestFn`, payload size limits, streaming body options, and denied hosts.; `packages/browser/src/extensions/replay/external/config.ts` implements built-in protections for browser network payload capture: redacts credential-bearing headers, scrubs suspicious payloads, enforces a 1MB body limit, denies known telemetry hosts, and ignores PostHog ingestion paths.; `packages/types/src/session-recording.ts` defines `CapturedNetworkRequest` with `requestBody` and `responseBody`, showing the browser payload shape that RN would likely need to align with.
- Fix assessment: Not a small JS-only change. RN network capture is delegated to native SDK/plugin code, and the current RN API only exposes network timing. Adding bodies safely needs native iOS/Android support, a public RN configuration shape, remote-config mapping, payload/header scrubbing parity, body size limits, and tests across platforms.

## 2026-07-06T09:49:15.358Z
- Item: issue #3809 — Capture additional properties along with autocapture events
- Conclusion: Already implemented in React Native autocapture
- Labels: enhancement, feature/autocapture, react-native, feature/mobile
- URL: https://github.com/PostHog/posthog-js/issues/3809
- Relevant files: `packages/react-native/src/autocapture.tsx`, `packages/react-native/test/autocapture.spec.ts`, `packages/react-native/CHANGELOG.md`, `packages/core/src/posthog-core.ts`, `packages/react-native/src/types.ts`, `packages/react-native/src/PostHogProvider.tsx`
- Findings: `packages/react-native/src/autocapture.tsx` defines `captureAttributePrefix = 'data-ph-capture-attribute-'` and scans React Native element props for keys with that prefix.; `packages/react-native/src/autocapture.tsx` copies non-empty string, number, and boolean `data-ph-capture-attribute-*` values into `autocaptureProperties` using the suffix as the property key.; `packages/react-native/src/autocapture.tsx` passes those properties into `posthog.autocapture('touch', elements, { ...autocaptureProperties, $touch_x, $touch_y })`.; `packages/core/src/posthog-core.ts` merges the provided autocapture properties into the `$autocapture` event payload before enqueueing.; `packages/react-native/test/autocapture.spec.ts` has tests asserting string, number, and boolean `data-ph-capture-attribute-*` values are included in autocapture properties and invalid values are ignored.; `packages/react-native/CHANGELOG.md` notes `posthog-react-native` 4.53.2 / PR #3971: 'Support capturing additional event properties from `data-ph-capture-attribute-*` props in autocapture, matching the browser SDK.'
- Fix assessment: No small PR appears necessary because the requested linked-docs behavior is already present on current main and released in `posthog-react-native` 4.53.2.

## 2026-07-07T09:33:45.447Z
- Item: issue #3796 — React Native session replay should allow wireframe mode
- Conclusion: Valid React Native session replay feature request; RN currently forces screenshot mode and has no supported wireframe mode option.
- Labels: enhancement, feature/replay, react-native, feature/mobile, iOS, Android
- URL: https://github.com/PostHog/posthog-js/issues/3796
- Relevant files: `packages/react-native/src/types.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/src/optional/OptionalPlugin.ts`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/posthog-react-native-plugin.podspec`, `packages/react-native-plugin/android/build.gradle`, `packages/react-native-plugin/CHANGELOG.md`
- Findings: `packages/react-native/src/types.ts` defines `PostHogSessionReplayConfig` with masking, logs, network telemetry, throttle/debouncer, `screenshotModeBackgroundCapture`, and `sampleRate`, but no `screenshot`, `screenshotMode`, `captureMode`, or `screenshotScale` option.; `packages/react-native/src/posthog-rn.ts` builds `sdkReplayConfig` from the exposed JS config and forwards it to the optional native plugin, but it does not include any capture-mode or screenshot-scale setting.; `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift` unconditionally sets `config.sessionReplayConfig.screenshotMode = true` before applying the rest of the replay config.; `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt` unconditionally sets `sessionReplayConfig.screenshot = true` before applying log/throttle/masking/sample-rate settings.; `packages/react-native/src/optional/OptionalPlugin.ts` resolves `@posthog/react-native-plugin` first and falls back to `posthog-react-native-session-replay`, so this native bridge path is the relevant integration path for RN replay.; `packages/react-native-plugin/CHANGELOG.md` for #3824 explicitly says replay config is applied so later recording uses screenshot mode instead of wireframe/default settings, which supports the maintainer comment that screenshot mode is intentional today.; Searches found no RN-specific wireframe capture implementation in the React Native package/plugin; the plugin delegates to native iOS/Android SDK dependencies declared in the podspec and Gradle file.
- Fix assessment: Changing the default to wireframe would be a behavior change and, per maintainer clarification and the reporter's patched test, the native SDK wireframe path does not produce usable React Native recordings today. Exposing a raw `screenshotMode=false` switch is small but would likely create an unsupported/broken capture mode rather than solving the feature request. A production fix needs RN-specific wireframe capture semantics and cross-platform native work.

## 2026-07-09T05:56:48.087Z
- Item: issue #3251 — Error "TypeError: t2.handlePageUnload is not a function" after page refresh
- Conclusion: Valid surveys unload bug; prior fix appears incomplete for a stale/version-skewed survey manager.
- Labels: feature/surveys, web
- URL: https://github.com/PostHog/posthog-js/issues/3251
- Relevant files: `packages/browser/src/posthog-core.ts`, `packages/browser/src/posthog-surveys.ts`, `packages/browser/src/extensions/surveys.tsx`, `packages/browser/src/__tests__/posthog-core-also.test.ts`, `packages/browser/CHANGELOG.md`
- Findings: `packages/browser/src/posthog-core.ts` currently calls `this.surveys?.handlePageUnload?.()` during `_handle_unload()`, so the top-level surveys extension method is guarded.; `packages/browser/src/__tests__/posthog-core-also.test.ts` has coverage for a stale `posthog.surveys` object missing `handlePageUnload`, matching the #3945 top-level fix.; `packages/browser/src/posthog-surveys.ts` still implements `handlePageUnload()` as `this._surveyManager?.handlePageUnload()`, which will throw if `_surveyManager` exists but was created from an older surveys extension without that method.; `packages/browser/src/posthog-surveys.ts` assigns `_surveyManager` from `generateSurveysFn(...)`, and that function can come from `window.__PosthogExtensions__.generateSurveys`, i.e. a separately loaded/cached surveys script.; `packages/browser/src/extensions/surveys.tsx` shows the current `SurveyManager` does define `handlePageUnload`, so same-version code is safe; the remaining plausible failure mode is version skew/cached older surveys extension.; `packages/browser/CHANGELOG.md` shows #3945 was released in `posthog-js@1.393.3`, but the current source suggests that fix only guards one of the two relevant call sites.
- Fix assessment: A small surgical guard in `PostHogSurveys.handlePageUnload()` would prevent the unload crash without changing normal behavior when the current `SurveyManager` method exists.
- PR: https://github.com/PostHog/posthog-js/pull/4122

## 2026-07-10T16:39:12.087Z
- Item: issue #3806 — Screen by screen basis recording
- Conclusion: Valid feature request; RN has manual replay controls and event-trigger gating, but not remote-config screen-name targeting.
- Labels: enhancement, feature/replay, react-native, feature/mobile
- URL: https://github.com/PostHog/posthog-js/issues/3806
- Relevant files: `packages/react-native/src/posthog-rn.ts`, `packages/react-native/src/types.ts`, `packages/react-native/test/session-replay-event-triggers.spec.ts`, `packages/core/src/types.ts`, `packages/browser/src/types.ts`, `packages/browser/src/extensions/replay/external/triggerMatching.ts`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`
- Findings: packages/react-native/src/posthog-rn.ts exposes public `startSessionRecording()` and `stopSessionRecording()` methods, which covers the documented imperative workaround.; packages/react-native/src/posthog-rn.ts `screen(name, ...)` registers `$screen_name` for the session and captures a `$screen` event with `$screen_name: name`.; packages/react-native/src/posthog-rn.ts currently evaluates replay remote config for linked flags and `sessionRecording.eventTriggers`, parsing event triggers as string event names only.; packages/react-native/test/session-replay-event-triggers.spec.ts verifies that `eventTriggers: ['$screen']` starts replay when any screen event is captured, but this is not per-screen-name matching.; packages/react-native/src/types.ts defines session replay local options such as masking, logs, network telemetry, throttle delay, and sample rate; it does not expose a screen allow/deny trigger option.; packages/core/src/types.ts models `sessionRecording` as a generic boolean/object remote config, so a new mobile screen-trigger payload shape would need to be defined and consumed intentionally.; packages/browser/src/types.ts and packages/browser/src/extensions/replay/external/triggerMatching.ts show web has URL triggers and URL blocklists using regex matching, but this web URL-targeting logic is not reused by RN for screen names.; The RN native plugin bridge passes `decideReplayConfig` through mainly for native replay setup values like `endpoint`; the inspected iOS and Android plugin code does not implement screen-trigger matching.
- Fix assessment: A small RN-only patch would be speculative because the requested behavior depends on a product/API contract for remote-config screen trigger conditions and should align with iOS, Android, Flutter, and the PostHog UI. Replay gating is privacy-sensitive, and the current RN implementation only has event-name triggers, not screen-name allow/deny semantics.
