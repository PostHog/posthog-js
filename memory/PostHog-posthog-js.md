# PostHog Watcher memory

...<older entries truncated>

es/core/src/eventemitter.ts` supports SDK event subscriptions, but the inspected survey provider does not subscribe to identify/person-property-related events.; `packages/react-native/src/surveys/components/Surveys.tsx` sends `survey shown`, `survey sent`, and `survey dismissed` with the `surveyLanguage` value passed from `PostHogSurveyProvider`, so a stale translated survey can also produce stale `$survey_language` metadata.
- Fix assessment: The product behavior and shared translation logic already exist; the missing piece is invalidating/recomputing the active React Native survey translation when the resolved display language changes.

## 2026-07-17T08:04:08.731Z
- Item: issue #4174 — Re-translate displayed survey on language change
- Conclusion: Valid browser SDK surveys enhancement; active surveys are not re-translated after display language changes.
- Labels: enhancement, feature/surveys, web
- URL: https://github.com/PostHog/posthog-js/issues/4174
- Relevant files: `packages/browser/src/extensions/surveys.tsx`, `packages/browser/src/utils/survey-translations.ts`, `packages/core/src/surveys/translations.ts`, `packages/browser/src/extensions/surveys/surveys-extension-utils.tsx`, `packages/browser/src/posthog-featureflags.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/posthog-surveys.ts`, `packages/browser/src/__tests__/utils/survey-translations.test.ts`
- Findings: `packages/browser/src/utils/survey-translations.ts` implements `detectUserLanguage()` with the documented priority order: `override_display_language`, stored person property `language`, then browser language; the same file has a TODO for dynamic language change detection and mid-session re-rendering.; `packages/core/src/surveys/translations.ts` applies survey-level and question-level translations and returns the matched translation key, so the translation helper can support comparing whether the active matched language changed.; `packages/browser/src/extensions/surveys.tsx` calls `_translateSurveyForRendering()` in `handlePopoverSurvey`, `_handleWidget`, `renderPopover`, and `renderSurvey`, confirming translation is currently tied to render attempts.; `callSurveysAndEvaluateDisplayLogic()` only calls `handlePopoverSurvey()` for popovers when `_surveyInFocus` is null, so an already displayed popover is not re-rendered by the 1-second survey evaluation loop.; `_handleWidget()` translates the survey, calls `retrieveSurveyShadow()`, and returns immediately when the widget container already exists, so mounted widgets are not re-rendered with newly resolved translations.; `generateSurveys()` starts a 1-second interval that re-evaluates survey display logic, but there is no current `languagechange` listener in the inspected browser source.; `posthog-featureflags.ts` `setPersonPropertiesForFlags()` updates stored person properties and may reload flags, but does not emit a survey-specific language/person-property change notification.; `posthog-core.ts` public `on()` currently documents only `eventCaptured` and `featureFlagsReloading`, so there is not an obvious existing public person-property-change subscription for surveys to use.; `packages/browser/src/extensions/surveys/surveys-extension-utils.tsx` includes `surveyLanguage` in survey sent/dismissed/in-progress event paths, meaning the rendered component's `surveyLanguage` prop controls later `$survey_language` metadata.; `packages/browser/src/__tests__/utils/survey-translations.test.ts` covers static language detection and translation application, but the inspected tests do not cover re-translating an already displayed survey.
- Fix assessment: A small PR is appropriate because the root behavior is localized to SurveyManager render gating and the existing translation helper already returns the matched language. The main risk is lifecycle/state handling, not API design.

## 2026-07-17T09:09:45.990Z
- Item: issue #4177 — React Native: apply error-tracking suppression rules from remote config
- Conclusion: Valid React Native error-tracking enhancement; RN currently ignores remote suppression rules.
- Labels: enhancement, react-native, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4177
- Relevant files: `packages/browser/src/posthog-exceptions.ts`, `packages/browser/src/__tests__/posthog-exceptions.test.ts`, `packages/browser/src/types.ts`, `packages/react-native/src/error-tracking/index.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/test/error-tracking-remote-config.spec.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/types.ts`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`
- Findings: `packages/browser/src/types.ts` types `RemoteConfig.errorTracking.suppressionRules` as `ErrorTrackingSuppressionRule[]`.; `packages/browser/src/posthog-exceptions.ts` stores suppression rules from remote config in `_suppressionRules` and `sendExceptionEvent()` returns without capturing when `_matchesSuppressionRule()` matches the `$exception_list`.; `packages/browser/src/__tests__/posthog-exceptions.test.ts` has direct coverage for persisting suppression rules and dropping exception events on matching type/value rules.; `packages/react-native/src/error-tracking/index.ts` `onRemoteConfig()` only reads `autocaptureExceptions` via `getRemoteConfigBool(...)`; there is no storage or evaluation of `suppressionRules`.; `packages/react-native/src/posthog-rn.ts` `captureException()` attaches exception steps and delegates to `super.captureException(...)`; it has no suppression-rule drop path.; `packages/core/src/posthog-core.ts` `captureException()` builds `$exception_list` and immediately calls `capture('$exception', ...)`; core also has no suppression hook/matcher.; `packages/core/src/types.ts` models `PostHogRemoteConfig.errorTracking` as a boolean or generic JSON map, so the payload can be cached/passed through but is not strongly typed for suppression rules in core/RN.; The native plugin setup currently receives only `nativeAutocapture` and `exceptionSteps` under `errorTracking`; inspected iOS/Android plugin code does not receive or evaluate `suppressionRules`.
- Fix assessment: For JS-managed React Native exceptions, the behavior is well defined by the existing browser implementation and can be added with a small matcher plus tests. Risk is moderate only because full parity may also imply native-plugin crash suppression, which is a separate cross-language path.

## 2026-07-17T09:11:28.178Z
- Item: issue #4176 — React Native: Expo config plugin options for CLI path and per-variant toggles
- Conclusion: Valid React Native Expo config plugin enhancement; keep open for API design.
- Labels: enhancement, react-native, iOS, Android, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4176
- Relevant files: `packages/react-native/src/tooling/expoconfig.ts`, `packages/react-native/tooling/posthog-xcode.sh`, `packages/react-native/tooling/posthog.gradle`, `packages/react-native/test/expoconfig.spec.ts`, `packages/react-native/test/posthog-xcode-parse.spec.ts`, `packages/react-native/package.json`
- Findings: `packages/react-native/src/tooling/expoconfig.ts` defines `PostHogPluginProps` with only `disableSandboxing?: boolean`, `uploadNativeSymbols?: boolean | { includeSource?: boolean }`, and `skipOnConflict?: boolean`; there is no CLI path option or scoped platform/variant/build-type option.; `withPostHogPlugin()` always applies the Android sourcemap plugin, optionally applies Android native-symbol support based on the single global `uploadNativeSymbols` value, then always applies the iOS plugin; there is no platform selector.; `withIosPlugin()` passes the single global `skipOnConflict` value to the Xcode bundle wrapper and dSYM phase, resolves native-symbol upload from the single global `uploadNativeSymbols` value, and disables sandboxing across all Xcode build configurations unless `disableSandboxing` is exactly false.; `disableUserScriptSandboxing()` iterates every entry from `pbxXCBuildConfigurationSection()` and sets `ENABLE_USER_SCRIPT_SANDBOXING = "NO"`; there is no build-configuration filter.; `packages/react-native/tooling/posthog-xcode.sh` contains the TODO `provide a config that users can force the location` and currently resolves `PH_CLI_PATH` through fixed checks under `$HOME/.posthog`, npm global, npm local, then `command -v posthog-cli`.; `packages/react-native/tooling/posthog.gradle` resolves the CLI via `PostHogCli.resolveCliPackagePath()` using a fixed order: `require.resolve('@posthog/cli')`, local `node_modules`, pnpm bin, `npm root`, global npm, then `posthog-cli`.; Android `skipOnConflict` is serialized as a single `project.ext.posthogReactNativeSkipOnConflict = true` line and consumed by all matching upload tasks; it is not scoped to individual variants.; `posthog.gradle` skips debug sourcemap upload tasks with a hard-coded `!v.name.toLowerCase().contains("debug")` check, but exposes no user-configurable per-variant or per-build-type include/exclude rules.; `packages/react-native/test/expoconfig.spec.ts` and `packages/react-native/test/posthog-xcode-parse.spec.ts` cover the existing global `skipOnConflict`, native-symbol, and shell wrapper behavior, but contain no tests for CLI path override or scoped toggles.
- Fix assessment: The need is valid, but a small surgical PR is not ideal without first agreeing on the public API shape and semantics for platform/configuration/build-type/variant filtering. The implementation crosses Expo config mutation, Xcode shell generation, iOS shell CLI resolution, Android Gradle task wiring, and native-symbol upload behavior.

## 2026-07-17T09:13:44.715Z
- Item: issue #4179 — React Native: native crash capture on Windows and Linux?
- Conclusion: Valid backlog/demand-gathering enhancement; not a small JS-only fix
- Labels: enhancement, react-native, feature/error-tracking, team/error-tracking, question
- URL: https://github.com/PostHog/posthog-js/issues/4179
- Relevant files: `packages/react-native/src/error-tracking/index.ts`, `packages/react-native/src/optional/OptionalPlugin.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/src/tooling/expoconfig.ts`, `packages/react-native-plugin/package.json`, `packages/react-native-plugin/posthog-react-native-plugin.podspec`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/android/build.gradle`, `packages/react-native-plugin/CHANGELOG.md`
- Findings: `packages/react-native/src/error-tracking/index.ts` documents `nativeCrashes` as enabling native iOS/Android/macOS crash autocapture through the optional native plugin.; `packages/react-native-plugin/package.json` ships `android` and `ios` native folders in package files; repository searches did not find Windows or Linux native-module sources under the plugin package.; `packages/react-native-plugin/posthog-react-native-plugin.podspec` declares Apple platforms `:ios` and `:osx` and uses the `ios/**/*` Swift/Obj-C sources; there is no equivalent Windows/Linux native package configuration.; `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift` configures `PostHogConfig.errorTrackingConfig.autoCapture` for the Apple native SDK, with macOS handled by guarding iOS-only APIs.; `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt` configures `PostHogAndroidConfig.errorTrackingConfig.autoCapture`, and `android/build.gradle` depends on `com.posthog:posthog-android:3.54.0`.; `packages/react-native/src/tooling/expoconfig.ts` documents native symbol upload only for iOS dSYMs and Android ProGuard/R8 mappings, which would not cover Windows/Linux native crash symbolication.; `packages/react-native-plugin/CHANGELOG.md` confirms macOS support was added in plugin 2.2.0 by declaring an `osx` platform and guarding iOS-only APIs; it does not indicate Windows/Linux support.
- Fix assessment: Supporting React Native Windows/Linux would require new platform-specific native module implementations and a native crash capture/symbol upload path for those platforms. The existing JS layer already delegates to the optional native plugin, but the plugin only contains Android and Apple implementations.

## 2026-07-17T09:15:57.906Z
- Item: issue #4178 — React Native: automatic source map upload for web builds
- Conclusion: Valid React Native error-tracking enhancement; web builds are explicitly skipped and have no current automatic sourcemap processing path.
- Labels: enhancement, react-native, feature/error-tracking, team/error-tracking, web
- URL: https://github.com/PostHog/posthog-js/issues/4178
- Relevant files: `packages/react-native/src/tooling/posthogMetroSerializer.ts`, `packages/react-native/src/tooling/metroconfig.ts`, `packages/react-native/src/tooling/utils.ts`, `packages/react-native/tooling/posthog.gradle`, `packages/react-native/tooling/posthog-xcode.sh`, `packages/plugin-utils/src/cli.ts`, `packages/webpack-plugin/src/index.ts`, `packages/rollup-plugin/src/index.ts`, `packages/core/src/error-tracking/chunk-ids.ts`
- Findings: `unstableBeforeAssetSerializationDebugIdPlugin()` in `packages/react-native/src/tooling/posthogMetroSerializer.ts` returns `premodules` unchanged when `graph.transformOptions.platform === 'web'`.; `getPostHogExpoConfig()` in `packages/react-native/src/tooling/metroconfig.ts` wires `unstableBeforeAssetSerializationDebugIdPlugin` into Expo's `unstable_beforeAssetSerializationPlugins`, so that early return affects the Expo/Metro config path.; `createDebugIdSnippet()` in `packages/react-native/src/tooling/utils.ts` writes to `globalThis/window._posthogChunkIds`, and `packages/core/src/error-tracking/chunk-ids.ts` later maps those chunk IDs onto captured exception stack frames.; The existing React Native automatic upload integrations are native-only: Android Gradle registers `posthog-cli hermes clone` and `posthog-cli hermes upload`, while the iOS Xcode wrapper runs the same Hermes clone/upload flow.; The browser build-plugin flow is different: `packages/webpack-plugin/src/index.ts` and `packages/rollup-plugin/src/index.ts` collect emitted JS chunks and call `runSourcemapCli()`, which builds `posthog-cli sourcemap process` arguments in `packages/plugin-utils/src/cli.ts`.; No inspected React Native tooling currently provides an equivalent web output-directory or emitted-file hook that calls the browser-style sourcemap processing path.
- Fix assessment: The missing behavior is real, but a safe fix is not just removing the web platform guard. It needs a deliberate Expo/Metro web build integration for both chunk-ID injection and post-build `sourcemap process` upload configuration, while preserving the existing native Hermes flows.

## 2026-07-17T09:17:11.994Z
- Item: issue #4180 — React Native: Expo config plugin options for CLI path and per-variant toggles
- Conclusion: Valid React Native Expo plugin enhancement, but appears to duplicate open issue #4176.
- Labels: enhancement, react-native, iOS, Android, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4180
- Relevant files: `packages/react-native/src/tooling/expoconfig.ts`, `packages/react-native/tooling/posthog-xcode.sh`, `packages/react-native/tooling/posthog.gradle`, `packages/react-native/test/expoconfig.spec.ts`
- Findings: `packages/react-native/src/tooling/expoconfig.ts` defines `PostHogPluginProps` with only `disableSandboxing?: boolean`, `uploadNativeSymbols?: boolean | { includeSource?: boolean }`, and `skipOnConflict?: boolean`; there is no `cliPath`, platform selector, variant selector, or build-type selector.; `withPostHogPlugin` always runs the Android plugin setup and then the iOS plugin setup, with only global booleans controlling behavior.; `withIosPlugin` modifies the React Native bundle phase, optionally adds the dSYM upload phase, and disables sandboxing for all Xcode build configurations when `disableSandboxing !== false`; there is no per-configuration or per-variant toggle.; `packages/react-native/tooling/posthog-xcode.sh` contains the explicit TODO mentioned in the issue: `TODO: provide a config that users can force the location`, followed by fixed lookup through `$HOME/.posthog`, global npm, local npm root, and `command -v`.; `packages/react-native/tooling/posthog.gradle` wires upload tasks for all non-debug Android variants and `PostHogCli.resolveCliPackagePath` uses a fixed search order; the only Expo-fed Gradle option found is the global `posthogReactNativeSkipOnConflict` project extension.; `packages/react-native/test/expoconfig.spec.ts` has tests for the current global `skipOnConflict` and `uploadNativeSymbols` behavior, but no coverage for CLI path override or platform/variant/build-type filtering.; The related same-repo issue list shows open issue #4176 with the identical title, matching labels, and the same requested scope, making #4180 very likely a duplicate.
- Fix assessment: The requested behavior is real, but it requires API design across the Expo config plugin, iOS shell wrapper, Android Gradle script, and tests. Per-platform/per-variant/per-build-type toggles need clear schema and semantics before implementation. Also, this issue appears to duplicate #4176.

## 2026-07-17T09:19:10.505Z
- Item: issue #4181 — React Native: apply error-tracking suppression rules from remote config
- Conclusion: Valid request, but this appears to duplicate #4177.
- Labels: enhancement, react-native, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4181
- Relevant files: `packages/browser/src/posthog-exceptions.ts`, `packages/browser/src/__tests__/posthog-exceptions.test.ts`, `packages/browser/src/types.ts`, `packages/react-native/src/error-tracking/index.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/test/error-tracking-remote-config.spec.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/types.ts`
- Findings: `packages/browser/src/types.ts` defines `RemoteConfig.errorTracking.suppressionRules?: ErrorTrackingSuppressionRule[]`.; `packages/browser/src/posthog-exceptions.ts` loads `response.errorTracking?.suppressionRules`, stores them in memory and persistence, and checks `_matchesSuppressionRule()` before capturing `$exception`.; `packages/browser/src/__tests__/posthog-exceptions.test.ts` covers persisting suppression rules and dropping exception events when type/value suppression rules match.; `packages/react-native/src/error-tracking/index.ts` `onRemoteConfig()` only reads `autocaptureExceptions` via `getRemoteConfigBool(...)`; it does not read, store, or evaluate `suppressionRules`.; `packages/react-native/src/posthog-rn.ts` forwards remote `errorTracking` config into `this._errorTracking.onRemoteConfig(...)` and `captureException()` delegates to `super.captureException(...)` after attaching exception steps, with no suppression-rule drop path.; `packages/core/src/posthog-core.ts` `captureException()` builds exception properties and immediately calls `capture('$exception', ...)`; shared core currently has no suppression hook or matcher.; `packages/core/src/types.ts` models `PostHogRemoteConfig.errorTracking` as `boolean | { [key: string]: JsonType }`, so React Native can receive the payload but does not strongly type suppression rules there.
- Fix assessment: A small implementation is plausible for JS-managed React Native exceptions, but full parity may need a product decision on whether native-plugin crash/error paths should also honor suppression rules. Because this issue duplicates #4177, the work should be tracked there rather than fixed separately from this issue.
