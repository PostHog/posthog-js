# PostHog Watcher memory

...<older entries truncated>

lative. Individual candidates such as the tag-name normalization are small, but they should be submitted and tested separately rather than treated as a fix for this tracker.

## 2026-09-08T14:16:07.696Z
- Item: issue #3766 — Track posthog-js rrweb divergences to contribute back upstream
- Conclusion: Valid Session Replay maintenance tracker for contributing general rrweb fork divergences upstream.
- Labels: enhancement, feature/replay, needs-to-be-contributed-back, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/3766
- Relevant files: `packages/rrweb/README.md`, `packages/rrweb/rrweb-snapshot/src/utils.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/rrweb-snapshot/test/snapshot.test.ts`, `packages/rrweb/rrweb/src/replay/index.ts`, `packages/rrweb/rrweb/src/record/stylesheet-manager.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `packages/rrweb/README.md` confirms this is PostHog's vendored rrweb copy and explicitly directs contributors to contribute upstream through a personal fork.; `hasEmptyShorthandLonghand()` detects empty non-custom CSS declarations emitted by CSSOM shorthand serialization, and style-text serialization retains the original `textContent` when stringified CSS contains that corruption.; `onceStylesheetLoaded()` deduplicates listeners through a module-level `Map<HTMLLinkElement, AbortController>`; `resetStylesheetLoadTracking()` aborts pending listeners and clears that map. The snapshot test verifies teardown and re-tracking across recorder sessions.; `serializeNodeWithId()` applies `DEFAULT_MAX_DEPTH` of 50, marks depth truncation, and emits the warning only once. Focused tests cover the default, truncation, and depth boundary behavior.; The replayer records synchronous scroll events in `lastScrollMap` and reapplies them on `ReplayerEvents.Flush`, covering scroll positions clamped before replay layout settles.; The replay incident registry identifies rrweb serialization as a silent-corruption risk. Any individual upstream contribution affecting stylesheet serialization or reconstruction should remain isolated and include a regression assertion for the observed browser/replay behavior.
- Fix assessment: This is intentionally a multi-item coordination issue, not a single bug with one safe repository patch. Each divergence has different upstream status and replay-fidelity implications, so its current upstream state and a focused regression test must be verified independently before work begins.

## 2026-09-08T22:21:31.135Z
- Item: issue #4852 — posthog-react-native Expo plugin silently skips `apply plugin: "com.posthog.android"` when an earlier plugin registers an appBuildGradle mod
- Conclusion: Confirmed React Native Expo config-plugin bug: an earlier appBuildGradle mod can cause Android native-symbol setup to silently omit the PostHog Gradle plugin.
- Labels: react-native, Android, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4852
- Relevant files: `packages/react-native/src/tooling/expoconfig.ts`, `packages/react-native/test/expoconfig.spec.ts`, `packages/react-native/CHANGELOG.md`, `packages/react-native/package.json`, `packages/react-native/CONTRIBUTING.md`
- Findings: `withAndroidNativeSymbolsPlugin` in `packages/react-native/src/tooling/expoconfig.ts` initializes `let classpathPresent = false`; its `withProjectBuildGradle` callback updates that local, while its `withAppBuildGradle` callback returns early if the local is false.; The same source explicitly documents the ordering constraint twice: the native-symbol helper says it must register before anything else touches `appBuildGradle`, and `withPostHogPlugin` says an existing app mod could make the app callback run before the project callback.; The app callback is responsible for calling `applyPostHogAndroidGradlePlugin`, which inserts `apply plugin: "com.posthog.android"`; returning early therefore leaves the project with the classpath but without the plugin that uploads R8 mappings.; `addPostHogAndroidGradlePluginClasspath` has an intentional failure path for project Gradle files without a `buildscript.dependencies` block: it warns and reports `classpathPresent: false`. A fix should not accidentally turn that currently non-breaking unsupported layout into an invalid Gradle build without explicitly deciding that behavior.; `packages/react-native/test/expoconfig.spec.ts` tests the classpath and app-plugin text helpers independently, but contains no Android config-mod ordering regression test involving an earlier `withAppBuildGradle` registration.; `packages/react-native/CHANGELOG.md` records #4219 as fixing a prior case where mod ordering skipped the app plugin, but the current implementation still has the documented limitation when another plugin pre-registers the app Gradle mod kind.
- Fix assessment: The fault is localized to one Expo config-plugin helper and has a deterministic reproduction. The implementation should be kept surgical: eliminate reliance on mutable state shared between separately ordered mods, and add a regression test for the earlier-appBuildGradle-mod case. Care is needed to retain the existing behavior when no compatible project-level classpath location exists.

## 2026-09-09T01:07:18.935Z
- Item: issue #4856 — Clarify owning license notice for @posthog/core 1.37.3
- Conclusion: Valid publisher-intent and licensing-provenance question; it needs an authoritative maintainer/legal response rather than a speculative code fix.
- Labels: team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4856
- Relevant files: `packages/core/package.json`, `LICENSE`, `packages/core/CHANGELOG.md`, `packages/core/src/vendor/uuidv7.ts`, `packages/core/src/index.ts`
- Findings: `packages/core/package.json` currently declares `"license": "MIT"` and lists only `dist` and `src` in its explicit published-files configuration.; No `LICENSE*` or `AGENTS.md` file exists under `packages/core` in the inspected checkout.; The repository-root `LICENSE` begins with `Copyright 2020 Posthog / Hiberly, Inc.` and `Copyright 2015 Mixpanel, Inc.`, then supplies Apache License 2.0 terms; it also contains separate notices for specifically identified third-party code.; `packages/core/CHANGELOG.md` records version 1.37.3 as a June 2026 feature-flag deduplication patch and does not document a licensing change.; `packages/core/src/vendor/uuidv7.ts` identifies its upstream source and explicitly marks that vendored component as Apache-2.0 with LiosK copyright; it is distinct from the core package's owning-license question.; The inspected checkout is currently `@posthog/core` 1.51.0, so it confirms the present manifest/repository mismatch but cannot by itself establish the publisher's historical intent for 1.37.3.
- Fix assessment: The technical packaging gap is small, but selecting or correcting the applicable owning license for an already-published version is a legal and publisher-intent decision. A code change should not be made until the intended notice is confirmed.

## 2026-09-09T02:28:32.948Z
- Item: issue #4857 — React Native: warm-start push notification opens are never captured on Android
- Conclusion: Valid Android React Native push-open capture gap; automatic warm-start handling is absent in the current plugin.
- Labels: react-native, feature/mobile, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4857
- Relevant files: `packages/react-native-plugin/android/build.gradle`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginPackage.kt`, `packages/react-native-plugin/android/src/test/java/com/posthogreactnativeplugin/PosthogReactNativePluginModuleTest.kt`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/test/push-notifications.spec.ts`, `packages/react-native-plugin/CHANGELOG.md`, `packages/react-native/CHANGELOG.md`
- Findings: `packages/react-native-plugin/android/build.gradle` pins `com.posthog:posthog-android:3.61.0`, confirming the repository has not yet adopted the reported 3.62.0 native API.; `PosthogReactNativePluginModule.setupNativeSdk()` calls `PostHogAndroid.setup(context, config)` and then `captureColdStartPushOpenIfNeeded(config)`.; `captureColdStartPushOpenIfNeeded` reads `reactApplicationContext.currentActivity?.intent`, requires `google.message_id`, calls the generic `PostHog.capturePushNotificationOpened(...)`, and removes `google.message_id` from the launch intent afterward.; The Android plugin contains no `ActivityEventListener`, `addActivityEventListener`, or `onNewIntent` implementation, so it has no plugin-owned warm-intent observation path.; `packages/react-native/src/posthog-rn.ts` explicitly says Android automatic capture sees cold starts only and identifies warm-start and foreground Android taps as cases for the manual `capturePushNotificationOpened` API.; Existing React Native tests cover the JavaScript-to-plugin manual capture bridge, while the plugin's Android unit test currently covers only a configuration helper; there is no focused native warm-intent or cold/warm deduplication coverage.; The package README directs usage documentation to posthog.com; the requested React Native push-page update is not located in this repository.
- Fix assessment: The source gap is localized, but this is event-counting behavior across React Native activity lifecycle callbacks and a native SDK version boundary. A safe change needs compile verification against the new native API plus targeted cold/warm intent and deduplication tests; the associated user documentation is maintained externally.

## 2026-09-12T23:17:55.238Z
- Item: issue #4931 — Dead-click false positives when DOM updates precede the window bubble listener
- Conclusion: Valid dead-click false-positive bug with a narrowly scoped, evidence-backed fix direction.
- Labels: feature/dead-clicks, javascript, web
- URL: https://github.com/PostHog/posthog-js/issues/4931
- Relevant files: `packages/browser/src/entrypoints/dead-clicks-autocapture.ts`, `packages/browser/src/__tests__/entrypoints/lazy-loaded-dead-clicks-autocapture.test.ts`, `packages/browser/playwright/mocked/dead-clicks.spec.ts`, `packages/browser-common/src/utils/general-utils.ts`, `packages/browser/src/extensions/dead-clicks-autocapture.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `_startClickObserver()` registers `click` on `assignableWindow` with no capture option, so the helper defaults it to `{ capture: false, passive: true }`; `stop()` likewise removes that listener without a capture option.; `asCandidate()` stamps the click candidate with `Date.now()`, while `_onMutation()` separately stamps `_lastMutation` with `Date.now()`.; `_checkClicks()` computes a mutation delay only when `click.timestamp <= this._lastMutation`; a mutation callback delivered before the window bubble click handler is therefore ignored, exactly as described.; The existing unit suite explicitly skips direct mutation-observer behavior due to jsdom limitations, while the Playwright suite already has a real-browser test asserting that a synchronous DOM update observed through the fallback MutationObserver prevents a dead click. It is the appropriate layer for this ordering regression.; The change would affect the lazy-loaded dead-click entrypoint, but it does not require a core/extension API, persisted-state, or configuration-shape change. The lazy-load compatibility failure mode documented in the incident guidance is therefore not directly present.; Moving to capture means the detector will also see clicks that application code later stops from bubbling. This can change dead-click candidate and event volume, so the behavioral change merits real-browser validation.
- Fix assessment: The implementation itself is likely a two-line listener-options change, but it changes globally observed click semantics in a lazily delivered analytics extension and can affect dead-click volume. A focused real-browser regression test is needed before treating it as a routine low-risk patch.

## 2026-09-14T08:12:49.222Z
- Item: issue #4920 — immediate delivery resolves after failed requests and terminal redirects
- Conclusion: Valid Node delivery-semantics bug; the terminal-3xx portion is already fixed in shared core, but immediate calls still suppress transport failures.
- Labels: node, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4920
- Relevant files: `packages/core/src/posthog-core-stateless.ts`, `packages/core/src/__tests__/posthog.flush.spec.ts`, `packages/core/src/__tests__/posthog.shutdown.spec.ts`, `packages/core/src/utils/promise-queue.ts`, `packages/core/CHANGELOG.md`, `packages/node/src/client.ts`, `packages/node/src/__tests__/immediate-delivery.spec.ts`, `packages/node/src/__tests__/posthog-node.spec.ts`, `packages/node/CHANGELOG.md`
- Findings: Shared core now rejects successful-write responses with status >= 300; core flush tests explicitly cover 300, 302, and 304 as failures while preserving 302 handling for required feature-flag reads.; @posthog/core 1.53.3 changelog attributes the final-2xx successful-write change to PR #4922 and explicitly says immediate-call error handling was preserved.; PostHogCoreStateless.sendImmediate() awaits sendBatch(), emits an error in its catch block, and does not rethrow.; PostHog._sendPreparedEvent() catches errors from both preparation and sending, logs them, and resolves; this is a second suppression layer for Node immediate methods.; The existing Node immediate-delivery test currently asserts that HTTP 300, 302, 304, and 503 emit an error without rejecting captureImmediate().; Immediate work is added to the shared PromiseQueue; shutdown joins that queue and separately catches PostHog fetch errors, so changing rejection propagation requires regression coverage for shutdown rather than only changing the two catches.
- Fix assessment: The code change could be small, but it changes established observable promise behavior and interacts with the pending-work/shutdown path. Maintainer agreement is needed on whether all immediate methods should reject transport failures and on the intended behavior for callers that do not handle those rejections.

## 2026-09-15T12:38:25.285Z
- Item: issue #4968 — Canvas recording enabled, but rrweb emits zero canvas mutations (posthog-js 1.428.6)
- Conclusion: Likely a canvas FPS capture compatibility/failure-path issue, distinct from a playback-only canvas replacement fix.
- Labels: feature/replay, javascript
- URL: https://github.com/PostHog/posthog-js/issues/4968
- Relevant files: `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/rrweb/rrweb/src/record/observers/canvas/canvas-manager.ts`, `packages/rrweb/rrweb/src/record/observers/canvas/2d.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/types/src/index.ts`, `packages/browser/playwright/mocked/session-recording/canvas-resolution.spec.ts`, `packages/browser/CHANGELOG.md`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: The SDK converts enabled remote canvas configuration into `recordCanvas: true` and `sampling.canvas: canvasFps`; the reported `canvasFps: 3` therefore selects rrweb's numeric FPS snapshot path.; The numeric FPS path returns immediately, with no canvas mutation observer or frame loop, when `OffscreenCanvas` is absent. rrweb's public type documentation explicitly says numeric canvas sampling is supported only where `OffscreenCanvas` is supported.; The same FPS path creates an inline encoding worker and silently disables canvas snapshotting if worker construction fails or its error handler fires. DOM recording is independent of this path.; The browser Playwright canvas-resolution test documents a pre-existing case where the FPS pipeline produces no canvas mutations under headless WebKit even though `OffscreenCanvas`, image-bitmap operations, and the worker are present; the test is skipped there.; The repository changelog records an intentional behavior change to skip FPS canvas recording on browsers without `OffscreenCanvas` support, specifically Safari before 16.4.; Initial full snapshots can serialize a nonblank 2D canvas as `rr_dataURL`; source-9 incremental events are not the only possible recorded canvas pixels.; The direct rrweb control is not yet independently reproducible from the issue because its complete options, browser, timing, and CSP are absent. If it used numeric sampling, it exercises the same FPS path; if it truly used default sampling, it should instead exercise the 2D method-patching observer.
- Fix assessment: A safe fix depends on distinguishing unsupported WebKit behavior, a blocked/failed inline worker, and a regression in the default per-call observer. A fallback between capture modes would change fleet-wide replay CPU, payload volume, and canvas-pixel capture behavior, so it needs a real-browser reproduction and regression test first.

## 2026-09-15T14:38:03.012Z
- Item: issue #4969 — Session replay: `hookSetter` defers on the zone.js-patched `setTimeout`, driving Angular change detection into an endless loop (tab at 100 % CPU)
- Conclusion: Confirmed Session Replay bug: rrweb's deferred input-setter callback can schedule zone.js-patched timers from Angular change detection, creating an unbounded change-detection loop.
- Labels: feature/replay, web, performance, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4969
- Relevant files: `packages/rrweb/rrweb/src/utils.ts`, `packages/rrweb/rrweb/src/record/observer.ts`, `packages/rrweb/rrweb/test/util.test.ts`, `packages/rrweb/rrweb/test/record.test.ts`, `packages/rrweb/utils/src/index.ts`, `packages/rrweb/record/src/index.ts`, `packages/browser/src/entrypoints/recorder.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `hookSetter()` in `packages/rrweb/rrweb/src/utils.ts` currently invokes an unqualified `setTimeout(..., 0)` for every hooked assignment before it forwards to the original setter synchronously.; `initInputObserver()` in `packages/rrweb/rrweb/src/record/observer.ts` installs `hookSetter()` on `HTMLInputElement.value`, `HTMLInputElement.checked`, `HTMLSelectElement.value`, `HTMLTextAreaElement.value`, `HTMLSelectElement.selectedIndex`, and `HTMLOptionElement.selected`, matching the report.; The input observer's `isBlocked()` check and `lastInputValueMap` deduplication occur inside the deferred mock-event handler, after `hookSetter()` has already scheduled the timer. This supports the reporter's finding that `blockSelector` does not prevent the loop.; Existing `hookSetter()` tests cover deferred callback execution, preservation of native setter behavior, and invalid receivers, but do not test zone.js-patched timer avoidance.; `packages/rrweb/utils/src/index.ts` already detects Zone's unpatched globals through `Zone.__symbol__(key)`, but its private helper is typed and used only for unpatched DOM constructor/prototype lookup.; `@posthog/rrweb-record` re-exports `record` from `@posthog/rrweb`, and the browser recorder entrypoint imports it, so this vendored implementation is on the PostHog Session Replay delivery path.; Replay incident-risk review: this is recorder-side code but not the known rrweb serialization failure mode. The relevant regression should assert real-browser input-capture behavior and task scheduling, since a capture-fidelity regression could otherwise be silent.
- Fix assessment: The code change is likely small and localized, but it modifies a globally installed recorder setter hook. It needs a browser-level regression test for Angular/zone timer semantics plus verification that programmatic input capture, synchronous native setter behavior, fallback behavior without Zone, and iframe/window handling remain intact.
