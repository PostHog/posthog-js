# PostHog Watcher memory

...<older entries truncated>

 this integration are consistent with the reported ownership gap.
- Fix assessment: The failing executable resolver belongs to the separately maintained Android Gradle plugin. Adding an Expo-only override here would duplicate resolution logic and would not cover non-Expo React Native applications.

## 2026-08-22T15:24:33.893Z
- Item: issue #1089 — Limit session length
- Conclusion: Valid session-replay feature request, but changing the cap affects session rotation and recording volume.
- Labels: feature, feature/replay, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/1089
- Relevant files: `packages/browser/src/sessionid.ts`, `packages/types/src/posthog-config.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/__tests__/sessionid.test.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts`, `packages/browser/playwright/mocked/session-recording/session-rotation-scenarios.spec.ts`, `packages/browser/playwright/mocked/session-recording/session-recording-idle-timeout.spec.ts`
- Findings: `packages/browser/src/sessionid.ts` defines `SESSION_LENGTH_LIMIT_MILLISECONDS` as 24 hours and rotates a session when its start timestamp exceeds that limit, including on read-only checks.; `session_idle_timeout_seconds` is already a documented public configuration, but it controls inactivity-based rotation only; it is bounded to 60 seconds through 10 hours and does not configure the 24-hour absolute cap.; No existing maximum-session-length configuration was found in the browser config types or defaults.; Replay listens for `sessionPastMaximumLength`, links the old and new recording sessions, and restarts recording on rotation, so this is not a replay-buffer-only setting.; Existing replay tests explicitly protect billing-sensitive invariants: an idle tab must ship zero recordings across repeated rotations, a session with one interaction must ship exactly one recording, and cap rotations must prevent multi-day recordings.
- Fix assessment: The code change can be localized, but it changes public session semantics and may alter replay volume and billing. It requires an explicit product/API decision and regression coverage for session rotation, cross-tab behavior, and replay volume invariants.

## 2026-08-22T15:25:37.215Z
- Item: issue #1100 — Do we need to handle `willReadFrequently` differently with Canvas
- Conclusion: This appears to be a Canvas replay performance-warning question, not evidence of broken replay. A blanket SDK-side change would be unsafe without a current reproduction.
- Labels: feature/replay, web, question
- URL: https://github.com/PostHog/posthog-js/issues/1100
- Relevant files: `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/rrweb/rrweb/src/record/observers/canvas/canvas-manager.ts`, `packages/rrweb/rrweb/src/record/observers/canvas/canvas.ts`, `packages/rrweb/rrweb/src/record/workers/image-bitmap-data-url-worker.ts`, `packages/rrweb/rrweb-snapshot/src/utils.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`
- Findings: Canvas recording is configured by the lazy-loaded replay recorder through `recordCanvas`, with an optional FPS capture mode.; The FPS capture path snapshots the customer canvas with `createImageBitmap`; it performs its `getImageData` fingerprint read on a separate reusable `OffscreenCanvas` in the worker, whose 2D context is already created with `{ willReadFrequently: true }`.; The recorder's `HTMLCanvasElement.prototype.getContext` wrapper records the context type and changes WebGL/WebGPU capture attributes when needed, but it does not add `willReadFrequently` to application 2D context creation.; Full-snapshot canvas serialization can call `canvas.getContext('2d')`, `getImageData`, and `toDataURL` to determine whether a 2D canvas is blank and capture its initial image.; Because `willReadFrequently` is effective only on the first context creation, replay cannot reliably add it after an application or third-party library has already initialized the canvas.; The reporter's follow-up confirms that applying the option before the context is created removed the warning for their js-confetti case, but no current SDK version or standalone reproduction is provided.
- Fix assessment: The smallest apparent code change—forcing `{ willReadFrequently: true }` in the recorder's 2D `getContext` wrapper—would affect every canvas initialized after replay starts and can degrade write-heavy canvas rendering. It also cannot help canvases initialized before the recorder. A reproduction is needed to identify the actual readback path and establish a narrowly safe policy. Any recorder change must additionally preserve lazy-loaded extension compatibility.

## 2026-08-22T15:26:40.942Z
- Item: issue #1101 — Allow marking rrweb as the top level even when in an iframe
- Conclusion: Confirmed replay feature gap: rrweb intentionally treats only the browser top-level window as the emitting recorder when cross-origin iframe recording is enabled.
- Labels: enhancement, feature/replay, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/1101
- Relevant files: `packages/rrweb/rrweb/src/record/index.ts`, `packages/rrweb/rrweb/src/record/iframe-manager.ts`, `packages/rrweb/rrweb/test/record/cross-origin-iframes.test.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/extensions/replay/types/rrweb.ts`, `packages/types/src/posthog-config.ts`, `packages/rrweb/rrweb/CHANGELOG.md`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `packages/rrweb/rrweb/src/record/index.ts` sets `inEmittingFrame` to `window.parent === window` when `recordCrossOriginIframes` is enabled.; For a non-top-level frame with a cross-origin parent, the recorder sets `passEmitsToParent` and posts rrweb events to `window.parent`; it does not invoke the local SDK emit callback.; `packages/rrweb/rrweb/test/record/cross-origin-iframes.test.ts` explicitly tests and expects that a non-top-level iframe has no locally emitted events.; `IframeManager` can receive and transform cross-origin child-frame messages, but its transformed events still flow through `wrappedEmit`, which forwards them upward when the middle frame is not the emitting frame.; The browser replay recorder seeds `recordCrossOriginIframes` as `false`; its rrweb option type exposes that setting, but no inspected option exists to designate an embedded frame as the recording root.; The checked-out rrweb fork includes prior nested-cross-origin iframe work (#1353) but contains no reference to or implementation attributed to upstream rrweb PR #1726.; This touches replay event routing and the lazy-loaded recorder boundary. A regression could silently suppress recordings or change the amount of iframe content recorded, so end-to-end and old-core/new-recorder compatibility coverage are required.
- Fix assessment: The likely rrweb change may be small, but the upstream PR has not been verified or integrated here, and a safe SDK implementation needs an explicit opt-in, a clear parent-frame trust boundary, replay-bundle compatibility coverage, and a real-browser three-origin test. A speculative local override would risk silently changing recording ownership or volume.

## 2026-08-22T15:45:10.691Z
- Item: issue #1683 — Type mismatch when using @segment/analytics-next
- Conclusion: Already fixed in posthog-js 1.415.4 via PR #4494.
- Labels: web
- URL: https://github.com/PostHog/posthog-js/issues/1683
- Relevant files: `packages/types/src/segment.ts`, `packages/types/src/__tests__/segment.spec.ts`, `packages/types/CHANGELOG.md`, `packages/browser/CHANGELOG.md`, `packages/browser/package.json`
- Findings: `SegmentAnalytics.user` accepts both synchronous and Promise-based Segment user objects, with nullable IDs supported.; `SegmentAnalytics.register` now uses a permissive Promise return type so the current analytics-next plugin registration signature remains assignable while preserving legacy consumers.; `packages/types/src/__tests__/segment.spec.ts` compile-checks representative analytics-next snippet and browser shapes, as well as the prior legacy Segment interface; the test documents coverage through @segment/analytics-next 1.84.1.; `packages/browser/CHANGELOG.md` records PR #4494 as the Segment type compatibility fix in `posthog-js` 1.415.4, released on 2026-08-11.
- Fix assessment: The reported incompatibility is already addressed and released; no new code change is appropriate unless it can be reproduced with current package versions.

## 2026-08-22T15:50:01.671Z
- Item: issue #1968 — existing buffer is not flushed when calling `stopSessionRecording`
- Conclusion: Already fixed in the current browser session-replay implementation.
- Labels: feature/replay, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/1968
- Relevant files: `packages/browser/src/posthog-core.ts`, `packages/browser/src/extensions/replay/session-recording.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `PostHog.stopSessionRecording()` sets `disable_session_recording: true`; `set_config()` then invokes `sessionRecording.startIfEnabledOrStop()`, which calls the lazy recorder's `stop()` path when recording is disabled.; `LazyLoadedSessionRecorder.stop()` stops rrweb producers first, invokes `_flushBuffer()`, clears the buffer, and tears down the recorder.; When async compression is pending, `_stopAfterCompressionQueueDrains()` stops rrweb immediately, waits for the compression queue, then flushes and clears the buffer before teardown.; The recorder stops rrweb before the final flush so rrweb teardown emissions, including deferred stylesheet mutations, enter the buffer before it is shipped.; Regression coverage asserts that stop-time rrweb-emitted deferred stylesheet mutations are captured in the final `$snapshot`; coverage also documents the compression-drain stop path.; The replay flush-risk guidance requires avoiding recording-volume regressions. The current code intentionally suppresses shipment of held interaction-less epochs, and tests assert those buffers are discarded on stop.
- Fix assessment: No new change is appropriate: the reported active-buffer loss is addressed in the current implementation. Broadening stop behavior to ship intentionally held interaction-less buffers would be a separate, volume-sensitive behavior change.

## 2026-08-22T15:51:15.489Z
- Item: issue #2097 — RemoteConfig/decide/flag async call behaviour could be improved for nextJS
- Conclusion: Core support for synchronously preloaded remote config exists, but @posthog/next does not provide a first-class SSR fetch/cache/bootstrap integration.
- Labels: enhancement, feature, feature/autocapture, feature/flags, frameworks/next-js, team/feature-flags, web
- URL: https://github.com/PostHog/posthog-js/issues/2097
- Relevant files: `packages/browser/src/remote-config.ts`, `packages/browser/src/autocapture.ts`, `packages/browser/src/utils/globals.ts`, `packages/browser/src/__tests__/remote-config.test.ts`, `packages/browser/src/__tests__/autocapture.test.ts`, `packages/browser/CHANGELOG.md`, `packages/next/src/app/PostHogProvider.tsx`
- Findings: `RemoteConfigLoader.load()` first consumes `window._POSTHOG_REMOTE_CONFIG[token].config`; the associated unit test verifies that this path neither loads the remote-config script nor makes the JSON request.; The global is token-scoped and includes both `config` and `siteApps`, but is currently an internal browser-SDK contract rather than an `@posthog/next` provider API.; `Autocapture.isEnabled` uses the persisted server opt-out value until a config result arrives. Thus a previously enabled value can still allow autocapture during an ordinary asynchronous initial config fetch; a preloaded response removes that observable async window.; On config failure or when `autocapture_opt_out` is absent, autocapture no longer defaults to enabled: it retains a known persisted value and stays off if none exists. Tests cover both the fresh-client and persisted-value cases.; `@posthog/next` currently server-bootstraps feature flags only (`bootstrapFlags`); no Next remote-config fetch, cache, serialization, or injection path was found.; Remote config also carries session-recording settings. Any new SSR cache must deliberately define stale, missing, and failed-response behavior; an accidental fail-closed change could silently suppress recordings, matching the remote-config/start-condition incident class.
- Fix assessment: A small core change is not warranted because the preload mechanism already exists. A first-class Next.js feature needs an explicit public contract, server endpoint/auth model, cache TTL and invalidation semantics, safe serialization, static-versus-dynamic rendering behavior, and tests for client fallback. Remote-config freshness also affects replay start conditions, so stale or unavailable SSR data must be a deliberate product decision.

## 2026-08-22T15:52:05.412Z
- Item: issue #2159 — Implement `element_allowlist`/`element_denylist`
- Conclusion: Valid cross-SDK autocapture feature request, but it needs SDK-specific API semantics before implementation.
- Labels: enhancement, feature/autocapture, react-native, feature/mobile, web-lite, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/2159
- Relevant files: `packages/types/src/posthog-config.ts`, `packages/browser-common/src/utils/autocapture-utils.ts`, `packages/browser/src/autocapture.ts`, `packages/browser/src/__tests__/autocapture.test.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/types.ts`, `packages/react-native/src/types.ts`, `packages/react-native/src/autocapture.tsx`, `packages/react-native/test/autocapture.spec.ts`, `packages/web/src/types.ts`, `packages/web/src/posthog-web.ts`
- Findings: `AutocaptureConfig` in `packages/types/src/posthog-config.ts` includes `element_allowlist` restricted to DOM element names, but no `element_denylist`.; `shouldCaptureDomEvent` in `packages/browser-common/src/utils/autocapture-utils.ts` checks the target and ancestors against `element_allowlist`; browser tests cover matching, non-matching, and an empty allowlist.; Browser autocapture has `css_selector_ignorelist`, which is the existing deny-style mechanism for matching a target or ancestor; no element-denylist source usage was found.; `PostHogCore.autocapture()` only enqueues a supplied `$autocapture` payload with supplied elements. `PostHogCoreOptions` has no autocapture filtering configuration, so core cannot safely infer DOM or React Native element semantics.; React Native's `PostHogAutocaptureOptions` exposes `noCaptureProp` and `ignoreLabels`. Its touch handler exits for `noCaptureProp` and omits labels in `ignoreLabels`, but it has no allowlist or denylist option.; React Native derives labels from `ph-label`, `displayName`, or `name`, compares `ignoreLabels` before sanitizing the emitted `tag_name`, and captures a component ancestry. This makes direct reuse of DOM-tag allowlist semantics inappropriate without an explicit API decision.; `posthog-js-lite` exposes `autocapture?: boolean` in `packages/web/src/types.ts`, while `packages/web/src/posthog-web.ts` has no interaction-autocapture implementation.
- Fix assessment: A React Native-only filter would be small, but the requested shared/core solution spans incompatible element models and web-lite currently has no DOM interaction autocapture to filter. The public API must first define matching and precedence semantics, particularly for React Native component labels.

## 2026-08-22T16:05:48.727Z
- Item: issue #2278 — Bug: Surveys safe local storage
- Conclusion: Already fixed: survey wait-period eligibility now safely handles denied localStorage access.
- Labels: feature/surveys, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/2278
- Relevant files: `packages/browser/src/extensions/surveys/surveys-extension-utils.tsx`, `packages/browser/src/storage.ts`, `packages/browser/src/__tests__/extensions/surveys-utils.test.ts`, `packages/browser/src/posthog-surveys.ts`, `packages/browser/src/__tests__/posthog-surveys.test.ts`, `packages/browser/CHANGELOG.md`
- Findings: `hasWaitPeriodPassed` now obtains `lastSeenSurveyDate` through `localStore._get` rather than directly calling `localStorage.getItem`.; `localStore._get` catches exceptions from `window.localStorage.getItem` and returns `null`, preventing a denied-storage SecurityError from escaping the survey eligibility path.; The survey utilities test explicitly mocks `Storage.prototype.getItem` to throw and verifies `hasWaitPeriodPassed(7)` does not throw.; The browser changelog records #3442 as `fix(surveys): guard survey seen localStorage access` in version `1.370.1`.; The changelog records #3832 as guarding the remaining direct survey localStorage accesses, including reset and the `lastSeenSurveyDate` write; the current source wraps those paths in try/catch.
- Fix assessment: No new PR is appropriate because the exact failing read path is already guarded and released, with regression coverage. A new fix should only be considered with a reproduction against a current version.

## 2026-08-22T16:06:54.889Z
- Item: issue #2475 — v.129.1 "chunking context (unknown) does not support external modules"
- Conclusion: Confirmed historical browser-package bundling regression in posthog-js 1.279.x; the failure is not compatible with a client-side Next.js initialization path.
- Labels: feature/replay, feature/heatmaps, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/2475
- Relevant files: `packages/browser/package.json`, `packages/browser/CHANGELOG.md`, `packages/browser/rollup.config.mjs`, `packages/browser/src/entrypoints/module.es.ts`, `packages/browser/src/entrypoints/module.no-external.es.ts`, `packages/browser/src/entrypoints/all-external-dependencies.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `posthog-js` declares `dist/module.js` as its ESM `module` entrypoint, matching the artifact named in the reported compiler error.; The reporter's working/failing boundary aligns with the changelog: 1.279.0 changed rrweb-types packaging, while 1.279.1 lists only a heatmap hidden-tab interval fix. This identifies the 1.279.0 packaging change as the primary regression candidate, not proof of the exact offending import chain.; No current browser runtime source import of `node:child_process` was found. The only browser-package matches are build/test scripts, not code included by the client entrypoint.; The current browser Rollup configuration contains a `block-node-protocol-imports` plugin that throws on every `node:` import specifically because such imports break browser and edge bundlers. This prevents recurrence during current package builds, although the inspected changelog does not identify the first release containing that guard.; The normal module entrypoint loads default extensions and initializes the browser singleton; the full bundle separately loads replay and other external dependencies. Any packaging change around rrweb or lazy recorder dependencies should retain old-core/new-extension compatibility coverage because lazy extension assets can be served across SDK versions.
- Fix assessment: The symptom is clear, but the checked-out source does not contain the historical 1.279 artifact or its resolved dependency graph, so naming a source-line fix would be speculative. The minimal safe fix is likely limited to the offending browser dependency/export path, but it must be demonstrated against the published-package reproduction before changing dependency declarations or entrypoints.
