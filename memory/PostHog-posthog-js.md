# PostHog Watcher memory

...<older entries truncated>

ific URL triggers and URL blocklists, but that logic is URL-based and not reused by RN for screen names.; The inspected RN native plugin files pass replay setup/configuration to the native SDKs for masking, logs, sample rate, endpoint, and start/stop recording, but do not implement screen-name trigger matching.
- Fix assessment: A small RN-only change would be speculative. The requested behavior needs a remote config payload contract, PostHog UI support, allow/block semantics, and coordinated behavior across mobile SDKs. Session replay targeting is privacy-sensitive, so implementing only one assumed shape in RN is high risk.

## 2026-07-11T13:06:16.385Z
- Item: issue #3765 — Track upstream rrweb changes to pull in
- Conclusion: Valid rrweb/session replay maintenance tracker; keep open and handle as one upstream change per PR.
- Labels: enhancement, feature/replay
- URL: https://github.com/PostHog/posthog-js/issues/3765
- Relevant files: `packages/rrweb/README.md`, `packages/rrweb/rrweb/package.json`, `packages/rrweb/record/package.json`, `packages/rrweb/rrweb/src/record/observer.ts`, `packages/rrweb/rrweb/src/types.ts`, `packages/rrweb/rrweb/src/record/index.ts`, `packages/browser/src/entrypoints/recorder.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/extensions/replay/external/mutation-throttler.ts`, `packages/browser/src/extensions/replay/types/rrweb.ts`, `packages/types/src/posthog-config.ts`
- Findings: `packages/rrweb/README.md` describes this as PostHog's copy of RRWeb that is built and published for use in posthog-js.; `packages/rrweb/rrweb/package.json` and `packages/rrweb/record/package.json` define workspace packages such as `@posthog/rrweb` and `@posthog/rrweb-record`, confirming the vendored fork is part of this monorepo.; `packages/browser/src/entrypoints/recorder.ts` imports `record` from `@posthog/rrweb-record` and exposes it through `__PosthogExtensions__`, so rrweb fork changes affect browser session replay recording.; `packages/rrweb/rrweb/src/record/observer.ts` creates a `MutationObserver` and calls `observe()` with `attributes: true`, `attributeOldValue: true`, `characterData`, `childList`, and `subtree`, but no native `attributeFilter` option.; `packages/rrweb/rrweb/src/types.ts` and `packages/rrweb/rrweb/src/record/index.ts` expose/default `ignoreCSSAttributes`, but there is no corresponding record option for native MutationObserver attribute filtering in the inspected code.; `packages/rrweb/rrweb/src/record/observer.ts` checks `ignoreCSSAttributes` inside `initStyleDeclarationObserver()` when proxying `CSSStyleDeclaration.setProperty` and `removeProperty`; that does not prevent inline attribute mutation observer callbacks.; `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts` builds a limited `sessionRecordingOptions` allowlist and passes it to `rrwebRecord()`, so new rrweb recording options would need deliberate type/config plumbing before users or remote config can use them.; `packages/browser/src/extensions/replay/external/mutation-throttler.ts` rate-limits mutation events after rrweb emits them, which is useful but different from preventing native observer callbacks from firing in the first place.
- Fix assessment: This issue intentionally tracks many upstream rrweb changes with different risk profiles, including clean cherry-picks, manual ports, open upstream PRs, performance behavior changes, replay fidelity fixes, and privacy/masking fixes. A small single auto-PR would be speculative; each item should be re-verified and implemented independently with focused tests, and batch-1 performance items need before/after evidence.

## 2026-07-12T10:46:46.097Z
- Item: issue #4132 — jsonStringify throws on circular references (DOM nodes with React fibers); with capture_exceptions the throw is recaptured in a loop
- Conclusion: Valid browser SDK bug; circular event properties can break request serialization and cause retries/internal exception noise.
- Labels: feature/error-tracking, web
- URL: https://github.com/PostHog/posthog-js/issues/4132
- Relevant files: `packages/browser/src/request.ts`, `packages/browser/src/__tests__/request.test.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/retry-queue.ts`, `packages/browser/src/extensions/exception-autocapture/index.ts`, `packages/browser/src/posthog-exceptions.ts`, `packages/browser/src/utils/property-utils.ts`
- Findings: `packages/browser/src/request.ts` defines `jsonStringify` as `JSON.stringify(data, (_, value) => (typeof value === 'bigint' ? value.toString() : value), space)`, which handles BigInt but not circular object graphs.; `encodePostData`, `encodeToDataString`, gzip/base64 handling, and async gzip pre-encoding all route request bodies through `jsonStringify`, so this helper is on the shared browser request serialization path.; `packages/browser/src/posthog-core.ts` builds capture payloads from user-supplied properties and sends them as `requestOptions.data`; there is no generic cycle removal before the request layer.; `packages/browser/src/__tests__/request.test.ts` has coverage for BigInt conversion and for JSON serialization failures being reported through callbacks, but no coverage for circular data being serialized successfully.; `packages/browser/src/retry-queue.ts` retries statusCode 0 failures up to the status-zero retry budget, so serialization failures reported as statusCode 0 can be retried rather than dropped immediately.; `packages/browser/src/extensions/exception-autocapture/index.ts` can send captured exceptions through `sendExceptionEvent`; `packages/browser/src/posthog-exceptions.ts` has some SDK-exception suppression, but the root serialization failure still needs to be avoided.
- Fix assessment: The root cause is localized to the shared JSON serialization helper. A fallback-only circular-safe stringify preserves normal payloads and existing fast-path behavior while making cyclic payloads serializable.

## 2026-07-14T09:55:30.894Z
- Item: issue #4142 — Expo skipOnConflict does not apply to native iOS dSYM uploads
- Conclusion: Valid bug: Expo skipOnConflict is not wired into the generated iOS dSYM upload phase.
- Labels: react-native, iOS, feature/error-tracking, feature/mobile
- URL: https://github.com/PostHog/posthog-js/issues/4142
- Relevant files: `packages/react-native/src/tooling/expoconfig.ts`, `packages/react-native/tooling/posthog-xcode.sh`, `packages/react-native/tooling/posthog.gradle`, `packages/react-native/test/expoconfig.spec.ts`, `packages/react-native/test/posthog-xcode-parse.spec.ts`, `packages/react-native/CHANGELOG.md`
- Findings: `PostHogPluginProps.skipOnConflict` in `packages/react-native/src/tooling/expoconfig.ts` is documented as appending `--skip-on-conflict` to `posthog-cli hermes upload` on iOS and Android, not native dSYM uploads.; `withIosPlugin()` passes `props.skipOnConflict === true` only to `modifyExistingXcodeBuildScript()`, which wraps the `Bundle React Native code and images` phase for sourcemap upload.; `buildDsymUploadShellScript(includeSource)` generates a separate dSYM upload phase that runs `/bin/sh "$PODS_SCRIPT"` or `/bin/sh "$SPM_SCRIPT"` with no additional arguments; the only native-symbol option currently forwarded is `POSTHOG_INCLUDE_SOURCE=1`.; `addDsymUploadBuildPhase(xcodeProject, includeSource)` accepts only `includeSource`, and `withIosPlugin()` calls it as `addDsymUploadBuildPhase(xcodeProject, nativeSymbols.includeSource)`, so `skipOnConflict` cannot affect the generated dSYM phase.; `packages/react-native/tooling/posthog-xcode.sh` maps `--posthog-skip-on-conflict` to `POSTHOG_UPLOAD_ARGS` and uses that only in the `posthog-cli hermes upload` command.; `packages/react-native/test/posthog-xcode-parse.spec.ts` explicitly tests the current behavior as `passes --skip-on-conflict only to hermes upload`, confirming that native dSYM upload is outside the existing skip-on-conflict path.; `packages/react-native/test/expoconfig.spec.ts` has dSYM build phase tests for shell validity, `upload-symbols.sh`, and `POSTHOG_INCLUDE_SOURCE`, but no test for conflict skipping.
- Fix assessment: The repository-side change appears small and localized to the Expo config plugin's dSYM shell-script generation and tests. The only caveat is verifying the external posthog-ios upload-symbols.sh argument-forwarding contract.

## 2026-07-16T00:38:04.489Z
- Item: issue #4162 — generated references: PostHogConfig publishes properties: []
- Conclusion: Valid generated-reference bug in the shared docs type parser.
- Labels: web, node, react-native
- URL: https://github.com/PostHog/posthog-js/issues/4162
- Relevant files: `scripts/docs/types.js`, `scripts/docs/utils.js`, `scripts/docs/parser.js`, `packages/browser/src/types.ts`, `packages/types/src/posthog-config.ts`, `packages/browser/references/posthog-js-references-latest.json`, `packages/browser/scripts/generate-docs.js`, `packages/node/scripts/generate-docs.mjs`, `packages/react-native/scripts/generate-docs.js`, `packages/node/references/posthog-node-references-latest.json`, `packages/react-native/references/posthog-react-native-references-latest.json`
- Findings: `processTypeAliasMember` in `scripts/docs/types.js` calls `extractCallbackSignature` before literal/object resolution and returns early when it gets a callback signature.; `extractCallbackSignature` treats `signature.includes('=>') || signature.includes(': ') || utils.isCallbackType(member.name) || member.name.includes('Callback')` as function-like, so object type aliases with property declarations are eligible for the callback path.; `extractFunctionFromTokens` also returns the cleaned full signature if it contains `=>` or `: `, which means an object literal alias can be stored as a raw `example` instead of having `params` extracted.; `resolveObjectTypes` only parses object properties when a type has `detailedType` and no `example`; the early callback return prevents that path.; `packages/browser/src/types.ts` defines `PostHogConfig` as `Omit<BasePostHogConfig, 'loaded'> & { loaded: ...; disableDeviceModel?: ...; __extensionClasses?: ... }`.; `packages/types/src/posthog-config.ts` defines the base `PostHogConfig` interface with many documented configuration properties, so the effective browser config type is object-shaped and should not have empty properties.; `packages/browser/references/posthog-js-references-latest.json` currently emits `PostHogConfig` with `properties: []` and the raw `Omit<BasePostHogConfig, 'loaded'> & { ... }` signature in `example`.; Browser, node, and react-native reference generation all call the shared `scripts/docs/parser.js` / `scripts/docs/types.js` pipeline.; Latest node and react-native references show the same pattern for object aliases such as `ActionStepType`: `properties: []` with the raw object signature in `example`.
- Fix assessment: A PR is appropriate, but a minimal heuristic-only patch is likely incomplete for the headline `PostHogConfig` case because it also needs safe resolution of `Omit`/intersections/imported base interface members and tighter literal-union detection. The shared parser affects multiple generated reference outputs, so it should be test-driven rather than auto-fixed speculatively.

## 2026-07-17T08:01:51.625Z
- Item: issue #4175 — Re-translate displayed survey on language change for React Native
- Conclusion: Valid React Native surveys enhancement; active survey translation is cached across language changes.
- Labels: enhancement, feature/surveys, react-native, feature/mobile
- URL: https://github.com/PostHog/posthog-js/issues/4175
- Relevant files: `packages/react-native/src/surveys/PostHogSurveyProvider.tsx`, `packages/react-native/src/surveys/survey-translations.ts`, `packages/core/src/surveys/translations.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/eventemitter.ts`, `packages/react-native/src/hooks/useFeatureFlags.ts`, `packages/react-native/src/surveys/components/Surveys.tsx`, `packages/react-native/test/surveyTranslations.spec.ts`, `packages/react-native/test/SurveyModal.spec.tsx`
- Findings: `packages/react-native/src/surveys/PostHogSurveyProvider.tsx` computes `translatedActiveSurvey` with `useMemo(..., [activeSurvey, posthog])`, so changes to language sources alone do not invalidate the translation.; `packages/react-native/src/surveys/survey-translations.ts` resolves language from `overrideDisplayLanguage`, persisted `PostHogPersistedProperty.PersonProperties.language`, then `getCommonEventProperties().$locale` before calling shared core translation logic.; `packages/core/src/surveys/translations.ts` already contains the shared matching and merge logic, including exact/base language matching and returning the matched translation key.; `packages/react-native/src/hooks/useFeatureFlags.ts` subscribes to feature flag changes and can re-render the survey provider, but the current translation memo can remain cached when `activeSurvey` and `posthog` identities are unchanged.; `packages/core/src/posthog-core.ts` persists person properties through `setPersonPropertiesForFlags`, and React Native `identify` in `packages/react-native/src/posthog-rn.ts` also caches identify properties for flag evaluation; these are the stored properties read by survey language detection.; `packages/core/src/eventemitter.ts` supports SDK event subscriptions, but the inspected survey provider does not subscribe to identify/person-property-related events.; `packages/react-native/src/surveys/components/Surveys.tsx` sends `survey shown`, `survey sent`, and `survey dismissed` with the `surveyLanguage` value passed from `PostHogSurveyProvider`, so a stale translated survey can also produce stale `$survey_language` metadata.
- Fix assessment: The product behavior and shared translation logic already exist; the missing piece is invalidating/recomputing the active React Native survey translation when the resolved display language changes.

## 2026-07-17T08:04:08.731Z
- Item: issue #4174 — Re-translate displayed survey on language change
- Conclusion: Valid browser SDK surveys enhancement; active surveys are not re-translated after display language changes.
- Labels: enhancement, feature/surveys, web
- URL: https://github.com/PostHog/posthog-js/issues/4174
- Relevant files: `packages/browser/src/extensions/surveys.tsx`, `packages/browser/src/utils/survey-translations.ts`, `packages/core/src/surveys/translations.ts`, `packages/browser/src/extensions/surveys/surveys-extension-utils.tsx`, `packages/browser/src/posthog-featureflags.ts`, `packages/browser/src/posthog-core.ts`, `packages/browser/src/posthog-surveys.ts`, `packages/browser/src/__tests__/utils/survey-translations.test.ts`
- Findings: `packages/browser/src/utils/survey-translations.ts` implements `detectUserLanguage()` with the documented priority order: `override_display_language`, stored person property `language`, then browser language; the same file has a TODO for dynamic language change detection and mid-session re-rendering.; `packages/core/src/surveys/translations.ts` applies survey-level and question-level translations and returns the matched translation key, so the translation helper can support comparing whether the active matched language changed.; `packages/browser/src/extensions/surveys.tsx` calls `_translateSurveyForRendering()` in `handlePopoverSurvey`, `_handleWidget`, `renderPopover`, and `renderSurvey`, confirming translation is currently tied to render attempts.; `callSurveysAndEvaluateDisplayLogic()` only calls `handlePopoverSurvey()` for popovers when `_surveyInFocus` is null, so an already displayed popover is not re-rendered by the 1-second survey evaluation loop.; `_handleWidget()` translates the survey, calls `retrieveSurveyShadow()`, and returns immediately when the widget container already exists, so mounted widgets are not re-rendered with newly resolved translations.; `generateSurveys()` starts a 1-second interval that re-evaluates survey display logic, but there is no current `languagechange` listener in the inspected browser source.; `posthog-featureflags.ts` `setPersonPropertiesForFlags()` updates stored person properties and may reload flags, but does not emit a survey-specific language/person-property change notification.; `posthog-core.ts` public `on()` currently documents only `eventCaptured` and `featureFlagsReloading`, so there is not an obvious existing public person-property-change subscription for surveys to use.; `packages/browser/src/extensions/surveys/surveys-extension-utils.tsx` includes `surveyLanguage` in survey sent/dismissed/in-progress event paths, meaning the rendered component's `surveyLanguage` prop controls later `$survey_language` metadata.; `packages/browser/src/__tests__/utils/survey-translations.test.ts` covers static language detection and translation application, but the inspected tests do not cover re-translating an already displayed survey.
- Fix assessment: A small PR is appropriate because the root behavior is localized to SurveyManager render gating and the existing translation helper already returns the matched language. The main risk is lifecycle/state handling, not API design.

## 2026-07-17T09:09:45.990Z
- Item: issue #4177 — React Native: apply error-tracking suppression rules from remote config
- Conclusion: Valid React Native error-tracking enhancement; RN currently ignores remote suppression rules.
- Labels: enhancement, react-native, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4177
- Relevant files: `packages/browser/src/posthog-exceptions.ts`, `packages/browser/src/__tests__/posthog-exceptions.test.ts`, `packages/browser/src/types.ts`, `packages/react-native/src/error-tracking/index.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/test/error-tracking-remote-config.spec.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/types.ts`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`
- Findings: `packages/browser/src/types.ts` types `RemoteConfig.errorTracking.suppressionRules` as `ErrorTrackingSuppressionRule[]`.; `packages/browser/src/posthog-exceptions.ts` stores suppression rules from remote config in `_suppressionRules` and `sendExceptionEvent()` returns without capturing when `_matchesSuppressionRule()` matches the `$exception_list`.; `packages/browser/src/__tests__/posthog-exceptions.test.ts` has direct coverage for persisting suppression rules and dropping exception events on matching type/value rules.; `packages/react-native/src/error-tracking/index.ts` `onRemoteConfig()` only reads `autocaptureExceptions` via `getRemoteConfigBool(...)`; there is no storage or evaluation of `suppressionRules`.; `packages/react-native/src/posthog-rn.ts` `captureException()` attaches exception steps and delegates to `super.captureException(...)`; it has no suppression-rule drop path.; `packages/core/src/posthog-core.ts` `captureException()` builds `$exception_list` and immediately calls `capture('$exception', ...)`; core also has no suppression hook/matcher.; `packages/core/src/types.ts` models `PostHogRemoteConfig.errorTracking` as a boolean or generic JSON map, so the payload can be cached/passed through but is not strongly typed for suppression rules in core/RN.; The native plugin setup currently receives only `nativeAutocapture` and `exceptionSteps` under `errorTracking`; inspected iOS/Android plugin code does not receive or evaluate `suppressionRules`.
- Fix assessment: For JS-managed React Native exceptions, the behavior is well defined by the existing browser implementation and can be added with a small matcher plus tests. Risk is moderate only because full parity may also imply native-plugin crash suppression, which is a separate cross-language path.
