# PostHog Watcher memory

...<older entries truncated>

ages/browser/scripts/strip-sourcemap-sources-content.js`, `packages/browser/src/__tests__/strip-sourcemap-sources-content.test.ts`, `packages/browser/rollup.config.mjs`, `packages/browser/CHANGELOG.md`
- Findings: `packages/browser/package.json` runs `scripts/strip-sourcemap-sources-content.js` during `prepack`, so every npm package removes `sourcesContent` from maps immediately before publishing.; The stripping script deliberately preserves the `.js.map` files and their `sources` entries, but deletes `sourcesContent` in place. Its own comment states that downstream bundlers chain through the maps.; The package's published files are limited to built outputs and selected subdirectories; `src/` is not included. Consequently, source-map consumers that resolve a map's source paths from the installed package cannot find PostHog TypeScript source files.; Rollup produces source maps for browser entrypoints (`sourcemap: true`), so this affects normal distributed browser bundles rather than an unused artifact.; The current changelog records #4520 as intentionally dropping npm `sourcesContent`, reducing the unpacked package from 40.8 MB to 17.4 MB. This confirms the report is a consequence of an active packaging decision, not a stale artifact.
- Fix assessment: The immediate cause is clear, but no one-line change preserves both the package-size reduction and valid chained maps. Restoring all embedded contents increases package size; publishing/rebasing source paths must also account for bundled dependency sources and avoid exposing build-machine absolute paths. Disabling maps would avoid the resolution failures but regress source-map support.

## 2026-09-01T21:05:06.956Z
- Item: issue #4734 — RN Android session replay: expose gesture-aware capture deferral (Android counterpart of screenshotModeBackgroundCapture)
- Conclusion: Valid React Native Android session-replay enhancement, but it is blocked on a concrete posthog-android API for gesture-aware/background screenshot capture.
- Labels: react-native, Android, feature/replay, enhancement, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4734
- Relevant files: `packages/react-native/src/types.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/android/build.gradle`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`, `packages/react-native-plugin/android/src/test/java/com/posthogreactnativeplugin/PosthogReactNativePluginModuleTest.kt`, `packages/react-native/test/session-replay-request-headers.spec.ts`, `packages/react-native-plugin/CHANGELOG.md`
- Findings: `PostHogSessionReplayConfig` exposes `screenshotModeBackgroundCapture`, explicitly documents it as iOS-only, and already exposes an Android-specific `verifyScreenshotMaskAlignment` option.; `packages/react-native/src/posthog-rn.ts` destructures replay settings and sends `sdkReplayConfig` in the native setup envelope, so the JS-to-native configuration path already exists.; The Android bridge unconditionally assigns `sessionReplayConfig.screenshot = true`; it maps masking, throttle delay, sample rate, log capture, and `verifyScreenshotMaskAlignment`, but contains no gesture-deferral or background-capture mapping.; The iOS bridge reads `sdkReplayConfig["screenshotModeBackgroundCapture"]` and assigns it to `config.sessionReplayConfig.screenshotModeBackgroundCapture`, verifying the #3552-style precedent.; The RN plugin currently compiles against `com.posthog:posthog-android:3.61.0`. No inspected repository source identifies an Android gesture-deferral/background-capture property available in that dependency, so an exact bridge assignment cannot be safely specified yet.; Existing tests snapshot the full JS native setup envelope and Android has focused mapping tests for `verifyScreenshotMaskAlignment`; these are the appropriate regression-test locations for a future concrete option.
- Fix assessment: The RN bridge work will be localized once the upstream Android option, semantics, and minimum supported posthog-android version are known, but implementing it now would require inventing an API and behavior. Capture deferral also changes screenshot timing, so replay completeness must be tested rather than assumed.

## 2026-09-07T13:35:46.469Z
- Item: issue #4825 — Session replay: idle tabs still produce 2-3 day recordings under one session id on 1.425.1
- Conclusion: Likely confirmed Session Replay idle/rotation bug with a specific event-ordering gap; it should not be closed as already fixed.
- Labels: feature/replay, javascript, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4825
- Relevant files: `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/sessionid.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording.test.ts`, `packages/browser/src/__tests__/extensions/replay/lazy-sessionrecording-rotation-invariants.test.ts`, `packages/browser/CHANGELOG.md`
- Findings: `SessionIdManager.checkAndGetSessionAndWindowId()` enforces `SESSION_LENGTH_LIMIT_MILLISECONDS` even for read-only calls, but it only runs when called; there is no separate 24-hour timer.; The session manager does schedule an idle timeout at `sessionTimeoutMs * 1.1`, but browser tab suspension can delay that callback. Its forced-idle path resets the session and waits for a subsequent session-ID check before the recorder restarts.; In `_updateWindowAndSessionIds()`, a non-interactive event observed after the recorder idle threshold sets `_isIdle`, emits `sessionIdle`, and calls `_flushBuffer()` before calling `checkAndGetSessionAndWindowId(!isUserInteraction, event.timestamp)`.; The current test titled `rotates a confirmed-idle session at the 24 hour session cap, into a held epoch` verifies that the recorder adopts a new session ID after a later non-interactive event, but does not verify that no old-session buffer was flushed before that check.; The repository's replay incident guidance identifies this exact rotation/idle/flush area as a prior over-recording and multi-day-recording incident class; changes must preserve zero-upload behavior for untouched tabs and prevent flushing events beyond the session age cap.; `posthog-js` 1.427.3 includes #4788, a related fix that resets the replay idle clock on session-ID rotation so a rotation-born recording does not start hours early. The reported version, 1.425.1, predates that release, but the changelog does not establish that #4788 fixes this flush-before-cap-check path.
- Fix assessment: The likely fault is localized, but changing the ordering of idle detection, lifecycle-marker routing, flushing, and session rotation is replay billing-sensitive. A fix must account for synchronous rrweb re-entry and preserve both old-session ending markers and held rotation-born buffers.

## 2026-09-08T10:40:26.568Z
- Item: issue #3766 — Track posthog-js rrweb divergences to contribute back upstream
- Conclusion: Valid Session Replay maintenance tracker for keeping the vendored rrweb fork thin and contributing general fixes upstream.
- Labels: enhancement, feature/replay, needs-to-be-contributed-back, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/3766
- Relevant files: `packages/rrweb/rrweb-snapshot/src/utils.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/rrweb-snapshot/test/snapshot.test.ts`, `packages/rrweb/rrweb/src/replay/index.ts`, `packages/rrweb/rrweb/src/record/stylesheet-manager.ts`
- Findings: `hasEmptyShorthandLonghand()` identifies empty non-custom CSS declarations produced by CSSOM shorthand serialization, and style serialization retains original `textContent` when stringified CSS would contain that corruption.; `onceStylesheetLoaded()` uses a module-level `Map<HTMLLinkElement, AbortController>` to deduplicate listeners; `resetStylesheetLoadTracking()` aborts all outstanding work and clears the map. A focused test covers teardown and re-tracking across recorder sessions.; `serializeNodeWithId()` enforces `DEFAULT_MAX_DEPTH` of 50, records that truncation occurred, and warns only once. Snapshot tests cover default behavior, truncation, and the depth boundary.; The replayer stores scroll events in `lastScrollMap` and re-applies them during `ReplayerEvents.Flush`, addressing scroll values that were clamped before layout settled.; These are recorder/replayer serialization and reconstruction paths, where changes can silently affect replay fidelity; this tracker should result in narrow, independently tested upstream contributions rather than a combined implementation PR.
- Fix assessment: This is intentionally an umbrella coordination issue spanning independent rrweb fork divergences. Each candidate needs its upstream state re-verified and requires a focused, separately reviewable patch and regression test.

## 2026-09-08T10:41:27.559Z
- Item: issue #3765 — Track upstream rrweb changes to pull in
- Conclusion: Valid session-replay maintenance tracker; keep open and address upstream rrweb changes individually.
- Labels: enhancement, feature/replay
- URL: https://github.com/PostHog/posthog-js/issues/3765
- Relevant files: `packages/rrweb/README.md`, `packages/rrweb/rrweb/src/types.ts`, `packages/rrweb/rrweb/src/record/index.ts`, `packages/rrweb/rrweb/src/record/observer.ts`, `packages/rrweb/rrweb/src/record/mutation.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/rrweb-snapshot/src/utils.ts`, `packages/rrweb/rrweb/src/replay/index.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/extensions/replay/external/mutation-throttler.ts`
- Findings: The repository vendors and publishes its rrweb copy for use by posthog-js, confirming this tracker is directly relevant to session replay.; The batch-1 native observer filtering work is present: `recordOptions` exposes `attributeFilter`, `record/observer.ts` passes a non-empty filter to native `MutationObserver.observe()`, and the browser lazy recorder explicitly includes and forwards the option.; The existing browser `MutationThrottler` filters rrweb mutation events after they are emitted; it is not a substitute for native `MutationObserver.attributeFilter`, which prevents callbacks for excluded attributes.; The current fork still has the rrweb#1921 mismatch: `rrweb-snapshot/src/snapshot.ts` only ignores `autoplay` for lowercase `video`/`audio`, while `record/mutation.ts` calls it with `target.tagName`, which is uppercase for HTML elements.; The fork contains the narrower custom-event seek behavior described in the issue: replay only re-casts the fullscreen custom event during synchronous seeking, rather than re-casting every custom event.; CSS URL conversion remains a fidelity-sensitive area: `absolutifyURLs` is implemented in `rrweb-snapshot/src/utils.ts` and has focused unit coverage, but no inspected test specifically covers a fragment-only URL such as `url(#filter)`.
- Fix assessment: This issue intentionally tracks many independent upstream changes with different semantics and risk profiles. A bulk synchronization would be speculative and could affect recorder CPU, replay fidelity, masking, or the lazy-loaded recorder contract. Each candidate should be independently re-verified, tested, and reviewed.

## 2026-09-08T13:45:33.062Z
- Item: issue #4840 — Proposal: HTTP cassette record/replay for SDK integration tests, starting with @posthog/ai
- Conclusion: Valid, well-scoped testing-infrastructure enhancement; an offline @posthog/ai pilot is justified, but it is not a small or low-risk PR.
- Labels: enhancement, node, team/ai-observability, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4840
- Relevant files: `packages/ai/package.json`, `packages/ai/tests/openai.test.ts`, `packages/ai/tests/anthropic.test.ts`, `packages/ai/tests/gemini.integration.test.ts`, `packages/ai/src/openai/index.ts`, `packages/ai/src/anthropic/index.ts`, `packages/ai/src/gemini/index.ts`, `examples/example-ai-vercel-ai-v7/integration.test.ts`, `packages/browser/playwright/fixtures/network.ts`, `packages/browser/functional_tests/mock-server.ts`, `packages/browser/playwright/fixtures/posthog.ts`, `packages/browser/playwright/fixtures/ingestion.ts`, `packages/mcp/harness/README.md`, `compliance/node/README.md`
- Findings: `packages/ai/tests/openai.test.ts` explicitly mocks both `posthog-node` and `openai`; `packages/ai/tests/anthropic.test.ts` does the same for `posthog-node` and `@anthropic-ai/sdk`. These tests cannot exercise the providers' real HTTP or streaming implementations.; `packages/ai/tests/gemini.integration.test.ts` is skipped when `GEMINI_API_KEY` is unavailable, calls the real Gemini provider when present, and mocks `posthog-node`. It is therefore a concrete starting point for credential-free replay plus real analytics-transport assertions.; `PostHogOpenAI` and `PostHogAnthropic` extend their respective provider clients and pass provider client options through after removing the PostHog client. Both wrappers retain the provider client's `baseURL` for telemetry, making local base-URL routing a plausible focused test seam.; `PostHogGoogleGenAI` constructs a real `GoogleGenAI` client, but its current integration test does not verify real PostHog serialization or delivery because the PostHog client is mocked.; The AI package exposes only the normal Vitest-based `test:unit` command; there is no existing isolated subprocess/cassette scenario harness to extend.; `examples/example-ai-vercel-ai-v7/integration.test.ts` already demonstrates a local HTTP ingestion collector with real OpenTelemetry/PostHog processing and assertions, although it deliberately uses `MockLanguageModelV4` rather than a real provider client.; Browser Playwright tests already serve current local `dist` assets and fulfill flags, remote config, surveys, and ingestion routes locally. Browser functional tests use MSW with unhandled requests configured as errors and decode captured request bodies for assertions.; The Playwright event fixture collects events through `before_send`, whereas the browser functional mock server and the proposed local AI collector can inspect received transport payloads directly; these are complementary boundaries.; The MCP harness documents an existing fork-safe pattern using real local HTTP, workspace-built package output, an in-process event recorder, and explicit assertions. It supports the proposal's preference for real protocol execution rather than replacing every integration with replay.; The Node compliance adapter already delegates behavioral SDK contracts to the external SDK test harness, so provider-specific cassettes should remain separate from that contract layer unless a shared PostHog response-fixture need is established.
- Fix assessment: The proposal requires new test infrastructure spanning a local HTTP replay server, deterministic matching and streaming behavior, failure-safe network isolation, recording/redaction controls, and a local ingestion collector. A generic monorepo abstraction or provider-version matrix would be speculative before validating one narrow AI scenario.

## 2026-09-08T14:15:16.513Z
- Item: issue #3765 — Track upstream rrweb changes to pull in
- Conclusion: Valid session-replay maintenance tracker; keep open and handle each upstream rrweb change independently.
- Labels: enhancement, feature/replay
- URL: https://github.com/PostHog/posthog-js/issues/3765
- Relevant files: `packages/rrweb/README.md`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/rrweb/src/record/mutation.ts`, `packages/rrweb/rrweb/src/record/observer.ts`, `packages/rrweb/rrweb/src/utils.ts`, `packages/rrweb/plugins/rrweb-plugin-console-record/src/index.ts`, `packages/rrweb/rrweb/src/types.ts`, `packages/rrweb/rrweb/test/record/attribute-filter.test.ts`
- Findings: The repository explicitly vendors and publishes its rrweb copy for use by posthog-js, so this tracker is directly relevant to the Session Replay SDK.; Native mutation observer filtering has already landed: `RecordOptions` exposes `attributeFilter`, `record/observer.ts` forwards a non-empty list to `MutationObserver.observe()`, and focused tests cover unset, populated, and empty filter behavior.; `ignoreAttribute()` in `rrweb-snapshot/src/snapshot.ts` compares only lowercase `video` and `audio`, while `rrweb/src/record/mutation.ts` supplies `target.tagName`; this preserves the reported uppercase-HTML-tag mismatch for `autoplay` mutations.; The fork still uses bare `new Proxy(...)` in `rrweb/src/record/observer.ts` and `rrweb/src/utils.ts`, with no `getUntaintedProxy` helper found.; The console recorder plugin wraps methods in an arrow function, calls `original.apply(this, args)`, and invokes `original(...)` in its error path; both calls can lose the original console receiver.; No `autocomplete="off"` rebuild handling was found in the inspected vendored TypeScript sources, so the privacy-oriented input/textarea replay change described in the issue does not appear to be present.
- Fix assessment: The issue deliberately groups many independent upstream changes with different semantics, compatibility requirements, and risks to recorder CPU, capture fidelity, privacy, and replay reconstruction. A broad synchronization would be speculative. Individual candidates such as the tag-name normalization are small, but they should be submitted and tested separately rather than treated as a fix for this tracker.

## 2026-09-08T14:16:07.696Z
- Item: issue #3766 — Track posthog-js rrweb divergences to contribute back upstream
- Conclusion: Valid Session Replay maintenance tracker for contributing general rrweb fork divergences upstream.
- Labels: enhancement, feature/replay, needs-to-be-contributed-back, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/3766
- Relevant files: `packages/rrweb/README.md`, `packages/rrweb/rrweb-snapshot/src/utils.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`, `packages/rrweb/rrweb-snapshot/test/snapshot.test.ts`, `packages/rrweb/rrweb/src/replay/index.ts`, `packages/rrweb/rrweb/src/record/stylesheet-manager.ts`, `.agents/skills/replay-incident-risk/INCIDENTS.md`
- Findings: `packages/rrweb/README.md` confirms this is PostHog's vendored rrweb copy and explicitly directs contributors to contribute upstream through a personal fork.; `hasEmptyShorthandLonghand()` detects empty non-custom CSS declarations emitted by CSSOM shorthand serialization, and style-text serialization retains the original `textContent` when stringified CSS contains that corruption.; `onceStylesheetLoaded()` deduplicates listeners through a module-level `Map<HTMLLinkElement, AbortController>`; `resetStylesheetLoadTracking()` aborts pending listeners and clears that map. The snapshot test verifies teardown and re-tracking across recorder sessions.; `serializeNodeWithId()` applies `DEFAULT_MAX_DEPTH` of 50, marks depth truncation, and emits the warning only once. Focused tests cover the default, truncation, and depth boundary behavior.; The replayer records synchronous scroll events in `lastScrollMap` and reapplies them on `ReplayerEvents.Flush`, covering scroll positions clamped before replay layout settles.; The replay incident registry identifies rrweb serialization as a silent-corruption risk. Any individual upstream contribution affecting stylesheet serialization or reconstruction should remain isolated and include a regression assertion for the observed browser/replay behavior.
- Fix assessment: This is intentionally a multi-item coordination issue, not a single bug with one safe repository patch. Each divergence has different upstream status and replay-fidelity implications, so its current upstream state and a focused regression test must be verified independently before work begins.
