# PostHog Watcher memory

...<older entries truncated>

hat object is `sessionPersistence`.; `packages/browser/src/utils/event-utils.ts` implements `getReferrerInfo()` using `document.referrer` and derives `$referring_domain` from that URL host, falling back to `$direct`.; `packages/browser/src/posthog-core.ts` merges event properties in this order: browser/info properties, regular `this.persistence.properties()`, `this.sessionPersistence.properties()`, then direct event properties. This means session persistence overrides values set by `posthog.register()`, while direct `capture()` properties still win.; `packages/browser/src/__tests__/posthog-core.test.ts` already asserts that referrer is stored in `sessionPersistence` and reused within a session, but there does not appear to be coverage for precedence between user-registered `$referrer` values and SDK-managed session referrer values.; `packages/browser/src/__tests__/posthog-persistence.test.ts` covers `update_referrer_info()` deriving `$referrer`/`$referring_domain` from `document.referrer`.
- Fix assessment: The bug is localized, but the correct precedence rule needs care because `$referrer` and `$referring_domain` are used for attribution/session behavior and existing tests intentionally rely on session-scoped referrer values. A small PR is plausible once the desired semantics are confirmed, but changing merge order globally would be too broad and could break other session super-property behavior.

## 2026-07-21T15:23:11.159Z
- Item: issue #4211 — Support Android Emulator
- Conclusion: Android emulator crash appears to be an upstream react-native-reanimated dev-build issue, not a PostHog SDK bug.
- Labels: react-native, Android
- URL: https://github.com/PostHog/posthog-js/issues/4211
- Relevant files: `packages/react-native/package.json`, `examples/example-expo-53/package.json`
- Findings: `packages/react-native/package.json` lists only `@posthog/core` and `@posthog/types` as direct dependencies; `react-native-reanimated` is not a dependency or peer dependency of `posthog-react-native`.; A repository search found `react-native-reanimated` only in `examples/example-expo-53/package.json`, not in the React Native SDK package source.; The issue comment reports a successful reproduction and attributes the crash to `react-native-reanimated` 4.1.x logging a reduced-motion warning before logger initialization in dev builds, with the upstream fix shipped in 4.2.0.; The reported behavior, working on a real device but crashing on an emulator, is consistent with an emulator-specific reduced-motion/animation setting rather than PostHog initialization code.
- Fix assessment: A small PostHog SDK PR is not appropriate because the implicated code is in an upstream React Native dependency that this package does not own or directly depend on. Adding SDK-side workarounds or dependency pins would be speculative and could affect unrelated React Native apps.

## 2026-07-22T06:22:56.236Z
- Item: issue #4217 — Session recording (rrweb) blocks the main thread for several seconds serializing a large  DOM
- Conclusion: Valid replay performance bug; not a small/safe fix.
- Labels: feature/replay, web
- URL: https://github.com/PostHog/posthog-js/issues/4217
- Relevant files: `packages/browser/src/extensions/replay/session-recording.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/extensions/replay/external/mutation-throttler.ts`, `packages/browser/src/entrypoints/external-scripts-loader.ts`, `packages/browser/src/posthog-core.ts`, `packages/types/src/posthog-config.ts`, `packages/rrweb/rrweb/src/record/index.ts`, `packages/rrweb/rrweb/src/record/mutation.ts`, `packages/rrweb/rrweb-snapshot/src/snapshot.ts`
- Findings: `SessionRecording._lazyLoadAndStart()` lazy-loads an external recorder script when rrweb/initSessionRecording are not already present, using a script kind from remote config or defaulting to `lazy-recorder`.; `external-scripts-loader.ts` loads `/static/${kind}.js?v=${posthog.version}` by default, while `strict_script_versioning` switches to `/static/${posthog.version}/${kind}.js`; `posthog-core.ts` defaults `strict_script_versioning` to `false`.; `LazyLoadedSessionRecording._startRecorder()` passes allowlisted rrweb options to `rrwebRecord(...)`, then handles emitted events; there is no node-count/time budget before rrweb emits.; `MutationThrottler` in `packages/browser/src/extensions/replay/external/mutation-throttler.ts` only filters attribute mutations by node rate after an rrweb mutation event exists, so it does not reduce rrweb's synchronous work for structural `adds`/`removes` or full snapshots.; `packages/rrweb/rrweb/src/record/index.ts` implements `takeFullSnapshot` by locking mutation buffers and calling `snapshot(document, ...)` synchronously before emitting the full snapshot event.; `packages/rrweb/rrweb-snapshot/src/snapshot.ts` recursively iterates `dom.childNodes(n)` in `serializeNodeWithId`; it has a max-depth guard but no max-node or time-slicing/yielding guard, so broad DOMs can still be expensive.; `packages/rrweb/rrweb/src/record/mutation.ts` handles `MutationObserver` batches synchronously: `processMutations` calls `genAdds` recursively over added subtrees, and `emit` serializes additions with `serializeNodeWithId` in the same processing flow.
- Fix assessment: A small browser-SDK patch would only add mitigations or knobs. The freezing comes from rrweb core snapshot/mutation serialization, where changing traversal scheduling, node budgets, or event semantics risks replay correctness, masking/blocking behavior, iframe/shadow DOM handling, and ingestion compatibility. This needs a targeted rrweb performance design plus regression/performance tests rather than a speculative one-line throttle.

## 2026-07-23T17:45:40.162Z
- Item: issue #4239 — replayer: pausing exactly at a FullSnapshot's timestamp skips the rebuild
- Conclusion: Confirmed replay seek-boundary bug with a narrow but stateful fix required.
- Labels: feature/replay, web
- URL: https://github.com/PostHog/posthog-js/issues/4239
- Relevant files: `packages/rrweb/rrweb/src/replay/machine.ts`, `packages/rrweb/rrweb/src/replay/index.ts`, `packages/rrweb/rrweb/src/replay/timer.ts`, `packages/rrweb/rrweb/test/replayer.test.ts`, `packages/rrweb/rrweb/test/fullscreen.test.ts`
- Findings: `packages/rrweb/rrweb/src/replay/machine.ts` classifies only `event.timestamp < baselineTime` as synchronous in the `play` action; equal-timestamp events are added to the timer.; `Replayer.pause(timeOffset)` in `packages/rrweb/rrweb/src/replay/index.ts` calls `play(timeOffset)` and immediately sends `PAUSE`, whose machine action calls `timer.clear()`. Thus a timer-queued equal-timestamp FullSnapshot cannot run during a paused seek.; A FullSnapshot cast calls `clearFullscreen()` and `rebuildFullSnapshot()`. Skipping that cast explains both the stale DOM and leaked `rr_fullscreen` marker described in the report.; The playback path conditionally emits `ReplayerEvents.PlayBack` when seeking backward; its handler resets the mirror and related replay state. `lastPlayedEvent` is not reset in that handler/action path, so broad boundary-classification changes can interact with stale delta bookkeeping.; `packages/rrweb/rrweb/test/fullscreen.test.ts` currently uses `Math.max(enterOffset - 1, 0)` for its pre-fullscreen checkpoint, consistent with avoiding an exact event-timestamp boundary.
- Fix assessment: The failure mechanism is clear and the code change should be small, but the obvious global `<` to `<=` change has documented replay-state regressions. The fix needs a focused boundary rule and regression coverage that verifies both paused FullSnapshot seeks and existing rewind/lastPlayedEvent behavior.

## 2026-07-23T23:24:50.015Z
- Item: issue #4242 — React Native: no supported way to defer auto-presented popover surveys (collide with native-stack formSheet/modal)
- Conclusion: Valid React Native surveys enhancement: popover surveys are always auto-presented and the provider has no public deferral control.
- Labels: enhancement, react-native, feature/mobile
- URL: https://github.com/PostHog/posthog-js/issues/4242
- Relevant files: `packages/react-native/src/surveys/PostHogSurveyProvider.tsx`, `packages/react-native/src/surveys/components/SurveyModal.tsx`, `packages/react-native/src/surveys/index.ts`, `packages/react-native/src/index.ts`, `packages/react-native/test/SurveyModal.spec.tsx`, `packages/react-native/CHANGELOG.md`
- Findings: `PostHogSurveyProvider` finds matching surveys whenever `activeSurvey` is empty and immediately calls `setActiveSurvey(popoverSurveys[0])` for the first `SurveyType.Popover`.; The provider renders `SurveyModal` whenever an active translated survey is a popover; its public props contain no `automaticSurveyModal`, `canShowSurvey`, or equivalent presentation-control option.; `useActiveSurvey`, `useFeedbackSurvey`, and `automaticSurveyModal` exist only as commented-out code in `PostHogSurveyProvider.tsx`, and neither hook is exported from `packages/react-native/src/surveys/index.ts`.; `SurveyModal` uses React Native's transparent full-screen `Modal` and renders a flex-fill backdrop View with an `onTouchStart` handler, supporting the report that an unintended mounted survey modal can intercept interaction.; The existing iOS fallback only ensures `onClose` eventually notifies the provider after a survey is dismissed. The changelog and implementation confirm it does not control whether a survey Modal is initially mounted.; `SurveyModal` is already publicly exported, but the provider does not publicly expose the active-survey context/hooks needed for an application to control automatic presentation through the currently commented-out design.
- Fix assessment: Assuming the intended contract is a declarative provider gate rather than a full imperative survey-display API, this is localized to provider selection/rendering and can be covered with focused provider tests. It does not require changes to the native plugin or survey matching logic.

## 2026-07-24T14:41:29.134Z
- Item: issue #4267 — @posthog/ai : Support the new Google Gemini Interactions API
- Conclusion: Valid @posthog/ai feature request: the Gemini wrapper does not currently instrument the Interactions API.
- Labels: enhancement, feature
- URL: https://github.com/PostHog/posthog-js/issues/4267
- Relevant files: `packages/ai/src/gemini/index.ts`, `packages/ai/tests/gemini.test.ts`, `packages/ai/package.json`, `pnpm-lock.yaml`
- Findings: `PostHogGoogleGenAI` exposes a `WrappedModels` instance only; it does not expose or wrap an Interactions API client.; `WrappedModels` captures only `generateContent`, `generateContentStream`, and `embedContent` calls.; The existing Gemini tests mock and cover those three model methods, with no Interactions API coverage.; `@posthog/ai` declares `@google/genai` as an optional peer dependency at `^1.43.0`, with the lockfile resolving version `1.43.0`; the exact Interactions API types and streaming lifecycle supported by that dependency version need verification before implementation.
- Fix assessment: The missing surface is clear, but a safe implementation needs API-specific mapping for Interactions inputs, outputs, token usage, tool/function activity, errors, and any streaming or asynchronous lifecycle. Reusing the generateContent formatter without verifying those response semantics would be speculative.

## 2026-07-24T14:45:00.603Z
- Item: issue #4267 — @posthog/ai : Support the new Google Gemini Interactions API
- Conclusion: Valid @posthog/ai tracing-parity feature request for Google Gemini's Interactions API.
- Labels: enhancement, feature
- URL: https://github.com/PostHog/posthog-js/issues/4267
- Relevant files: `packages/ai/src/gemini/index.ts`, `packages/ai/tests/gemini.test.ts`, `packages/ai/package.json`, `pnpm-lock.yaml`
- Findings: `PostHogGoogleGenAI` exposes a wrapped `models` client only; it does not expose an Interactions client.; `WrappedModels` delegates exclusively to `client.models.generateContent`, `client.models.generateContentStream`, and `client.models.embedContent`.; No references to `interactions` exist in `packages/ai`.; The Gemini unit-test mock and tests cover the three models methods, with no Interactions API coverage.; `@google/genai` is declared as an optional peer and dev dependency at `^1.43.0`, and the lockfile resolves version `1.43.0`; the reported Interactions API usage is from `@google/genai` 2.13.0.
- Fix assessment: The missing API surface is clear, but a safe implementation requires verifying the Interactions API types and response lifecycle in the supported Google SDK version. Input/output formatting, usage metadata, tool activity, streaming behavior, and error handling should not be assumed to match `generateContent`.

## 2026-07-24T16:40:05.690Z
- Item: issue #4268 — @posthog/ai/otel: support per-span (per-request) distinct id & groups for shared multi-tenant processes
- Conclusion: Valid AI observability enhancement: the SDK currently forwards OTLP spans without implementing per-span identity or group semantics, while its examples configure identity on the process-level Resource.
- Labels: enhancement, feature, team/llm-analytics
- URL: https://github.com/PostHog/posthog-js/issues/4268
- Relevant files: `packages/ai/src/otel/exporter.ts`, `packages/ai/src/otel/processor.ts`, `packages/ai/src/otel/redact.ts`, `packages/ai/tests/otel.test.ts`, `packages/ai/tests/processor.test.ts`, `examples/example-ai-openai/chat-completions.ts`, `examples/example-ai-langchain/callback-handler.ts`
- Findings: `PostHogTraceExporter` filters AI spans, redacts them, and passes the resulting OTel spans directly to the OTLP exporter at `/i/v0/ai/otel`; it contains no handling for `posthog.distinct_id`, groups, or AI session attributes.; `PostHogSpanProcessor` has the same behavior: on end, it filters and redacts an AI span before delegating it to an OTLP `BatchSpanProcessor`; it does not apply identity precedence or translate group attributes.; The checked-in OpenAI and LangChain OTel examples configure `posthog.distinct_id` in `resourceFromAttributes(...)`, demonstrating the currently documented process/Resource-level setup.; The OTel unit tests cover exporter configuration, AI-span filtering, and binary-content redaction, but do not cover identity, group, session, or Resource-versus-span precedence.; No `posthog.distinct_id` or `$ai_session_id` mapping exists under `packages/ai/src`; whether span-level values are currently interpreted by the ingestion service cannot be verified from this repository.
- Fix assessment: The requested behavior is primarily a server-side OTLP-to-`$ai_generation` ingestion contract. A client-only change that rewrites per-span attributes into Resource data would be speculative, may affect OTel resource grouping, and would not safely establish group/session semantics. The backend must first define and test the supported span attribute keys, value encoding, and precedence.

## 2026-07-26T15:23:35.163Z
- Item: issue #4267 — @posthog/ai : Support the new Google Gemini Interactions API
- Conclusion: Valid feature request for Gemini Interactions API tracing support in @posthog/ai.
- Labels: enhancement, feature, team/llm-analytics
- URL: https://github.com/PostHog/posthog-js/issues/4267
- Relevant files: `packages/ai/src/gemini/index.ts`, `packages/ai/tests/gemini.test.ts`, `packages/ai/package.json`, `pnpm-lock.yaml`
- Findings: `PostHogGoogleGenAI` constructs a `GoogleGenAI` client and exposes only a wrapped `models` client.; `WrappedModels` instruments only `client.models.generateContent`, `client.models.generateContentStream`, and `client.models.embedContent`.; No `interactions` references exist under `packages/ai`.; The Gemini test mock and test suite cover only the three `models` methods, with no Interactions API coverage.; `packages/ai/package.json` declares `@google/genai` as an optional peer and development dependency at `^1.43.0`, and the lockfile resolves version `1.43.0`; the reporter uses 2.13.0, which is outside that declared peer range.
- Fix assessment: The missing public API surface is clear, but a safe implementation depends on the actual Interactions API types and response lifecycle in the supported 2.x Google SDK. Mapping inputs, outputs, usage metadata, tool activity, streaming behavior, and errors from the legacy generateContent assumptions without verification would be speculative.

## 2026-07-26T16:46:27.007Z
- Item: issue #4275 — @posthog/nuxt: deleteAfterUpload leaves stale .js.map entries in the Nitro asset manifest, causing 500s
- Conclusion: Confirmed @posthog/nuxt sourcemap-cleanup bug: deleting maps in the Nuxt close hook can leave Nitro's already-generated public-asset manifest pointing at missing files, producing runtime 500s.
- Labels: nuxt, feature/error-tracking, team/error-tracking
- URL: https://github.com/PostHog/posthog-js/issues/4275
- Relevant files: `packages/nuxt/src/module.ts`, `packages/nuxt/tests/sourcemaps-ssr.test.mjs`, `packages/nuxt/package.json`
- Findings: `packages/nuxt/src/module.ts` configures hidden client and server sourcemaps during `build:before` when PostHog sourcemaps are enabled.; The module runs `sourcemap inject` for `publicDir` in the `nitro:build:public-assets` hook, but runs `sourcemap upload --directory outputDir` only in the `close` hook.; `getUploadArgs()` adds `--delete-after` when `sourcemaps.deleteAfterUpload` is omitted, so deletion is enabled by default.; Because the upload scans `outputDir`, the close-hook upload deletes both public and server maps after Nitro's output has been generated, matching the stale-manifest ordering described in the report.; The existing `sourcemaps-ssr.test.mjs` verifies mocked CLI call ordering and the SSR/non-SSR server-injection branch, but does not build Nitro output or assert manifest contents and HTTP behavior.
- Fix assessment: The affected behavior is localized to the Nuxt module's sourcemap lifecycle hooks. The reported ordering directly matches the current implementation, though a real Nuxt/Nitro integration regression test is needed to verify the exact hook ordering and preserve SSR and SPA sourcemap uploads.

## 2026-07-28T00:27:03.586Z
- Item: issue #3046 — feat(nuxt): ability to disable the module
- Conclusion: Valid Nuxt enhancement, but the disabled-mode behavior for `usePostHog()` needs an explicit API decision.
- Labels: enhancement, feature, nuxt
- URL: https://github.com/PostHog/posthog-js/issues/3046
- Relevant files: `packages/nuxt/src/module.ts`, `packages/nuxt/src/runtime/vue-plugin.ts`, `packages/nuxt/src/runtime/nitro-plugin.ts`, `packages/nuxt/src/runtime/composables/usePostHog.ts`, `packages/nuxt/tests/vue-plugin.test.mjs`
- Findings: `ModuleOptions` in `packages/nuxt/src/module.ts` has no module-level `enabled` or `disabled` field.; `setup()` unconditionally registers the Vue plugin, Nitro plugin, and composable imports via `addPlugin`, `addServerPlugin`, and `addImportsDir`.; The Vue plugin initializes `posthog-js` whenever it runs and the singleton is not already loaded; it has no module-disable guard.; The Nitro plugin always creates a `posthog-node` client and can register exception autocapture, so client-only configuration cannot fully disable server-side tracking.; `usePostHog()` invokes the injected `$posthog` provider optionally and returns `undefined` when it is absent. Therefore, omitting the plugins would stop tracking but would not preserve a usable PostHog client for composable consumers.; The existing Vue-plugin test covers Vue exception autocapture only; there is no disabled-module coverage.
- Fix assessment: Adding a boolean option and skipping plugin registration is mechanically small, but it fails the reporter's stated need to retain `usePostHog()`. Preserving that API while guaranteeing no events requires a deliberate contract for browser consent state and for the server client, so a simple guard would be speculative.
