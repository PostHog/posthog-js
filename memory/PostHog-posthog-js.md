# PostHog Watcher memory

...<older entries truncated>

`, so selecting only that extension bundle still executes Autocapture logic.; `Autocapture.isEnabled` directly invokes `this.instance._shouldDisableFlags()`, matching the missing-method error in the report.; `PostHog` defines `_shouldDisableFlags()` in `posthog-core.ts`; the failure is therefore an inter-bundle property-name mismatch rather than an absent source implementation.; `module.slim` and `extension-bundles` are separate Rollup outputs and property mangling is configured for them, creating a private cross-bundle ABI boundary.; `terser-cross-bundle-properties.cjs` now lists `_shouldDisableFlags` in `crossBundlePrivateProperties`, and `rollup.config.mjs` reserves that list for both relevant outputs.; `slim-bundle.spec.ts` contains a regression test explicitly stating that `AnalyticsExtensions` Autocapture accesses the mangled `_shouldDisableFlags()` method and verifies initialization does not crash.
- Fix assessment: The failing boundary is narrowly identified and the minimal mitigation is already represented in source: preserve the one private method name across the independently emitted slim core and extension bundle, guarded by an integration regression test.
- PR: https://github.com/PostHog/posthog-js/pull/4389

## 2026-08-03T04:43:10.328Z
- Item: issue #4391 — [react-native-plugin] posthog-ios pinned to `~> 3.64.7`, locking RN apps inside the rage-click sheet-dismissal regression (fixed in 3.68.2)
- Conclusion: Confirmed dependency-resolution bug: the React Native plugin caps posthog-ios at 3.64.x, preventing consumers from receiving the reported 3.68.2 rage-click fix.
- Labels: react-native, iOS, feature/replay, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4391
- Relevant files: `packages/react-native-plugin/posthog-react-native-plugin.podspec`, `packages/react-native-plugin/package.json`, `packages/react-native-plugin/CHANGELOG.md`, `packages/react-native-plugin/ios/PosthogReactNativePlugin.swift`
- Findings: `packages/react-native-plugin/posthog-react-native-plugin.podspec` defines `posthog_ios_version = '3.64.7'` as the shared native dependency version.; The podspec declares `s.dependency 'PostHog', "~> #{posthog_ios_version}"`; with a three-component pessimistic constraint, this is limited to versions below 3.65.0.; The optional SPM path uses `requirement: { kind: 'upToNextMinorVersion', minimumVersion: posthog_ios_version }`, likewise limiting resolution to the 3.64.x minor line.; `packages/react-native-plugin/package.json` identifies the current plugin version as `2.2.3`.; The 2.2.2 changelog entry says the native SDK floor was raised to posthog-ios 3.64.7 or later, but the checked-in CocoaPods/SPM constraints actually prevent later minor releases.; `PosthogReactNativePlugin.swift` constructs and configures `PostHogConfig` from selected SDK, replay, and error-tracking options, but does not read or assign a rage-click configuration.
- Fix assessment: The immediate resolution cap is isolated to one shared podspec version constant. Raising that baseline to a post-fix posthog-ios release updates both CocoaPods and SPM consistently; native iOS build coverage should verify compatibility before release.
- PR: https://github.com/PostHog/posthog-js/pull/4392

## 2026-08-03T19:32:20.940Z
- Item: issue #4405 — @posthog/ai: $ai_http_status is always 500 for Vercel AI SDK provider errors
- Conclusion: Confirmed bug in the shared AI generation error-status fallback.
- Labels: team/llm-analytics
- URL: https://github.com/PostHog/posthog-js/issues/4405
- Relevant files: `packages/ai/src/captureAiGeneration.ts`, `packages/ai/src/vercel/middleware.ts`, `packages/ai/tests/captureAiGeneration.test.ts`, `packages/ai/src/langchain/callbacks.ts`, `packages/ai/package.json`
- Findings: `captureAiGeneration` initializes `httpStatus` from the caller, then for errors only accepts a numeric `options.error.status`; all other errors are assigned status 500 before the final defaulting logic.; `withTracing` in `packages/ai/src/vercel/middleware.ts` supplies `httpStatus: 200` on successful `doGenerate` and `doStream` captures, but its two catch paths pass only `error`, leaving status derivation to `captureAiGeneration`.; The current shared-helper tests cover `error.status`, no status, and an explicitly supplied status. A later serialization test constructs an error with `statusCode: 502`, but does not assert the generated `$ai_http_status`, so it currently permits the reported regression.; `@posthog/ai` currently declares optional `@ai-sdk/provider` peer support for `^2.0.0 || ^3.0.0`, matching the affected integration range described in the report.; LangChain has a separate implementation that assigns `(output as any).status || 500` directly rather than calling `captureAiGeneration`; no evidence in this report or the inspected code establishes a LangChain `statusCode`-only provider error, so it should not be bundled into this focused fix.
- Fix assessment: The faulty decision point is a single shared status-extraction branch. Adding a numeric `statusCode` fallback after the established `status` check preserves existing integration behavior and directly covers the Vercel wrapper's error paths.
- PR: https://github.com/PostHog/posthog-js/pull/4406

## 2026-08-04T03:33:41.834Z
- Item: issue #4409 — Manual masking does not work on Android for react-native app replays
- Conclusion: Credible Android React Native session-replay privacy bug, but it needs confirmation on the currently supported native-plugin pairing before selecting a fix.
- Labels: Android, react-native, feature/replay, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4409
- Relevant files: `packages/react-native/src/PostHogMaskView.tsx`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/src/optional/OptionalPlugin.ts`, `packages/react-native/package.json`, `packages/react-native/CHANGELOG.md`, `packages/react-native-plugin/android/src/main/java/com/posthogreactnativeplugin/PosthogReactNativePluginModule.kt`, `packages/react-native-plugin/android/build.gradle`, `packages/react-native-plugin/CHANGELOG.md`
- Findings: PostHogMaskView deliberately renders a non-collapsible native View with accessibilityLabel="ph-no-capture", so the wrapper itself is intended to be visible to the native recorder rather than handled by React Native autocapture.; The current React Native SDK defaults sessionReplayConfig.maskAllTextInputs to true and passes that value to its optional native plugin.; The current Android native plugin assigns maskAllTextInputs and maskAllImages to PostHogAndroidConfig.sessionReplayConfig, but contains no JavaScript-side handling for individual ph-no-capture labels; recognition of those labels is therefore delegated to the underlying Android session recorder.; The current SDK first loads @posthog/react-native-plugin and falls back to posthog-react-native-session-replay only when the newer plugin is absent. Its package metadata now declares a >=1.6.0 peer range for the legacy session-replay package, whereas the report uses 1.5.1.; The checked-in Android plugin depends on com.posthog:posthog-android:3.54.0. The legacy session-replay package source and its transitive Android recorder version are not present in this repository, so this investigation cannot verify its masking behavior.; The React Native changelog records PR #3429 as an iOS-specific PostHogMaskView detection fix released in 4.42.2. No checked-in changelog entry establishes that the reported Android masking cases were fixed.
- Fix assessment: The current JS and Android bridge already forward the all-text-input masking setting, while ph-no-capture detection occurs in the underlying native recorder. A bridge-only change would be speculative without reproducing on the current plugin and identifying whether the legacy package, the embedded posthog-android version, or Android view-hierarchy/frame timing is responsible.

## 2026-08-05T07:52:44.996Z
- Item: issue #4422 — Node SDK: steady memory leak in capture() + flush() traced to native-gzip path in @posthog/core gzipCompress
- Conclusion: Likely Node SDK compression-path memory-retention bug; the reported workaround and current implementation make this credible, but a focused reproducer is needed before choosing a fix.
- Labels: node, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4422
- Relevant files: `packages/core/src/gzip.ts`, `packages/core/src/posthog-core-stateless.ts`, `packages/core/src/__tests__/gzip.spec.ts`, `packages/node/src/client.ts`, `packages/node/src/capture-v1/sender.ts`, `packages/node/src/__tests__/capture-v1/sender.spec.ts`, `packages/node/package.json`
- Findings: `packages/core/src/gzip.ts` implements gzip with the platform `CompressionStream`, obtains the result through `new Response(compressedStream.readable).blob()`, and validates it by creating two additional sliced Blobs and reading their ArrayBuffers.; `gzipCompress()` allocates these stream and Blob objects on every call; it has no pooling or Node-specific implementation.; `PostHogCoreStateless.sendBatch()` calls the shared `gzipCompress()` for each outgoing `/batch/` payload whenever `disableCompression` is false, so capture followed by flush per request can invoke it once per request.; The Node Capture V1 sender also defaults its compression hook to the same `gzipCompress()` implementation and calls it once per send attempt when compression is enabled.; The gzip validation is covered by core unit tests, including rejection of malformed stream output; the tests compare valid output with `node:zlib`, but `packages/node` has no runtime `zlib` compression path.; Current Node package metadata supports Node `>=22.22.0`, which includes the reporter's Node 24 runtime family; no existing runtime test exercises sustained high-frequency compression or checks retained heap growth.
- Fix assessment: The evidence supports investigating the shared native gzip path, but does not yet isolate whether retention is caused by Node's CompressionStream implementation, Response/Blob materialization, or the validation reads. A Node-only zlib path would also need to preserve browser and edge compatibility and cover all Node send paths that currently call the shared core helper.

## 2026-08-10T14:41:41.342Z
- Item: issue #4483 — `enableConversationId` appends tool-output instructions that Claude flags as prompt injection
- Conclusion: Confirmed MCP compatibility and security-UX bug: the opt-in conversation correlation feature intentionally places imperative instructions in tool results, which Claude can reasonably reject as prompt injection.
- Labels: feature/mcp-analytics, team/ai-observability
- URL: https://github.com/PostHog/posthog-js/issues/4483
- Relevant files: `packages/mcp/src/extensions/conversation-id.ts`, `packages/mcp/src/extensions/instrumentation.ts`, `packages/mcp/src/extensions/output-instructions.ts`, `packages/mcp/src/__tests__/conversation-id.test.ts`, `packages/mcp/src/__tests__/output-instructions.test.ts`, `packages/mcp/docs/adr/0004-conversation-id-as-session-anchor.md`
- Findings: `buildConversationIdPromptBack()` in `packages/mcp/src/extensions/conversation-id.ts` constructs the exact `[SERVER]: Reuse conversation_id=... on every subsequent tool call...` text described in the report.; `applyConversationInstructions()` in `packages/mcp/src/extensions/instrumentation.ts` appends that text to the minting response and, for eligible tools, mirrors an instruction-bearing handle into `structuredContent`.; The current implementation injects an optional `conversation_id` input-schema property for eligible tools, with a description instructing clients to echo the previous server-provided value; regression tests explicitly assert this schema injection.; The `_mcp_instructions` structured-content field also contains imperative text telling the client to send `conversation_id` on every subsequent tool call, so moving the data from text content to structured content alone does not eliminate the prompt-instruction concern.; ADR-0004 documents that this agent-carried handle is the current session anchor for stateless MCP traffic and that removing delivery channels would prevent correlation when no other session state exists.
- Fix assessment: The visible injection can be removed with a small code change, but that would deliberately break the only implemented cross-request session-correlation path for some MCP tool shapes. A safe fix needs a product and protocol decision for a documented, host-supported conversation-handle lifecycle rather than a speculative change that silently degrades analytics.

## 2026-08-11T12:50:55.869Z
- Item: issue #4491 — Add official WeChat Mini Program SDK
- Conclusion: Valid, well-scoped feature request for a new officially supported runtime SDK; it is not a small compatibility fix.
- Labels: enhancement, web
- URL: https://github.com/PostHog/posthog-js/issues/4491
- Relevant files: `packages/core/src/posthog-core-stateless.ts`, `packages/core/src/posthog-core.ts`, `packages/core/src/types.ts`, `packages/web/src/posthog-web.ts`, `packages/react-native/src/posthog-rn.ts`, `packages/react-native/src/storage.ts`, `packages/core/package.json`, `pnpm-workspace.yaml`
- Findings: `PostHogCoreStateless` explicitly requires platform implementations to provide `fetch`, library metadata, and synchronous persisted-property access through abstract methods, while retaining shared queueing, batching, retries, capture, identity, and session behavior.; `PostHogFetchOptions` and `PostHogFetchResponse` define a fetch-like promise interface, so a WeChat implementation would need a small `wx.request` adapter that translates its callback response into `status`, `text()`, and `json()`.; `posthog-js-lite` is browser-specific: `packages/web/src/posthog-web.ts` uses browser storage, `window`, history APIs, and the Fetch API, so it is not a suitable Mini Program runtime target.; `posthog-react-native` demonstrates the existing supported pattern of extending `PostHogCore`, supplying storage and transport implementations, delaying initialization until asynchronous storage preload completes, and flushing/persisting on application lifecycle transitions.; `packages/react-native/src/storage.ts` provides React-Native-specific storage wrappers and cannot be reused directly for WeChat storage APIs.; No WeChat/Mini Program package or runnable example was found under the workspace packages or examples.
- Fix assessment: Although @posthog/core supplies the core analytics behavior, this request requires a new supported and published runtime package plus native transport, persistence durability, lifecycle semantics, test infrastructure, documentation, and a Mini Program example. Incorrect lifecycle or storage handling could lose or duplicate queued events.

## 2026-08-11T12:51:53.818Z
- Item: issue #4492 — Add official Uni-App integration
- Conclusion: Valid cross-platform integration feature request, but it is not a small SDK change and is blocked in part by the separate official WeChat Mini Program SDK work in #4491.
- Labels: enhancement, web, team/client-libraries
- URL: https://github.com/PostHog/posthog-js/issues/4492
- Relevant files: `packages/browser/package.json`, `packages/browser/src/posthog-core.ts`, `packages/browser/README.md`, `packages/web/package.json`, `packages/web/README.md`, `examples/`
- Findings: `packages/browser/package.json` identifies `posthog-js` as the primary browser SDK distribution, matching the requested H5 runtime choice.; `packages/browser/src/posthog-core.ts` imports browser globals and browser storage (`localStore` and `sessionStore`), so it should not be assumed to run unchanged in Mini Program or native Uni-App targets.; There is no Uni-App- or WeChat-specific package in `packages/`, and a repository-wide search found no existing Uni-App integration or documentation.; The existing examples include browser, Next.js, Nuxt, React Native, and other SDK examples, but no Uni-App example.; `posthog-js-lite` is documented as a reduced web package and does not provide a cross-runtime Uni-App adapter.; The issue explicitly identifies #4491 as the dependency for the WeChat Mini Program runtime; without that SDK and its transport, persistence, and lifecycle contract, the requested multi-target integration cannot be completed safely.
- Fix assessment: Completing the acceptance criteria requires a new integration surface, target-specific runtime contracts, runnable projects, and behavior tests across runtimes with different storage, networking, and lifecycle APIs. The WeChat path also depends on the still-open #4491 SDK work. A browser-only wrapper or runtime-detection shim would imply unsupported compatibility and would not satisfy the requested target matrix.

## 2026-08-12T13:55:35.238Z
- Item: issue #4507 — Dead clicks: treat an in-flight network request as a liveness signal
- Conclusion: Valid enhancement proposal, but the requested signal is not available through the existing recorder hook for all dead-click users and safely adding it needs a deliberate instrumentation design.
- Labels: enhancement, feature/dead-clicks, team/analytics-platform
- URL: https://github.com/PostHog/posthog-js/issues/4507
- Relevant files: `packages/browser/src/entrypoints/dead-clicks-autocapture.ts`, `packages/browser/src/__tests__/entrypoints/lazy-loaded-dead-clicks-autocapture.test.ts`, `packages/types/src/posthog-config.ts`, `packages/browser/src/extensions/replay/external/network-plugin.ts`, `packages/browser/src/extensions/replay/external/lazy-loaded-session-recorder.ts`, `packages/browser/src/extensions/replay/external/config.ts`, `packages/browser/src/utils/request-router.ts`, `packages/browser/src/extensions/replay/rrweb-plugins/patch.ts`
- Findings: `LazyLoadedDeadClicksAutocapture` currently stores timestamps only for mutations, scrolls, selection changes, and visibility changes. `_checkClicks` suppresses candidates with an in-window signal, otherwise requeues them until the mutation threshold and ultimately captures on an absolute timeout of `mutation_threshold_ms * 1.1`.; The dead-click configuration defaults the mutation threshold to 2500 ms, so an asynchronous UI whose DOM update occurs later can indeed be classified as dead despite a successful request.; The existing replay network plugin is not a general request-start feed. It is initialized only when replay is active and network capture is enabled; its `PerformanceObserver` callback observes resource entries after timing is available, while fetch/XHR wrapping is enabled only when request headers or bodies are being recorded.; The replay XHR instrumentation records its start timestamp in a `send` wrapper; the fetch/XHR code uses shared patch-chain handling because tracing headers and replay can both wrap the same primitives. A separate dead-click wrapper would re-enter this high-risk compatibility area.; Replay network capture already excludes PostHog ingestion requests using `requestRouter.isIngestionEndpoint`, with a path-based fallback for `/s/`, `/e/`, and `/i/`. `RequestRouter` accounts for rewritten ingestion URLs, but a new lazy bundle must feature-detect any newer-core API it calls.; The dead-click detector is itself a lazy-loaded entrypoint. The incident guidance requires old-core/new-bundle compatibility when adding cross-boundary APIs or assumptions.; Existing unit tests already exercise suppression and timeout behavior for mutation, scroll, selection, and visibility signals, providing a focused place to add the proposed signal tests.
- Fix assessment: The behavioral change is narrow, but request-start observation cannot simply reuse the existing replay hook without missing users and requests. Adding or altering fetch/XHR instrumentation has a documented high compatibility risk, while changing the lazy entrypoint must remain compatible with older SDK cores. The required timing window and scope are also not specified.
