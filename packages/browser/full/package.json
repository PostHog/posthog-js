{
    "name": "posthog-js-full",
    "version": "1.0.0",
    "private": true,
    "description": "Subpath entry point for the bundle with every extension inlined, so no extension is fetched from the CDN at runtime. Ships an ES module for bundlers and a CommonJS build for require. Types come from the canonical dist/module.d.ts: PostHog is a class with private members, so a duplicate declaration file would be a nominally incompatible type.",
    "repository": {
        "type": "git",
        "url": "https://github.com/PostHog/posthog-js",
        "directory": "packages/browser/full"
    },
    "main": "../dist/module.full.cjs",
    "module": "../dist/module.full.js",
    "types": "../dist/module.d.ts"
}
