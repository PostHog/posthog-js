---
'@posthog/openfeature-provider': minor
---

Add official PostHog providers for the OpenFeature SDK: `PostHogServerProvider` (`/server`, backed by `posthog-node`) and `PostHogWebProvider` (`/web`, backed by `posthog-js`).
