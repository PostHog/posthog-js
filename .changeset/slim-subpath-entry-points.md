---
'posthog-js': minor
---

Add `posthog-js/slim`, `posthog-js/slim/no-external` and `posthog-js/extensions` entry points, so the tree-shakeable core can be imported without a deep path into `dist/`
