---
"posthog-react-native": minor
---

feat(react-native): support `__add_tracing_headers` to inject `X-POSTHOG-DISTINCT-ID` and `X-POSTHOG-SESSION-ID` headers on outgoing `fetch` requests for linking LLM traces and session replays
