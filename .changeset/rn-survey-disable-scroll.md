---
'posthog-react-native': minor
---

Add `disableSurveyScroll` prop to `PostHogSurveyProvider` to render survey question content in a non-scrollable `View` instead of a `ScrollView`. Defaults to `false`, preserving the existing scrollable behavior.
