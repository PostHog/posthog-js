---
---

No package changes: moves the `@posthog/mcp` integration harness into the repo
(`packages/mcp/harness/`) and gates affected PRs with the new `MCP harness` CI workflow.
Dev-dependency-only changes to `@posthog/mcp`; nothing shipped changes.
