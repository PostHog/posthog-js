---
'posthog-js': patch
'@posthog/types': patch
---

Add the browser-v1 adapter for the shared extension runtime, including the required Core capability, project token and direct request transport access, persistence, session and remote-config observation, and bounded extension teardown during shutdown.
