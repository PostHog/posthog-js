{
    "name": "@posthog-tooling/release",
    "private": true,
    "type": "module",
    "scripts": {
        "test:unit": "node --test src/*.test.ts",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.1027.0",
        "mime-types": "^3.0.2"
    }
}
